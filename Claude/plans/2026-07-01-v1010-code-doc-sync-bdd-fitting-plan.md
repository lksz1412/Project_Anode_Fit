# ★MASTER 계획서 — v1.0.10 교재 증판본: 코드 ↔ Ch1·Ch2 동기화·완성 (순차 6-페이즈)

> **이 문서 = v1.0.10 작업의 단일 마스터 진입점**(컴팩션 복구 시 여기부터 읽는다). 페이즈별 세부 계획·result·ledger 위치는 §6·§아래 도서관 규약. 인덱스 = `plans/INDEX.md`.
> **기획 단계 — Phase 1 착수 전 GO 대기.**

## 0. 사용자 합의(7-01) — 순차, 병렬 X
- 사용자: "동시(병렬) 원하지 않음. 퀄리티 유지하며 병렬할 능력 안 됨을 안다." → **순차 진행**. 내 vertical-slice(동시) 제안 **철회**.
- 합의 순서: **1) 코드 → 2)+3) 챕터1 → 4) 챕터2 → 5) 코드 개정 → 6) 상호 충실도·내용 완성도 최종 동시 점검.**
- ★**LCO·Ch2(발열) 의 코드 구현 = 5) 코드 개정에서.** (2·4 단계는 이론/문건 먼저, 코드는 5에서 따라옴 → 6에서 상호 정합 마감.)
- ★**순서 근거(7-02 확정 — 재질의 X)**: LCO·발열은 **이론 내용 자체를 아직 더 갈고닦아야 하는 상태**. 미완성 이론을 기반으로 코드를 먼저 짜면 문제가 생길 우려 → **코드(P1: 기존 음극 검증) → 문건(P2·P3: LCO·발열 이론 완성) → 코드 개정(P4: 완성 이론 구현) → 점검(P5)** 순서가 의도된 설계. P2~P3 동안 문건이 코드보다 앞서는 것은 정상(P4·P5에서 정합).
- ★**Ch2 = 발열(이번 세션 확정). 2장 체계.** (6-08 의 "Ch3=발열·4장"은 supersede — [[feedback_anode_fit_chapter_dependency_tree]] 갱신함. 재질의 금지.)

## 1. Summary
v1.0.10 = **한 권의 물리화학 교재 증판본**(이론 Ch1·Ch2 + 그 위 코드, 한 폴더 `docs/v1.0.10/`). 현재 코드는 흑연 음극 dQ/dV 전용 → 목표 = **BDD 양·음극 dQ/dV 물리수식 기반 피팅 함수**(흑연 음극 + LCO 양극 + 발열). 본 마스터는 합의 6-페이즈로 코드·문건을 동기화·완성하고, 그래프로 검증, 피팅 파라미터를 관찰·추천한다. 코드작업 = **Serena 적극**.

## 2. 문건 품질 기준(3차 누적 — 절대 준수)
- **형식 = 물리화학 교재**: 줄글 풀이 X. **화학 반응식·물리 공식 중심 전개 + 유도 과정(식→식 사슬) 빠짐없이.**
- **독자 = 타 전공 석·박사급(비전문)**: 읽기만으로 "무엇을·어떤 의도로 푸는지" 이해되게 자기완결(비약·암묵전제 0, 앞에서 깔고 뒤에서 씀).
- **어투 = 교과서적 안정·객관(필수)**: 구어·과장·메타발언 X. ★독자 수준 평가·가늠 표현 절대 금지("아시다시피"·"자명"·"유능한 독자라면" 류 0).
- **3대 무결 동시**: (1) 물리 백그라운드 무결 (2) 그 기반 코드 무결 (3) 사용자 의도 온전 반영.
- 정합: [[feedback_anode_fit_textbook_style]]·[[feedback_communication_style]].

## 3. Current Ground Truth (전제 검증 완료 — 추정 아님)
- 코드 `docs/v1.0.10/Anode_Fit_v1.0.10.py`: `GraphiteAnodeDischargeDQDV` + func_w/U_j/U_j_hys/ksi_eq/L_q/dU_hys/U_branch/dH_a_eff/chi_d. **LCO·cathode·발열(q_rev)·전자엔트로피(dS_e) 0건** = 흑연 음극 전용.
- Ch1(34p): broadening·w 이중지위·LCO/양극/전자엔트로피 105곳. Ch2(13p): 통계열역학·가역발열. → 문건이 코드에 없는 LCO·발열 보유.
- 폴더: `docs/v1.0.10/` = Ch1·Ch2·코드·plot_dqdv·figs 통합(이동 완료). `results/code/` = 빌드계보(v11·v12).

## 4. Phase Range
| Phase | 이름(사용자 번호) | 산출 | Steps |
|---|---|---|---|
| P1 | **코드**(1) — 기존 음극 코드 검증·플로우차트 앵커 확립 | 플로우차트 맵·조건 audit·피팅 파라미터 인벤토리 | 1–4 |
| P2 | **챕터1**(2+3) — 코드 플로우차트 충실 교과서화 + v9 LCO 검토 반영 | Ch1 보완본(코드근거·v3~v9·LCO 이론) | 5–12 |
| P3 | **챕터2**(4) — 발열 교과서화 + v5 검토 반영 | Ch2 보완본(발열 이론) | 13–18 |
| P4 | **코드 개정**(5) — LCO 양극 + 발열을 코드에 구현(Serena) | 확장 코드(양·음극+발열)·회귀 | 19–24 |
| P5 | **최종 동시 점검**(6) — 상호 충실도·내용 완성도 | 그래프 suite·피팅 추천·adversarial·교재 마감 | 25–30 |

## 5. Non-goals
- 병렬/동시 작업 X(순차). 사이즈/PSD 모델 재도입 X. v11/v12·이전 result·radius 산출 불변·원본 불가침. 새 챕터(Ch3·4) 신설 X(2장). 코드에 없는 내용을 **최종(P5)에서** 문건에 남기지 않음(P2·P3 는 P4 코드구현 전제로 이론 선기술 허용, P5 에서 정합 확정).

## 6. Phase 상세 (각 페이즈 = 9종 경쟁-체리픽 + 5-stage 루프)
> ★★**작업 방식 = 9종 경쟁-체리픽 (모든 페이즈, 7-02 사용자 확정)**. 근거: 몇 달간 의도 반영 실패를 이 방법 하나로 단번에 대폭 개선 — **토큰 비용 감수하고 가져갈 방향.** 코드·문건·분석 전부 적용.
> - **9 독립 드래프트**(3 Sonnet · 3 Opus · 3 Codex, 상호 통신 X — 각자 그 페이즈 산출물 *전체*를 독립 작성). [작업 세션들, **Agent 도구 · Workflow X**]
> - **검토1(별도 세션)** → 보완(supplement) → **검토2(별도 세션)** → master(Opus) **체리픽**(9개 장점 통합 = vN-10) → **adversarial 재검수(별도 세션)** → master **finalizer**(vN-11).
> - ★**검수(검토1·2·adversarial)는 작업/드래프트 세션과 반드시 다른 세션**(같은 세션 자기검사 = 같은 blind spot → 금지). refute + 가장약한1곳 + 빈통과금지.
> - **드래프트·체리픽·finalizer 가 따르는 품질 기준** = §2 물리화학 교재 기준 + 단위 완결성(orphan 0) + (코드면) Serena 심볼·흑연 회귀 0·코드근거 1:1.
> - **커밋 ~4회/페이즈**(master 전용): ①9 드래프트 ②검토1+보완 ③체리픽 vN-10 ④adversarial+finalizer vN-11.
>
> ★도서관 규약(지침 [[feedback_phase_execution_loop]]·[[feedback_plan_template_11sections]]):
> 1. **각 Phase 착수 = 세부 스텝 계획서** `plans/2026-07-01-v1010-P{n}-*.md`(그 페이즈의 9종 단계·세션 분담 구체화).
> 2. **페이즈는 순차**(병렬 X) — 페이즈 내부 9 드래프트만 목적적 병렬(경쟁-체리픽).
> 3. **9종 마일스톤마다 results 저장** `results/process/V1010_P{n}_*.md` — 컴팩션 복구점.
> 4. **Phase 종료 = result 의무 저장** + **ledger 갱신**. result 없이 다음 Phase 금지.
> - **주의점**: 추정/"기억으로는" 금지(직전 result 재정독) · 원본 불가침(v11·v12·radius) · commit/push 는 master 만(sub 금지).

### P1 코드 (Steps 1–4) — 기존 음극 코드 = Ch1 의 설명 앵커 확립
- Serena get_symbols_overview·find_symbol·find_referencing_symbols 로 **전체 플로우차트 맵**(입력→equilibrium→dqdv[분극·꼬리·히스]→curve, 각 함수 물리식). **조건 audit**: 음극 dQ/dV 피팅 함수로서 충족도·결함(가드·死코드·정합)·**피팅 파라미터 인벤토리**(n_j·U_j·Ω_j·dH_a·χ·L_V… 각 지배영역 초벌). Gate: 맵·audit 가 명령+증거로 완결.

### P2 챕터1 (Steps 5–12) — 코드 플로우차트 충실 교과서 + v9 LCO
- Ch1 ↔ 코드 step 1:1 coverage 매트릭스 → 누락 유도·다리 보완(물리화학 교재 형식·유도 식→식). v3~v9 교과서 요소 통합. **v9 LCO 검토분 타당성** 반영 + **LCO 이론 자체 갈고닦기**(dQ/dV·전자 엔트로피 — 코드 구현은 P4 예고, 문건엔 이론 기술). **9종 체리픽 산출**(§6). Gate: coverage 0누락·교재 형식·독자 자기완결·orphan 0.

### P3 챕터2 (Steps 13–18) — 발열 교과서 + v5
- Ch2 통계열역학→발열(가역 q_rev=−T·I·∂U/∂T 또는 T·ΔS 경로·비가역 옵션) 유도를 물리화학 교재 형식으로 — **발열 이론 자체 갈고닦기**(아직 미완성 인지). v5 검토분 타당성 반영. 코드 구현은 P4 예고. **9종 체리픽 산출**(§6). Gate: 발열 유도 완결·Ch1 과 정합·교재 형식.

### P4 코드 개정 (Steps 19–24) — LCO 양극 + 발열 구현 (★Serena)
- Serena insert_after_symbol/replace_symbol_body 로 **LCO 양극 dQ/dV(전자 엔트로피 포함) + 발열 모듈** 추가. ★**흑연 음극 거동 회귀 0 diff** 보장. P2·P3 이론과 1:1 구현. round-trip·면적·개형 검증. Gate: 회귀 0·각 도메인 면적보존·이론-코드 1:1.

### P5 최종 동시 점검 (Steps 25–30) — 상호 충실도·완성도
- 통합 그래프 suite(음극·양극·full cell·발열) 생성·정상작동 수치 확정 → **피팅 파라미터 관찰·추천**(지배영역·민감도·초기값·자유/고정 표) → §3 N회 가변-청크 adversarial 검수(물리·코드정합·그래프·교재 충실; refute)→master 직접 정정 → Ch1·Ch2 PDF 재빌드·교재 문체 점검·v1.0.10 폴더·INDEX 마감 → 종합 result + 버전 commit·push. Gate: **3대 무결 동시 충족**·CRIT/HIGH 0·코드에 없는 내용 0.

## 7. Implementation Interfaces
- **작업 방식 = 9종 경쟁-체리픽**(전 페이즈, §6 정의). 검수 = **작업/드래프트와 다른 세션**의 Agent adversarial(Workflow X·팝업 0).
- 코드 = **Serena 우선**(심볼 도구; 텍스트 치환·rename 금지). 검증 = 실제 실행·round-trip·회귀(추측 0).
- 커밋 = 9종 진행 중 **통상 4회/페이즈**(master 전용 · Anode_Fit 버전 자동 commit+push 규칙)·sub commit/push 금지.

## 8. Test Plan
- 코드: LCO·발열 추가 후 흑연 음극 회귀 0 diff·각 도메인 면적/개형 타당·guards·round-trip.
- 문건: 코드 step↔식 1:1·코드에 없는 서술(P5 기준) 0·v3~v9 유도 충실·**물리화학 교재 형식·타 전공 자기완결·안정객관 어투·독자평가 0**·orphan 0.
- 그래프: 음극 종·양극 개형·full cell·발열 곡선 정상·면적/부호/추세.
- 피팅: 파라미터별 지배영역·민감도 표.

## 9. Assumptions (기본값 — 이의 없으면 진행)
- BDD 양·음극 = 흑연 음극 + LCO 양극 dQ/dV 동시 피팅. LCO = 전자 엔트로피(Sommerfeld) 포함. 발열 = 가역 q_rev 곡선 우선·비가역 옵션. matplotlib·xelatex·Serena 가용.

## 10. Decision Gate
- 스코프·순서 합의 완료(§0) → **별도 DG 없이 GO 후 P1→P5 연속**. 정지 5조건(새 의존성/FAIL gate/사용자 stop/두 통제문서 모순/Decision 필요)만 정지.

## 11. Correction History
- 2026-07-01 v1: vertical-slice(동시) 구조로 초안.
- 2026-07-01 v2: 사용자 합의 반영 — **순차 6-페이즈**(병렬 철회), LCO·발열 코드구현=P4, **Ch2=발열 확정**(dependency_tree memory supersede·재질의 철회), 도서관 규약(페이즈별 result/ledger·plans INDEX) 명문화, 물리화학 교재 품질기준 3차 누적.
- 2026-07-02 v3(본): 사용자 7-02 피드백 — ① **순서 근거 명문화**(LCO·발열 이론 미완성 → 미완성 이론으로 코드 먼저 = 문제 → 코드→문건→코드 의도 설계, 재질의 X) ② **모든 페이즈 = 9종 경쟁-체리픽**(토큰 비용 감수, 몇 달 의도반영 실패를 단번에 개선한 입증된 방향) ③ **검수 = 작업/드래프트와 다른 세션**(같은 세션 자기검사 금지) ④ **커밋 = 9종 통상 4회/페이즈**(master 전용).
