# P3 세부 스텝 계획 — 챕터2 발열 교과서화 + v5 (9종 경쟁-체리픽)

> 마스터 §P3. result = `results/process/V1010_P3_ch2_RESULT.md`. 대상 = `docs/v1.0.10/graphite_ica_ch2_v1.0.10.tex`(통계열역학·가역발열). 앵커 = P1 result(코드 엔트로피항 dS_rxn·dS_a — **발열 함수 q_rev 부재 확정**)·Ch1(전자엔트로피·broadening 정합). **순서 코드→문건→코드: 발열 코드 구현은 P4, P3는 이론 갈고닦기(미완성 인지).**

## P3 산출물 (supplement)
1. **Ch2 ↔ 코드/Ch1 정합 매트릭스**: Ch2의 통계열역학(분배함수→점유분포→S_config/vib/electronic·섞임)·가역발열이 코드 엔트로피항(dS_rxn·dS_a)·Ch1 전자엔트로피와 정합하나 → 누락·과잉(코드에 없는 발열은 P4 예고)·Ch1-Ch2 중복/모순.
2. **★발열 이론 갈고닦기(중점)**: 가역열 q_rev = −T·I·∂U/∂T(Bernardi) 또는 T·ΔS 경로 유도를 물리화학 교재급으로 — 통계열역학 ΔS와 q_rev 연결, 비가역(I²R·η 과전압) 옵션. 부호·차원·극한 무결.
3. **v5 검토분 타당성** 반영 + v3 가역발열 survey 좋은 요소 통합.
4. 품질 = 마스터 §2(화학식·물리식 중심·유도 식→식·타 전공 자기완결·안정객관·독자평가 0).

## 9종 경쟁-체리픽 (P1·P2 동일 구조)
- **Step 1 — 9 드래프트**(3S·3O·3C, 독립): Ch2.tex + P1 result + Ch1.tex(정합) 정독 → supplement(정합 매트릭스 + 발열 이론 정련 + v5) → `V1010_P3_draft_<id>.md`. **커밋①**.
- **Step 2 — 검토1(별도 세션)** + 보완: 9 supplement 교차검증(발열 유도 물리 무결·통계열역학 정합·교재 형식·Ch1-Ch2 정합). refute+가장약한1곳. **커밋②**.
- **Step 3 — master 체리픽**(vN-10): 통합 supplement(Ch2.tex 삽입위치 명시). **커밋③**.
- **Step 4 — adversarial(별도 세션)** + finalizer: master 가 **Ch2.tex 실제 통합·재빌드** → `V1010_P3_ch2_RESULT.md`. **커밋④**.

## Gate (P3 종료)
- 발열 유도 완결(q_rev↔ΔS 연결·식→식)·Ch1과 정합(전자엔트로피·broadening 중복 0)·코드에 없는 발열은 P4 예고 명기·교재급·PDF 0-error.

## 주의
- 검수=작업과 다른 세션. commit master 전용. 추정 금지(코드·Ch1 줄 근거). Ch2 통계열역학 본체·기존 식 byte 보존(추가만). 발열 코드는 P4.
