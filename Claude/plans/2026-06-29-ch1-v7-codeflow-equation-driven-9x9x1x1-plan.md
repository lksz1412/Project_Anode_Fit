# Ch1 v7 — 코드 흐름(v11_final 클래스 진행) 정렬·수식 주도 문건 / 9+9+1+1 경쟁·체리픽 Plan

## Summary

**목표.** 흑연 음극 dQ/dV(ICA) Chapter 1 의 **v7 문건**을 만든다. v7 = **`Anode_Fit_v11_final.py`(20번째 코드 산출, 706줄)의 클래스 계산 진행 순서**를 척추로 삼아, *그 순서를 이해하는 데 실제로 필요한 물리식만* 그 순서대로 전개하는 문건이다. 형식은 **v5(수식 주도 — 산문 최소, 식 사슬이 논리를 운반)** 를 따르고, 배열은 **v6가 했던 "코드 흐름 정렬"** 을 *이번 최종 코드(v11_final)* 의 클래스 진행에 맞춰 다시 수행한다. 즉 한 줄 정의: **"v5 형식을 v6처럼 실제 코드에 맞게 어레인지하되, 대상 코드를 v11_final 로 갱신."**

**그림(figure)도 새로 작성한다.** 기존 v5/v6 그림 다수가 이해를 돕기는커녕 더 혼란스럽게 했다는 판단 — v7 은 기존 그림을 *수정·재사용하지 않고*, 각 에이전트가 **이해에 그림이 필요한 위치를 스스로 판단해 신규 작성·배치**한다(그림도 경쟁 대상). 그림 안 텍스트·라벨은 **영어(간단)만** — 한글은 렌더가 깨진다.

**왜.** (1) 코드(v11_final)와 이론 문건의 *식별자·식·진행 순서*가 1:1 정합하는 교육·근거 문건이 필요(회사 배포·Optuna 피팅 근거). (2) 직전 코드 작업에서 **9+9+1+1 경쟁→체리픽** 방식이 충전 꼬리 부호결함·보완 회귀를 표면화하며 좋은 결과를 냈다. 본 작업은 **그 방식이 코드뿐 아니라 교과서급 문건 작성에도 효과적인지 검증하는 테스트 베드**다(효과 확인 시 스킬화 검토).

**이번 계획 범위.** 공유 골격(v11_final 플로우차트) 작성 → 9종 독립 경쟁(v5만 제공) → 검토1 → 방향성-만 재지시 보완 9종 → 검토2 → Opus 체리픽 v7-10(v6 합류) → 재검수·보완 v7-11(최종) → 종합·메타평가. 산출 = 문건 11종(v7-01…v7-11) + 검토 4 + 종합 1. **커밋 4회**(9초본/9보완/10/11), ★**푸쉬 없음**.

## Current Ground Truth

**확인된 사실 (정독·실측 기반):**
- 활성 프로젝트 = `D:\Projects\Project_Anode_Fit` · git 브랜치 = `rb-rebuild-2026-05-30`(비-main → 커밋 가능). ★**푸쉬 금지**(Codex 월간 한도 소진 — 사용자 명시).
- **코드 원천**(목표 진행 순서): `Claude/results/v7-00_spine/Anode_Fit_v11_final.py` (706줄, 방금 BD_Display 임시폴더에서 복사 도입). 전 검증 PASS(부호·히스·극한·per-tr override 격리·원형 byte보존).
- **형식·내용 레퍼런스**: `Claude/docs/graphite_ica_ch1_Opus_v5.tex` (1883행, 수식 주도, §1.1–§1.17). `Claude/docs/graphite_ica_ch1_Opus_v6.tex` (1903행, v5 를 *구* 코드 흐름도 순으로 재조립 + V5RR·10회로 부호 교정 완료).
- **빌드 엔진**: **xelatex 2-pass**(kotex + D2Coding monofont + amsmath/tikz). v5/v6 공통 preamble.
- **참고문헌 단계 제공**(사용자 6-29 결정): **9종(Phase 1·3)에는 v5 만** 제공(자유도↑ 다양성 극대화) → **v7-10·v7-11(Phase 5·7)에서 v6 합류**(부호 교정·어레인지 선례 정합).
- 직전 코드 산출의 회귀 교훈: 보완 단계가 self-test 부재로 **신규 부호회귀**(−σ_d)를 유입했다 → 문건에서도 **부호 사슬**이 최우선 결함 클래스(Codex sign-review 선례: eq:eqcond/eq:logistic/eq:dxidV/eq:LqV/eq:tail/분극표).

**기존 결정·선행 gate:** v5 = V5RR 4라운드(R0–R3) 수렴본. v6 = 흐름도 재조립 + 10회 검토 수렴본. v3·v4 = v5/v6에 이미 흡수된 구 중간본 → **본 작업 참고문헌서 제외**(노이즈·토큰).

**미확인/미검독:** v5/v6 본문 전문은 master 아직 부분만 확인(섹션 골격·preamble). → Phase 0.1 에서 master 가 v11_final 전문 + v5/v6 섹션 매핑을 정독해 플로우차트의 노드↔식 대응을 확정(미검독은 Read Coverage 에 명시).

## Phase Range

| Chapter | Phase | 이름 | Steps | Gate(확인 가능 조건) |
|---|---|---|---:|---|
| A 준비·골격 | 0.1 | 인벤토리·코드/레퍼런스 정독 | 1–4 | v11_final 전문 정독 행범위 기록·곡선식 인벤토리 표 완성 |
| A | 0.2 | 플로우차트·공유 작가브리프 작성 | 5–9 | 플로우차트 노드 N개↔v11 메서드/식 1:1 매핑(missing=0)·AUTHOR_BRIEF 저장 |
| B ①9 경쟁 | 1.1 | 9종 독립 작성(v5만, 각 10회 자체검수) | 10–14 | 9 .tex 전부 xelatex 0-error·NOTE 자체 10R 로그·spine 노드 커버 |
| C 검토1 | 2.1 | 9종 교차 검토(청크·렌즈) | 15–18 | 버전별 결함표+점수·부호체크리스트 채움·PHASE2 저장 |
| D ②보완9 | 3.1 | 방향성-만 재지시 보완 9종 | 19–22 | 9 보완 .tex 0-error·검토1 결함 정정 매핑·신규회귀 self-체크 |
| E 검토2 | 4.1 | 보완 9종 재검토 | 23–25 | 보완 결함표·신규회귀 적발 여부 명시·PHASE4 저장 |
| F ③체리픽 | 5.1 | Opus 체리픽 v7-10(v6 합류) | 26–30 | v7-10 0-error·강점 출처 매핑표·v6 부호 정합 대조 |
| G ④재검수·보완 | 6.1 | v7-10 adversarial 재검수 | 31–33 | PHASE6 결함목록(빈통과 금지·refute 3)·보완 권고 |
| G | 7.1 | v7-11 최종 작성 + 정식 10회 | 34–39 | v7-11 0-error·10R 수렴(연속2R 0결함)·전 gate PASS |
| H 종합 | 8.1 | 종합 RESULT·ledger·메타평가 | 40–42 | RESULT 11항목·ledger 완료·경쟁법 효과 판정(스킬화 의견) |

## Non-goals

- **원본 불가침**: `graphite_ica_ch1.tex`(메인)·`_Opus_v4/v5/v6.tex`·`_Fable*.tex`·`Anode_Fit_v11_final.py`·`Codex/` 거울 **수정 금지**(읽기 전용 레퍼런스). v7 은 전부 신규 파일.
- **범위 외 물리 금지**: v11_final 의 곡선식 범위(아래 Implementation Interfaces 의 11식)만. Optuna·피팅 절차·새 물리(ch2 반속·ch3 발열 등)·§1.18 적층 준안정 미포함.
- **v6 prose 복붙 금지**(9종 단계엔 애초에 v6 미제공; 10·11 단계에도 v6 는 *부호·정합 대조용*이지 문장 복사원 아님 — 정독근거 의무).
- **기존 그림 재사용·수정 금지**: v5/v6 의 figure 를 복사·편집하지 않는다. v7 그림은 전부 신규(위치도 에이전트 자체 발굴). ★**그림 내부 텍스트 한글 금지 — 영어 ASCII 라벨만**(TikZ 노드·축·범례 전부). 장식용 덩그러니 그림 금지(앞 식 동기·뒤 본문 \ref 사용 = orphan 0).
- **푸쉬 금지**(월 한도). Workflow·권한팝업 도구 금지 → **Agent 도구만**. 단위마다 사용자 확인 팝업 금지(GO 후 무중단).
- 사용자 산출물(v5 등)을 "내가 만든 것"으로 허위 attribution 금지.

## Implementation Changes

생성할 것(전부 신규):
- **공유 골격**(이미 일부 생성): `Claude/results/v7-00_spine/Anode_Fit_v11_final.py`(완료) + `…/v11_flowchart.md`(Phase 0.2) + `…/AUTHOR_BRIEF.md`(Phase 0.2).
- **문건 11종**: `Claude/results/v7-01/ … v7-11/` 하위폴더. 각 폴더 = `v7-NN.tex` + 빌드산출(`.pdf/.log`) + `NOTE.md`(자체 검수 로그·Read Coverage). 보완본은 원본 보존 위해 같은 폴더에 `v7-NNb.tex`(초본 v7-NN.tex 비파괴).
- **검토 문건**: `Claude/results/PHASE2_v7_review9.md` · `PHASE4_v7_review9b.md` · `PHASE6_v7_review_v10.md`.
- **종합·추적**: `Claude/results/PHASE8_v7_FINAL_RESULT.md`(11항목) + `Claude/results/V7_9x9x1x1_LEDGER.md`(12-col).
- 본 계획서: `Claude/plans/2026-06-29-ch1-v7-codeflow-equation-driven-9x9x1x1-plan.md`.

## Phase 0.1 — 인벤토리·코드/레퍼런스 정독 (Steps 1–4)

- **Step 1** master 가 `Anode_Fit_v11_final.py` **전문 정독**(head→tail, 행범위 기록). 클래스 메서드 호출 순서(`__init__` seed → `curve()/dqdv()` → 전이 루프 내부 계산 단계 → `equilibrium()`)를 추출.
- **Step 2** 곡선식 인벤토리 표 작성: v11 이 실제 쓰는 물리식 목록(아래 11식)을 메서드·코드 라인·기호와 1:1로. 각 식이 코드 진행 어디서 호출되는지.
- **Step 3** v5·v6 의 **섹션 골격·식 라벨 목록** 확보(grep), 각 곡선식이 v5/v6 어느 절·eq 라벨에 있는지 매핑(내용 출처 추적). 전문 정독은 작가 에이전트 몫(master 는 매핑까지).
- **Step 4** 입력 = 위 3 산출. **Gate**: 곡선식 인벤토리 표(11식 × {메서드, 코드행, v5 eq라벨}) 완성·missing=0. Read Coverage 기록.

입력: v11_final.py·v5.tex·v6.tex. 산출물: 인벤토리 표(Phase 0.2 의 flowchart·brief 에 삽입). 중단조건: v11 식과 v5 내용이 매핑 불가한 식 발견 시 → 그 식의 출처를 Open Issues 등록(체리픽 단계 v6 합류로 보강). 다음 phase 조건: Gate PASS.

## Phase 0.2 — 플로우차트·공유 작가브리프 작성 (Steps 5–9)

- **Step 5** **v11_final 클래스 진행 플로우차트**(`v11_flowchart.md`) 작성: 노드 = 계산 단계, 엣지 = 데이터 의존(예: V_app→V_n→{U_j(T), branch center}→w→ξ_eq→dξ/dV→L_q→L_V→causal tail(충전 역전)→ΔH_a^eff→ΣQ_j→dQ/dV). 코드와 대조 검증(노드↔메서드 라인).
- **Step 6** 플로우차트를 **9 에이전트 공유 척추(spine)** 로 고정: 모든 v7 은 이 진행 순서를 절 순서로 따른다(배열 통제 → 경쟁은 *식 선택·유도 깊이·서술*에서).
- **Step 7** `AUTHOR_BRIEF.md` 작성: ① 역할 고지(작업 sub·경계·범위밖 금지·허위 attribution 금지) ② 목표(v7 정의 한 줄) ③ spine(플로우차트) ④ 형식 규범(v5 수식 주도 + 교과서 문체 규범: 괄호 전보체 금지·보편식 먼저·절 도입/마무리 다리·피팅 실행력) ⑤ ★부호 사슬 체크리스트 ⑥ 제약(v5만 참고·원본 불가침·xelatex 빌드·복붙금지·정독근거) ⑦ 자체 검수 규약(절별/수식별 루프 + **자체 10회 가변-청크·렌즈 로테이션**, NOTE.md 로그) ⑧ **그림 규약**: 기존 그림 미사용, 이해 보조가 필요한 지점을 자체 발굴해 신규 figure 작성·배치(**TikZ 우선** — v5/v6 의 tikz/positioning 재사용, xelatex 정합). **라벨·축·범례·노드 텍스트 영어 ASCII 만**(한글 0). 각 그림은 앞 식이 동기·본문이 \ref 로 사용(orphan 0). 캡션 한글 prose 는 허용(본문과 동일), 단 그림 *내부* 그래픽 텍스트만 영어.
- **Step 8** 빌드 템플릿 확정: v5 preamble(kotex/D2Coding/amsmath/tikz) 재사용, `xelatex -interaction=nonstopmode` 2-pass.
- **Step 9** **Gate**: 플로우차트 노드 ↔ v11 메서드/식 1:1 매핑 missing=0(체크표). AUTHOR_BRIEF 7항 완비. 저장 확인.

## Phase 1.1 — 9종 독립 작성 (Steps 10–14)

- **Step 10** 모델 배정: **v7-01,02,03 = Sonnet** · **v7-04,05,06 = Opus** · **v7-07,08,09 = Codex Rescue**. 9건 **상호 무통신**.
- **Step 11** 각 에이전트(Agent 도구) 프롬프트 = AUTHOR_BRIEF + 입력(**v5.tex + v11_final.py + 플로우차트**, ★v6 미제공) + 산출 지정(`v7-NN/v7-NN.tex` + `NOTE.md`). 각자 **절별/수식별 단위 구성 루프 + 자체 10회 검수**까지 완주(토큰 각오). **그림은 자체 판단 위치에 신규 TikZ(영어 ASCII 라벨)로 작성·배치** — 기존 그림 미사용.
- **Step 12** Claude 6건(Sonnet·Opus) 병렬 디스패치. **Codex 3건은 단계 구동**(한 번에 안 시키고 진행 모니터·단계 지시 — 과거 행걸림 회피).
- **Step 13** 각 산출 수집 후 master 가 **xelatex 2-pass 빌드**·NOTE 자체검수 로그(10R·수렴) 존재 확인.
- **Step 14** **Gate**: 9 .tex 전부 xelatex **0-error**·PDF 생성·NOTE 에 10R 로그·spine 노드 전부 절로 커버. → **커밋 #1 "v7 9종 초본"**(푸쉬 X). ledger 9행.

입력: AUTHOR_BRIEF·v5·v11·flowchart. 산출: v7-01…09. 중단조건: Codex 행걸림·빌드 반복실패 시 그 1건 격리·사유기록(나머지 진행). 다음: Gate PASS 후 검토1.

## Phase 2.1 — 9종 교차 검토 (Steps 15–18)

- **Step 15** 검토 Agent 다수 디스패치(검수 sub·코드/문건 변경 금지). 분량 큰 .tex 는 **~500행 청크 분할**(통째 금지). 라운드 렌즈 = {①구조·spine 정합 ②물리·**부호 사슬** 적대검산 ③G-follow(앞 식만으로 따라가나) ④G-usable(이 문건으로 피팅 코드 작성 가능한가) ⑤완결성 orphan ⑥시각·**그림 효과**(PDF 렌더 — 그림이 실제 이해를 돕나, 신규·영어-only 인가, 혼란 유발 그림 적발)}.
- **Step 16** 각 검토는 **refute mandate + 가장 약한 1곳 필수 지목 + 빈 통과 금지**. 버전별 장점·단점·확정결함(행근거)·점수.
- **Step 17** master 삼각검증·통합 → 9종 **장단점 매트릭스 + 부호 체크리스트(전 버전)** + 체리픽 후보 베이스 식별.
- **Step 18** **Gate**: 9종 전부 6렌즈 커버·부호결함 유무 버전별 명시·`PHASE2_v7_review9.md` 저장. 다음: 보완.

## Phase 3.1 — 방향성-만 재지시 보완 9종 (Steps 19–22)

- **Step 19** 각 *원* 모델에 재지시: **구체 tex 제공 금지 — 방향성만**(검토1이 짚은 약점의 *방향*만, 자율로 고치게). 입력은 여전히 v5(+자기 초본). v6 미제공 유지.
- **Step 20** 산출 = `v7-NN/v7-NNb.tex`(초본 비파괴). 각자 보완 후 **자체 재검수**(특히 신규 부호회귀 self-test — 직전 코드 v09b 교훈).
- **Step 21** master 빌드·보완 정합(검토1 결함↔보완 매핑) 확인.
- **Step 22** **Gate**: 9 보완 .tex 0-error·검토1 결함 정정 매핑·신규회귀 self-체크 기록. → **커밋 #2 "v7 9종 보완"**(푸쉬 X). ledger 갱신.

## Phase 4.1 — 보완 9종 재검토 (Steps 23–25)

- **Step 23** 검토2 디스패치(청크·렌즈 로테이션, 검토1과 *다른* 청크 스킴). 보완이 **새 결함을 유입했는지** 1급 점검(부호회귀·orphan 신설·흐름 단절).
- **Step 24** master 통합 → 보완본 점수·무결함 후보 + 체리픽 플랜(어느 버전의 어느 절·식을 취하고 무엇을 배제).
- **Step 25** **Gate**: 보완 결함표·신규회귀 적발 여부 명시·체리픽 플랜 확정·`PHASE4_v7_review9b.md` 저장. 다음: 체리픽.

## Phase 5.1 — Opus 체리픽 v7-10 (v6 합류) (Steps 26–30)

- **Step 26** ★**여기서 v6 합류**: master(Opus) 가 보완본 9종의 장점만 체리픽하되, 이제 **v6 를 부호 교정·어레인지 선례 대조용**으로 함께 본다(복붙 아님). 베이스 = 검토2 무결함 후보.
- **Step 27** 절별/수식별 루프로 v7-10 조립: 각 절·식·**그림**을 최적 출처에서 취사 + master 직접 정합. 출처 매핑표(절·그림↔기여 버전) 기록. 그림도 가장 이해를 돕는 신규본을 채택(혼란 그림 배제).
- **Step 28** **부호 사슬을 v6 와 전수 대조**(v6 가 10회로 고친 부호가 v7-10에 정합한지). 불일치 시 v6 기준 채택·사유.
- **Step 29** 빌드·spine 커버·완결성 확인.
- **Step 30** **Gate**: v7-10 0-error·강점 출처 매핑표·v6 부호 정합 대조표. → **커밋 #3 "v7-10 체리픽"**(푸쉬 X). ledger 갱신.

## Phase 6.1 — v7-10 adversarial 재검수 (Steps 31–33)

- **Step 31** 재검수 Agent(Opus·청크 분할): v7-10 을 **한 번에 OK 금지** — 결함을 찾는다. 렌즈 = 부호·G-follow·G-usable·완결성·시각 + *체리픽 이음새*(취합 경계의 기호·표기 불일치).
- **Step 32** refute mandate·가장 약한 3곳·빈통과 금지. 확정결함 목록(행근거) + v7-11 보완 권고.
- **Step 33** **Gate**: `PHASE6_v7_review_v10.md` 저장(확정결함 0 여부 명시). 다음: 최종.

## Phase 7.1 — v7-11 최종 작성 + 정식 10회 (Steps 34–39)

- **Step 34** master 가 PHASE6 권고를 v7-10에 반영해 `v7-11/v7-11.tex` 작성(v7-10 비파괴, 최종본).
- **Step 35** ★**정식 10회 가변-청크 검수**(연속 2R 0결함 수렴까지·하한 10): 매 라운드 청크 스킴 전환(통독/절/식/라인/도메인) + 렌즈 로테이션(문체→흐름→코드시뮬→물리적대→완결성→해설→그림→검증→실용→시각(pdftoppm)→실행실증). coverage missing=0.
- **Step 36** 매 라운드 Agent 병렬(refute+약한1곳+빈통과금지) → master 삼각검증·**직접 수정**.
- **Step 37** **피팅 실행력 검증**(G-usable 1급): "이 문건만으로 v11 곡선을 재현하는 코드를 짤 수 있나" — 식·기호·초기값·진행이 닫혔는지 코드-시뮬 점검.
- **Step 38** 최종 빌드·전 gate 재확인.
- **Step 39** **Gate**: v7-11 0-error·10R 수렴 로그·spine 커버·부호 체크리스트 전건 PASS·G-usable PASS. → **커밋 #4 "v7-11 최종"**(푸쉬 X). ledger 갱신.

## Phase 8.1 — 종합·ledger·메타평가 (Steps 40–42)

- **Step 40** `PHASE8_v7_FINAL_RESULT.md`(11항목): 11종 산출·검토 경위·v7-11 근거·부호/완결성/실행력 게이트 증거·Read Coverage·Open Issues·Next.
- **Step 41** `V7_9x9x1x1_LEDGER.md` 완료 갱신(12-col, 11+검토 행).
- **Step 42** ★**메타평가**(테스트 베드 본 목적): 경쟁→체리픽이 *문건*에서도 효과 있었나 — (a) 9종 다양성이 결함/관점을 표면화했나 (b) 보완이 회귀를 냈나 (c) v7-11 이 임의 단일 버전보다 나은가, 근거와 함께. **스킬화 권고/보류** 의견. → 사용자 보고.

## Implementation Interfaces

**(1) v11_final 곡선식 = v7 가 다룰 물리식 (spine 노드, 진행 순서):**
1. 분극 전위 `V_n = V_app − s_I|I|·R_n` (전이별 R_n; 3×3 분극).
2. 평형 전위 `U_j(T) = (−ΔH_rxn + TΔS_rxn)/F` (또는 직접 `U`).
3. 히스 분기 중심 `U^branch = U_j + ½·σ_d·h_η·γ·ΔU_hys(T,Ω)` (`func_U_branch`).
4. 스피노달 갭 `ΔU_hys = func_dU_hys(T,Ω)`; 극한 Ω≤2RT→0.
5. 폭 `w = nRT/F` 또는 유효폭 `w^eff = (RT/F)(1−Ω/2RT)` (per-tr `use_w_eff`).
6. 평형 점유 `ξ_eq = 1/(1+exp[−s(V_n−U)/w])` (`func_ksi_eq`).
7. 피크 형상 `dξ/dV = s·ξ(1−ξ)/w` (방전/충전 부호).
8. 동역학 진폭 `L_q = func_L_q(ΔH_a^eff,ΔS_a,T)` → `L_V = |dV/dq|·L_q`.
9. ★인과 저역통과 꼬리(`_causal_lowpass`) — **충전(s<0)은 격자 역전**(`[::-1]`), 차분 부호로 충전 dQ/dV 양수(방전 거울).
10. 유효 엔탈피 `ΔH_a^eff = ΔH_a − χ_d·Ω` (`func_dH_a_eff`); 방향 `χ_d`(충전 χ→1−χ).
11. 합산 `Σ_j Q_j → dQ/dV`. (기준선 `equilibrium()` = γ 미반영 정적 평형.)

**(2) 작가 에이전트 프롬프트 골격(Phase 1.1·3.1):** 머리 = 4-세션 고지(작업 sub·경계·범위밖 금지·허위 attribution 금지). 본문 = AUTHOR_BRIEF + 입력파일 + "v11 클래스 진행을 절 순서로, 각 절은 해당 식을 *유도*(서술 아님)하고 코드 식별자와 1:1, v5 수식 주도 형식, 절별/수식별 루프, 자체 10회(청크·렌즈 전환), NOTE.md 에 Read Coverage·라운드별 결함추이". 산출 경로 고정.

**(3) ★부호 사슬 체크리스트**(전 검토·작성 공통): eq U_j(T)=(−ΔH+TΔS)/F · ξ_eq 의 s(V−U) 방향 · dξ/dV 부호 · ∂lnL_q/∂V<0 · 꼬리 충전 역전·차분 부호 · 분극 +s_I|I|R_n. *방전 시 V↑→탈리튬화↑, 충전 dQ/dV = 방전 거울(양수)* 와 정합.

**(4) 검토 Agent 인터페이스:** 청크 ~500행, 렌즈 로테이션, refute mandate+가장 약한 1곳+빈통과 금지, 결함 = {식별자/행, 무엇이 틀림, 옳은 형태, 심각도}.

**(5) Result 11항목·Ledger 12-col** = 메모리 양식 그대로.

**(6) 그림(figure) 인터페이스:** 신규 figure 만(기존 v5/v6 그림 미사용·미수정). 우선 **TikZ**(v5/v6 preamble 의 tikz/positioning 재사용 → xelatex 정합·벡터). 텍스트·라벨·축·범례·노드 = **영어 ASCII 만**(한글 codepoint 0). 위치 = 에이전트 자체 판단(이해 보조가 실제로 필요한 지점) — 각 그림 앞 식이 동기, 본문이 `\ref{}` 로 사용(orphan 0). 후보 그림 예: 분극 사슬 모식도(V_app→V_n), ξ_eq 로지스틱·dξ/dV 피크, 히스 분기(방전/충전 중심 이동), 인과 꼬리(충전 격자 역전), dQ/dV 합산 흐름. **그림 내부 그래픽 텍스트는 영어**, 캡션 prose 는 본문과 동일(한글 허용).

## Test Plan

- **빌드**: 각 버전 `xelatex -interaction=nonstopmode` 2-pass → exit 0·`.log` 에 Undefined/Missing/Overfull-error 0·PDF 페이지수 ≥ 기대.
- **Spine 커버리지**: 플로우차트 11노드가 각 문건의 절/식으로 모두 등장(노드→eq라벨 매핑표, missing=0).
- **식 완결성**: 11식 전부 *유도 포함*(단순 진술 금지) — 각 식 앞 동기·뒤 사용(orphan 0).
- **부호 사슬**: (3) 체크리스트 전건, 코드(v11) 동작과 대조(방전/충전 거울).
- **자체 검수 로그**: NOTE.md 에 ≥10R·청크 스킴 변화·렌즈 로테이션·연속 2R 0결함 수렴·Read Coverage.
- **교차 검토 커버리지**: PHASE2/4 가 9종을 청크 분할로 전수(통째 위임 금지 준수).
- **G-usable(피팅 실행력)**: v7-11 만으로 v11 곡선 재현 코드 설계가 막힘없이 닫히는지 코드-시뮬.
- **그림**: 신규 figure 가 xelatex 렌더 정상(pdftoppm 시각 판독)·**그림 환경 내 한글 codepoint 0**(grep `figure`/`tikzpicture` 블록)·각 그림 본문 `\ref` 사용(orphan 0)·"혼란 유발" 그림 부재(검토 시각 렌즈).
- **최종 10회**: v7-11 수렴(연속 2R 0결함).

## Assumptions

- v5 preamble(kotex/xelatex)이 9 에이전트 환경에서 동일 빌드된다고 가정(불성립 시 Phase 0.2 에서 최소 preamble 고정).
- v11_final 의 곡선식 11개가 v7 범위의 *완전한* 집합이라고 가정(Phase 0.1 정독으로 검증; 누락식 발견 시 인벤토리 표 갱신).
- v5 의 물리·부호가 대체로 옳다고 가정하되, **v6 가 교정한 부호가 우선**(Phase 5.1 대조). 9종 단계의 부호결함은 검토1/2가 잡는다고 가정(다양성 효과 검증 대상).
- 각 에이전트의 자체 10회가 균일 품질이 아닐 수 있다고 가정 → 메타 검토1·2 + 최종 10회가 안전망.

## Correction History

- 본 계획은 직전 BD_Display 코드 작업의 `…-9x9x1x1-orchestration-plan.md`(코드용)을 *문건용*으로 이식한 1차본. 차이: (a) 산출=LaTeX 문건(xelatex 게이트) (b) **각 에이전트도 자체 10회**(코드판은 메타 검토 위주였음 — 사용자 6-29 명시) (c) **참고문헌 단계 제공**(9종 v5만 → 10·11 v6 합류, 사용자 6-29 결정) (d) spine = v11_final 클래스 진행 플로우차트(코드판은 식 구현이 목표였음).
- v3·v4 참고문헌 제외 결정(v5/v6 흡수분 — 노이즈·토큰 회피).
- 이전 대비 개선점: 부호 사슬을 1급 게이트로 승격(코드판에서 부호결함·회귀가 반복된 교훈) + 보완 단계 신규회귀 self-체크 명문화(v09b 교훈).
- **그림 재작성 추가(사용자 6-29)**: 기존 v5/v6 그림이 이해를 저해(혼란 유발)한다는 판단 → v7 은 기존 그림을 수정·재사용하지 않고 **신규 그림을 자체 판단 위치에 작성**(그림도 경쟁 대상·체리픽 대상). 그림 내부 텍스트는 **영어 ASCII 만**(한글 렌더 깨짐). 시각·그림 효과 렌즈를 검토에 1급 추가.
