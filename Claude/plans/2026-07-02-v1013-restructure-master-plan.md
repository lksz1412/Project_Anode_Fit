# Anode_Fit v1.0.13 — 통계역학-first 재구조화 + LCO 후방 통합 + 수식-주도 문체 회복 + 문건-코드 루프 Master Plan

> 본 문서 = **마스터 플랜**(전 Phase 조망·Steps 1–45 누적). 페이즈별 세부계획서는 각 Phase 착수 시 `Claude/plans/2026-MM-DD-v1013-P<n>-*-plan.md` 로 별도 작성. step별 작업이력 = `Claude/results/`, 최신 산출물 = `docs/v1.0.13/`, 페이즈 끝마다 커밋+푸쉬.

## Summary

사용자 지적 7건(2026-07-02 야간)을 반영하는 v1.0.13 재구조화:
① 흑연/LCO 혼재 배치 해소(흑연 완결 → LCO 전부 후방) ② LCO·흑연 공통 산문 과다 — **v6 수식-주도 원칙("수식만 읽어도 대부분 이해") 회복** ③ eq 1.85/1.86 overfull 정리 ④ 약자(MIT 등) 첫 출현 원어 병기 + 전문 용어 일본식 번역어 → 영어 원어(서술 베이스는 한글 유지) ⑤ **문건을 통계역학에서 시작해 거시 물리로 확장하는 구조로 전환**(사용자 확정 — Ch2 가 이미 분배함수 시작이라 두 챕터 정합; 통계역학 배경은 미수강 독자 기준으로 상세히) ⑥ 코드에 없어도 수식 전개·개념 도입에 필요한 내용은 서술 ⑦ **문건-코드-문건-코드 교대 루프**(사용자 확정): 문건은 코드 플로우차트의 배경 문건으로 충실해야 하고, 코드는 문건의 물리·화학 수식 논리를 실제 반영해야 함 — 한쪽만 작성 금지, Part 완성마다 코드 대응 pass 를 끼워 양방향 충실도를 그때그때 닫는다.
물리 결과식·부호·수치·v1.0.12 확정 판정(verify10, f=+σ_d 등)은 **전부 불변** — 바뀌는 것은 구조·순서·문체·표기 + 문건 정합에 직결되는 코드 정합분.

**방식(사용자 확정, 2026-07-02)**: 앞선 시행(v1.0.12 N=10)의 주의점·방법론·지침은 전부 승계하되 **병렬수만 조절** — 작성 경쟁 = **N=6(Sonnet 2·Codex 2·Fable 2)**, Fable 체리픽(가중 3·채택분 전량 재유도 재확인) 유지. **검수 = 이종 N종 union 폐지**, 기존 **10회 가변-청크 검수(매 라운드 청크 스킴·렌즈 전환, 수렴=연속 2R 확정결함 0)를 Fable 단독으로** 적용. 전 시행의 채택 매트릭스·결함 사례(교훈 카드)를 작성 sub 입력으로 주입해 병렬수 축소를 보완한다.
**★이미지 경쟁 복원(사용자 지적)**: 오리지널 9종 체리피킹 방법론대로 — 각 드래프트는 **설명에 필요하다고 판단하는 figure(TikZ 코드 또는 생성 스크립트+PNG)까지 각자 제작해 제출**하고, 체리픽 단계에서 **이미지도 함께 체리픽**한다(v1.0.12 P4.3 은 문안만 경쟁 — 이번에 복원). 세부 = Interfaces "figure 경쟁·체리픽 양식".

## Current Ground Truth

- baseline = `docs/v1.0.12/`(커밋 `ea4c169`, main 푸쉬 완료): Ch1 41p·Ch2 14p·0-err·ref 경고 0. 물리 판정 확정분 = `results/process/V1012_P43_verify10.md`.
- **LCO 혼재 실측**(7개 지점 산재): `sec:lco-map`(L301, N0 내)·`sec:lco-center`(L476, N2 내)·`sec:lco-hys`(L752, N3 내)·`sec:lco-electronic`(L1141, 독립 절)·`sec:lco-peak`(L1416, N6 내)·`sec:lco-decomp`(L1970)·`sec:lco-code`(L2064, N9 내).
- **overfull 실측**: eq 1.85 = `eq:lco-U1V`·1.86 = `eq:lco-plugin`(둘 다 `sec:lco-code`, p.37 — 한 줄 수식이 본문 폭 초과). 문서 전체 Overfull 전수 스캔은 **미실시**(Phase 4 게이트).
- **MIT 첫 출현** = L319(`sec:lco-map`) "절연체$\to$금속 천이(MIT)" — 영어 원어(metal–insulator transition) 미병기. 용어 전수 목록 **미작성**(Phase 1 산출물).
- **ξ_eq 유도 현행 구조**: N4/N5(L953)에서 열역학 경유 유도 후 L1066 분포 관점 후행 — grand canonical 형식이 뒤늦게 등장(지적 ⑤의 근거). Ch2 는 이미 단일 자리 2-상태 분배함수 `eq:Z1`(L126) 시작.
- **문건-코드 양방향 충실도 현황**: FABLE_AUDIT_03(코드 적합성) = 24 boxed 전부 대응·orphan 0·확정 결함 0 — 단 **일회성 최종 감사**였음. 코드 측 이월 부채(문건 논리 대비): `_direction_to_sigma` 전극 인지 부재(방향 규약 F-2 와 불일치 잠복)·전자항 dict 가 T1 아닌 중간 dict 에 배정(문건 표와 불일치, tier-C 시연)·다온도 T² 동결 근사·ν 기본값. 루프(⑦)가 이를 Part 단위 상시 게이트로 전환.
- v6 수식-주도 원칙 = 사용자 재확인 유효. 절별 산문/수식 비율 정량 실측 **미실시**(Phase 1).
- 문체 규범 정합: `feedback_anode_fit_textbook_style`(완결 문장·다리 의무)은 유지하되 문장 **수**를 줄이는 방향으로 v6 원칙과 결합(수식이 서사의 골격, 산문은 짧은 완결 문장 다리).

## Phase Range

| Phase | 이름 | Steps | 비고 |
|---|---|---|---|
| 1.1 | 설계 확정(구조 맵·용어표·산문 예산·코드 대응 기준표) | 1–5 | master 직접 |
| 2.1 | 문건: Part 0 통계역학 기초 신설 | 6–11 | 신설 블록 = N=6 경쟁(S2·C2·F2) |
| 2.2 | 문건: Part I 흑연부 재배열·수식-주도 압축 | 12–17 | 절별 루핑 |
| 2.3 | **코드 루프 A**: Part 0·I ↔ 코드(흑연 경로) 양방향 대응 | 18–20 | 흑연 회귀 13/13 불변 필수 |
| 3.1 | 문건: Part II LCO 후방부 재조립·압축 | 21–26 | 절별 루핑 + overfull 분할 |
| 3.2 | **코드 루프 B**: Part II ↔ 코드(LCO 경로) — 정합 정정·확장 | 27–30 | 의도 변경 = ledger 재베이스라인 |
| 4.1 | 용어·약자·overfull 전수 정정 | 31–34 | 문맥 정독 치환(기계 치환 금지) |
| 5.1 | 검수 — Fable 10회 가변-청크(수렴까지) + finalizer | 35–40 | 물리 불변·구조·G-follow·수식-주도·**상호충실도** |
| 6.1 | Ch2 정합·가이드·샘플·마감 | 41–45 | result·ledger·INDEX·커밋+푸쉬 |

## Non-goals

- 물리 결과식·부호·수치·확정 판정(verify10 의 f=+σ_d·F-1·B-3 등) 변경 X. 새 물리 개념 무근거 도입 X(⑥은 전개에 필요한 기초·다리의 서술 허용이지 신규 물리 아님). 코너케이스 메인 승격 금지.
- `docs/v1.0.12/` 이하 기존 폴더 전부 불가침 — v1.0.13 은 새 폴더 증판.
- **코드 변경은 루프 게이트 안에서만**: 흑연 경로 회귀 13/13 은 절대 불변. LCO 경로의 의도적 변경(dict 재정렬 등)은 변경 전후 출력 대조 + ledger 재베이스라인 기록 없이 금지. 루프 범위 밖 코드 확장(다온도 T² 해제·ν 상향·Ω round-trip 배정 등 실데이터 필요분)은 피팅 단계로 계속 이월 — Decisions 4.
- Ch2 대개편 X — Part 0 참조화·용어 정책·참조 갱신만.

## Implementation Changes

- 신규 `docs/v1.0.13/`: ch1/ch2 tex·`Anode_Fit_v1.0.13.py`(리네임 승계)·FITTING_GUIDE·demo/plot/test/sample 승계.
- Ch1 신설 **Part 0 — 통계역학 기초**: ensemble·미시상태·Boltzmann 인자·분배함수·화학퍼텐셜의 의미(미수강 대학원생 눈높이 배경) → grand canonical 단일 자리 점유 → lattice gas + mean-field → regular solution μ(θ) → 거시 열역학(G·Nernst) 연결. ξ_eq logistic 이 여기서 자연 유도.
- Ch1 **Part I — 흑연 음극**(N0–N9, LCO 0접촉) / **Part II — LCO 양극**(도입[전극-중립·방향 규약 일원화]→map→center→hys→electronic→peak→decomp→code).
- **코드(루프 산출)**: (A) 문건↔코드 양방향 매핑 표 현행화 + 주석·docstring 섹션 참조 동기화 (B) `_direction_to_sigma` 전극 인지 확장(전극 타입 인자 → 탈리튬화 부호 환산; 기존 호출 시그니처 보존) + LCO 전자항 dict 를 T1(MIT, 최고 x·최저 V)로 재정렬(tier-C 시연값 정합) — Decisions 4 기본값.
- 산출 문서: `V1013_TERMS_POLICY.md`·`V1013_STRUCTURE_MAP.md`·`V1013_CODE_MAP.md`(양방향 매핑 표)·`V1013_EXECUTION_LEDGER.md`·Phase별 result.

## Phase 1.1 — 설계 확정 (Steps 1–5)

- **S1** 구조 맵: 신규 절 순서·이동 라벨 전수 표(라벨 이름 불변)·`\ref`/`\eqref` 참조 영향 전수 실측 → `V1013_STRUCTURE_MAP.md`.
- **S2** 용어·약자 전수 감사표 → `V1013_TERMS_POLICY.md`(양식은 Interfaces). 원칙: 서술 베이스 한글 + 전문 명사 영어 원어, 약자 첫 출현 원어 병기 + 신개념 한 줄 물리 설명.
- **S3** 산문 예산: 절별 산문 문장수/수식수 실측 → 절별 감축 목표 추가 기록.
- **S4** 코드 대응 기준표 초기화: v1.0.12 boxed 식 전수 ↔ 코드 함수/분기 매핑(AUDIT_03 승계·현행화) → `V1013_CODE_MAP.md`. 문건-만·코드-만 항목에 지위 라벨(동결 근사/시연값/미구현 과제) 부여.
- **S5** gate: 네 산출물 존재·참조 카운트 aux 일치·CODE_MAP orphan 0(지위 라벨 포함). 중단 조건: 라벨 이동으로 Ch2 교차 참조가 깨지는 구조 모순 발견 시 STOP·Correction History 재baseline.

## Phase 2.1 — 문건: Part 0 통계역학 기초 신설 (Steps 6–11)

- **S6** 원천 정독: Ch2 `eq:Z1` 절·Ch1 N4/N5(L918–1139)·`sec:dist` — 중복·승계 경계 확정.
- **S7–S9** N=6 경쟁 작성(Sonnet 2·Codex 2·Fable 2, 무통신 독립 — 신설 블록 한정): 배경 상세·양자 불요·수식-주도(산문 예산 준수)·거시 확장 다리 포함. **각 드래프트는 설명에 필요한 figure 까지 자체 판단·제작해 제출**(Interfaces 양식 — Part 0 후보 예: 단일 자리 점유 vs 전위 곡선·lattice gas 이중웰/μ(θ) 개형·ensemble 개념도). **전 시행 교훈 카드를 각 sub 머리에 주입** — 병렬수 축소 보완(사용자 확정 취지). Codex 는 스텝별 구동+지속 모니터링(한번에 주고 대기 금지).
- **S10** Fable 체리픽(가중 3·동급 충돌 시 Fable 우선) + master 재유도 검증(부호·차원·극한·detailed balance) — 채택분 전량 독립 재확인, 드래프트 결함 계승 스캔(전 시행 F-1 계승 사례 재발 방지). **figure 도 체리픽 대상**: 물리 정확성(좌표·개형을 식으로 검산)·설명 기여도·문서 스타일 정합(기존 tikzpicture 관례)·본문-캡션 정합(F-4형 모순 방지)으로 채택/기각/혼합 판정.
- **S11** gate: ξ_eq logistic 이 Part 0 안에서 완결 유도·Ch2 Z1 서술 중복 0(참조 대체)·산문 예산 준수.

## Phase 2.2 — 문건: Part I 흑연부 재배열·압축 (Steps 12–17)

- **S12–S16** 절별 루핑(N0→N9): LCO 절 제거(Part II 이동 예약)·N4/N5 를 Part 0 기반 재접속(유도 중복 삭제·참조화)·산문 다이어트·절 도입/마무리 다리 유지.
- **S17** gate: 흑연부 lco-* 참조 잔존 0(전방 참조 금지)·중간 빌드 0-err·물리 불변 수치 grep 일치.

## Phase 2.3 — 코드 루프 A: Part 0·I ↔ 흑연 경로 (Steps 18–20)

- **S18** 문건→코드: Part 0·I 의 전 boxed 식·유도 결론이 코드 흑연 경로(함수·분기)와 1:1 인지 CODE_MAP 갱신 — 문건이 새로 명시한 지위(동결·근사·모델 선택)가 코드 주석에 반영됐는지.
- **S19** 코드→문건: 코드에 있으나 문건 설명이 빠진 흑연 경로 요소(가드·분기·기본값) 역점검 — 필요 시 문건 측 백필(플로우차트 배경 문건으로서의 충실성).
- **S20** gate: CODE_MAP 흑연 구간 orphan 0·주석 동기화 커밋·**흑연 회귀 13/13 불변**(코드 동작 무변경 확인). 위반 시 이 Phase 안에서 해소 후 진행.

## Phase 3.1 — 문건: Part II LCO 재조립·압축 (Steps 21–26)

- **S21** Part II 도입 신설: 전극-중립 논리·방향 규약(F-2 한정·FITTING_GUIDE §0 승격)·MSMR 위치 예고 일원화(현행 산재 서술 통합) — 신설 블록이므로 **N=6 경쟁+체리픽(figure 포함)** 파이프라인 적용(예: 방향 규약을 닫는 충·방전↔탈리튬화 대응 개념도).
- **S22–S25** 절별 루핑(map→center→hys→electronic→peak→decomp→code): 이동 + 산문 압축(v6 기준 최우선 구간) + `eq:lco-U1V`·`eq:lco-plugin` `multline`/`split` 분할(수식 내용 불변·항 1:1 대조 기록). 압축 재서술 블록에 figure 신설이 필요하다고 판단되면 해당 블록도 경쟁 파이프라인으로.
- **S26** gate: LCO부 산문 감축 목표 달성·라벨/참조 전수 해소·중간 빌드 0-err.

## Phase 3.2 — 코드 루프 B: Part II ↔ LCO 경로 (Steps 27–30)

- **S27** 문건→코드 정합 정정·확장(Decisions 4 기본값): ① `_direction_to_sigma` 전극 인지 확장(전극 타입 → 탈리튬화 부호 환산; 기존 호출 보존 — 흑연 회귀 불변) ② LCO 전자항 dict T1 재정렬(시연값 정합 — LCO demo 출력 의도 변경: 전후 대조 + ledger 재베이스라인) ③ 주석·docstring 의 Part II 섹션 참조 동기화.
- **S28** 코드→문건: LCO 경로의 실제 분기(Ω=0 폴백·dH_a 부재 시 꼬리 off 등)가 Part II 서술과 1:1 인지 역점검·백필.
- **S29** FITTING_GUIDE 방향 규약 §0·tier 표를 확장된 코드 시그니처와 동기화.
- **S30** gate: CODE_MAP LCO 구간 orphan 0·흑연 회귀 13/13 불변·LCO 의도 변경분 전후 대조 기록·샘플/데모 재실행 정상(glyph 0).

## Phase 4.1 — 용어·약자·overfull 전수 (Steps 31–34)

- **S31–S32** TERMS_POLICY 적용: 항목별 문맥 정독 치환(일괄 기계 치환 금지 — 수식/코드/인용 오염 방지)·약자 전수(MIT·OD·MSMR·GITT·AIC·SOC 등) 첫 출현 원어 병기.
- **S33** overfull 전수: 빌드 로그 `Overfull \hbox` 스캔 → >10pt 0건까지 분할·개행 정리.
- **S34** gate: 용어표 전 항목 처분 기록·Overfull(>10pt) 0·빌드 0-err.

## Phase 5.1 — 검수: Fable 10회 가변-청크(수렴까지) + finalizer (Steps 35–40)

> 사용자 확정(2026-07-02): 이종 N종 union 폐지 — 기존 10회 가변-청크 검수 방법(★방법론 §3)을 **Fable 단독**으로 적용.

- **S35** 검수 설계: 라운드별 청크 스킴 로테이션 표 확정(통독/Part·절 단위/식·유도 단위/라인 단위/도메인[물리·구조·문체·용어·코드] — 매 라운드 상이) + 렌즈 로테이션 표(① 물리 불변 diff — 결과식·부호·수치·verify10 판정 보존 ② 구조 — 참조 무결·전방 참조 0·Part 경계 ③ G-follow/G-derive — 비약 0·Part 0 만으로 후속 유도 추종 ④ 수식-주도 달성도 — 산문 예산·"수식만 읽어도" 스팟 테스트 ⑤ 용어 일관 ⑥ 상호충실도 — CODE_MAP 양방향 ⑦ **시각(figure) — 본문·캡션·수식과 그림 좌표/개형의 3자 정합(F-4형 모순 스캔)·재현 스크립트 실행 일치** ⑧ 직전 수정의 새 결함).
- **S36–S38** 라운드 실행(기본 10회·**수렴 = 연속 2R 확정결함 0 까지**): 각 라운드 = Fable 병렬 Agent(refute mandate·가장 약한 1곳 필수·빈 통과 금지, 청크 전담 분할로 coverage missing=0 — 거친 단일 청크 "100% 정독" 과장 금지) → master 삼각검증·직접 수정 → 라운드별 커밋. 검수 기록 = `V1013_REVIEW_R{n}.md`.
- **S39** 잔여 쟁점 최종 판정(master 직접 — 근거·실측 명시) → **S40** finalizer 편입·재빌드·최종 게이트(Test Plan 전 항목)·커밋.

## Phase 6.1 — Ch2 정합·가이드·샘플·마감 (Steps 41–45)

- **S41** Ch2: Part 0 참조로 Z1 절 중복 정리(요약+참조)·용어 정책 적용·참조 갱신.
- **S42** 코드 최종 회귀(흑연 13/13·LCO 재베이스라인분 검증)·**S43** FITTING_GUIDE·샘플 재실행(glyph 0).
- **S44** 최종 점검(두 축 gate·상호충실도) → **S45** result·ledger·INDEX·커밋+푸쉬(페이즈별 커밋은 각 Phase 말 수행).

## Implementation Interfaces

- **라벨 불변 원칙**: 절 이동 시 `\label` 이름 보존(참조 자동 재해소) — 신설 라벨만 `eq:sm-*`(Part 0)·`sec:p2-*`(Part II 도입) 프리픽스.
- **용어표 양식**: | 현행(번역어) | 정정(영어 원어) | 첫 출현 병기 문안 | 출현 수 | 처분 |.
- **산문 예산 표**: | 절 | 현행 문장수/수식수 | 목표 | 실측(사후) |.
- **CODE_MAP 양식**(양방향): | 문건 항목(boxed/유도 결론) | 코드 대응(함수·분기·줄) | 방향(문건→코드/코드→문건) | 지위(1:1/동결 근사/시연값/미구현 과제) | 처분 |.
- **figure 경쟁·체리픽 양식**: 드래프트 제출물 = 문안 + figure 목록(각 figure 당 | 목적(어느 수식·개념을 닫나) | 형식(TikZ 코드 인라인 / matplotlib 생성 스크립트+PNG) | 배치 위치 | 캡션 문안 |). 제작 규율 = 라벨·범례 영어/ASCII(glyph 0)·문서 tikz 관례(box/lab 스타일) 정합·개형은 실제 식 평가로 생성(손그림 개형 날조 금지 — 스크립트 재현 가능). 체리픽 판정 기록 = map 문서에 figure 채택 매트릭스 별도 절. 채택 figure 의 생성 스크립트는 `docs/v1.0.13/figs/` 에 보존(재현성).
- **전 시행 교훈 카드**(작성 sub 머리 주입, v1.0.12 실증 근거 = map_v10 §10·verify10): ① 부호는 반드시 독립 재유도(전 시행에서 F1 제외 전 드래프트가 config 역부호 계승 — 타 드래프트·이전 문안 복붙 금지) ② **"함수형 동형 ≠ 물리량 동일" 가드 의무**(MSMR pairing 은 같은 물리량끼리 — f=+σ_d 확정, 재론 금지) ③ 보존식 출발 + C_bg 완비(생략 금지) ④ 기존 라벨·박스는 최소 diff(재수록 금지 — 참조 다운스트림 보존) ⑤ 표기 = `\mathrm{artanh}`·`Ω_j^cat` 계열 승계 ⑥ 수치 날조 금지(Ω 미배정 지위 유지)·코너케이스 한정어 동반 ⑦ 산문 예산 준수(수식-주도).
- **수식 분할**: `multline`/`aligned`, boxed 상자 유지 — 분할 전후 항 1:1 대조 기록.
- ledger row = V1012 양식 승계.

## Test Plan

- xelatex 2-pass 0-err·ref/cite 경고 0·**Overfull(>10pt) 0**(신설 게이트).
- 물리 불변: 수치 grep(+0.83/+80/−46/1.1k_B/0.47/1.49/8RT/3F/4958/25.7)·부호 sweep((1−ξ)/ξ 0건·f=−σ_d 단정 0)·boxed 결과식 전수 1:1 대조(이동 전후).
- 구조: 흑연부 lco-* 참조 0·전방 참조 0·라벨 정의 각 1회.
- 코드: 흑연 회귀 13/13 **불변**·LCO 의도 변경분 전후 대조+재베이스라인 기록·demo/샘플 glyph 0·CODE_MAP orphan 0(양방향).
- 문체: 절별 산문 예산 실측 대조·G-follow 스팟 테스트·용어표 전 항목 처분 완료.
- figure: 채택 figure 전수 — 생성 스크립트 재실행 재현(PNG 해시 또는 TikZ 컴파일)·개형-수식 검산 기록·캡션-본문-그림 3자 정합·glyph 0.

## Assumptions

- LCO 후방부 = **Ch1 내 Part II**(별도 Chapter 3 아님) — Decisions 1.
- 버전 = v1.0.13, 새 폴더 증판(원본 불가침).
- 경쟁 작성(N=6)은 신설·재작성 블록(Part 0·Part II 도입·압축 재서술)에 적용, 기계적 이동·치환은 master 직접 + 10회 검수가 커버.
- 산문 감축 정량 목표는 P1 실측 후 확정(임시: LCO부 문장수 ≥35% 감축) — Decisions 3.
- "통계역학-first"·"문건-코드 루프" = 사용자 확정(2026-07-02 추가 메시지 2건) — 결정 아님, 전제.

## Decisions Required (GO 시 기본값으로 진행)

1. **LCO 배치 형식** — (권고 a) Ch1 한 문서 안의 Part II 후방 통합: 흑연 식 `\eqref` 직접 참조·빌드/버전 관리 단일. (b) 별도 Chapter 3 분리: 파일 독립성 대신 교차 참조 격하·챕터 번호 체계 재정의 필요. **기본값 = (a).**
2. ~~검수 규모~~ — **사용자 확정(2026-07-02)**: 작성 = N=6(Sonnet 2·Codex 2·Fable 2, 신설·재작성 블록 한정)·Fable 체리픽 가중 3 유지·전 시행 교훈 카드 주입 / 검수 = 이종 union 폐지, **Fable 단독 10회 가변-청크(수렴까지)**. 결정 완료 — 재질의 금지.
3. **산문 압축 강도** — P1 실측 후 절별 예산을 본 계획서에 추가 기록하고 진행. **기본값 = LCO부 ≥35% 감축·흑연부는 v1.0.12 증가분 회복 이상.**
4. **코드 루프 B 의 변경 범위** — (권고) 문건 정합 직결 3건만: `_direction_to_sigma` 전극 인지·전자항 dict T1 재정렬·주석/docstring 동기화. 다온도 T² 해제·ν 상향·LCO Ω/dH_a round-trip 배정은 실데이터 피팅 단계로 계속 이월(데이터 없이 값을 넣으면 허위 정밀). **기본값 = 권고안.**

## GO 조건 (사용자, 2026-07-02 야간 — 실행 전권)

- **작업 우선순위 서열(사용자 확정)**: ① **물리적 논리 문제 제거 — 이론 전개 중 문제 발견 시 수정하면서 가는 것이 필수**(결과식 불변은 기본 전제이되, 물리 결함이 확정되면 정정이 우선 — 정정 요건: 독립 재유도+실측 근거+전후 대조 ledger 기록, verify10 확정 판정과 충돌 시 재검 기록 필수) ② 논리 비약 제거 ③ 수식-주도 구성.
- 사용자 부재(취침) — 중간 의사결정 불가, 마지막 phase 까지 자율 완주. 잔여 결정 3건은 기본값으로 진행. 피드백은 익일 일괄.

## Correction History

- rev.4(당일, GO): 우선순위 서열 명문화 — "물리 결과식 불변" 절대 게이트를 "물리 결함 확정 시 근거·기록 동반 정정 우선"으로 완화(사용자 지시). GO 조건 절 신설.
- rev.3(당일): **이미지 경쟁 복원**(사용자 지적 — 오리지널 9종 방법론은 각 드래프트가 필요 figure 까지 제작·체리픽 대상이었으나 v1.0.12 P4.3 은 문안만 경쟁했음을 확인): S7–S10·S21 에 figure 경쟁·체리픽 명시, Interfaces figure 양식 신설, 검수 렌즈 ⑦ 시각 추가, Test Plan figure 게이트 추가.
- rev.2(당일): 사용자 확정 — 작성 병렬수 N=10→**N=6(S2·C2·F2)**·검수 이종 union 폐지→**Fable 단독 10회 가변-청크**(Phase 2.1·5.1 개정, Decisions 2 확정 처리, 교훈 카드 인터페이스 신설). 본 문서를 **마스터 플랜**으로 지정 — 페이즈별 세부계획서는 각 Phase 착수 시 `Claude/plans/`에 별도 작성.
- rev.1(당일): 사용자 추가 확정 2건 반영 — ⑤ 통계역학-first 를 결정에서 전제로 승격(Ch2 정합 근거), ⑦ 문건-코드 교대 루프 신설(Phase 2.3·3.2 삽입, Step 재번호 1–45, CODE_MAP 인터페이스·검수 렌즈 ⑥ 추가, Non-goals 의 "코드 동작 변경 X"를 "루프 게이트 內 변경"으로 개정, Decisions 4 신설).
- 신규 계획(v1.0.12 완료 커밋 `ea4c169` 직후). v1.0.12 까지의 혼재 배치·(a)–(d) 산문 사슬은 v9 계보 승계였으며, 본 계획이 사용자 지적 7건으로 재정의. 물리 판정 자산(verify10)은 전량 승계·불변.
