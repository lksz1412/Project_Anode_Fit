# HANDOVER v1.0.25.2 — 진행 이력 · 문제점 · 개선할 점

> 작성 2026-07-27 (최종) · 대상 = `Claude/docs/v1.0.25.2/` (현행 최신)
> 세부 이력 = 같은 폴더 `ARCHIVE_NOTE.md` **U1~U12**(충돌 시 ARCHIVE_NOTE 우선)
> 착수 명세·미반영 목록 = `Claude/results/HANDOVER_v1025_2_CARRYOVER.md`

---

## 0. 한 줄 요약

v1.0.25.1 위에 **v1.0.26 regsol 재검증의 채택분을 반영**하고 **두-상 정칙용액 dQ/dV 닫힌형을 신설**한
리비전. 확정 피팅 구성(skew-logistic 7/7/14 + 자유 배경)은 **시드·스위치로 제공**하되 **기본값은 아니다** ---
그 시드가 열역학 입력을 결여하기 때문이다(§3 P1). **남은 작업 = PDF 빌드 + P1 해소.**

---

## 1. 사용자 확정 사항 (시간순)

| # | 결정 | 반영 |
|---|---|---|
| D1 | 이론 = 정칙용액(regsol) 유지 · 피팅 = 로지스틱 계열 — **두 층 분리 선언** | ✅ |
| D2 | 기준 문건 = v1.0.25 계열 유지(v1.0.26 은 실측 결과 폴더) | ✅ |
| D3 | 확정 구성 = **skew-logistic 흑연 7 · Si 7 · 블렌드 14 + 자유 배경**, `L_V` 동결 | ✅ 시드·스위치 |
| D4 | 그 구성을 **기본값**으로, 4-전이를 opt-in 으로 | ⚠️ **철회**(§3 P1) |
| D5 | 문건은 코드 이야기를 하지 않는다 — 이론 문서여야 한다 | ✅ 이관 완료 |
| D6 | Codex 감사 브랜치 대조 | ✅ |

---

## 2. 진행 이력 (커밋 순)

| 커밋 | 내용 |
|---|---|
| `db19c87` | carryover 명세 인계문 작성(A/B/C/D/E 분류) |
| `878cb16` | 확정 구성 기록 + N10(피팅 배경 결함) 등재 |
| `ce417e7` | 피팅 스크립트 N10 수정 + `C_skew` 판 추가 |
| `fb26cf3` | **v1.0.25.2 착수** — 7-gallery 시드 · §5b/§18/sifr 문건 3건 |
| `8e8bd9d` | **`eq:sifr-twophase` 신설**(두-상 정칙용액 dQ/dV 닫힌형) + Ω/FWHM/면적보존/미분규약 |
| `99a6017` | 폴더 분리(v1.0.25.1 동결 / v1.0.25.2 작업본) + 결과 리포트 |
| `905eac0` | **P3-8 위반 이관** — 본문의 수치 구현 서술 → 코드 절·부록 |
| `c768153` | Si skew 시드 추가(블렌드 14 완성) + 신규 식 수치 검산 |
| `77ae0d9` | 기본값 역전(D4) + 연속/불연속 각주 |
| `ab196b2` | 마감 인계 · 리포트 정위치 · 코드 release 1.0.25.2 |
| `eb6b88b` | Codex 감사 대조(U11) |
| `7b342dd` | **기본값 역전 철회(U12)** — 온도 의존 소거 발견 |
| `3b5fd05` | 마감 핸드오버 재작성(이력·문제점·개선할 점) |
| `2802395` | **문턱 스케일링 각주 오류 정정(U13)** — Codex 교차검증 수용 |

### 실측 (본 세션에서 직접 실행)

3 판 적합, 같은 데이터·같은 절차, 자유 배경 포함:

| 소재 | A regsol | B logistic | **C skew-logistic** | ΔBIC(B→C) |
|---|---:|---:|---:|---:|
| 흑연 | 2554.9 (N=4) | 1771.7 (N=7) | **682.3** (N=7) | **−1089.4** |
| 실리콘 | −2672.8 (N=4) | −3447.9 (N=7) | **−3922.1** (N=7) | −474.2 |
| 블렌드 | −2281.3 (N=8) | −4243.0 (N=14) | **−4760.7** (N=14) | −517.7 |

흑연 Ω/RT = **[1.916, 2.027, 2.472, 2.604]** — 전부 문턱 2RT 근방(marginal 두-상).

---

## 3. ★문제점 (심각도 순)

### P0 — Codex 대조 브랜치 신규 산출물 **미검토** (**최우선 · 차단성**)

`origin/codex/v1025_2-physics-conformance` (tip **`11f9054`**, 2026-07-27)가 본 브랜치 최신
`3b5fd05`(U11·U12 포함)을 **머지 완료**했고, 신규 문건 4종 + 프로브 3종을 냈다. **이 중 검토한 것은
regsol 교차검증 하나뿐이다.**

| 산출물 | 상태 |
|---|---|
| `PHASE_054_V1025_2_REGSOL_CROSSCHECK.json` + `phase054_regsol_crosscheck.py` | ✅ 검토 → **U13 정정 유발**(아래) |
| `PHASE_054_V1025_2_LATEST_LINEAGE_REVIEW_ADDENDUM.md` (372행) | ⬜ **미검토** |
| `V1025_2_LATEST_RELEASE_ALIGNMENT_MATRIX.md` | ⬜ **미검토** --- P1 배정 판정이 들어 있을 수 있다 |
| `HANDOVER_V1025_3_CONFORMANCE_BRANCH_CORRECTION_20260727.md` | ⬜ 미검토 |
| `PHASE_054_..._EXECUTION_LEDGER.md` · `..._SOURCE_PROBES.json` · `..._SOURCE_FREEZE_MANIFEST.json` | ⬜ 미검토 |
| 이전 푸시분 `conformance_model/` 패키지 · 자체 원고 · PDF (26,385행) | ⬜ **미검토** |

**다음 세션 첫 작업.** 특히 alignment matrix 는 P1(군집→staging 배정)에 직접 답을 줄 수 있다.

**대조 브랜치에 대한 기제출 지적 4건**(`2abf019` 기준): ①베이스라인이 결함 커밋 → **해소**(머지 완료)
②handover 자기보고 불일치 → **해소**(정정 문건 발행) ③`eq:sifr-twophase` 대조 부재 → **해소**(위 교차검증)
④**"conformance" 명칭이 규모를 감춘다** --- 이는 정합성 *검사* 가 아니라 **물리 구현 병행 재작성 + 별도 원고 +
별도 PDF** 다. 계보가 하나 더 생겼다는 사실이 브랜치명에 드러나지 않는다. **미해소 --- 계보 결정은 사용자 몫.**

### P1 — 확정 구성 시드가 열역학 입력을 결여한다 (**미해소**)

`GRAPHITE_MSMR7_LIT` · `SI_MSMR7_SKEW_LIT` 는 `U`·`w`·`Q`·`alpha` **네 키뿐**이다.
레거시가 가진 `dH_rxn`·`dS_rxn`(중심 $U_j(T)$) · `n`(폭 $w(T)$) · `Omega`(히스테리시스) ·
`dH_a`·`dS_a`·`dVdq_qa`(동역학 지연)를 **전부 결여**한다.

| 기본 셋 | 288→308 K $\max|\Delta|$ |
|---|---|
| 4-전이(현 기본) | **0.5252** |
| 7-gallery skew | **0.0000** |

기여 분해: 중심 몫 **1.135** · 폭 몫 **0.3447**. → **둘 다 0.**
따라서 D4(기본값 역전)를 **철회**했다. 현재 7-skew 는 `use_skew7_default(True)` 또는 명시 인자로만 쓴다.

**해소 방법 — 산술은 확정, 결정 하나만 남았다:**
$n_j = w_jF/(RT_0)$, $\Delta H_{\rxn,j} = -FU_j + T_0\Delta S_{\rxn,j}$ (적합 $U_j$ 보존, 재현 오차 **<5 nV** 검산 완료).
**미결 = 각 gallery 군집이 어느 staging 전이에 속하는가**(그 $\Delta S_\rxn$ 을 상속).
최근접-$U$ 자동 배정은 **쓸 수 없다** — 103 mV 군집 4 개가 $\Delta S=-16$ 과 $-5$ 로 쪼개진다.
**물리 배정이므로 사용자 결정 사항.** 이것이 풀리면 D4 를 그대로 되살릴 수 있다.

### P2 — XeLaTeX 3-pass 미수행 (**차단 · 유일한 빌드 잔여**)

본 환경에 TeX 배포판 부재. 신규 요소 = `eq:sifr-twophase`(식 1) · 각주 1 ·
`\subsection` 2(`ssec:code-twophase`·`ssec:code-dqdv-numeric`) · `enumerate` 3.
0-error · undefined ref/cite 0 확인 후 PDF 3종 커밋하면 마감.

### P3 — Codex 감사 지적 중 잔존 (확인됨 · 미수정)

- **P0-6(a)** `n`·`w` 둘 다 부재 경로: 폭은 $RT/F$(=25.691 mV, $n{=}1$ 취급)인데 $\partial w/\partial T$ 는 **0** 반환.
  $n{=}1$ 폭이면 $R/F$ 여야 한다. 현 프리셋은 전부 `n` 또는 `w` 보유 → 사용자 제공 dict 에서만 발현되는 잠재 결함.
- **P0-6(c)** `n`·`w` 동시 보유 시 `n` 이 조용히 이기고 `w` 는 **불활성**:
  `GRAPHITE_STAGING_LIT` 4/4 · `LCO_MSMR_LIT` 3/3.
- **P0-2~P0-5 는 대조하지 않았다 — 미검토.** 특히 **P0-4(블렌드 정규화·용량 분모)** 는 본 리비전의 블렌드 14 구성과
  직결되므로 다음 순번 1 순위.

### P4' — ★해소된 문건 오류: 문턱 근방 스케일링 (U13, 기록 보존)

`eq:sifr-twophase` 각주에 "문턱 접근이 매끄럽지 않다 --- $\Omega$-도함수 발산" 이라 적었으나 **틀렸다.**
Codex 교차검증: $\max|\Delta|/\varepsilon = 5.90$ 이 $\varepsilon=10^{-3}$--$10^{-5}$ 에서 **불변**
$\Rightarrow$ 차이는 $\varepsilon$ 에 **선형** $\Rightarrow$ **도함수 유한**. 갭 무게가 $\sqrt\varepsilon$ 로
열릴 때 고용체 두 가지가 **같은 무게를 동시에 잃어** 선행 차수가 상쇄된다. 곧 $\dd Q/\dd V$ 는
$\Omega$ 에 대해 **$C^1$** 이다. 각주 정정 완료.

교차검증은 나머지 두 주장을 **더 높은 정밀도로 확인**했다: 면적 오차 $7.8\times10^{-16}$(자체 검산은
$10^{-4}$ --- 유한 창·성긴 격자) · 아임계 갭 무게 정확히 $0$.

**교훈(§4 와 같은 뿌리)**: 자체 검산 표(U9)에 이미 $57.6/57.0/54.9$ 로 선형 스케일링이 찍혀 있었는데,
옆 열의 갭 무게($\sqrt\varepsilon$)를 곡선 차이에 잘못 전이해 읽었다. **수치를 냈지만 읽지 않았다.**

### P4 — 서술 정합 잔여 (경미)

§5b 가 앵커로 병기한 2-sublattice 면내 $\Omega_a\approx3.4RT$ 는 **모델 계열이 다른 양**이다
(단일 $\Omega$ 적합값과 직접 비교 불가). 기존 `srcbox` 가 "단일 $\Omega$ 는 면내 응축 sharpness 만 포착"
이라 적어 대응 자체는 정당하나 **한 문장 단서**가 필요하다.

---

## 4. 개선할 점 — 작업 방식 (재발 방지)

이번 세션에서 실제로 발생한 결함과 그 교훈. **다음 세션은 이것부터 읽을 것.**

| # | 무슨 일이 있었나 | 규칙으로 |
|---|---|---|
| 1 | 기본값을 뒤집으면서 **새 기본이 모델의 핵심 결합(온도 의존)을 보존하는지 확인하지 않았다** | **기본 경로를 바꾸면, 바꾼 기본 경로를 재는 검사를 먼저 만든다.** 기존 게이트는 레거시를 복원하므로 새 기본을 재지 않는다 |
| 2 | N10(피팅 배경 결함)을 **검증 전에 원인으로 단정**해 인계문·커밋에 기록했다. 재실측에서 부분 오류 판명(로지스틱은 거의 불변, 실제 원인은 α 부재) | **실행 가능한 진단은 기록 전에 실행한다.** 미검증이면 "가설" 로 표기 |
| 3 | 인계문이 **부록 등재로 지시한 항목을 예산을 이유로 본문에 넣었다**(P3-8 위반, 사용자가 적발) | **예산 부족은 규칙을 우회할 근거가 아니다.** 못 하면 안 하고 남긴다 |
| 4 | 신규 boxed 식의 **수치 검산을 문건 반영 후에** 했다 | **식은 검산 후 반영한다** |
| 5 | Ω 구간을 실측이 아닌 README 기억으로 적었다가 정정(1.9–2.5 → 1.9–2.6) | **수치는 원자료에서 읽는다** |
| 6 | "게이트 전건 GREEN" 을 정당성 근거로 보고했으나 **검사 대상이 아닌 경로**였다 | **GREEN 은 '검사한 것' 에 대해서만 유효하다.** 무엇을 재지 않았는지 함께 적는다 |

| 7 | 자체 검산 표에 **선형 스케일링이 이미 찍혀 있었는데** 옆 열(갭 무게 $\sqrt\varepsilon$)을 곡선 차이에 전이해 읽고 "도함수 발산" 이라 적었다(U13) | **낸 수치는 끝까지 읽는다.** 스케일링 주장은 비($\Delta/\varepsilon$, $\Delta/\sqrt\varepsilon$)를 **직접 표로 만들어** 어느 쪽이 불변인지 보고 쓴다. 복합 항은 **상쇄되는 짝을 함께 센다** |

**공통 원인 = 확인 가능한 것을 확인하지 않고 진행.** 자기 검증 없이 결론을 문서에 쓰지 말 것.
**그리고 검증했더라도 그 출력을 끝까지 읽지 않으면 같은 결과다(7).**

---

## 5. 현재 상태

### 코드 (`Anode_Fit_v1.0.24.py`, release 1.0.25.2)

- **기본 = 레거시 4-전이 + Si 케이스셋**(열역학 입력 보유). 골든 bit-exact 계약 무변경.
- 신규 상수: `GRAPHITE_MSMR7_LIT`(7, α 보유) · `SI_MSMR7_SKEW_LIT`(7, α 보유)
  · `DEFAULT_CBG_GRAPHITE`(0.550) · `DEFAULT_CBG_SI`(0.051)
- 신규 스위치: `use_skew7_default(enable)` — 7-skew 전환. **정온 곡선 적합 전용**(가역 발열·다온도 금지).
- 시드 tier: 단일 셀 · 비평형 pOCV · **α 경계 포화**(흑연 5/7 · Si 1/7 상한) · w 0.1 mV 급은 분해 한계 아래
  · **배경 반드시 동반**.

### 문건

| 파일 | 내용 |
|---|---|
| `ch3v22_sec02b_sifr.tex` | **`eq:sifr-twophase`** 두-상 정칙용액 dQ/dV 닫힌형 + 연속/불연속 각주 + 모수절약 확정 |
| `ch1_sec05b_gr2L.tex` | 해상도 사다리에 7-gallery(정온 전용 명시) · Ω 실측 대응(1.9–2.6 marginal) · 최저 전위 평탄 분해 한계 |
| `ch1_sec18_inputs.tex` | 식별 가드에 Ω 추가 · gallery+α 조건부 · **면적 보존 진단 $\Delta_Q$** |
| `ch3v22_sec05_code.tex` | `ssec:code-twophase` 수치 요구명세(본문에서 이관) |
| `ch1_appB_codemap.tex` | `ssec:code-dqdv-numeric` dQ/dV 생성 규약(본문에서 이관) |

**본문/부록 경계**: 본문 전 `.tex` 구현 어휘 **0** · 코드 토큰 **0**.

### 검증

게이트 전건 GREEN(`v1024` R6-G1/G2/G3+coverage · `v1025` 9/9 · `reflect` 4/4 · `selfconsistent` 5/5) ·
`STRUCTURE_CHECK: PASS` · `eq:sifr-twophase` 수치 검산 3/3(면적=Q · Ω→2RT 연속 · Ω<2RT 갭 0) ·
기본 경로 T-의존 0.5252 복원 · NaN/Inf 0.
**미수행: XeLaTeX 3-pass.**

---

## 6. 다음 세션 착수 순서

1. **P0** — Codex `11f9054` 신규 산출물 검토. 순서: `V1025_2_LATEST_RELEASE_ALIGNMENT_MATRIX.md`
   (P1 배정 답 가능) → `PHASE_054_..._ADDENDUM.md`(372행) → `conformance_model/` 패키지
2. **P2** — XeLaTeX 3-pass → PDF 3종 커밋 (마감). **U13 정정으로 각주가 길어졌으니 그 조판도 확인**
3. **P1** — gallery 군집 → staging 전이 배정 **사용자 확정** → 시드에 `dH_rxn`·`dS_rxn`·`n` 채움 →
   기본을 7-skew 로 되뒤집고, **기본 경로 T-의존을 재는 게이트 신설**
4. **P3** — Codex P0-4(블렌드 정규화) 대조, 이어 P0-2·P0-3·P0-5
5. **P4** — Ω 앵커 모델 계열 단서 1 문장
6. 잔여: N7(skew-regsol N=7) · N8(평형 데이터 GITT/pOCV+hold) · N9(bootstrap CI) ·
   C-4(서지→M4 해소) · D(함정 기록 Ch2 부록 A)

---

## 7. 위치

| 무엇 | 경로 |
|---|---|
| 현행 최신 | `Claude/docs/v1.0.25.2/` |
| 동결 아카이브(PDF 보유) | `Claude/docs/v1.0.25.1/` |
| 결과 리포트(이미지 9종) | `results/KERNEL_COMPARISON_REPORT_v1025_2.html` |
| 적합 스크립트·산출물 | `Claude/results/comp_v26_data/` |
| Codex 과학감사(v1.0.10–23) | `origin/codex-local-audit-20260720` : `Codex/results/PHASE_010_STRENGTHENING_ROADMAP.md` |
| **Codex 대조 브랜치(현행)** | **`origin/codex/v1025_2-physics-conformance`** tip **`11f9054`** --- `3b5fd05` 머지 완료. `Codex/results/PHASE_054_*` · `Codex/work/v1025_2_physics_branch/` |
