# V1025_CHANGE_LEDGER — v1.0.25 변경대장 (계획 → 실집행 확정 원장)

> 작성: 기계적 실무 서브세션(Opus 4.8), 2026-07-26.
> 대상 = 계획서 `Claude/plans/2026-07-26-v1025-surgical-skew-consistency-plan.md` 의 변경대장 **C1~C9 · G0** 을
> **실제 집행 결과로 확정**한 원장. 열 구성은 `V1024_REFLECT_EXECUTION_LEDGER.md`(12-col) 관행을 따르되,
> v1.0.25 는 Phase 원장이 아니라 **변경 ID 원장**이라 열을 ID·계획(원안)·실집행·코드 지점·문건 지점·게이트·판정·비고로 재구성했다.
>
> **출처·attribution 고지**
> - 코드 지점(함수·상수·줄 번호)·문건 지점(파일·라벨)·줄 수·라벨 증감은 **본 서브세션이 리포에서 직접 대조**했다.
> - **게이트 PASS/FAIL 은 전부 마스터 세션 실행분 인용**이다. 본 서브세션은 게이트를 **재실행하지 않았다**
>   (재실행 시 그림·산출물이 생겨 "명시된 파일만 생성" 경계를 벗어나므로). 게이트 *정의*의 이름·개수만 직접 확인했다.
> - 문건 편집 상세·검증 원문 출력 = `V1025_DOC_EDIT_REPORT.md`(마스터 Opus 5.0 집행분).
>   데이터 addendum·버전 문자열 = `V1025_T13_T14_REPORT.md` · `V1025_DATA_ADDENDUM.md`(Opus 4.8 서브 집행분).
> - 지시서 = `V1025_DOC_CASCADE_TODO.md`(T1~T14).

---

## 0. 요약

| 항목 | 값 |
|---|---|
| 변경 ID | **C1~C9 + G0 = 10** (그중 **C4 = C3 에 흡수**, 설계상 독립 항목 아님 → 실 항목 **9**) |
| 판정 분포 | 완료 **6**(C1·C2·C5·C7·C8·C9) · **변경집행 3**(C3·C6·G0) · 미착수 **0** · 해당없음 **1**(C4) |
| ★계획 이탈 (별도 표 = §2) | **3건** — (a) C3 상수 "즉시 교체" → **opt-in** · (b) C6 regsol "강등" → **완전 삭제** · (c) 신규 라벨 **1 예정 → 3 집행** |
| 부수 이탈·예외 (§3) | **4건** — G0 의 `G-금지` 미구현 · C9 집행 방식(원본 수정 → addendum) · D-D additive 예외(문장 삭제 1건) · 지시서 대비 편집 범위 축소 2건(C2 각주·C8 배경) |
| 코드 | `Anode_Fit_v1.0.24.py` **파일명 유지**(DG-2(a)) · 내부 `release 버전 = 1.0.25` · 아카이브 **1734 → 1917줄(+183)** |
| 문건 | `_sections/*.tex` **14파일 +250줄**(마스터 보고 기준 · 2026-07-26 **02:06** 시점) · 마스터 `.tex` 3파일 각 **+2**. ★본 서브세션 재계수 = **+257**(02:25 스냅샷) — 계수 중에도 마스터가 편집을 계속했다(§4) |
| 라벨 | 신규 **3**(`eq:skewpeak`·`eq:skewapex`·`eq:gr2l-fwhm`, 전부 Ch1) · 삭제 **0** · `\boxed` **38 → 39** |
| 게이트 | 구 4종 전건 PASS(`v1024` G1 골든 bit-exact max\|d\|=0.0 · `_reflect` 4/4 · `_selfconsistent` 5/5) + 신규 `test_gates_v1025.py` **8/8** · 모듈 self-test `overall OK: True` |
| 잔여 필수 게이트 | ★**실제 LaTeX 빌드 미수행**(이 PC 에 TeX 배포판 부재) — `MERGE_READINESS_v25.md` AMBER-1 |

---

## 1. 확정 원장 (C1~C9 · G0)

> 셀 안 `L###` = `docs/v1.0.25/Anode_Fit_v1.0.24.py` 의 줄 번호(본 서브세션 직접 grep).
> 문건 셀의 `(+n)` = `docs/v1.0.24.1/_sections/` 원본 대비 줄 증가(본 서브세션 직접 계수).

| ID | 이름 | 계획(원안) | 실집행 | 코드 지점 | 문건 지점 | 게이트 | 판정 | 비고 |
|---|---|---|---|---|---|---|---|---|
| **C1** | @2 skew-logistic 비대칭(opt-in) | `func_dxi_eq(z,α,w)` 신설 · 전이키 `alpha`(부재=1.0) · `equilibrium`·`dqdv`·`entropy_coefficient`·`solve_U_oc` 를 그 경유로 통일. 문건: §6 skew peak boxed 식 1개 신규 · §7 α 축 · §18 축퇴 가드 · Ch3 완화 · 부록 B 행 | **원안대로 + 정량 강화 2건**: 정점 이동(`eq:skewapex`)·반높이 폭 축퇴(4.45/3.53/3.02/2.74 w)를 식·수치로 승격 | `func_dxi_eq` **L148** · `_alpha_factor` **L511** · 호출 4곳 = `equilibrium` **L684** / `dqdv` **L792** / `entropy_coefficient` **L950** / `solve_U_oc` **L1021** · 축퇴 경고 `__init__` **L457–463** | `ch1_sec06_eqpeak.tex`(+39; `eq:skewpeak` **boxed**·`eq:skewapex`) · `ch1_sec05_width.tex`(+8) · `ch1_sec07_broadening.tex`(+18) · `ch1_sec18_inputs.tex`(+22, `warnbox` 신설) · `ch3v22_sec02b_sifr.tex`(본문 2곳 완화) · `ch1_appB_codemap.tex`(+29) | **G-α1**(alpha 부재 == 현행 `array_equal`) · **G-α2**(면적, α∈{0.25,0.5,1,2,4}) · **G-α3**(C¹ 격자 반감비≈0.5) · **G-α4**(α≠1 ∧ L_V>0 경고) — `test_gates_v1025.py` 8/8 중 4건 **PASS**(마스터 실행분) | **완료** | 표기 반결: logistic 을 $\sigma$ 로 쓰지 않고 $\xi^{(\alpha)}_{\eq,j}=[\xi_{\eq,j}]^{\alpha_j}$ — 문건의 $\sigma_d$(방향 부호)와 기호 충돌 회피. `par:skewpeak` 라벨은 **의도적 미생성**(`\paragraph*` 비번호·참조처 0) |
| **C2** | 인과 기억 pad (검수 B) | `_causal_pad(V,L)` 신설 — dqdv **로지스틱·ratio·regsol 세 경로**에 진행방향 $5L_V$ pad 후 절단. 문건 §1.4·§9 각주 + 부록 E 코드맵 행 | **적용 경로 2곳으로 집행**(regsol 경로는 C6 삭제로 소멸) · 문건 각주는 **§9 + 부록 E 두 곳**에 집약 | `_LAG_PAD_NLV = 5.0` **L195** · `_LAG_PAD_MAXPTS = 4000` **L196** · `_causal_pad` def **L199**(간격 ≤ `lag_length/20`) · 호출 **L829**(동결 경로)·**L841**(ratio 경로) | `ch1_sec09_tail.tex`(+8, `eq:lag` 규격화 검산 직후 각주) · `ch1_appE_selfconsistent.tex`(+5, ratio 경로 동일 pad) | **G-창** 평가창 불변 — 실측 상대산포 **원본 74.31% → pad 후 2.17e-04(0.02%)** · 골든 **G1 bit-exact max\|d\|=0.0** 유지 · `_selfconsistent` **5/5**(동결 극한 회수) | **완료** | 문건 수치 정정 1건: 초고 "100% → ≤0.005%" 를 **게이트 실측 74.3% → 0.02%** 로 교체(+ pad 잔여 $e^{-5}\approx0.674\%$ 가 잔차 하한). 지시서 T9 가 지목한 `ch1_sec00_intro.tex`·`ch1_sec08_lag.tex` 는 **미편집**(§3-D) |
| **C3** | 물리 상수 SI (검수 C) | ★원안 = **즉시 교체**(문건 25.693 mV 에 코드를 맞춤) | ★**opt-in 으로 변경집행** — `R_SI`·`F_SI`·`use_si_constants()` 추가, **기본은 레거시 `R=8.314`·`F=96485.0` 유지** | `R_SI = 8.314462618` **L106** · `F_SI = 96485.33212` **L107** · `use_si_constants(enable=True)` **L111** | `ch1_sec10_sum.tex`(각주: 25.693 = CODATA-2018 / 구현 기본 25.6912) · `ch2_sec08_synthesis.tex`(+7, worked example 병기) · `ch2_appB_codemap.tex`(+2, B.2 "정합 판정은 raw 값으로") · `ch1_appB_codemap.tex`($R,F$ 행) | **G-SI**(`test_gates_v1025.py`) **PASS** | ★**계획 이탈 (a)** — §2 참조. 사유 = 골든 bit-exact 게이트(G1 max\|d\|=0)와 **수학적으로 양립 불가** |
| **C4** | (C3 에 흡수) | 계획서에서 이미 "= 위 C3 에 흡수(별도 없음)" | 별도 집행 없음 — 번호만 보존 | — | — | — | **해당없음** | 계획서 원문 그대로 유지. 결번이 아니라 **의도된 흡수**임을 명기해 후속 세션의 "누락" 오독을 막는다 |
| **C5** | $w_\eff$ "연속화" 문장 정정 (검수 E) | `ch1_sec05b_gr2L.tex:53` 의 "$w_\eff\to0$ 델타 수렴 / 이 한 식으로 연속화" **삭제** + FWHM $\propto\lambda^{3/2}$ 병기(높이 $1/(4w_\eff)$ 는 정확). Part T warnbox 를 두-상 한정으로. 금지어 등록(G-금지 grep) | **원안 + 정량 강화**: FWHM **닫힌형을 신규 식 `eq:gr2l-fwhm` 로 승격**($\mathrm{FWHM}=(4RT/F)\artanh x-(2\Omega/F)x$, $x=\sqrt{\lambda/(1+\lambda)}$, 점근 $(16/3)(RT/F)\lambda^{3/2}$) + Ch3 sifr 의 $w_\eff$ 폭 오독 1건 **추가 정정**(지시서 밖 결함) | — (문건 전용) | `ch1_sec05b_gr2L.tex`(+42 중 해당분; `eq:gr2l-fwhm`) · `ch2_sec05_mixing.tex`(+10, warnbox 조준 좁힘 + `[C-2]` 자산 행 병기) · `ch3v22_sec02b_sifr.tex`(폭 오독 정정) | ★계획의 **G-금지 는 미구현**(§3-A) → 현재 이 항목의 기계 검증은 **없음**. 정적 근거 = `STRICT CHECK: ALL PASS`(마스터) | **완료** | ★자기 정정 1건: 초고 "점근식은 $\lambda\le0.05$ 에서 1% 이내" 가 doc↔code 감사에서 실측 **2.97% → FAIL** → 실측값(0.6/1.2/3.0/27%)으로 교체 후 **PASS**. 문장 삭제 1건은 여기 아님(C6 소관) |
| **C6** | @3 regsol | ★원안(DG-1 기본) = **(a) 미채택 강등** — 코드 경로·G-R3 게이트 **존치**, 문건 boxed 식만 강등 | ★**(b) 완전 삭제로 변경집행**(사용자 결정 DG-1(b) "regsol 삭제") | **삭제**: `_REGSOL_XG`·`_regsol_binodal_xa`·`_regsol_dqdv` 함수 3종 + `equilibrium()` 의 `'kernel'` 분기 → 커널 계통 = **로지스틱 단일계**. 잔존 = 이력 주석뿐(**L12·L13** 심볼명 · **L16·L683** `'kernel'`; 정의·디스패치 0). **보존**: `func_dU_hys` **L312** · `func_dH_a_eff` **L330**(Ω 계통 전량) | `ch3v22_sec02b_sifr.tex`(+39 — 절 도입부 지위 `warnbox` 신설 "해석적 기록 · 미채택·코드 미구현" · (v) 재구현 명세 단서 · `keybox` 코드 계약 표현 제거). `eq:sifr-kernel`·`eq:sifr-blend` **식·라벨·boxed 전부 보존** | **G-R3 재작성**(`test_gates_v1024_reflect.py`) = "regsol 삭제 확인"(심볼 부재 **3/3** + `'kernel'` 키 무시 legacy dict `array_equal` + 면적 = Q = **0.999881**) → reflect **4/4 유지 PASS** | **변경집행** | ★**계획 이탈 (b)** — §2 참조. ★줄 수 혼동 주의: **1957 → 1917(−40)** 은 *v1.0.25 개발 중 regsol 삭제 그 단계*의 변화이고, 아카이브→v1.0.25 총 변화는 **1734 → 1917(+183)** 이다("v1.0.24 대비 1957→1917" 은 스테일 표기) |
| **C7** | gallery opt-in (D-B) | `GRAPHITE_STAGING_MSMR6_LIT` 존치 + `SI_MSMR7_LIT`(0.433·0.456 V feature 포함) opt-in 추가. 문건 §5b 해상도 사다리 + "gallery≠상" 재강조 | 원안대로 | `GRAPHITE_STAGING_MSMR6_LIT` **L1336**(존치) · `SI_MSMR7_LIT` **L1357**(신규·additive·순수 로지스틱) | `ch1_sec05b_gr2L.tex`(사다리에 **7-gallery(Si, opt-in)** 추가 — 근거 실측: hold 전용 feature·검출 피크 3→4→5·중심 U 가 105/141/227 mV 에 ΔU<12 mV 근축퇴) · `ch3v22_sec02_cases.tex`(+11) | **G-si7**(7전이·0.433/0.456·순수 로지스틱·면적 보존) **PASS** · 기본 셋 bit-exact | **완료** | 결론 문구 = "사다리를 올려도 상은 늘지 않는다"(XRD staging 4 · 물리 두-상 2 는 §7 소관·불변) · "봉우리 수를 상 수의 증거로 제시하지 말 것" |
| **C8** | 배경 $C_\bg$ 정직화 | 문건 **§6·§7·§10** 의 $C_\bg$ 를 "창-국소 상수 근사" 로 한정 명기 | **§10 1곳에 집약 집행**(`eq:sum` 직후 단락) — §6·§7 배경 언급부는 **미편집** | — (문건 전용) | `ch1_sec10_sum.tex`(+17 중 해당분): 실측 0.3–0.9 V 단조 감쇠(창 평균의 ~230%·4셀·2프로토콜) · 광폭 종 이득은 **조건부**(단독 대체는 악화, skew α 동반 시 개선) · 세 조건(창 명시·창 밖 외삽 금지·창 확장 시 α 동반 검토) | 없음(정적 서술). 근거 = `V1025_DATA_ADDENDUM.md` **A5**(마스터 측정) | **완료** | 편집 범위 축소 = §3-D. 배경의 물리적 출처 분해는 **범위 밖**으로 명시 |
| **C9** | 데이터 정직화 | `results/comp_v24/` 하위 `FIT_CHECK_v1024.md`·`DATA_REGISTRY.md`·`SOURCES.md` **를 정정**(추기) | **원본 무수정 + 신규 addendum 방식으로 변경집행** — `results/V1025_DATA_ADDENDUM.md`(291줄) 에 **A1~A7** 등재, 각 항에 supersede 대상(파일 + 절 이름) 명기 | — | `V1025_DATA_ADDENDUM.md` (comp_v24 **git clean** 확인) | 없음(문서 정정). 독립 확인분 = A1 프로토콜 매핑(`gr.csv`=gr_A=`p-ocv` / `si.csv`=si_Dhold=`p-ocvhold`) · A4 코드 상태 | **완료** | 집행 방식 변경 근거 = 결과 보호 규약(Addendum/Supersession) + 원본 불가침. §3-B 참조 |
| **G0** | 게이트 신설 | `test_gates_v1025.py` — **G-α1~4 · G-창 · G-금지 · G-극단**(7종) | **8종으로 변경집행**: G-α1 · G-α2 · G-α3 · G-α4 · G-창 · G-극단 · **G-SI**(신규) · **G-si7**(신규). ★**G-금지 미구현** | `test_gates_v1025.py`(신규 파일) · `test_gates_v1024_reflect.py` G-R3 재작성 | — | **8/8 PASS**(마스터 실행분) | **변경집행** | G-SI·G-si7 은 C3·C7 이 opt-in 으로 집행되면서 필요해진 증빙 게이트. G-금지 부재 = §3-A(비-차단 갭) |

---

## 2. ★계획과 다르게 집행된 3건 (별도 표)

> 아래 3건은 "계획서에 적힌 것과 실제로 집행된 것이 다르다". 전부 **근거 있는 의도적 변경**이며 은폐 없이 등재한다.
> 승인 주체가 "사용자" 인 항목은 재질의 없이 baked, "집행자 판정" 인 항목은 사후 승인 대상이다.

| # | 대상 | 계획 원안 | 실집행 | 변경 근거 | 승인 주체 | 파급(연계 영향) |
|---|---|---|---|---|---|---|
| **(a)** | **C3** 물리 상수 SI | 상수를 **즉시 교체**한다(코드 `R=8.314`·`F=96485.0` → CODATA-2018). 문건 25.693 mV 와 코드 25.6912 mV 의 불일치를 코드 쪽에서 해소 | **opt-in 으로 축소** — `R_SI`/`F_SI`/`use_si_constants()` 를 추가하되 **기본값은 레거시 유지**. 문건은 "표시값은 CODATA, 구현 기본은 레거시, SI 는 opt-in" 을 각주로 명시 | 상수를 바꾸면 **모든 계산값이 바뀌므로** 골든 회귀 게이트(`test_gates_v1024` **G1 max\|d\|=0**)와 **수학적으로 양립 불가**. 계획서 자체가 이 항을 "★집행 정정" 으로 이미 표시 | 집행자 판정(계획서 본문에 정정 명기 → 마스터 확인) | 문건 3파일에 opt-in 지위 각주 필요(C3 행 참조) · 회귀 기준 표시값 중 **`U_oc(x̄=0.25)` 만** `.1f` 표시가 74.4→74.3 으로 뒤집힘(raw 74.3511→74.3497, −1.42 µV, 74.35 반올림 절벽 인공물) → 부록 B.2 에 "정합 판정은 **raw 값**으로" 명기. 나머지 기준(−0.204/−0.134/−0.070 mV/K · +60.8 mV · 25.693 mV)은 두 상수계에서 표시 자리까지 동일 |
| **(b)** | **C6** regsol 배제 방식 | **DG-1(a) 미채택 강등**(계획서 권고·기본값) — 코드의 `_regsol_*` 경로와 reflect **G-R3 게이트를 존치**하고 문건 boxed 식만 "추가 후보" 로 강등. "국소 수정 원칙에 부합·편집량 최소" 가 권고 사유였다 | **DG-1(b) 완전 삭제** — 함수 3종 + `equilibrium()` 의 `'kernel'` 분기 제거(−40줄), **G-R3 을 "삭제 확인" 게이트로 재작성**. 커널 계통 = 로지스틱 단일계 | **사용자 결정**(2026-07-26 "regsol 삭제"). 계획서 §10 DG-1 이 열어둔 결정 게이트를 사용자가 (b) 로 확정 | **사용자** | ① 문건 Ch3 sifr 절의 지위 문구가 "코드 존치·강등" → **"미채택·코드 미구현"** 으로 재작성돼야 함(집행됨) ② 그로 인해 **문장 삭제 1건 발생**(§3-C — D-D additive 원칙의 유일한 예외) ③ C2 의 pad 적용 대상이 3경로 → **2경로**로 축소 ④ `'kernel'` 키가 남은 legacy 전이 dict 의 하위호환을 게이트로 증빙해야 함(G-R3 이 담당) ⑤ **Ω 코드는 전량 보존** — 삭제된 것은 dQ/dV 커널뿐(히스 gap·$\Delta H_a^\eff$·§7 상성격 판정은 불변) |
| **(c)** | **신규 `\label` 개수** | **1개** 예정(`eq:skewpeak`). 계획서 Step 13·19 는 skew peak boxed 식 하나만 신설하도록 지시 | **3개 집행** — `eq:skewpeak`(C1/T1) · **`eq:skewapex`**(C1/T1) · **`eq:gr2l-fwhm`**(C5/T4). 삭제 라벨 **0** | 지시서 초안의 정성 서술로는 **정량 정정이 불가**했다. ① $\alpha\ne1$ 이면 정점이 전이 중심에서 $\sigma_dw_j\ln\alpha_j$ 만큼 밀려(α=2 → +17.8 mV) "정점 = 중심" 이 α=1 한정이 되므로 `eq:skewapex` 로 명시 ② T4 초안은 "$\lambda^{3/2}$ 스케일" 서술만 요구했으나 **닫힌형을 주는 편이 사용 가능**하므로 `eq:gr2l-fwhm` 로 승격 | 집행자(마스터) 판정 | 구조 검사 라벨 수 **263 → 266**(dup 0·미해소 ref 0) · `\boxed` **38 → 39**(+1 = `eq:skewpeak`; `eq:skewapex`·`eq:gr2l-fwhm` 는 비-boxed) · 본 서브세션 독립 대조: 신규 라벨 집합 = {`eq:gr2l-fwhm`(gr2L), `eq:skewapex`·`eq:skewpeak`(eqpeak)}, 삭제 **0**, `_sections` 원시 `\boxed` 계수 **60 → 61**(+1, 일치) |

**C4 확인**: C4 는 계획서에서 이미 "= 위 C3 에 흡수(별도 없음)" 로 정의됐다. 본 원장도 **별도 항목을 만들지 않고** 번호만 보존한다(§1 의 C4 행). 곧 C4 는 이탈이 아니다.

---

## 3. 부수 이탈·원칙 예외 (은폐 없이 등재 — 위 3건과 성격이 다르므로 분리)

| # | 항목 | 내용 | 근거·평가 | 차단성 |
|---|---|---|---|---|
| **A** | **G-금지 게이트 미구현** | 계획서 G0·지시서 T4 는 "재도입 금지"/"쓰지 않는다" 선언 표를 등록해 본문 grep 위반 0 을 확인하는 **G-금지** 게이트를 요구했다. 실제 `test_gates_v1025.py` 의 게이트는 **8종(G-α1~4·G-창·G-극단·G-SI·G-si7)** 이고 **G-금지 는 없다**(본 서브세션이 게이트 파일 정의를 직접 grep 확인) | C5 의 "연속화" 표현 재사용 금지가 **기계 검증 없이 프로즈로만** 지켜지고 있다. 대체 근거 = 마스터의 `STRICT CHECK: ALL PASS` + doc↔code 감사 30/30(단 둘 다 금지어 스캔은 아니다) | **비-차단**(현재 위반 사실은 보고되지 않음) · 차기 게이트 후보 |
| **B** | **C9 집행 방식 변경** | 계획 Step 24 는 `comp_v24/` 의 `FIT_CHECK`·`DATA_REGISTRY`·`SOURCES.md` **를 직접 추기**하도록 지시. 실집행은 **원본을 한 줄도 고치지 않고** 신규 `V1025_DATA_ADDENDUM.md` 로 supersede | 원본 불가침 + 결과 보호 규약(Addendum/Supersession)이 in-place 추기보다 우선. comp_v24 `git status` clean 으로 증빙 | **비-차단**(내용은 동일하게 등재됨). 단 "원본만 읽는 독자" 가 addendum 을 못 볼 위험 → INDEX·MERGE 문서에서 연결 |
| **C** | **D-D additive 원칙 예외 1건** | v1.0.25 전체에서 **문장 삭제는 단 1건**: `ch3v22_sec02b_sifr.tex` 세 확인 (3) 끝의 "커널 분기가 이 Ω(모양) 자유도 하나를 열되, 미지정 시 로지스틱 폴백으로 bit-exact 회수돼…" | C6 의 코드 삭제로 **사실이 아니게 된 서술**이라 존치 불가. 그 역할을 실제로 담는 것이 $\alpha_j$·gallery 임을 대체 서술로 밝힘. 계획서 D-D("신규는 전부 additive")의 의도적 예외이며 마스터가 근거를 명시 | **비-차단** |
| **D** | **지시서 대비 편집 범위 축소 2건** | ① **C2/T9**: 지시서는 `ch1_sec00_intro.tex`·`ch1_sec08_lag.tex`·`ch1_sec09_tail.tex` 세 곳을 지목했으나 집행은 **`ch1_sec09_tail.tex` + `ch1_appE_selfconsistent.tex`** 2곳. ② **C8/T10**: 지시서는 §6·§7·§10 세 곳, 집행은 **§10(`ch1_sec10_sum.tex`) 1곳** | 어느 쪽도 서술 누락(orphan)을 만들지 않는 집약이며, 마스터 보고 T9·T10 이 편집 파일을 명시한다. 다만 **지시서 항목 대비 미달**은 사실이므로 등재 | **비-차단** · 재검 시 §1.4·§6·§7 의 관련 서술이 새 각주와 모순되지 않는지 확인 권고 |
| **E** | **빌드 게이트 미집행**(계획 Step 4·20·26·27) | 계획서는 P0·P2·P3 각 단계 끝에서 `xelatex` 3-pass 0-error 를 요구. 이 PC 에 TeX 배포판이 없어 **전 단계 미집행**, 정적 구조 검사로 대체 | 계획서 Test Plan 자체가 이 환경 제약을 이미 명기하고 "머지 전 필수 잔여 게이트" 로 남겼다 | ★**머지 차단**(`MERGE_READINESS_v25.md` AMBER-1) |

---

## 4. 계수 대조 (본 서브세션 직접 측정 — 마스터 보고와의 차이 포함)

> ★**측정 조건 고지**: 본 서브세션이 계수하는 동안 **마스터가 `_sections` 편집을 계속 진행하고 있었다.**
> 따라서 아래 재계수는 **시각이 박힌 스냅샷**이며 최종 확정값이 아니다. 확정은 마스터 소관.

| 대상 | 마스터 보고(`V1025_DOC_EDIT_REPORT.md`, **02:06** 시점) | 본 서브세션 재계수(**02:25** 스냅샷) | 평가 |
|---|---|---|---|
| `_sections` 편집 파일 수 | 14 | **14**(v1.0.24.1 대비 byte-diff) | 일치 |
| `_sections` 줄 증가 합 | **+250** | **+257** | ★차이 7줄 — 아래 주 참조 |
| 신규 `\label` | 3 (`eq:skewpeak`·`eq:skewapex`·`eq:gr2l-fwhm`) | **3**, 동일 집합 | 일치 |
| 삭제 `\label` | 0 | **0** | 일치 |
| `\boxed` | 38 → 39 (구조 검사 기준) | `_sections` 원시 계수 **60 → 61**(+1) | 증분 일치(분모 정의만 다름) |
| 마스터 `.tex` 3파일 | 각 +2 | 각 **+2**(60→62 · 32→34 · 32→34) | 일치 |
| 코드 줄 수 | 아카이브 1734 → v1.0.25 **1917**(+183) | **1917줄 · 124501 바이트** | 일치 |

**★ +250 vs +257 주 — 마스터의 편집이 진행 중이었다.**
파일 mtime 을 보면 마스터는 보고서를 쓴 뒤(보고서 mtime **02:06:22**)에도 `_sections` 4파일을 계속 수정했다:

| 파일 | mtime | 보고서 기재 | 02:25 재계수 |
|---|---|---|---|
| `ch1_sec06_eqpeak.tex` | 02:08:44 | +38 | **+39** |
| `ch1_sec05b_gr2L.tex` | 02:09:21 | +39 | **+42** |
| `ch1_sec18_inputs.tex` | 02:11:33 | +22 | +22 (줄 수 동일 — 내용 수정) |
| `ch3v22_sec02b_sifr.tex` | 02:17:13 | +36 | **+39** |

나머지 10파일은 01:37~01:58 에 확정돼 보고서 기재와 **전건 일치**한다(합 +145).
곧 차이 7줄 = eqpeak +1 · gr2L +3 · sifr +3 이며, 본 서브세션 계수 자체는 두 스냅샷(02:10 **+254** → 02:25 **+257**)
사이에도 증가했다. **라벨(+3 / −0) · `\boxed`(+1) · 편집 파일 수(14) 는 모든 스냅샷에서 동일**하므로
구조적 결론은 바뀌지 않는다. 본 원장은 §0·§1 에 **마스터 확정값 +250** 을 싣고 재계수를 여기 병기한다(직접 측정 · 창작 아님).

→ **마스터 확인 사항 2건**
1. 보고서 §0 의 `+250` 을 최종값(편집 종료 시점 계수)으로 갱신할 것.
2. `STRUCTURE_CHECK`·`STRICT CHECK`·doc↔code **30/30 은 전부 02:06 이전 실행분**이고 그 뒤 4파일이 수정됐다 →
   **편집 종료 후 재실행이 필요하다**(`MERGE_READINESS_v25.md` AMBER-2).

---

## 5. 게이트 원장 (전건 — 마스터 실행분 인용 · 본 서브세션 미재실행)

| 게이트 파일 | 게이트 | 결과(마스터 실행) | v1.0.25 에서의 변경 |
|---|---|---|---|
| `test_gates_v1024.py` | G1(골든 회귀)·G2·G3·n(T)·R6-G1/G2/G3/coverage | **전건 PASS** — G1 module max\|d\| = **0.0e+00** · golden max\|d\| = **0.0e+00** · bit-exact = **True** | 무변경(파일 미수정). 국소 수정이 기본 경로를 안 건드림의 증명 |
| `test_gates_v1024_reflect.py` | G-R1(@5 5-feature) · G-R2(LCO 토글) · **G-R3(재작성)** · G-R4(#1 단위계약) | **ALL PASS (4/4)** | ★**유일한 게이트 파일 수정** — G-R3 을 "@3 regsol 커널 삭제 확인"(심볼 부재 3/3 + `'kernel'` 키 무시 `array_equal` + 면적 = Q = 0.999881)으로 재작성 → 분모 4/4 유지 |
| `test_gates_v1024_selfconsistent.py` | G-E1~G-E5 | **ALL PASS (5/5)** | 무변경. C2 pad 를 ratio 경로에도 넣어야 동결 극한 정확 회수가 유지됨을 이 게이트가 노출(한쪽만 pad 시 실패 → 양 경로 적용으로 $\lVert r_1-r_0\rVert=0$ 복구) |
| `test_gates_v1025.py` | G-α1 · G-α2 · G-α3 · G-α4 · G-창 · G-극단 · G-SI · G-si7 | **ALL PASS (8/8)** | 신규 파일. G-창 실측 = 74.31% → **2.17e-04** |
| `Anode_Fit_v1.0.24.py` (모듈 self-test) | `python Anode_Fit_v1.0.24.py` | **`>>> overall OK: True`** | 내부 release 문자열 1.0.25 |
| 정적 검사 | `tools_check_structure.py check` | **`STRUCTURE_CHECK: PASS`**(라벨 266 dup 0 · ref 1112 미해소 0 · cite-undef 0 · bib-uncited 0 · env 0 · boxed 39) | 편집 전 baseline 도 PASS(라벨 263·ref 1064·boxed 38) |
| 정적 검사(엄격) | `tex_strict_check.py` | **`STRICT CHECK: ALL PASS`** — 장별 xr 참조 미해소 0 · `$` 패리티 불변 · 중괄호 균형 · 라벨 회귀 0 | 상용 도구가 못 잡는 "xr 없이 타 장 라벨 참조" 를 잡는 보강 검사 |
| doc↔code 감사 | `doc_code_audit.py` | **30/30 PASS** | 문건 v1.0.25 의 신규 수치·거동 주장 전건을 코드로 재현 |
| ★**LaTeX 빌드** | `xelatex` ch1→ch2→ch3 3-pass | ★**미수행**(TeX 배포판 부재) | **머지 전 필수 잔여 게이트** |

---

## 6. 원본 불가침 확인

| 대상 | 상태 | 증빙 |
|---|---|---|
| `Claude/docs/v1.0.24/` | **무변경** | `git status --porcelain` 출력 없음 · 코드 LF-sha256 앞 16 `f230f59bb10bcc49` |
| `Claude/docs/v1.0.24.1/` | **무변경** | 동일 · 두 폴더 sha256 동일 · 추적 파일 261 |
| `Claude/results/comp_v24/` | **무수정** | git clean — C9 는 addendum 방식(§3-B) |
| 줄바꿈·인코딩 | 편집 14 + 3 + 코드/게이트 전건 **순수 CRLF**(bare LF 0) · **BOM 0** | 마스터 `editstat.py` 대조 |

---

## 7. 한계 (이 원장이 보증하지 않는 것)

1. ★**실제 LaTeX 빌드가 수행되지 않았다.** §5 의 정적 검사는 **참조·환경·라벨·매크로**만 커버하고
   **TeX 문법·조판·플로트 배치·페이지 수는 커버하지 못한다.** 특히 빌드에서 처음 검증되는 곳 3군데 =
   ① `ch1_sec18_inputs.tex` 의 `warnbox` 안 `enumerate` 중첩 ② `ch1_appB_codemap.tex` `longtable` 셀의
   긴 서술 행 넘침 ③ `ch1_sec09_tail.tex`·`ch1_sec10_sum.tex` 의 신규 `\footnote` 배치.
2. **게이트 PASS/FAIL 은 인용값이다** — 본 서브세션은 재실행하지 않았고, 게이트 *정의*의 이름·개수만 확인했다.
3. **정적 검사·doc↔code 감사 시점은 02:06 이전**이고, 그 뒤 `_sections` 2파일이 수정됐다(§4). 재실행 판단 = 마스터.
4. **미해결 물리 판단 1건**(흑연 물리 두-상 **4 vs 2**)은 v1.0.24 부터의 **선행 불일치**이고 v1.0.25 가
   만든 것이 아니다. v1.0.25 신규 서술은 전부 §7(2개) 기준이라 챕터 내부 정합은 유지된다.
   통일에는 Dahn PRB **44**, 9170(1991) **본문**(초록 아님) 확인이 필요해 본 버전 범위 밖 →
   `MERGE_READINESS_v25.md` 미해결 표 · `V1025_DATA_ADDENDUM.md` **M4** 와 동일 항목.
5. 본 원장은 **변경의 존재·지점·판정**을 확정하지만 **물리적 타당성**을 재심하지 않는다(그 판단은 마스터 소관).
