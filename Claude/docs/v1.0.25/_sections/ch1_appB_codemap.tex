% ====================================================================
% 부록 B — 구현 대응표 (단방향 연계: 구현→식번호)
% ====================================================================
\section{곡선 계산 구현 대응표}\label{sec:appendix-code}
문건과 구현의 연계는 단방향이다 --- 구현(\code{Anode\_Fit\_v1.0.24.py})의 주석$\cdot$docstring 이 본 장의 식 번호를 참조한다. 본 부록은 그 역방향 조회, 곧 ``본문의 물리량$\cdot$노드가 구현의 어느 식별자에
대응하는가''를 표로 제공하여, 본문을 코드 언급 없이 물리로만 읽을 수 있게 하면서 재현 가능성(본 장과
구현 사이의 1:1 대조)을 보존한다. 진입점은 \code{curve}(실험조건 facade --- 방향 문자열$\cdot$c-rate 를
식~\eqref{eq:n0map} 으로 환산)와 \code{dqdv}(물리 부호 $s$ 직접 입력)이며, LCO 서브클래스
\code{LCOCathodeDQDV} 는 전극 인지 플래그 \code{\_delith\_is\_discharge}$=$\code{False} 로 셀 라벨을
탈리튬화 부호로 환산한다(\code{'charge'}$\mapsto\sigma_d{=}+1$, 식~\eqref{eq:lco-sigmaslot}). $|I|\to0$
평형 기준선은 메서드 \code{equilibrium}(식~\eqref{eq:eqpeak} 만 $U_j$ 중심으로 합산 --- 분기
미반영이라 $\gamma_j$ 무관 가역 기준선, \S\ref{sec:eqpeak} 의 구분 참조)이다. 본문 표~\ref{tab:staging} 의
흑연 전이 초기값 리스트는 코드 변수 \code{GRAPHITE\_STAGING\_LIT} 에, 표~\ref{tab:lco-staging} 의 LCO
초기값 리스트는 \code{LCO\_MSMR\_LIT} 에 대응한다(파라미터 \emph{구조}의 대응이며, \code{LCO\_MSMR\_LIT}
시연 리스트는 전이 구성$\cdot$순서가 표의 물리 anchor 와 다른 tier-C 데모라 전이별 1:1 값 대응은 아니다).

\emph{v1.0.25 갱신 요지.} 아래 표에 네 항이 추가되었다 --- 전이 키 \code{alpha}$\cdot$함수
\code{func\_dxi\_eq}(skew 종, 식~\eqref{eq:skewpeak})$\cdot$\code{\_causal\_pad}(식~\eqref{eq:lag} 하한
$-\infty$ 실현)$\cdot$상수 opt-in \code{use\_si\_constants()}. 넷 다 \emph{additive} 이며 미지정 시 v1.0.24.1
경로가 부동소수점까지 보존된다(골든 게이트 \code{max|d|}$=0$). 반대로 \emph{삭제}된 것은 하나다 --- 구
정칙용액(Frumkin) 평형 커널 경로(\code{\_regsol\_dqdv} 등 3 심볼과 \code{equilibrium} 의 \code{'kernel'}
분기)가 제거되어 커널 계통이 로지스틱 단일계가 되었다(Chapter 3 의 Si-host 정칙용액 커널 소절 지위 상자
참조 --- 문건의 해석적 유도는 보존, 구현만 없음). 파일명은 규약상 \code{Anode\_Fit\_v1.0.24.py} 를 유지하며
내부 release 버전 문자열만 1.0.25 다(본 부록의 코드 참조 키를 깨지 않기 위한 v1.0.24.1 선례 승계).

\begin{longtable}{p{0.20\textwidth} p{0.34\textwidth} p{0.38\textwidth}}
\caption{물리 기호 $\leftrightarrow$ 구현 식별자 대응(본문 기호표의 구현 열).}\label{tab:symcode}\\
\toprule 기호 & 구현 식별자 & 비고 \\ \midrule \endfirsthead
\toprule 기호 & 구현 식별자 & 비고 \\ \midrule \endhead
$\sigma_d$ & \code{s},\,\code{sigma\_d} & 방향 부호 \\
$s$ & (유도 전용 --- 직접 대응 없음) & 유도 전용 고정 부호(항상 $+1$ 대입 소멸). 코드의 \code{s}
파라미터(\code{dqdv} 인자)는 방향 부호 $\sigma_d$ 를 받는 자리라(윗행) 이 유도-전용 $s$ 와 별개다 \\
$|I|$ & \code{I\_abs} & 전류 크기 \\
$Q_\cell$ & \code{Q\_cell} & 셀 용량 \\
$T$ & \code{T} & 온도(스칼라/배열) \\
$R_n$ & \code{Rn} & 분극 저항 \\
$V_\app$ & \code{V\_app} & 인가(측정) 전위 배열 \\
$U_j(T)$ & \code{U},\,\code{func\_U\_j} & 평형 중심 \\
$\Delta H_{\rxn,j},\Delta S_{\rxn,j}$ & \code{dH\_rxn},\,\code{dS\_rxn} & 반응 열역학 입력 \\
$n_j$ & \code{n} & 폭 다중도 \\
$w_j$ & \code{func\_w},\,\code{\_width} & 폭(직접 입력 키 \code{w} 는 $n$ 역산 \code{\_n\_factor});
MSMR 문헌의 무차원 $\omega_j$ 는 $w=\omega\cdot RT/F$ 로 환산해 넣는다(\S\ref{sec:lco-code} 의
재모수화 --- 문헌 $\omega$ 값을 \code{w}[V] 에 직접 대입 금지) \\
$\xi_{\eq,j}$ & \code{func\_ksi\_eq} & 평형 진행률 \\
$\alpha_j$ & 전이 dict 키 \code{alpha},\,\code{\_alpha\_factor} & skew 지수(식~\eqref{eq:skewpeak}) --- 키 부재
$=1.0=$ 현행 로지스틱과 부동소수점까지 동일. \code{equilibrium}$\cdot$\code{dqdv}$\cdot$
\code{entropy\_coefficient}$\cdot$\code{solve\_U\_oc} 네 경로가 같은 규약을 공유 \\
$\xi^{(\alpha)}_{\eq,j}=[\xi_{\eq,j}]^{\alpha_j}$ & \code{func\_dxi\_eq(...)[1]} & skew 진행좌표(단조 $0\!\to\!1$).
같은 호출의 \code{[0]} 이 미분 종 $(\alpha_j/w_j)[\xi_\eq]^{\alpha_j}(1-\xi_\eq)$ 다. \code{func\_dxi\_eq} 는
\code{func\_ksi\_eq} 의 logistic 을 재사용하며 $\alpha_j{=}1$ 분기에서 기존 식의 \emph{연산 순서까지} 동일하게
반환한다 --- 이것이 bit-exact 계약이고 게이트 G-$\alpha$1 이 \code{np.array\_equal} 로 증빙한다 \\
$Q_j$ & \code{Q} & 용량 가중 \\
$R,\,F$ & \code{R},\,\code{F}(레거시 기본) / \code{R\_SI},\,\code{F\_SI},\,\code{use\_si\_constants()} &
물리 상수. \emph{기본은 레거시} $R{=}8.314$$\cdot$$F{=}96485.0$ 이고 CODATA-2018 값은 \emph{opt-in} 이다 ---
즉시 교체는 골든 bit-exact 계약(G1, \code{max|d|}$=0$)과 양립하지 않기 때문. 발효 시 회귀 기준 표시값은
불변이나 $U_\oc(\bar x{=}0.25)$ 만 표시 반올림이 뒤집힌다(\S\ref{sec:synthesis} 각주) \\
$C_\bg$ & \code{Cbg} & 배경 미분용량 \\
$\Omega_j$ & \code{Omega} & 상호작용 에너지 \\
$\gamma_j$ & \code{gamma} & 분기 축소 인자 \\
$\Delta U_j^\hys$ & \code{func\_dU\_hys} & 분기 gap \\
$h_{\eta,j}$ & \code{h\_eta} & 부분 cycle 인자 \\
$U_j^{\,d}$ & \code{func\_U\_branch} & 방향별 분기 중심 \\
$\chi$ & \code{chi}(생성자 인자 \code{x}) & 전달 계수(분할 규칙 주입 \code{chi\_split}) --- 인자명
\code{x} 는 조성 $x$(Li 함량)와 무관한 역사적 명칭 \\
$\chi_d$ & \code{func\_chi\_d} & 방향별 전달 계수 \\
$\mathcal A$ & \code{A} & 컷 affinity \\
$\Delta H_{a,j},\Delta S_{a,j}$ & \code{dH\_a},\,\code{dS\_a} & 활성화 열역학 입력 \\
$\Delta H_{a,j}^\eff$ & \code{func\_dH\_a\_eff},\,\code{\_chi\_and\_dH\_eff} & 유효 장벽(토글 \code{use\_dH\_eff}) \\
$L_{q,j}$ & \code{func\_L\_q},\,\code{\_resolve\_lag\_length} & 용량축 지연 길이 \\
$L_{V,j}$ & \code{lag\_len\_V} & 전압축 지연 길이 \\
$|\dd V/\dd q|_{q_a}$ & \code{dVdq\_qa} & 컷점 OCV 기울기 \\
$z_\mathrm{cut}$ & \code{z\_cut} & 컷 배수 \\
$A_\mathrm{cap}$ & \code{A\_cap\_RT} & 컷 상한 배수 \\
$\xi_{\mathrm{lag},j}$ & \code{ksi\_lag} & 인과 기억 적분 진행률(식~\eqref{eq:lag}) \\
\bottomrule
\end{longtable}

\begingroup
\footnotesize
\renewcommand{\arraystretch}{1.18}
\begin{longtable}{@{}p{0.29\textwidth} p{0.07\textwidth} p{0.19\textwidth} p{0.29\textwidth}@{}}
\caption{전체 입력 인자(코드 식별자)와 기본값. 전이 dict 키는 전이별, 생성자 인자는 모델 공통이며 일부는 전이 dict 로
per-transition override 된다($z_\mathrm{cut}$,$A_\mathrm{cap}$,\code{use\_dH\_eff}). 사용자 실무에서 이 인자들은
Optuna 등 최적화 도구의 탐색 변수로 노출된다.}\label{tab:inputs}\\
\toprule
인자(코드) & 자리 & 기본값 & 곡선 내 역할(식) \\
\midrule
\endfirsthead
\toprule
인자(코드) & 자리 & 기본값 & 곡선 내 역할(식) \\
\midrule
\endhead
\multicolumn{4}{l}{\emph{— 전이 dict 키(전이별) —}}\\
\code{U} 또는 (\code{dH\_rxn},\code{dS\_rxn}) & 전이 & --- & 평형 중심 $U_j(T)$ (\eqref{eq:Uj}) \\
\code{w} 또는 \code{n} & 전이 & --- / $1$ & 폭 $w_j=n_jRT/F$ (\eqref{eq:wbase}) \\
\code{n\_T1},\code{n\_T\_ref} & 전이 & 부재$=0$ / $298.15$ & 폭 다중도의 $T$-선형 잔여
$n_j(T)=n_j+n_{T1}(T-T_\mathrm{ref})$ --- 부재 시 상수 $n$ 과 동일(bit-exact), 발효 시 폭$\cdot$가역 발열
config 계수에 동반 전파(Part T 폭 $T$-의존 일반화, \S\ref{sec:mixing}) \\
\code{alpha} & 전이 & 부재 $=1.0$ & skew 지수 $\alpha_j$ (\eqref{eq:skewpeak}) --- 선택$\cdot$tier C(피팅).
부재 시 현행 bit-exact. 정점이 $\sigma_dw_j\ln\alpha_j$ 만큼 이동하고 반높이 폭도 함께 변하므로
(\eqref{eq:skewapex}) \code{n}$\cdot$\code{L\_V}$\cdot$gallery 세분과 축퇴 --- 동시 자유화 금지
(\S\ref{sec:inputs} 가드; 같은 전이에 \code{alpha}$\ne1$ 과 \code{L\_V}$>0$ 를 함께 주면 경고, 게이트 G-$\alpha$4) \\
\code{Q} & 전이 & --- & 용량 가중(면적) \\
\code{Omega},\code{gamma} & 전이 & $0$,\,$0$ & 상호작용$\cdot$분기(히스, \eqref{eq:dUhys}\eqref{eq:Ubranch}) \\
\code{h\_eta} & 전이 & $1.0$ & 부분 cycle 분기 인자 \\
\code{dH\_a},\code{dS\_a} & 전이 & --- / $0$ & 활성화(꼬리, \eqref{eq:Lqfull}) \\
\code{dVdq\_qa} & 전이 & $0$ & 컷 OCV 기울기 $L_q\!\to\!L_V$ (\eqref{eq:LV}) \\
\code{L\_V} & 전이 & --- & 직접 지연 길이(동역학 우회) \\
\code{z\_cut},\code{A\_cap\_RT} & 전이/공통 & $4.357$,\,$4.0$ & 컷 affinity (\eqref{eq:Acut}) \\
\code{use\_dH\_eff} & 전이/공통 & True & 유효 장벽 토글(\eqref{eq:dHeff}) \\
\multicolumn{4}{l}{\emph{— 생성자 인자(모델 공통) —}}\\
\code{x}/\code{chi},\,\code{chi\_split} & 공통 & $0.5$,\,\code{func\_chi\_d} & 전달 계수 $\chi_d$ (\eqref{eq:chid}) \\
\code{Rn} & 공통 & $0.0$ & 분극 저항 (\eqref{eq:vn}) \\
\code{Cbg} & 공통 & $0.0$ & 배경 $C_\bg$ (상수 또는 $V$ 함수) \\
\code{seed\_T/I/Q\_cell} & 공통 & $298.15$,\,$0.1$,\,$1.0$ & 진단용 seed $L_V$ 대표 조건 \\
\multicolumn{4}{l}{\emph{— 호출 인자(\code{curve}/\code{dqdv}) —}}\\
\code{V\_app},\code{T},\code{c\_rate}/\code{I\_abs},\code{Q\_cell},\code{direction} & 호출 & --- & 실험조건 (\eqref{eq:n0map}) \\
\multicolumn{4}{l}{\emph{— LCO 추가 인자(전이 dict 키$\cdot$서브클래스, \S\ref{sec:lco-code}; 게이트 3키$+$\code{x\_center} 는 \code{electronic} 켤 때 필수) —}}\\
\code{electronic},\code{x\_center} & 전이 dict & \code{False}, — & 전자항 plug-in 스위치$\cdot$동결 조성 \\
\code{g\_max\_eV},\code{x\_MIT},\code{dx\_MIT} & 전이 dict & 문헌 $13/0.85/0.05$ (출처$\cdot$tier 는 \S\ref{sec:lco-gate}) & MIT 게이트 초기값 (\eqref{eq:ggate}) \\
\code{\_delith\_is\_discharge} & 클래스 & 흑연 \code{True}/LCO \code{False} & 셀 라벨$\to$탈리튬화 부호 환산 (\eqref{eq:lco-sigmaslot}) \\
\multicolumn{4}{l}{\emph{— vib Einstein 양자보정(전이 dict; 부재 $=$ 동결 bit-exact) —}}\\
\code{theta\_E},\code{theta\_E\_Tref} & 전이 dict & --- $/\,298.15$ & Einstein 온도 $\theta_{E,j}$[K]$\cdot$기준온도 (Part T Einstein 절, \S\ref{sec:einstein}) \\
\bottomrule
\end{longtable}
\endgroup

\begingroup
\renewcommand{\arraystretch}{1.3}
\begin{longtable}{l p{0.78\textwidth}}
\caption{노드 $\leftrightarrow$ 구현 식별자 대응(본문 표~\ref{tab:nodemap} 의 구현 열).}\label{tab:nodecode}\\
\toprule
노드 & 구현 식별자 \\
\midrule
\endfirsthead
\toprule
노드 & 구현 식별자 \\
\midrule
\endhead
N0 & \code{curve},\,\code{\_direction\_to\_sigma} \\
N1 & \code{dqdv}(분극 환산부: \code{V\_n = V\_in - sigma\_d * I\_abs * self.Rn}) \\
N2 & \code{func\_U\_j}(seam \code{\_effective\_dS\_rxn} 경유) \\
N3 & \code{func\_dU\_hys},\,\code{func\_U\_branch} \\
N4 & \code{func\_w},\,\code{\_width} \\
N5 & \code{func\_ksi\_eq}(오버플로 방지 \code{np.where} 이분 평가) \\
N6 & 평형 극한($L_V\!\to\!0$) 수치 가드: \code{peak\_shape = dxi\_eq}(\code{func\_dxi\_eq} 반환) ---
\code{alpha} 부재($=1$)에서 구 인라인 \code{ksi\_eq*(1-ksi\_eq)/w} 와 부동소수점까지 동일(bit-exact 계약),
\code{alpha}$\ne1$ 에서 skew 종~\eqref{eq:skewpeak} \\
N7 & \code{\_resolve\_lag\_length},\,\code{func\_L\_q} \\
N8 & 기억 적분 $\xi_\mathrm{lag}$(\eqref{eq:lag}) 의 점별 수치 적분(\code{ksi\_lag}); 충전 분기는 상한 반전(\eqref{eq:reversal}).
하한 $-\infty$ 실현 = \code{\_causal\_pad(V\_prog, L\_V)} --- 진행방향 \emph{과거}로 $5L_V$ 만큼 격자를 pad
(간격 $\le L_V/20$ 의 coarse 격자, 상한 4000점)한 뒤 적분하고 원 격자만 절단 반환. 동결 경로와 ratio 경로
모두 같은 pad 를 쓴다(부록 E G-E2 동결 회수 불변) \\
N9 & peak 모양 용량 가중 합산 $\to$ $V_n$ 위 점별 \code{dqdv\_out} \\
N0$'$ & \code{LCOCathodeDQDV}$\cdot$\code{LCO\_MSMR\_LIT} \\
N2$'$ & \code{func\_U\_j}(동일) \\
N3$'$ & \code{func\_dU\_hys},\,\code{func\_U\_branch}(LCO 값, 동일 함수) \\
N5$+$ & \code{func\_dSe\_molar} \\
N9$'$ & \code{\_effective\_dS\_rxn} \\
$\bar x$ 진입점 & \code{solve\_U\_oc}(Part T 전하 보존 음함수~\eqref{eq:implicit} 의 유일한 근 솔버)$\cdot$
\code{entropy\_coefficient\_x}$\cdot$\code{reversible\_heat\_x} --- 조성 $\bar x$ 에서
$U_\mathrm{oc}\cdot\partial U_\mathrm{oc}/\partial T\cdot\dot Q_\mathrm{rev}$ 를 직접 산출(additive --- 기존 $V_n$ 경로 무섭동; 요구명세는 열특성 부록 D) \\
\bottomrule
\end{longtable}
\endgroup

구현 스니펫 두 곳(본문 N2$\cdot$N7 에서 이관)을 원문 그대로 둔다.
\begin{codebox}
\textbf{N2.} \code{if 'dH\_rxn' in tr and 'dS\_rxn' in tr: U\_j = func\_U\_j(T\_prog, dH\_rxn, dS\_rxn)}
--- 배열 $T$ 대응. 없으면 \code{U\_j = float(tr['U'])} 직접. 실 호출의 $\Delta S$ 인자는 seam
\code{\_effective\_dS\_rxn(tr, T\_prog)} 를 경유한다(흑연 base 는 항등으로 \code{tr['dS\_rxn']} 그대로 ---
LCO 전자항 plug-in 자리: \S\ref{sec:lco-code}). 여기에 vib 양자 보정 \code{\_vib\_dU(tr, T\_prog)} 가
가산된다($\theta_E$ 부재 시 항등 $0$ --- 보정 도입 전과 bit-exact). \code{GRAPHITE\_STAGING\_LIT} 의 stage 2$\to$1 은
$\Delta H_\rxn=-13000$ J/mol, $\Delta S_\rxn=-16$ J/(mol\,K) $\Rightarrow$ $U(298)=(13000-298.15\times16)/96485
\approx0.0853$ V(목표 $0.085$ V 정합).
\end{codebox}
\begin{codebox}
\textbf{N7 진행.} \code{n\_safe=|n\_j|} $\to$ $\mathcal A$(식~\eqref{eq:Acut}) $\to$
\code{(chi\_d, dH\_a\_use)=\_chi\_and\_dH\_eff(...)} (식~\eqref{eq:chid}$\cdot$\eqref{eq:dHeff}) $\to$
\code{L\_q=func\_L\_q(...,x=chi\_d,A=A)} (식~\eqref{eq:Lqfull}) $\to$ \code{L\_V=|dVdq\_qa|*L\_q} (식~\eqref{eq:LV}).
전이당 \emph{상수}(대표 $T_\rep\cdot$대표 $n$)로 한 번 평가한다.
\end{codebox}

% 자산: [B2-145] [B2-146] [B2-147] [B2-148] [B2-149] [B2-150] [B2-151] [B2-152] [B2-153]
