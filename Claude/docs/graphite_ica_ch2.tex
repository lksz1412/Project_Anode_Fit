% ====================================================================
% graphite_ica_ch2.tex — Chapter 2
%   흑연 음극의 발열: 가역 entropic 열 + 비가역 소산열을 Chapter 1 의
%   전하 보존·평형 logistic·완화 동역학 위에서 무비약 유도한다.
% ====================================================================
\documentclass[11pt,a4paper]{article}
\usepackage[margin=22mm]{geometry}
\usepackage{kotex}
\usepackage{amsmath,amssymb,mathtools,bm}
\usepackage{booktabs,longtable,array}
\usepackage{enumitem}
\usepackage{amsthm}
\usepackage{hyperref}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{setspace}
\setstretch{1.12}
\setlength{\parskip}{0.45em}\setlength{\parindent}{0pt}\setlength{\headheight}{14pt}
\hypersetup{colorlinks=true, linkcolor=blue, urlcolor=blue, citecolor=blue,
  pdftitle={흑연 음극의 발열 — Chapter 2}}
\pagestyle{fancy}\fancyhf{}
\lhead{흑연 음극의 발열 — Ch.2}\rhead{\thepage/\pageref{LastPage}}
\renewcommand{\headrulewidth}{0.4pt}

\newtheorem*{keybox}{핵심}
\newtheorem*{boundbox}{유효 범위}
\newtheorem*{flagbox}{비고}
\newtheorem*{rigorbox}{심화}
\newtheorem*{verifybox}{검산}

\newcommand{\dd}{\mathrm{d}}
\newcommand{\eff}{\mathrm{eff}}
\newcommand{\eq}{\mathrm{eq}}
\newcommand{\app}{\mathrm{app}}
\newcommand{\drive}{\mathrm{drive}}
\newcommand{\cell}{\mathrm{cell}}
\newcommand{\bg}{\mathrm{bg}}
\newcommand{\ext}{\mathrm{ext}}
\newcommand{\OCV}{\mathrm{OCV}}
\newcommand{\sstat}{\mathrm{ss}}
\newcommand{\peak}{\mathrm{peak}}
\newcommand{\src}{\mathrm{src}}
\newcommand{\rev}{\mathrm{rev}}
\newcommand{\irr}{\mathrm{irr}}
\newcommand{\rlx}{\mathrm{rlx}}
\newcommand{\rxn}{\mathrm{rxn}}
\newcommand{\amb}{\mathrm{amb}}
\newcommand{\kin}{\mathrm{kin}}
\newcommand{\Th}{\mathrm{th}}
\newcommand{\loss}{\mathrm{loss}}
\newcommand{\conv}{\mathrm{conv}}
\newcommand{\prog}{\mathrm{prog}}
\newcommand{\coord}{\mathrm{coord}}
\newcommand{\ohm}{\mathrm{ohm}}
\newcommand{\conc}{\mathrm{conc}}
\newcommand{\cand}{\mathrm{cand}}

\title{\textbf{\LARGE Chapter 2}\\[0.35em]\Large 흑연 음극의 발열: 가역 entropic 열과 비가역 소산열}
\author{}
\date{}

\begin{document}
\maketitle

% ====================================================================
\section*{서론}
\addcontentsline{toc}{section}{서론}
% ====================================================================
Chapter 1 은 흑연 음극의 $\dd Q/\dd V$ peak 가 온도$\cdot$C-rate$\cdot$극판 전위에 어떻게 반응하는지를 전하 보존과
평형 isotherm, 동역학 지연으로 닫았다. 그 과정에서 \emph{온도}는 평형 폭 $w_j=RT/F$ 와 속도상수 $k_j$ 의 Arrhenius
인자로 들어왔다. 그러나 셀 자신이 \emph{열을 낸다}: 충방전 중 음극은 가역 entropic 열과 비가역 소산열을 발생시켜
국소 온도 $T$ 를 바꾸고, 그 $T$ 가 다시 Chapter 1 의 모든 온도 의존 항으로 되먹임된다.

본 장은 그 발열을 \emph{Chapter 1 의 식 위에서} 유도한다. 새 상태식을 들여오지 않고, 전하 보존식의 해 $V_n$ 과
평형 진행률 $\xi_{\eq,j}$, 완화 동역학에서 가역열과 비가역열을 \emph{기원별}로 분해한다. 핵심은 세 가지다.
첫째, 가역열의 토대인 \emph{평형 전위 온도계수} $(\partial V_{n,\OCV}/\partial T)_q$ 를 외부 lookup 이 아니라 전하
보존식에서 유도한다(Chapter 1 의 ``OCV 는 보존식의 평형 특수해'' 흐름을 열 계층에서도 유지). 둘째, 그 온도계수의 \emph{전이별}
성분인 위치 온도계수 $\partial U_j/\partial T=\Delta S_j/(sF)$ 가 \emph{reaction entropy} $\Delta S_j$ 를 주며(전체
$(\partial V_{n,\OCV}/\partial T)_q$ 는 배경$\cdot$폭$\cdot$$D_q$ 정규화까지 묶은 가중 계수라 $\Delta S_j$ 와 동일물이
아니다), 이 $\Delta S_j$ 가 Chapter 1 의 \emph{activation entropy} $\Delta S_{a,j}$ 와 \emph{별개}임을 못박는다. 셋째, 비가역열의 동역학 성분이 Chapter 1 의 지연 꼬리와 같은 $k_j^{\eff}$ 로 닫혀 ICA
꼬리의 \emph{열적 거울}이 됨을 보인다.

\tableofcontents
\newpage

% ====================================================================
\section{기호와 규약 (Notation \& Conventions)}\label{sec:ch2_notation}
% ====================================================================
기호$\cdot$단위$\cdot$부호 규약은 Chapter 1 을 그대로 이어받는다. 방전 방향 부호 $s=+1$, 내부 전위 $V_n$(전하
보존식의 해), 평형 진행률 $\xi_{\eq,j}$, 완화 동역학 $\dd\xi_j/\dd t=k_j^{\eff}(\xi_{\eq,j}-\xi_j)$, 위치 온도계수
$\partial U_j/\partial T=\Delta S_j/(sF)$ 는 재정의하지 않는다. 속도상수 세 종은 Chapter 1 의 구분을 그대로
유지한다: $k_j\equiv k_{j,\mathrm{act}}$(활성화 완화율, Eyring 형), 직렬 율속 병목과의 합성
$1/k_j^{\eff}=1/k_{j,\mathrm{act}}+1/k_{j,\lim}$(직렬 합성 유효 속도), 그리고 율속 단계 $k_{j,\lim}$. 본 장의
꼬리식은 Chapter 1 과 같이 $k_j^{\eff}$ 를 쓴다.

\textbf{구동력의 일반형.} 전이 $j$ 의 구동력은 일반적으로
\begin{equation}
\mathcal A_j=s\,n_j^{\eff}\,F\,(V_\drive-U_j),\qquad n_j^{\eff}=\frac{RT}{Fw_j},
\label{eq:ch2_affinity_general}
\end{equation}
이며, Chapter 1 본문의 $\mathcal A_j=sF(V_\drive-U_j)$ 는 ideal width $w_j=RT/F$($n_j^{\eff}=1$)의 특수형이다.
본 장은 그 ideal-width 특수형($n_j^{\eff}=1$)을 기본으로 쓰되, 비가역열의 $n_j^{\eff}$ 가중 일반형은
Chapter 3--5 와의 cross-chapter 정합을 위해 일반형 식~\eqref{eq:ch2_affinity_general} 을 기준으로 한다(특수형과
일반형은 모순이 아니라 위계). 본 장에서 \emph{새로 도입}하는 기호만 아래에 정리한다(통합 시 Chapter 1 기호표와
병합 가능하도록 같은 열 구조).
\begin{longtable}{p{0.16\textwidth} p{0.17\textwidth} p{0.59\textwidth}}
\toprule 기호 & 단위 & 의미 \\ \midrule \endhead
$\dot{\mathcal Q}$ & W & 열률(시간율); 첨자 $\src$(열원)$\cdot$$\rev$(가역)$\cdot$$\irr$(비가역)$\cdot$$\rlx$(휴지 완화) \\
$\Delta S_j$ & J/(mol\,K) & 전이 $j$ 의 \emph{reaction} entropy(평형량, products$-$reactants); Chapter 1 의 위치 온도계수 $\partial U_j/\partial T=\Delta S_j/(sF)$ \\
$\Delta S_{a,j}$ & J/(mol\,K) & \emph{activation} entropy(전이상태; Chapter 1 $\Delta G_{a,j}=\Delta H_{a,j}-T\Delta S_{a,j}$). $\Delta S_j$ 와 \textbf{별개} \\
$\Pi_j$ & V & Peltier-type 계수 $=T\,\partial U_j/\partial T$ \\
$\eta_j$ & V & 과전압(국소 평형 이탈) $=V_\drive-V_{\eq,j}(\xi_j)$ \\
$V_{\eq,j}(\xi_j)$ & V & $\xi_j$ 가 현재값일 때 진행이 멈추는 국소 평형 전위(logistic 역함수) \\
$J_j$ & mol/s & 전이 $j$ 의 진행률 mole flux $=Q_j(\dd\xi_j/\dd t)/F$ \\
$n_j^{\eff}$ & --- & effective 전자수 $=RT/(Fw_j)$; ideal width 에서 $1$ \\
$D_q$ & C/V & 평형 chemical capacitance $=\partial Q_\bg/\partial V_n+\sum_j Q_j\,\partial\xi_{\eq,j}/\partial V_n$ \\
$h_\bg$ & V/K & background \textbf{전위} 온도계수 $(\partial V_{\bg,\eq}/\partial T)_{Q_\bg}$ (entropy coefficient 아님) \\
$\sigma_j$ & {\footnotesize W/(mol\,K)} & per-mode near-equilibrium entropy production rate(밀도) \\
$g''_j$ & J/mol & 국소 자유에너지 곡률 $\partial^2 G_j/\partial\xi_j^2|_\eq>0$(thermodynamic factor; 안정성) \\
$C_\Th$ & J/K & 음극 control volume 유효 열용량 \\
$h_T,A,T_\amb$ & {\footnotesize W/(m$^2$K), m$^2$, K} & 대류계수$\cdot$표면적$\cdot$주위 온도 \\
$s^{\conv}$ & --- & 발열-양 규약 bookkeeping 부호 $\in\{+1,-1\}$(피팅 대상 아님) \\
\bottomrule
\end{longtable}
$F=96485\ \mathrm{C/mol}$, $R=8.314\ \mathrm{J/(mol\,K)}$, $k_B,h$=Boltzmann$\cdot$Planck 상수. 열률 dot 표기는
시간율 [W].

% ====================================================================
\section{평형 전위의 온도계수 $(\partial V_{n,\OCV}/\partial T)_q$}\label{sec:ch2_ocvT}
% ====================================================================
가역 열은 평형 상태함수의 온도 의존에서 나온다. Chapter 1 의 전하 보존식이 $V_n$ 을 결정하므로, 가역 열의 기초가
되는 전위 온도계수도 외부 lookup 이 아니라 전하 보존식에서 유도해야 한다. 그래야 Chapter 1 의 ``OCV 는 보존식의
평형 특수해''라는 중심식 흐름이 열 계층에서도 끊기지 않는다. 평형($\xi_j=\xi_{\eq,j}$)에서 Chapter 1 의 전하
보존식은
\begin{equation}
Q_\cell\,q=Q_\bg(V_n,T)+\sum_{j=1}^{N_p}Q_j\,\xi_{\eq,j}(V_n,T),\qquad V_n=V_{n,\OCV}(q,T).
\label{eq:ch2_eqbal}
\end{equation}
본 유도에서 $Q_\cell,Q_j$ 는 온도 무관 기준 용량으로 둔다(accessible capacity 의 온도 의존은 별도 용량 보정으로
분리; 일반형은 절 끝).

\subsection{고정 $q$ 에서의 온도 미분}
$q$ 를 고정하고 식~\eqref{eq:ch2_eqbal} 의 양변을 $T$ 로 전미분한다. 좌변 $Q_\cell q$ 는 $q$ 고정이면 상수라
그 $T$-미분이 $0$ 이다. 우변에서 $\xi_{\eq,j}$ 는 \emph{두 경로}로 $T$ 에 의존한다: (가) $V_{n,\OCV}(T)$ 를 통한
간접 의존(연쇄율)과 (나) $U_j(T),w_j(T)$ 를 통한 $V_n$ 고정 명시적 의존. $Q_\bg(V_n,T)$ 도 같다. 이변수 전미분
$\dd f=\partial_{V_n}f\,\dd V_n+\partial_T f\,\dd T$ 를 $\dd T$ 로 나누고 $q$ 를 고정하면
\begin{equation}
0=\frac{\partial Q_\bg}{\partial V_n}\Big(\frac{\partial V_{n,\OCV}}{\partial T}\Big)_q+\frac{\partial Q_\bg}{\partial T}
+\sum_{j}Q_{j}\Big[\frac{\partial\xi_{\eq,j}}{\partial V_n}\Big(\frac{\partial V_{n,\OCV}}{\partial T}\Big)_q
+\Big(\frac{\partial\xi_{\eq,j}}{\partial T}\Big)_{V_n}\Big].
\label{eq:ch2_implicitT_raw}
\end{equation}
$(\partial V_{n,\OCV}/\partial T)_q$ 가 든 항을 분배법칙으로 묶으면
\begin{equation}
0=\underbrace{\Big[\frac{\partial Q_\bg}{\partial V_n}+\sum_{j}Q_{j}\frac{\partial\xi_{\eq,j}}{\partial V_n}\Big]}_{\equiv\,D_q\ (\text{평형 chemical capacitance})}
\Big(\frac{\partial V_{n,\OCV}}{\partial T}\Big)_q
+\frac{\partial Q_\bg}{\partial T}+\sum_{j}Q_{j}\Big(\frac{\partial\xi_{\eq,j}}{\partial T}\Big)_{V_n}.
\label{eq:ch2_implicitT}
\end{equation}
$D_q$ 가 \emph{평형 chemical capacitance} 인 것은 단위가 $\mathrm{C/V}$ 이고 $V_n$ 변화에 대한 저장 전하의 응답을
재기 때문이다. $D_q>0$ 은 Chapter 1 의 단조성에서 보장된다: $\partial Q_\bg/\partial V_n\ge\epsilon_Q>0$ 이고
$\partial\xi_{\eq,j}/\partial V_n=s\,\xi_{\eq,j}(1-\xi_{\eq,j})/w_j\ge0$(방전 $s=+1$, $0\le\xi_{\eq,j}\le1$,
$w_j>0$)이라 두 항이 비음이고 첫 항이 양의 하한을 가지므로 $D_q\ge\epsilon_Q>0$. 따라서
식~\eqref{eq:ch2_implicitT} 을 $(\partial V_{n,\OCV}/\partial T)_q$ 에 대해 풀 때 $0$ 으로 나누는 특이점이 없고
\begin{equation}
\boxed{\;\Big(\frac{\partial V_{n,\OCV}}{\partial T}\Big)_q
=-\,\frac{\dfrac{\partial Q_\bg}{\partial T}+\displaystyle\sum_{j}Q_{j}\Big(\dfrac{\partial\xi_{\eq,j}}{\partial T}\Big)_{V_n}}
{\dfrac{\partial Q_\bg}{\partial V_n}+\displaystyle\sum_{j}Q_{j}\dfrac{\partial\xi_{\eq,j}}{\partial V_n}}\;.}
\label{eq:ch2_dVocvdT}
\end{equation}

\begin{verifybox}
\textbf{차원.} 분자 $=\partial Q_\bg/\partial T$ 의 $\mathrm{C/K}$, 분모 $=D_q$ 의 $\mathrm{C/V}$, 전체
$=(\mathrm{C/K})/(\mathrm{C/V})=\mathrm{V/K}$ — 전위 온도계수의 올바른 단위. 이 식은 \emph{평형} 상태함수의
온도계수이며 apparent 전위 경로 미분이 아니다: $(\partial V_{n,\OCV}/\partial T)_q\ne\dd V_\app/\dd T$. 후자에는
$R_n(q,T,|I|)$ 의 온도 의존(비가역 분극)이 섞인다(\S\ref{sec:ch2_revOCV}).
\end{verifybox}

\subsection{logistic 평형 진행률의 온도 항}
식~\eqref{eq:ch2_dVocvdT} 를 닫으려면 $\partial\xi_{\eq,j}/\partial V_n$ 과 $(\partial\xi_{\eq,j}/\partial T)_{V_n}$ 가
필요하다. Chapter 1 의 평형 logistic(방전 $s=+1$)에서 출발한다. 보조변수 $z_j\equiv(V_n-U_j(T))/w_j(T)$ 로 두면
$\xi_{\eq,j}=(1+e^{-z_j})^{-1}$ 이고, Chapter 1 에서 유도된 표준 logistic 항등식
\begin{equation}
\frac{\dd\xi_{\eq,j}}{\dd z_j}=\xi_{\eq,j}(1-\xi_{\eq,j})
\label{eq:ch2_logistic_id}
\end{equation}
을 두 미분에 공통으로 쓴다.

\emph{(i) $V_n$ 미분.} $z_j$ 의 $V_n$ 의존은 $\partial z_j/\partial V_n=1/w_j$ 뿐이므로, 연쇄율로
\begin{equation}
\frac{\partial\xi_{\eq,j}}{\partial V_n}=\frac{\dd\xi_{\eq,j}}{\dd z_j}\frac{\partial z_j}{\partial V_n}
=\frac{\xi_{\eq,j}(1-\xi_{\eq,j})}{w_j(T)}.
\label{eq:ch2_dxidV}
\end{equation}
이는 Chapter 1 평형 logistic 의 $V_n$-미분($s=+1$ 특수형)과 일치한다(검산).

\emph{(ii) $V_n$ 고정 $T$ 미분.} $z_j=(V_n-U_j)/w_j$ 는 $T$ 에 두 경로($U_j(T)$ 와 $w_j(T)$)로 의존한다. 몫미분
$\dd(u/v)=(u'v-uv')/v^2$ 에 $u=V_n-U_j$, $u'=-U'_j$, $v=w_j$, $v'=w'_j$($U'_j\equiv\dd U_j/\dd T$,
$w'_j\equiv\dd w_j/\dd T$)를 대입하면
\begin{equation}
\Big(\frac{\partial z_j}{\partial T}\Big)_{V_n}
=\frac{-U'_j\,w_j-(V_n-U_j)\,w'_j}{w_j^2}
=-\frac{U'_j}{w_j}-\frac{(V_n-U_j)\,w'_j}{w_j^2},
\label{eq:ch2_dzdT}
\end{equation}
이고, 다시 연쇄율 $\partial\xi_{\eq,j}/\partial T=(\dd\xi_{\eq,j}/\dd z_j)(\partial z_j/\partial T)$ 로
\begin{equation}
\Big(\frac{\partial\xi_{\eq,j}}{\partial T}\Big)_{V_n}
=\xi_{\eq,j}(1-\xi_{\eq,j})\Big[-\frac{U'_j(T)}{w_j(T)}-\frac{V_n-U_j(T)}{w_j(T)^2}\,w'_j(T)\Big].
\label{eq:ch2_dxidT}
\end{equation}
식~\eqref{eq:ch2_dVocvdT} 에 식~\eqref{eq:ch2_dxidV} 와 식~\eqref{eq:ch2_dxidT} 를 넣으면
$(\partial V_{n,\OCV}/\partial T)_q$ 가 $Q_\bg,U_j,w_j,\xi_{\eq,j}$ 의 온도 의존이 결합된 결과로 드러난다. 이
결합을 단일 경험 보정으로 흡수하면 물리 해석성이 약해지므로 명시적으로 둔다.

\begin{boundbox}
\textbf{ideal width 와 SOC 부호 반전.} 식~\eqref{eq:ch2_dxidT} 의 첫 항 $-U'_j/w_j$ 는 reaction-entropy
기원(아래 \S\ref{sec:ch2_entropy}; $U'_j=\partial U_j/\partial T$)이고, 둘째 항 $-(V_n-U_j)w'_j/w_j^2$ 는
$(V_n-U_j)$ 부호에 따라 전이 양쪽에서 부호가 바뀐다(중심 $V_n=U_j$ 에서 $0$). 단 $w'_j>0$ 은 ideal $w_j=RT/F$
한정($w'_j=R/F$)이며, non-ideal $w_j(T)$ 에선 $w'_j$ 부호가 미정이라 둘째 항 결론도 ideal 범위 내 한정이다. 실제
graphite 의 $U_j(T)$(entropy profile)는 비단조라 $(\partial V_{n,\OCV}/\partial T)_q$ 가 SOC 에 따라 부호 반전할
수 있으며 \cite{reynier2004,thomasnewman2003}, 이는 측정된 anode entropy 곡선과 정합 검증할 대상이다(본 식은
logistic ideal 모델 내에서만 정확).
\end{boundbox}

% ====================================================================
\section{가역 반응열의 토대: $\partial U_j/\partial T=$ reaction entropy}\label{sec:ch2_entropy}
% ====================================================================
가역 반응열을 쓰려면 평형 전위 온도계수가 무엇인지를 열역학 1법칙 수준에서 먼저 확정해야 한다. 그 정체는
reaction entropy 이며, 이는 Chapter 1 Eyring rate 의 \emph{activation entropy} 와 별개의 양이다(\S\ref{sec:ch2_two_entropy}
에서 분리한다). 이 절이 본 장 가역열 유도의 출발점이다.

\subsection{$\partial U_j/\partial T=\Delta S_j/(sF)$ 의 유도}
전이 $j$ 의 intercalation 반쪽반응의 reaction Gibbs energy $\Delta G_{\rxn,j}$ 는 평형 전위 $U_j(T)$ 와 전기화학
평형으로 연결된다. 한 전자 이동(본 장 ideal 특수형 $n_j^{\eff}=1$)에 대해 \cite{bardfaulkner,newman2004}
\begin{equation}
\Delta G_{\rxn,j}=-\,sF\,U_j(T)
\label{eq:ch2_dGrxn}
\end{equation}
이며, 일반형 prefactor 는 $s\,n_j^{\eff}F$ 다(구동력 일반형 식~\eqref{eq:ch2_affinity_general} 과 같은
위계). 여기서 $s$ 는 방향 지시자로, Chapter 1 의 구동력 $\mathcal A_j$ 와 동일 부호 규약을 따른다(방전 $s=+1$).
다만 $\Delta G_{\rxn,j}$(평형 reaction Gibbs energy)는 전위 $U_j$ \emph{자체}에 비례하는 평형량인 반면, Chapter
1 의 $\mathcal A_j$ 는 구동 전위가 $U_j$ 에서 \emph{벗어난} 정도에 비례하는 비평형 구동력으로, 둘은 별개의 양이다
($\Delta G_{\rxn,j}\ne-\mathcal A_j$).

표준 열역학 항등식 $(\partial\Delta G/\partial T)_p=-\Delta S$ 를 식~\eqref{eq:ch2_dGrxn} 에 적용한다. 좌변은
$s,F$ 가 온도 무관이므로 $\partial\Delta G_{\rxn,j}/\partial T=-sF\,\partial U_j/\partial T$ 이고, 우변은
$-\Delta S_j$ 이다. 두 변을 같게 두면
\begin{equation}
-sF\,\frac{\partial U_j}{\partial T}=-\Delta S_j,
\label{eq:ch2_GH}
\end{equation}
양변을 $-sF$ 로 나누어
\begin{equation}
\boxed{\;\frac{\partial U_j}{\partial T}=\frac{\Delta S_j}{sF}\;}
\label{eq:ch2_entropy_coeff}
\end{equation}
를 얻는다. 곧 Chapter 1 의 위치 온도계수이며, 평형 전위의 온도의존이 reaction entropy 를 줌을 보인다. 이는
potentiometric entropy 측정(준평형 OCV 를 여러 $T$ 에서 취함)으로 ICA 와 독립하게 얻는 양이며, 흑연
$\mathrm{Li_xC_6}$ 의 $\partial U/\partial T$ 가 staging 에 따라 부호$\cdot$크기를 달리함이 실측돼 있다
\cite{reynier2004,thomasnewman2003}.

\begin{verifybox}
\textbf{차원.} 우변 $=[\mathrm{J/(mol\,K)}]/[\mathrm{C/mol}]=\mathrm{J/(C\,K)}=\mathrm{V/K}$($\mathrm{J/C}=\mathrm V$)
— 전위 온도계수와 일치. 또 식~\eqref{eq:ch2_dxidT} 의 첫 항 $-U'_j/w_j$ 가 바로 이 $\partial U_j/\partial T$ 이므로,
$(\partial V_{n,\OCV}/\partial T)_q$ 와 transition reaction entropy 가 한 뿌리($U_j(T)$)에서 나옴이 확인된다.
\end{verifybox}

\subsection{reaction entropy $\ne$ activation entropy}\label{sec:ch2_two_entropy}
Chapter 1 의 Eyring rate \cite{eyring1935} $k_j=k_0\,e^{-\Delta G_{a,j}/RT}$ 에 활성화 자유에너지의 분해
$\Delta G_{a,j}=\Delta H_{a,j}-T\Delta S_{a,j}$ 를 넣는다. 지수 안에서
\begin{equation}
-\frac{\Delta G_{a,j}}{RT}=-\frac{\Delta H_{a,j}-T\Delta S_{a,j}}{RT}=-\frac{\Delta H_{a,j}}{RT}+\frac{\Delta S_{a,j}}{R}
\label{eq:ch2_eyring_split}
\end{equation}
로 갈리고, 지수의 합은 지수의 곱이므로
\begin{equation}
k_j=k_0\,\exp\!\Big(\frac{\Delta S_{a,j}}{R}\Big)\exp\!\Big(-\frac{\Delta H_{a,j}}{RT}\Big)
\label{eq:ch2_eyring_entropy}
\end{equation}
가 된다. 곧 \emph{activation} entropy $\Delta S_{a,j}$ 는 rate 의 prefactor($e^{\Delta S_{a,j}/R}$ 의 온도무관
인자)에 들어가며, 이는 전이상태와 reactant 의 entropy 차다. 반면 $\Delta S_j$(식~\eqref{eq:ch2_entropy_coeff})는
\emph{평형}(products$-$reactants) entropy 로, 둘은 들어가는 자리가 다르다.
\begin{flagbox}
\textbf{두 entropy 의 별개성.} $\Delta S_{a,j}$ 와 $\Delta S_j$ 은 일반적으로 \emph{다르며} 서로 대입할 수 없다.
둘은 전이상태 entropy 를 통해(BEP 류 가정 하) 부분적으로만 연결될 수 있으나 본 장은 그 연결을 \emph{가정하지
않는다}. 따라서 $\partial U/\partial T$ 로 $\Delta S_{a,j}$ 를 추정하거나 그 역을 하는 것을 금한다 —
$\partial U/\partial T$ 는 reaction entropy 만, rate-prefactor 는 activation entropy 만 준다. 이 구분이 무너지면
Chapter 1 의 spectrum 파라미터 $\Delta S_a$ 와 본 장의 가역열 $\Delta S_j$ 가 오염되어 식별이 불가능해진다.
\end{flagbox}

% ====================================================================
\section{열원 항의 물리적 분해와 control-volume 열수지}\label{sec:ch2_decomp}
% ====================================================================
음극 control volume 내부 열원을 \emph{물리 기원별}로 분해한다(피팅 편의 분해가 아니라 entropy$\cdot$affinity$\cdot$
overpotential 기원 분해) \cite{bernardi1985,rao1997}:
\begin{equation}
\dot{\mathcal Q}_{\src}=\dot{\mathcal Q}_{\rev}+\dot{\mathcal Q}_{\irr}+\dot{\mathcal Q}_{\rlx}^{\cand}.
\label{eq:ch2_srcdecomp}
\end{equation}
$\dot{\mathcal Q}_{\rev}$(가역, 열역학 기원)은 흡열일 수 있어 부호가변이고, $\dot{\mathcal Q}_{\irr}$(비가역,
동역학 기원)은 $\ge0$ 이며, $\dot{\mathcal Q}_{\rlx}^{\cand}$ 는 외부 전류가 작아도 내부 $\xi_j$ 완화로 생길 수
있는 \emph{후보} 항이다(\S\ref{sec:ch2_rest}; 가역/비가역 정량 분배는 forward/backward 비대칭이 필요해 Chapter
3--4). 외부 열손실까지 포함한 control-volume 1법칙 열수지는
\begin{equation}
C_\Th\frac{\dd T}{\dd t}=\dot{\mathcal Q}_{\src}-\dot{\mathcal Q}_{\loss},\qquad
\dot{\mathcal Q}_{\loss}=h_T A(T-T_\amb),
\label{eq:ch2_balance}
\end{equation}
(좌변 $\mathrm{(J/K)(K/s)=W}$, $\dot{\mathcal Q}_{\loss}$ 단위 $\mathrm{(W/(m^2K))\,m^2\,K=W}$). 이는
Bernardi--Pawlikowski--Newman 의 2항 balance($q_\rev+q_\irr$)에 본 장 후보항 $\dot{\mathcal Q}_{\rlx}^{\cand}$
를 더한 형태다 \cite{bernardi1985}.

\begin{keybox}
\textbf{Chapter 1 분업의 열적 대응.} $\dot{\mathcal Q}_{\rev}$ 는 Chapter 1 의 ``방향$=$열역학(target$\cdot$entropy)''에,
$\dot{\mathcal Q}_{\irr}$ 는 ``속도$=$동역학(mobility$\cdot$affinity)''에 대응한다. Chapter 1 에서 $\chi\mathcal A_j$
는 방향성 없는 mobility 가속이고 방향(target)은 열역학이 전담했다. 그 분업이 열에서는 \emph{가역열$=$entropy(방향)},
\emph{비가역열$=$소산(속도)} 으로 나타난다. 가역/비가역의 \emph{정량} 분해는 forward/backward rate 비대칭을
요하므로 Chapter 3--4 에서 닫힌다.
\end{keybox}

% ====================================================================
\section{가역 열 (1): 평형 전위 온도계수 기반}\label{sec:ch2_revOCV}
% ====================================================================
평형 준정적 경로에서 한 transition 의 reaction rate 는 Faraday 법칙으로
\begin{equation}
\dot n_j=\frac{I_j}{sF}\quad[\mathrm{mol/s}],\qquad \sum_j I_j=I,
\label{eq:ch2_faraday}
\end{equation}
이다($I_j$=전이 $j$ 의 partial current). entropic 가역 열률은 reversible 경로의 $T\,\Delta S$ 흐름
$q_\rev=T\,\Delta S_j\,\dot n_j$ 이며, 여기에 식~\eqref{eq:ch2_entropy_coeff} 의 $\Delta S_j=sF\,\partial U_j/\partial T$
를 넣으면 $sF$ 와 $\dot n_j$ 의 $1/(sF)$ 가 상쇄되어
\begin{equation}
\boxed{\;\dot{\mathcal Q}_{\rev,j}=T\,\Delta S_j\,\frac{I_j}{sF}=I_j\,T\,\frac{\partial U_j}{\partial T}=I_j\,\Pi_j,\qquad
\Pi_j\equiv T\,\frac{\partial U_j}{\partial T}.\;}
\label{eq:ch2_qrev}
\end{equation}
$\Pi_j$ 는 Peltier-type 계수[V]이며, $\dot{\mathcal Q}_{\rev,j}=I_j\Pi_j$ 는 전류$\times$Peltier 전위 형이다.
$I_j$ 가 부호 있는 전류이므로 $\dot{\mathcal Q}_{\rev,j}$ 는 부호가변이다($\partial U_j/\partial T>0$, $I_j>0$ 이면
흡열). 정전류에서 $|I|=|\sum_j I_j|$ 이고 $(\partial V_{n,\OCV}/\partial T)_q$ 가 식~\eqref{eq:ch2_dVocvdT} 로
모든 transition 기여를 집계하므로, 음극 control volume 의 가역 열률은
\begin{equation}
\dot{\mathcal Q}_{\rev}^{\OCV}=s^{\conv}\,|I|\,T\Big(\frac{\partial V_{n,\OCV}}{\partial T}\Big)_q
\label{eq:ch2_revOCV}
\end{equation}
가 된다. 여기서 $s^{\conv}$ 는 음극 전위 부호$\cdot$전류 방향$\cdot$발열-양(heat-positive) 규약을 명시한 뒤
고정하는 bookkeeping 부호(피팅 대상 아님)이며, $(\partial V_{n,\OCV}/\partial T)_q$ 는 \S\ref{sec:ch2_ocvT} 에서
전하 보존식으로 유도된 값이지 외부 lookup 이 아니다.

\begin{verifybox}
\textbf{차원.} $|I|\,T\,(\partial V/\partial T)=\mathrm A\cdot\mathrm K\cdot\mathrm{(V/K)}=\mathrm{A\,V}=\mathrm W$.
\textbf{금지되는 연결.} $\dot{\mathcal Q}_{\rev}\not\propto|I|\,T\,\dd V_\app/\dd T$ — $V_\app=V_n+s_I|I|R_n$ 는
평형 전위가 아니라 그 경로 미분에 $R_n(q,T,|I|)$ 의 온도 의존(비가역 분극)이 섞이므로, 이를 가역열로 쓰면 비가역
소산을 가역 entropy 로 오계상한다.
\end{verifybox}

% ====================================================================
\section{가역 열 (2): transition entropy basis ($\xi_j$ 기반)}\label{sec:ch2_revxi}
% ====================================================================
Chapter 1 의 핵심 내부 상태변수는 $\xi_j$ 다. 따라서 transition entropy 변화는 진행률 시간율 $\dd\xi_j/\dd t$ 를
통해서도 열 항으로 표현된다. 이는 식~\eqref{eq:ch2_revOCV} 의 OCV 계수 방식과 같은 entropy 정보를 더 미시적인
수준에서 쓴 것이다. 전이 $j$ 의 진행률 mole flux 는
\begin{equation}
J_j=\frac{Q_j}{F}\frac{\dd\xi_j}{\dd t}\quad[\mathrm{mol/s}]
\label{eq:ch2_Jdef}
\end{equation}
이며($Q_j\,\dd\xi_j$ 가 진행에 든 전하[C], $F$ 로 나눠 몰수), 그 reversible 열률은 $T\,\Delta S_j\,J_j$ 이므로
\begin{equation}
\dot{\mathcal Q}_{\rev,\xi}=\sum_{j=1}^{N_p}s^{\conv}\,T\,\Delta S_j\,\frac{Q_j}{F}\,\frac{\dd\xi_j}{\dd t}
\label{eq:ch2_revxi}
\end{equation}
가 된다. 정전류 방전 구간에서만 Chapter 1 의 완화식을 정전류 좌표 변환
\begin{equation}
\frac{\dd\xi_j}{\dd t}=\frac{|I|}{Q_\cell}\frac{\dd\xi_j}{\dd q}\qquad(\text{정전류 한정})
\label{eq:ch2_revxi_q}
\end{equation}
로 $q$-좌표식으로 바꿀 수 있다. 휴지$\cdot$가변전류 구간에서는 $|I|/Q_\cell$ 이 시간 상수가 아니므로 이 변환을
쓰지 않고 $\dd\xi_j/\dd t$ 를 직접 쓴다(\S\ref{sec:ch2_rest}).

\begin{verifybox}
\textbf{차원.} $T\,\Delta S_j\,(Q_j/F)\,\dd\xi_j/\dd t\sim\mathrm K\cdot\tfrac{\mathrm J}{\mathrm{mol\,K}}\cdot
\tfrac{\mathrm C}{\mathrm{C\,mol^{-1}}}\cdot\mathrm s^{-1}=\mathrm W$($Q_j/F$ 단위 $\mathrm{mol}$, $\xi_j$ 무차원).
\end{verifybox}

\subsection{두 방식의 정합 (double counting 금지)}\label{sec:ch2_double}
$\dot{\mathcal Q}_{\rev}^{\OCV}$(식~\eqref{eq:ch2_revOCV})와 $\dot{\mathcal Q}_{\rev,\xi}$(식~\eqref{eq:ch2_revxi})
는 \emph{같은 평형 entropy 정보}를 서로 다른 수준에서 표현한다. 둘을 독립 발열항으로 동시에 자유롭게 더하면 double
counting 이다. 평형 준정적 방전($\xi_j=\xi_{\eq,j}$, $V_n=V_{n,\OCV}$)에서 두 표현이 같은 가역 열률을 주어야 한다:
\begin{equation}
\boxed{\;s^{\conv}|I|\,T\Big(\frac{\partial V_{n,\OCV}}{\partial T}\Big)_q
=s^{\conv}\,T\,h_\bg\,\dot Q_\bg^{\prog}+\sum_{j}s^{\conv}\,T\,\Delta S_j\,\frac{Q_j}{F}\,\frac{\dd\xi_{\eq,j}}{\dd t}\;.}
\label{eq:ch2_consistency}
\end{equation}
\begin{boundbox}
이 정합은 \emph{reaction-entropy 성분($\propto\partial U_j/\partial T$)에 한정}해 성립한다. OCV 방식 좌변은 추가로
(i) logistic width 온도의존 항 $-(V_n-U_j)w'_j/w_j^2$(식~\eqref{eq:ch2_dxidT})과 (ii) 분모 $D_q$ 정규화를
포함하므로, 두 방식은 \emph{부분적으로만} 같은 정보다. 따라서 double-counting 차단은 reaction-entropy 성분에
부과되며, 등호는 등온$\cdot$준정적 $+$ 단일 우세 transition(또는 $D_q$ 정규화 명시) 하에서 닫힌다. 좌$\cdot$우변의
$s^{\conv}$ 가 \emph{동일한 heat-positive 규약}에서 유도돼야 부호 모순 없이 닫힌다.
\end{boundbox}
저율 OCV series 가 좌변 $(\partial V_{n,\OCV}/\partial T)_q$ 를 고정하면 우변의 $\{h_\bg,\Delta S_j\}$ 합이
제약되어 double counting 이 제거된다.

% ====================================================================
\section{배경 용량과 background entropy coefficient}\label{sec:ch2_bg}
% ====================================================================
$Q_\bg$ 는 잔류 background capacity 저장소이지 entropy 자체가 아니다. background reversible heat 를 쓰려면 별도
entropy coefficient
\begin{equation}
h_\bg(V_n,T)\equiv\Big(\frac{\partial V_{\bg,\eq}}{\partial T}\Big)_{Q_\bg}\quad[\mathrm{V/K}]
\label{eq:ch2_hbg}
\end{equation}
가 필요하다($V_{\bg,\eq}$=background 저장의 국소 평형 전위). 그 후보 열률은
\begin{equation}
\dot{\mathcal Q}_{\rev,\bg}^{\cand}=s^{\conv}\,T\,h_\bg(V_n,T)\,\dot Q_\bg^{\prog},
\label{eq:ch2_revbg}
\end{equation}
이며 $\dot Q_\bg^{\prog}$ 는 실제 background 진행 율[C/s]이다.

\textbf{progress 와 coordinate-shift 의 구분.} Chapter 1 의 전하 보존식을 시간 미분하면 $Q_\bg(V_n,T)$ 의
전미분으로
\begin{equation}
\frac{\dd Q_\bg}{\dd t}=\frac{\partial Q_\bg}{\partial V_n}\frac{\dd V_n}{\dd t}+\frac{\partial Q_\bg}{\partial T}\frac{\dd T}{\dd t}
\label{eq:ch2_dQbgdt}
\end{equation}
이다. 비등온 조건에서 $\dd Q_\bg/\dd t$ 전체를 곧바로 reaction progress 로 해석하면 안 된다. 온도 변화에 의한
항이 섞이기 때문이다. 개념적으로 분리하면
\begin{equation}
\dot Q_\bg^{\prog}=\frac{\partial Q_\bg}{\partial V_n}\frac{\dd V_n}{\dd t},\qquad
\dot Q_\bg^{\coord}=\frac{\partial Q_\bg}{\partial T}\frac{\dd T}{\dd t},
\label{eq:ch2_bgsplit}
\end{equation}
여기서 $\dot Q_\bg^{\prog}$(전위 구동 progress)만 heat-generating 진행으로 들어가고, $\dot Q_\bg^{\coord}$(온도
변화에 따른 capacity-coordinate shift)는 reaction rate 로 넣지 않는다. 이 구분을 빠뜨리면 단순 온도 drift 가
가역 발열로 오계상된다. 온도 경로($h_\bg$)와 $V_n$ 경로($\dot Q_\bg^{\prog}$)를 잇는 좌표 연결은 background 평형
항등식
\begin{equation}
\Big(\frac{\partial Q_\bg}{\partial T}\Big)_{V_n}=-\Big(\frac{\partial Q_\bg}{\partial V_n}\Big)\,h_\bg
\label{eq:ch2_bgidentity}
\end{equation}
로 주어진다.

% ====================================================================
\section{비가역 열: 과전압과 entropy production}\label{sec:ch2_irr}
% ====================================================================
가역 열은 평형 entropy 가 주지만, 전류가 흐르면 평형에서 벗어난 만큼 \emph{소산}이 생긴다. 그 비가역 열은
비평형 열역학의 flux$\times$force(entropy production)에서 출발하며 2법칙으로 항상 $\ge0$ 이다
\cite{degrootmazur1962,onsager1931}.

\subsection{공액 force $=$ 과전압 $\eta_j$}\label{sec:ch2_fluxforce}
진행률 mole flux $J_j$(식~\eqref{eq:ch2_Jdef})에 공액인 열역학적 force 는 국소 평형으로부터의 이탈, 즉 과전압
$\eta_j$ 다. entropy production 과 그 정의는
\begin{equation}
T\dot S_{\irr}=\sum_j J_j\,F\eta_j\ \ge\ 0,\qquad
\eta_j\equiv V_\drive-V_{\eq,j}(\xi_j),\quad V_{\eq,j}(\xi_j)=U_j(T)+w_j(T)\ln\frac{\xi_j}{1-\xi_j}.
\label{eq:ch2_fluxforce}
\end{equation}
국소 평형 전위 $V_{\eq,j}(\xi_j)$ 는 ``$\xi_j$ 가 현재값일 때 그것을 평형으로 만드는 전위''로, Chapter 1
logistic($s=+1$)의 역함수다. $\xi_j=[1+e^{-(V_{\eq,j}-U_j)/w_j}]^{-1}$ 에서 출발해
\begin{equation}
\frac{1-\xi_j}{\xi_j}=e^{-(V_{\eq,j}-U_j)/w_j}
\;\xrightarrow{\ \ln\ }\;
\ln\frac{\xi_j}{1-\xi_j}=\frac{V_{\eq,j}-U_j}{w_j}
\;\Longrightarrow\;
V_{\eq,j}(\xi_j)=U_j+w_j\ln\frac{\xi_j}{1-\xi_j}
\label{eq:ch2_Veqinv}
\end{equation}
를 얻는다(로그를 취한 뒤 부호를 뒤집어 정리; 식~\eqref{eq:ch2_fluxforce} 셋째 식과 동일).

\textbf{부호 정합($\ge0$, 기본형 $V_\drive=V_n$).} $V_{\eq,j}$ 는 $\xi_j$ 의 증가함수다($\ln[\xi_j/(1-\xi_j)]$ 가
증가). $\xi_j<\xi_{\eq,j}$ 이면 $V_{\eq,j}(\xi_j)<V_\drive$ 라 $\eta_j>0$ 이고, 동시에 Chapter 1 의 완화식에서
$\dd\xi_j/\dd t=k_j(\xi_{\eq,j}-\xi_j)>0$ 라 $J_j>0$ 이므로, 두 양이 같은 부호라 $J_jF\eta_j>0$ 이다.
$\xi_j>\xi_{\eq,j}$ 이면 둘 다 음이라 곱은 여전히 양이다. 평형에서는 $\eta_j\to0$ 과 $\dd\xi_j/\dd t\to0$ 이
동시에 일어나 entropy production 이 사라진다. 따라서 기본형에서 $T\dot S_{\irr}=\sum_j J_jF\eta_j\ge0$ 이 항별로
보장된다.
\begin{flagbox}
\textbf{rate-affinity 와 heat-force 의 구분.} Chapter 1 의 rate-구동 affinity $\mathcal A_j=sF(V_\drive-U_j)$ 는
전이 \emph{중심} $U_j$ 기준이라 평형에서도 0 이 아니다(배리어 낮춤용). heat-구동 force 는 국소 평형 이탈
$\eta_j$ 다. 둘의 관계는 식~\eqref{eq:ch2_Veqinv} 의 $V_{\eq,j}$ 를 대입해
\[
\frac{\mathcal A_j}{F}=V_\drive-U_j
=\underbrace{\big(V_\drive-V_{\eq,j}(\xi_j)\big)}_{=\,\eta_j}
+\underbrace{\big(V_{\eq,j}(\xi_j)-U_j\big)}_{=\,w_j\ln[\xi_j/(1-\xi_j)]}
\quad\Longrightarrow\quad
\frac{\mathcal A_j}{F}=\eta_j+w_j\ln\frac{\xi_j}{1-\xi_j}
\]
로 쪼개진다. 둘째 항(configurational 가역 혼합 entropy)은 비가역 열이 아니라 가역 entropy basis
(\S\ref{sec:ch2_revxi})로 간다. 즉 \textbf{rate 에는 $\mathcal A_j$, dissipation 에는 $\eta_j$} 를 쓴다. 이 둘을
혼동하면 가역 혼합 entropy 가 비가역 열로 오계상된다(logistic 평형 모델 내에서 정확).
\end{flagbox}

\subsection{전류$\times$과전압 축약형}\label{sec:ch2_Ieta}
flux$\times$force(식~\eqref{eq:ch2_fluxforce})를 전류와 과전압으로 축약하면, 일관된 부호 규약에서 비가역 열률은
\begin{equation}
\boxed{\;\dot{\mathcal Q}_{\irr}=|I|\,\eta\ \ge\ 0,\qquad \eta=\sum_j\frac{J_jF}{I}\,\eta_j\ (\ge0).\;}
\label{eq:ch2_qirr}
\end{equation}
각 항 $J_jF\eta_j\ge0$ (식~\eqref{eq:ch2_fluxforce})이라 $\ge0$ 이 이미 닫혀, ``$I\eta$ 단독''이 아니라 $|I|\eta$
형이다. 과전압 $\eta$ 는 charge-transfer($\eta_\kin$, Chapter 1 effective barrier 활성화), ohmic($\eta_\ohm$,
$R_{n,\eff}$ 분극), transport($\eta_\conc$) 기여의 합으로 분해되며 각각 $\ge0$ 이다.
\begin{boundbox}
\textbf{$n^{\eff}=1$ 특수형.} 식~\eqref{eq:ch2_qirr} 는 ideal-width($n_j^{\eff}=RT/(Fw_j)=1$) 특수형이다.
effective width 일반형에서는 $\dot{\mathcal Q}_{\irr}=\sum_j n_j^{\eff}\,I_j\,\eta_j$ 로 $n_j^{\eff}$ 가중을
받으며, 그 일반형과 양정치 보존($\sum_j n_j^{\eff}I_j\eta_j\ge0$)의 완전 유도는 \textbf{Chapter 4} 로 넘긴다.
본 장은 그 ideal-width 특수해로 정합한다(모순이 아니라 일반/특수 위계).
\end{boundbox}

\subsection{kinetic-lag 의 entropy production $\sigma_j\propto k_j r_j^2$}\label{sec:ch2_sigma}
Chapter 1 의 완화 $\dd\xi_j/\dd t=k_j r_j$($r_j=\xi_{\eq,j}-\xi_j$)를 비평형 열역학의 미시 형태로 본다. 국소
자유에너지 $G_j(\xi_j)$ 를 평형점 $\xi_{\eq,j}$ 근처에서 Taylor 전개하면, 평형점에서 1차항이 0
($\partial G_j/\partial\xi_j|_\eq=0$)이므로
\begin{equation}
G_j(\xi_j)=G_j(\xi_{\eq,j})+\tfrac12\,g''_j\,(\xi_j-\xi_{\eq,j})^2+\cdots,\qquad
g''_j\equiv\frac{\partial^2 G_j}{\partial\xi_j^2}\Big|_\eq.
\label{eq:ch2_Gtaylor}
\end{equation}
복원력(affinity)은 자유에너지 기울기의 음수이므로, 1차(선형) 근사에서
\begin{equation}
\mathcal X_j\equiv-\frac{\partial G_j}{\partial\xi_j}\Big|_{\xi_j}\simeq-g''_j(\xi_j-\xi_{\eq,j})=g''_j\,r_j,
\qquad g''_j>0,
\label{eq:ch2_Xaffinity}
\end{equation}
이다($g''_j>0$ 은 평형 안정성, regular-solution thermodynamic factor \cite{bazant2013}). 무차원 진행률 flux
$J^\xi_j=\dd\xi_j/\dd t=k_j r_j$ [1/s]에 대해 entropy production 은
\begin{equation}
\boxed{\;\sigma_j=\frac{J^\xi_j\,\mathcal X_j}{T}=\frac{g''_j\,k_j}{T}\,r_j^{\,2}\ \ge 0,\;}
\label{eq:ch2_sigma}
\end{equation}
즉 entropy production 은 lag $r_j$ 의 \emph{제곱}에 비례해 항상 $\ge0$. $g''_j$[J/mol]이라 $T\sigma_j=g''_j k_j r_j^2$
는 per-mode [W/mol]이며, control-volume balance([W])의 형제항과 차원을 맞추려면 mode 의 몰 함량 $(Q_j/F)$[mol]을
곱해
\begin{equation}
\dot{\mathcal Q}_{\irr,\kin,j}=\frac{Q_j}{F}\,T\sigma_j=\frac{Q_j}{F}\,g''_j k_j r_j^2\quad[\mathrm W].
\label{eq:ch2_qirrkin_ext}
\end{equation}
\begin{boundbox}
식~\eqref{eq:ch2_sigma}--\eqref{eq:ch2_qirrkin_ext} 는 평형 근처 1차(선형) 전개라 tail 영역($r_j$ 작음)에서
유효하며 큰 $r_j$ 의 전역 정당화가 아니다(Chapter 1 의 near-equilibrium 완화와 동일 bound; 큰 $r_j$ 는 고차 보정).
\end{boundbox}

\subsection{$R_n$ 과 heat-generation resistance 의 구분}\label{sec:ch2_Rn}
Chapter 1 의 $R_n$ 은 \emph{apparent 전압축 이동 계수}($V_\app-V_n=s_I|I|R_n$)이지 실제 dissipated-heat
resistance 가 아니다. 작은 과전압(선형 응답 $\eta\simeq R_{n,\eff}|I|$)에서만 reduced form
$\dot{\mathcal Q}_{\irr}\approx I^2R_{n,\eff}$($q_\irr=|I|\eta=|I|R_{n,\eff}|I|=I^2R_{n,\eff}$)이며,
$R_{n,\eff}\ne R_n$ 이 원칙이다. $R_{n,\eff}$ 가 ohmic/charge-transfer/transport 중 무엇을 대표하는지는 독립
실험(pulse/current-interrupt/impedance) 또는 Chapter 3 kinetics 로 제한돼야 한다 — 전압 자료만으로
$R_n,R_{n,\eff},R_{\mathrm{ct}}$ 를 동시 자유 피팅하면 apparent shift 와 실제 dissipated heat 가 혼동된다.

% ====================================================================
\section{휴지 구간 완화와 열 발생 가능성}\label{sec:ch2_rest}
% ====================================================================
휴지 구간에서는 $I=0$, $\dd q/\dd t=0$ 이지만 내부 진행률은 Chapter 1 의 완화식으로 계속 이완된다
($\dd\xi_j/\dd t=k_j^{\eff}r_j$). 전하 보존식은 매 시간점에서 다시 풀린다($q$ 는 고정, $\xi_j(t)$ 변화에 맞춰
$V_n(t)$ 가 추종):
\begin{equation}
Q_\cell\,q_{\mathrm{rest}}=Q_\bg(V_n(t),T)+\sum_j Q_j\,\xi_j(t).
\label{eq:ch2_rest_balance}
\end{equation}
휴지 중 외부 $I^2R$ 열은 $I=0$ 이라 사라지지만, $\xi_j$ 완화가 있으면 entropy exchange 후보
\begin{equation}
\dot{\mathcal Q}_{\xi,\rlx}^{\cand}=\sum_j s^{\conv}\,T\,\Delta S_j\,\frac{Q_j}{F}\,\frac{\dd\xi_j}{\dd t}
\qquad(\text{정전류 좌표 변환 금지: }\dd\xi_j/\dd t\text{ 직접})
\label{eq:ch2_relax}
\end{equation}
와 비가역 floor 가 남는다. 비가역 floor 는 $r_j\ne0$, $\dd\xi_j/\dd t=k_jr_j\ne0$ 이므로
식~\eqref{eq:ch2_qirrkin_ext} 로 정량 확정된다: $\dot{\mathcal Q}_{\irr,\kin,j}=(Q_j/F)\,g''_j k_j r_j^2\ge0$.
\begin{flagbox}
휴지 완화의 가역/비가역 분리는 forward/backward rate 의 비대칭(detailed balance 로부터의 이탈)에 달려 있다.
Chapter 1 의 $k_j=r_j^++r_j^-$ 는 정/역 속도의 \emph{합}만 줄 뿐 비대칭은 주지 않으므로,
$\dot{\mathcal Q}_{\xi,\rlx}$ 의 가역/비가역 \emph{정량} 분해는 Chapter 3--4 의 forward/backward rate 구조가
있어야 확정된다. 따라서 이 항은 \emph{후보}로 둔다. 비가역 floor 는 near-equilibrium 에서 정량 확정($\ge0$)이며,
Chapter 3--4 가 닫는 것은 가역 entropic 성분의 배분과 큰-$r_j$ 일반화다.
\end{flagbox}

% ====================================================================
\section{온도 되먹임 구조와 계산 순서}\label{sec:ch2_feedback}
% ====================================================================
온도는 Chapter 1 의 $Q_\bg(V_n,T),\ \xi_{\eq,j}(V_n,T),\ U_j(T),\ w_j(T),\ \rho_G(G;T),\ k_j^{\eff},\ k_{j,\lim},
\ R_n,\ V_{n,\OCV}$ 에 모두 되먹임된다. 따라서 비등온 계산에서 $V_n,\xi_j,T$ 를 독립 후처리하지 않고 같은 시간
적분 루프에서 갱신한다(Chapter 1 의 순환 의존과 같은 implicit 구조):
\begin{enumerate}[label=\arabic*),leftmargin=2.0em,itemsep=1pt]
\item 외부 전류에서 $q(t)$ 업데이트.
\item 현재 $q,T,\{\xi_j\}$ 에서 전하 보존식으로 $V_n$ root-find(단조성 $\partial Q_\bg/\partial V_n\ge\epsilon_Q>0$,
  $D_q\ge\epsilon_Q>0$ 로 유일$\cdot$수렴; implicit 비선형 방정식, 수치 구현은 부록).
\item $V_n,T$ 에서 $\xi_{\eq,j}$, 활성화 $k_{j,\mathrm{act}}$, 필요 시 $k_{j,\lim}$ 및 직렬 합성 $k_j^{\eff}$.
\item Chapter 1 의 완화식으로 $\dd\xi_j/\dd t$ 계산.
\item 물리적으로 정의된 과전압$\cdot$affinity$\cdot$entropy coefficient 로 열원 항(식~\eqref{eq:ch2_srcdecomp}):
  가역 $\dot{\mathcal Q}_{\rev}$(식~\eqref{eq:ch2_revOCV} 또는 \eqref{eq:ch2_revxi}, 정합 식~\eqref{eq:ch2_consistency}),
  비가역 $\dot{\mathcal Q}_{\irr}=|I|\eta$(식~\eqref{eq:ch2_qirr}).
\item 열수지식(식~\eqref{eq:ch2_balance})으로 $T(t)$ 업데이트.
\end{enumerate}
\begin{boundbox}
\textbf{되먹임이 ICA tail 에 미치는 영향.} 발열 $\to T\uparrow\to k_j\uparrow$ ($L=|I|/(Q_\cell k_j)\downarrow$)
$\to$ 꼬리 단축; 동시에 ideal $w_j=RT/F\uparrow$ 로 평형 peak 폭 증가($\partial\xi_{\eq,j}/\partial V_n\propto1/w_j$).
두 효과가 경쟁하므로 비등온 ICA tail 을 단일 부호로 단정하지 않는다(Chapter 1 의 온도 tail 분해와 정합). 이것이
본 장 발열이 ICA tail 로 되먹임되는 핵심 고리다.
\end{boundbox}

% ====================================================================
\section{비가역열 tail $=$ ICA tail 의 열적 거울(정합 가설)}\label{sec:ch2_thermal_tail}
% ====================================================================
Chapter 1 은 ICA tail 을 완화 길이 spectrum 의 kernel 적분으로 설명했다. 같은 완화가 비가역 열(kinetic-lag 성분,
$\sigma_j$ 채널)을 만들므로(식~\eqref{eq:ch2_sigma}), 같은 kinetic 정보가 비가역열 tail 에도 나타나야 한다. 이는
ICA tail 의 ``열적 거울''이며, calorimetry 로 Chapter 1 의 kinetic 귀속을 독립 modality 로 반증할 길을 연다.

단일 mode 의 유도부터 본다. post-peak tail 에서 단일 mode 의 잔차는 Chapter 1 single-mode kernel 로
$r_j(q)=r_j(q_a)e^{-(q-q_a)/L}$($L=|I|/(Q_\cell k_j)$)다. 이를 식~\eqref{eq:ch2_sigma} 의 per-mode 비가역
열률에 넣으면 $r_j^2=r_j(q_a)^2 e^{-2(q-q_a)/L}$ 이므로
\begin{equation}
\frac{\dd q_{\irr,\kin,j}}{\dd q}\ \propto\ g''_j\,k_j\,r_j(q)^2=g''_j\,k_j\,r_j(q_a)^2\,e^{-2(q-q_a)/L}.
\label{eq:ch2_thermal_single}
\end{equation}
이를 Chapter 1 의 ICA progress kernel 과 비교한다. 후자는 $\dd\xi_j/\dd q=r_j/L\propto(1/L)e^{-(q-q_a)/L}$ 로
특성 길이 $L$ 이다. 비가역열을 같은 \emph{용량축}으로 환산하면 $\dd t/\dd q=Q_\cell/|I|$, $k_j=|I|/(Q_\cell L)$
이므로
\begin{equation}
g''_j\,k_j\,r_j^2\,\frac{Q_\cell}{|I|}\ \propto\ \frac{1}{L}\,g''_j\,r_j^2,
\label{eq:ch2_thermal_qaxis}
\end{equation}
즉 같은 kinematic $1/L$ 인자를 품되 $r_j^2$ 때문에 감쇠 지수가 2배 $-2(q-q_a)/L$ 라 특성 길이가 절반 $L/2$(2배
빠른 감쇠)이고 진폭에 $g''_j$ 가 붙는다. spectrum 으로 중첩하면
\begin{equation}
\boxed{\;\frac{\dd q_{\irr,\kin}}{\dd q}\simeq\int_0^\infty A_L^{(2)}(L;T,V_\drive)\,e^{-2(q-q_a)/L}\,\dd L,\;}
\label{eq:ch2_thermal_kernel}
\end{equation}
$A_L^{(2)}$ 는 Chapter 1 spectrum 의 \emph{지지집합} $\{L\}$ 을 그대로 쓰되 per-mode weight $g''_j k_j r_j(q_a)^2$
($g''_j$ 진폭과 $1/L$ 포함)을 실은 \textbf{재가중} 분포다 — Chapter 1 의 $A_L$ 자체와 같지 않다.
\begin{flagbox}
\textbf{정합 가설.} ``decay length 비 $=2$''는 단일 우세 mode($A_L\to\delta(L-L_0)$)에서만 엄밀하고,
broad spectrum 에서는 per-mode arc-length doubling $q-q_a\to2(q-q_a)$ 이 \emph{재가중}과 함께 나타난다(``정확히
2배''가 아님). ``저온 긴 ICA tail $\leftrightarrow$ 저온 큰 비가역열 tail''의 대응은 본 장이 제시하는 정합 가설로,
\emph{확정}은 calorimetry cross-check($q_\rev\cdot$mixing$\cdot$ohmic$\cdot$transport 차감 후 비가역열 tail 이
Chapter 1 spectrum $\{L\}$ 로부터의 forward prediction 과 정합하는지)를 전제로 한다(Chapter 1 forward-only
반증 원칙의 calorimetry 확장). $r_j(q_a)$ 근사를 Chapter 1 kernel 과 공유하므로 완전 독립이 아니라
\emph{준독립} cross-modality 정합 검정이다.
\end{flagbox}

% ====================================================================
\section{식별성과 필요한 실험 제약}\label{sec:ch2_ident}
% ====================================================================
물리 우선 모델에서도 식별성은 사라지지 않으며, 해결책은 임의 clipping 이 아니라 독립 실험과 물리적 prior 다. 주요
상관 항은 다음이다: (i) $R_n$ 과 $R_{n,\eff}$(apparent shift vs dissipated heat, \S\ref{sec:ch2_Rn}),
(ii) $(\partial V_{n,\OCV}/\partial T)_q$ 와 $\Delta S_j$(둘 다 가역열 — 식~\eqref{eq:ch2_consistency} 제약 필요),
(iii) $\Delta S_j$(reaction)와 $\Delta S_{a,j}$(activation; 대입 금지), (iv) $Q_\bg$ 와 $h_\bg$(저속 OCV SOC
분해 + 식~\eqref{eq:ch2_bgsplit} 로 해소), (v) $h_T A$ 와 내부 열원(표면 온도만으론 heat generation 과 loss 가
상호 보상). 필요 실험: 저율 OCV 온도 series($\partial V_{n,\OCV}/\partial T$, $U_j(T)$, $w_j(T)$, reaction
entropy), rate series($k_j^{\mathrm{act}}\cdot k_{j,\lim}\cdot$broadening), pulse/impedance(ohmic vs
charge-transfer), 휴지 완화($\xi_j$ vs thermal relaxation), calorimetry(가역/비가역 분리, $h_TA$, 비가역열 tail
forward-prediction 검정). calorimetry 부재 시 가역/비가역 분해는 전압 자료만으로 닫히지 않는다.

% ====================================================================
\section{Chapter 2 의 결론}\label{sec:ch2_concl}
% ====================================================================
\begin{keybox}
\textbf{요지.} 발열을 Chapter 1 의 식 위에서 \emph{기원별}로 닫았다. (1) 가역열의 토대인 평형 전위 온도계수
$(\partial V_{n,\OCV}/\partial T)_q$ 를 전하 보존식에서 유도했고(식~\eqref{eq:ch2_dVocvdT}), 그 정체가 reaction
entropy $\partial U_j/\partial T=\Delta S_j/(sF)$(식~\eqref{eq:ch2_entropy_coeff}; Chapter 1)임을 못박되
activation entropy $\Delta S_{a,j}$ 와 \emph{별개}임을 분리했다. (2) 가역열을 OCV 계수$\cdot$transition entropy
두 방식으로 쓰고 double counting 정합(식~\eqref{eq:ch2_consistency})으로 묶었다. (3) 비가역열을 flux$\times$force
entropy production($\dot{\mathcal Q}_{\irr}=|I|\eta\ge0$, 식~\eqref{eq:ch2_qirr})과 kinetic-lag $\sigma_j\propto
k_j r_j^2$(식~\eqref{eq:ch2_sigma})로 닫았다. (4) 그 kinetic-lag 비가역열 tail 이 Chapter 1 ICA tail 의 ``열적
거울''(특성 길이 $L/2$ 재가중, 식~\eqref{eq:ch2_thermal_kernel})임을 보여 calorimetry 독립 반증 길을 열었다.
가역/비가역의 \emph{정량} 분배는 forward/backward 비대칭을 요해 Chapter 3--4 로 넘긴다.
\end{keybox}

% ====================================================================
\begin{thebibliography}{99}
\bibitem{bernardi1985} D. Bernardi, E. Pawlikowski, J. Newman, ``A General Energy Balance for Battery Systems,'' \emph{J. Electrochem. Soc.} \textbf{132}, 5 (1985). DOI: 10.1149/1.2113792.
\bibitem{rao1997} L. Rao, J. Newman, ``Heat-Generation Rate and General Energy Balance for Insertion Battery Systems,'' \emph{J. Electrochem. Soc.} \textbf{144}, 2697 (1997). DOI: 10.1149/1.1837884.
\bibitem{reynier2004} Y. Reynier, R. Yazami, B. Fultz, ``Thermodynamics of Lithium Intercalation into Graphites and Disordered Carbons,'' \emph{J. Electrochem. Soc.} \textbf{151}, A422 (2004). DOI: 10.1149/1.1646152.
\bibitem{thomasnewman2003} K. E. Thomas, J. Newman, ``Heats of mixing and of entropy in porous insertion electrodes,'' \emph{J. Power Sources} \textbf{119--121}, 844 (2003). DOI: 10.1016/S0378-7753(03)00283-0.
\bibitem{bardfaulkner} A. J. Bard, L. R. Faulkner, \emph{Electrochemical Methods: Fundamentals and Applications}, 2nd ed., Wiley (2001). ISBN 978-0-471-04372-0.
\bibitem{newman2004} J. Newman, K. E. Thomas-Alyea, \emph{Electrochemical Systems}, 3rd ed., Wiley-Interscience (2004). ISBN 978-0-471-47756-3.
\bibitem{eyring1935} H. Eyring, ``The Activated Complex in Chemical Reactions,'' \emph{J. Chem. Phys.} \textbf{3}, 107 (1935). DOI: 10.1063/1.1749604.
\bibitem{bazant2013} M. Z. Bazant, ``Theory of Chemical Kinetics and Charge Transfer based on Nonequilibrium Thermodynamics,'' \emph{Acc. Chem. Res.} \textbf{46}, 1144 (2013). DOI: 10.1021/ar300145c.
\bibitem{degrootmazur1962} S. R. de Groot, P. Mazur, \emph{Non-Equilibrium Thermodynamics}, North-Holland (1962). ISBN 978-0-486-64741-8.
\bibitem{onsager1931} L. Onsager, ``Reciprocal Relations in Irreversible Processes. I,'' \emph{Phys. Rev.} \textbf{37}, 405 (1931). DOI: 10.1103/PhysRev.37.405.
\end{thebibliography}

\end{document}
