# INTERCHAPTER_REPORT — Ch1↔Ch2 연계성·용어 통일성 심층 검토 (스트림 3, 읽기 전용)

> 대상: `v1.0.20/_sections/` ch1_*(24)·ch2_*(15) + appendix_phase_separation.tex
> 기준: results/V1020_STYLE_RUBRIC.md(C2 대역표 포함)·V1020_CHANGE_LOG.md·V1020_REFERENCE_LEDGER.md
> 전제: P5 Step 70(리터럴 참조 23곳 대조)·P6 스윕(용어 grep) 통과 기정. 본 검토는 그보다 깊은 축(기호 인계 전수·개념 양방향 다리·참조 완전성·잔존 어조).
> 금지: 문건 수정·comp_P7_review/ 열람. 확정 판정("2상역/두-상" 분업·부록 번호 방식·appendix 혼합 계열·Gn 공백 선언·% 자산 주석 불변) 존중.
> 심각도 범례: **H** = 독자 오해/식 정합 위험, **M** = 다리 누락·예고/상술 불일치, **L** = 표기 흔들림·미세 개선.

작성 진행: 파트 완료 시마다 즉시 append(조기 저장).

---

## (a) 기호 인계 대응표 — 두 챕터에 걸치는 기호 전수

> 정의 위치 표기: Ch1 파일은 `sec0N`(§N)로, Ch2 파일은 `sec0N`(§2.N)로 약기. 라벨은 실제 `\label`.
> "일치" 열: ○=완전 일치 / △=일치하나 스코프·형태 미묘차(가드 유무 병기) / ×=불일치.

| 기호 | Ch1 정의 위치 | Ch2 정의/참조 위치 | 표기 일치 | 독자 혼동 위험·비고 |
|---|---|---|---|---|
| **θ** (Li 점유율) | §2 `eq:fermifn`(sec02a): `θ≡⟨n⟩=1/(1+e^{+βΔμ})` | §2.1 `eq:occ`/`eq:logistic`: `θ_eq=⟨n⟩=1/(1+e^{+(V−U_j)/w})` | ○ | 양측 "만충에서 1" 일관. Ch2 keybox·appA 가 θ/ξ 명시 가드 |
| **ξ** (탈리튬화 진행률) | §2 sec02b `ξ=1−θ`; §5 `eq:xieq` | §2.1 `eq:logistic` `ξ≡1−θ`; appA 가드 | ○ | "희박에서 1" 일관. §5 sec:dist "여집합" 정체 = §2.1 keybox와 동일 |
| **w=RT/F**, **w_j=n_jRT/F** | §2 `eq:sm-logistic` `w≡RT/F`; §5 `eq:wbase` `w_j=n_jRT/F` | §2.1 `eq:logistic` `w≡RT/F`; keybox `w_j=n_jRT/F` | ○ | 폭 이중지위(단상 평형예측/두-상 현상학)를 §5 ↔ §2.5 파생C 상호 명시 |
| **n_j** (폭 다중도) | §5 `eq:wbase`; §2.5(Ch2 유도 받음 명시) | §2.1 keybox `n_j=1 서식`; §2.2 `n_jR/F`; §2.5 `eq:dwdT-nT` `n_j(T)` | ○ | Ch1 §5 "Chapter 2 겹침 가중 파생 A 에서 유도" ↔ Ch2 §2.5 상호 정합 |
| **Ξ₁** (단일자리 대정준 분배함수) | §2 `eq:partfn`(내부자유도 q(T) 포함 완전형); 원형은 `Ξ₁⁰` `eq:sm-bare` | §2.1 `eq:Z1`: `Ξ₁=1+e^{−β(ε0−μ)}` | △ | **Ch1→Ch2 리터럴 참조 실재 확인**: Ch1 sec02a "그 문건 식 (eq:Z1)…같은 기호 Ξ₁ 로 통일" ↔ Ch2 sec02 "Chapter 1 §2 Part 0…통일한 표기". 미묘점: Ch1은 원형=`Ξ₁⁰`/완전형=`Ξ₁` 위첨자로 구분, Ch2의 `eq:Z1`은 형태상 Ch1 `Ξ₁⁰`와 동일하나 "ε₀를 유효값 ε̃로 읽는다"고 선언해 완전형 Ξ₁에 대응. 위첨자 ⁰ 규약이 Ch2에 없음(→아래 (d) L-1) |
| **Ξ₁⁰** (원형 2-상태) | §2 `eq:sm-baresum`/`eq:sm-bare`(sec02a) | (없음 — Ch2는 위첨자 ⁰ 미사용) | △ | Ch2가 ε₀ 유효값 선언으로 대체하므로 논리 충돌은 아님. 표기 규약 비대칭만 |
| **q(T)** (내부자유도 분배함수) | §2 `eq:partfn`; 각주로 `q=Q/Q_cell`(용량 좌표)와 별개 명시 | §2.1 `f_int=−k_B T ln q(T)` | ○ | Ch1 각주가 `q(T)`(열역학량) vs `q`(곡선 좌표) 충돌 가드. Ch2엔 용량좌표 q 부재라 충돌 없음 |
| **ε̃** (유효 자리 자유에너지) | §2 `eq:sm-epstilde` `ε̃(T)≡ε0−k_BT ln q(T)` | §2.1 `ε̃=ε0−k_B T ln q(T)` | ○ | 문자까지 동일(P5 게이트 "ε̃ 정의 문자 일치" 재확인) |
| **Ω** (평균장 상호작용) | §2 `eq:sm-omega` `Ω≡−(z/2)N_A u`; 표 `Ω_j`, Part II `Ω_j^cat` | §2.1 `eq:BW` `Ω`; `Ω>0 인력(상분리)` | ○ | 부호 규약 일치(Ω>0 상분리 경향). 임계 Ω=2RT 양측 동일(§4 spinodal ↔ §2.1 `eq:slope_BW`) |
| **σ_d** (방향 부호) vs **s** (유도 전용 고정 부호) | §2 `eq:sm-eqcond`(s=+1); §0·§5 σ_d(±1); §4 분기 | §2.1 `eq:muV` "s(=+1)…Chapter 1의 유도 전용 고정 부호" + ★각주; σ_d는 §2.7·appA | ○ | 양측 s≠σ_d 명시 가드(여집합 오류 경고). appA 첫 행이 집약 |
| **g (4종)** | §2 자유에너지 `g(θ)`(`eq:sm-gtheta`)·`g_j(ξ)`(`eq:gxi`)·`g_j^0`·`g_j''` | §2.1 BW `g(θ)`(`eq:BW`)·`g_0`; §2.3 DOS `g(E)`/`g(E_F)`; §2.5 봉우리가중 `g_j(x)`(`eq:gj`) | △ | **명시적 "g 4종 충돌" 가드**: Ch2 `eq:gj` 직후 + appA 가 4종 열거. 미묘점: Ch1 상수몫 `g_j^0`(j첨자) vs Ch2 `g_0`(무첨자) — 같은 역할 다른 표기(→(d) L-2). DOS `g`는 rubric C4 "상태밀도 병기" 대상 |
| **u_j** (동명 충돌) | §4 `eq:spinodal` `u_j≡√(1−2RT/Ω_j)`(spinodal 근) | §2.4 `u_j≡θ_{E,j}/T`(Einstein 비) | ○(가드로 분리) | **교차챕터 동명**: Ch2 §2.4 각주 + appA "u_j vs Ch1 u_j" 행이 별개 명시. Ch1 §4쪽엔 역방향 예고 없음(→(d) L-3, 저심각) |
| **U_j** (전이 중심) / **V** (측정 전위) / **V_n** (분극 후 내부 전위) | §3 `eq:Uj` `U_j=(−ΔH_rxn+TΔS_rxn)/F`; §1 `V_n=V_app−σ_d\|I\|R_n`; V=측정전위 | §2.1 `eq:muV` U_j=중심전위; V=(평형)전극전위; U_oc | ○ | **V_n은 Ch1 전용**(분극 N1은 Ch1 소관). Ch2는 평형 전용이라 V_n 부재·U_oc 사용 → 정합. U_j 정의식 문자 동일(단 ΔH/ΔS의 j첨자: §0 spine 그림은 무첨자, 본문·Ch2는 j첨자 — (d) L-4) |
| **x̄** (탈리튬화 분율) / **x** (Li 함량) | §17 "추출 전하=여집합 가중합"(x̄ 기호는 본문 미사용); appB에서 Ch2 솔버 참조로만 x̄ 등장 | §2.5 `eq:implicit` `x̄≡1−x`; §2.8·appA·appB 전반 | △ | **x̄는 실질 Ch2 좌표**. Ch1 forward 좌표는 V_n·ξ(전위 구동), Ch2는 x̄(조성 구동)로 독립변수 자체가 다름 → 구조적 비대칭(결함 아님, (b)에서 상술). x는 양측 Li 함량으로 일치. x̄/x 배향반전은 Ch2 §2.5·appA 가드 |
| **ΔS_rxn / ΔS_{rxn,j}** (반응 엔트로피) | §3 `∂U_j/∂T=ΔS_rxn,j/F`; §14 `ΔS_rxn,j^cat` | §2.0·§2.2·§2.7 `ΔS(x)`, `ΔS_rxn,j` | ○ | 역할 일치. Ch1 §14는 cat(LCO) 상첨자 부가 |
| **ΔS⁰ (ΔS^0_j / ΔS_j^0)** (전이 중심 표준값) | §14 `ΔS_j^{0,config}`(config 중심)·`ΔS_j^0`(config 슬롯 중심값)·`eq:lco-configsplit` | §2.2 warnbox `ΔS^0_j≡ΔS_rxn,j≡[ΔS(x)]_{ξ=1/2}` (**vib·elec 중심 흡수한 전체 중심값**) | △ | **스코프 차이(M급)**: Ch2 `ΔS^0_j`=vib+elec 중심까지 흡수한 *전체* 중심값(흑연 주사례). Ch1 §14 `ΔS_j^0`=*config 슬롯 전용* 중심값(LCO는 vib·elec가 별개 슬롯). Ch2 warnbox가 "config 중심/분포 분리에 한정한 대응"으로 스코프 한정 명시 → 부분 가드됨. 그러나 동일 기호 ΔS^0_j의 스코프 차가 기호 수준에서 재조정되진 않음(→(c)/(d) M-1) |
| **ΔS_vib** | §14 `ΔS_j^vib`(슬롯); §15 vib baseline 언급 | §2.3 `∂S_vib/∂x`(부분몰); §2.4 `ΔS_vib(T)`(T-편차, `eq:dSvib`) | △ | **Ch2 내부 이중의미**를 §2.4 + appA가 명시 가드(`ΔS_vib(T)` T-편차 ≠ `∂S_vib/∂x` 부분몰). Ch1 §15 "잔여 vib 양자곡률…Ch2 Einstein 절" 로 T-의존을 Ch2로 위임 → 정합 |
| **ΔS_e / ΔS_{e,j}** (전자 엔트로피) | §15 `eq:dSe` `ΔS_{e,j}=∂S_e/∂x`; §14 `ΔS_{e,j}^mol=N_A∂S_e/∂x` | §2.3 `ΔS_e=∂S_e/∂x` | ○ | 삽입기준 부호(<0) 일치. Ch2 §2.3 "그 전개는 Chapter 1 §13·§15가 상술" 로 위임 |
| **ΔS_a / ΔS_{a,j}** (활성화 엔트로피) | §1 표·§8 `eq:Lq`·§9·§10 `ΔS_a` | §2.3 ssec:threedist 참조; appA "ΔS(x) vs ΔS_{a,j}" 행 | ○ | 반응 엔트로피와 명시 구분(양측). Ch2가 "Chapter 1 §8·§9 동역학 꼬리" 로 소관 명시 |
| **S_e** (자리당 전자 엔트로피) 라벨 | §15 `eq:Se` `S_e=(π²/3)k_B²T g(E_F,x)` | §2.3 `eq:Se-ch2`(동일 식, **라벨 개명**) | ○(가드로 분리) | **라벨 충돌 회피**: Ch2 `eq:Se-ch2`로 개명 + appA "S_e 라벨" 행이 Ch1 `eq:Se`와 별개 컴파일 객체임 명시 |
| **Ξ₁ 리터럴 참조 사슬** | §2 "(그 문건 식 (eq:Z1))" | — | ○ | (c) 참조완전성에서 상술 |

### (a) 소결
- 12개 기호군 전수 중 **명시적 충돌 가드 완비**: θ/ξ, s/σ_d, g 4종, u_j 동명, S_e 라벨, x̄/x, ΔS_vib(T)/부분몰, ΔS(x)/ΔS_a — Ch2 appA 함정표(11행)가 교차챕터 충돌까지 단일 조회점으로 집약(강점).
- **미묘차(△) 잔존**: (1) `Ξ₁⁰` 위첨자 규약 비대칭, (2) `g_j^0` vs `g_0` 표기차, (3) `ΔS^0_j` 스코프차(Ch2 전체중심 vs Ch1 config중심), (4) U_j 정의식 ΔH/ΔS의 j첨자 유무(§0 spine 그림), (5) x̄ 좌표의 Ch2 편재 — 상세·심각도는 (d)·(e).
- **문자 완전일치 확인**: ε̃ 정의, U_j 정의식(본문), eq:muV/eq:Vxi/eq:Z1/eq:logistic 리터럴 참조.

---

## (b) 개념 인계 지도 — 양방향 다리 실재 여부

> "발신"=개념을 상술/정의한 절, "수신"=그 개념을 넘겨받아 참조/사용한 절. 다리 문장이 실제로 존재하는지 확인.

### (b-1) Ch1 → Ch2 (Ch1이 세우고 Ch2가 받는 개념)

| 개념 | Ch1 발신(상술) | Ch2 수신 문장 | 다리 |
|---|---|---|---|
| logistic 평형 종(ξ_eq) | §5 `eq:xieq`·sec:dist 분포 다리 | §2.1 "Chapter 1 이 이미 쓰고 있던 logistic…의 기원", §2.2 "= Ch1 logistic" | 양방향 ✓ |
| 폭 w_j 이중지위(단상/두-상) | §5 ★이중지위 "Chapter 2 파생 C 와 동일 취지" | §2.5 파생 C "…폭 w_j 의 지위를 규정", §2.10 요약 | 양방향 ✓ |
| broadening 3출처 | §7(전문 상술) | §2.5 파생 C "broadening 상세 물리…는 Chapter 1 §7…본 장은 결과만 받아" | 단방향 예고→상술 ✓ |
| 히스 분기 중심(σ_d·γ_j·h_η) | §4 `eq:Ubranch`(상술) | §2.5 파생 D "U_j^(d)…Chapter 1 §4 분기 중심식에서 γ_j h_η=1 특수형" | ✓ |
| MSMR 동형(U↔V·ω_j↔w_j 등) | §17 `eq:lco-msmrmap`(상술) | §2.9 "MSMR(Multi-Species, Multi-Reaction) 절차(Chapter 1 §17)와 직접 대응" | ✓ |
| 전자 엔트로피(FD→Sommerfeld) | §15 `eq:Se`(상술)·§13 MIT 2상역 | §2.3 "Chapter 1 §15…전자 엔트로피 유도가 상술했고, 본 장은 그 결과를 분포 언어로 확장해 받는다" | ✓ (§13·§15 실재) |
| 반응 엔트로피 삼분해·슬롯 | §14 `eq:lco-decomp`(상술) | §2.2 warnbox "Chapter 1 §14…골격 공유(config 중심/분포 분리 **한정**)" | ✓ (스코프 명시) |
| Einstein θ_E 온도 보정 | §14·§15·appB(입력으로 사용, 유도 위임) | §2.4(전문 상술) | ✓ (다만 §14·appB 지칭 부정확 — (c)) |
| 가역 발열(잠긴 열) | §15 "Ch2 의 가역 발열로 확장" | §2.0 "Chapter 1 곡선의 평형 골격에 잠긴 열을…정량화", §2.7 `eq:qrev` | 양방향 ✓ |

### (b-2) Ch2 → Ch1 (Ch2가 세워 Ch1로 되먹이는 개념)

| 개념 | Ch2 발신 | Ch1 수신/예고 문장 | 다리 |
|---|---|---|---|
| S_config 기원(= logistic·Nernst log의 통계 정체) | §2.1·§2.2 `S_config=−R∑p ln p`, `∂S_config/∂θ` | §2 sec02b "Nernst ln[ξ/(1−ξ)]…배치 셈법에서 옴(Chapter 2 의 식 (eq:Vxi)가 발열 관점 재사용)", §5 sec:dist 분포 다리 | 양방향 ✓ |
| Ξ₁ 표기 통일 | §2.1 "Chapter 1 §2 Part 0…통일한 표기" | §2 sec02a "Chapter 2 의 단일 자리 분배함수(그 문건 식 (eq:Z1))…같은 기호 Ξ₁ 로 통일" | 양방향 문자 상호 ✓ |
| BE/FD 양자통계 배경 | §2.3 "…양자 배경은 Chapter 1 §2 의 배경 박스가 한자리에 정리" | §2 sec02a bgbox "진짜 양자 통계가 일하는 곳은 Chapter 2 다(포논 BE·전자 FD)" | 양방향 ✓ |
| ε̃·s_int 단일자리 vib 원형 | §2.1 "Chapter 1 §2 Part 0 의 같은 자리당 엔트로피" | §2 sec02a "이것이 Chapter 2 의 vibrational 엔트로피 사슬의 단일 자리 원형" | 양방향 ✓ |
| w=RT/F 가 config를 이미 담음 | §2.2 "Chapter 1 의 logistic 폭 w=RT/F 가 이미 부분몰 config 담음" | §5·§2(w=RT/F 정의), §14 `eq:lco-configsplit` "혼합 분포항은 w_j 가 담음" | ✓ |

### (b) 소결 — 빠진 다리 판정
- **양방향 다리 누락은 발견되지 않음.** 상술↔예고가 모든 주요 개념에서 짝을 이룸(강점). 비대칭은 자연스러운 "기반 챕터(Ch1)/확장 챕터(Ch2)" 구조에서 온다 — Ch2→Ch1 참조(≈30건)가 Ch1→Ch2 참조(≈18건)보다 조밀.
- **관찰 1(중복, 비결함)**: logistic의 통계역학적 기원이 Ch1 §2 Part 0와 Ch2 §2.1 양쪽에서 유도되나, Ch2 §2.1이 "Chapter 1 §2 Part 0가 처음부터 세웠다…여기서는 최소 사슬만 자기완결로 다시 놓는다"로 명시 → 의도된 자기완결 중복(정합).
- **관찰 2(구조적 비대칭, 비결함)**: 독립변수가 챕터별로 다름 — Ch1 forward는 전위 구동(V_n→ξ_eq→dQ/dV, 전하보존식 `eq:lco-charge`에 배경 C_bg 포함), Ch2는 조성 구동(x̄→U_oc, `eq:implicit`은 배경 제외 Q=ΣQ_j=0.97Q_cell). Ch2 x̄는 구성상 배경을 제외하므로 정합이나, 두 전하보존식의 배경항 유무는 명시 대비되지 않음(→(e) L 관찰).

---

## (c) 참조 완전성 — 예고↔상술 짝맞춤

### (c-1) "~가 상술한다" 예고 → 실제 상술 존재 여부 (전건 확인)
- Ch1 §2 → Ch2 `eq:Z1`(Ξ₁)·`eq:muV`·`eq:Vxi` : **3건 전부 Ch2 §2.1에 실재**(라벨·식형 문자 일치). ✓
- Ch1 §5 → Ch2 `eq:logistic` "부호까지 동일" : Ch2 §2.1 `eq:logistic` 실재, 부호 일치. ✓
- Ch1 §2 sec02a → "Ch2 vibrational 엔트로피 사슬 단일자리 원형(s_k)" : Ch2 §2.3 `eq:Svib_mode` `s_k=k_B[(1+n_k)ln(1+n_k)−n_k ln n_k]` 문자 일치. ✓
- Ch2 §2.0·§2.3 → "Chapter 1 §15" (전자 엔트로피 상술) : §15 `eq:Se`~`eq:dSegate` 완전 유도 실재. ✓
- Ch2 §2.3 → "Chapter 1 §13·§15 가 상술" : §13 "T1 MIT 2상역 — 구조/전자 슬롯 분리"(`eq:` MIT 분리식)·§15 전자 유도 실재. ✓
- Ch2 §2.5 → "Chapter 1 §7 broadening" : §7 3출처 전문 상술 실재. ✓
- Ch2 §2.9 → "Chapter 1 §17 MSMR" : §17 `eq:lco-msmrmap` 대응표 실재. ✓
- Ch2 §2.2 → "Chapter 1 §14 삼분해" : §14 `eq:lco-decomp` 실재(스코프 한정 명시). ✓
- Ch1 §15 → "Ch2 Einstein 절"(잔여 vib 양자곡률) : Ch2 §2.4 실재. ✓

### (c-2) 상술이 있는데 예고가 없는 경우
- 없음(주요 상술은 모두 상대 챕터에서 예고됨). Ch1 §2 Part 0의 완전한 통계역학 유도는 Ch2가 "최소 사슬만 다시" 방식으로 예고·수신.

### (c-3) 참조 정밀도 결함 1건
- **[L-5] Ch1 §14(2곳: `eq:lco-slots` 주석·본문)·appB(`theta_E` 행)가 θ_E 온도 보정을 "Ch2 vibrational 절"로 지칭.** 그러나 θ_E·ΔS_vib(T)·ΔU_vib의 실제 상술은 Ch2 **§2.4("전이 중심의 온도 확장 — Einstein 진동 보정")**이고, §2.3("vibrational·electronic 엔트로피")은 T-편차 문제만 세운 뒤 §2.4로 넘긴다. **Ch1 §15는 같은 대상을 이미 "Ch2 Einstein 절"로 정확히 지칭** → Ch1 내부에서 §14/appB와 §15의 지칭이 불일치. 상술 자체는 존재(§2.3이 §2.4로 forward)하므로 심각도 L(참조 정밀도).

### (c) 소결
- 리터럴 식 참조(eq:Z1·eq:muV·eq:Vxi·eq:logistic·eq:Se)와 절-번호 참조(Ch2→Ch1 §1·§2·§3·§4·§5·§7·§8·§9·§13·§14·§15·§17) **전건 유효**(잘못된 절번호 0건). P5 Step 70 통과를 심층에서 재확인.
- 유일 결함은 L-5(θ_E 지칭 "vibrational 절" vs "Einstein 절" Ch1 내부 불일치).

---

## (d) 용어·어조 잔존 불일치 — C2 대역표 밖

> 확정 판정(두-상/2상역 분업·appendix 혼합 계열·부록 번호·Gn 공백·% 자산) 존중. 재론 항목은 근거 병기.

### (d-1) 확정 판정 존중 — 재확인만 (변경 제안 없음)
- **혼합/섞임 (P6 유지 판정 존중)**: Ch1 §2는 "섞임 엔트로피(S_mix)" 일관, Ch2 §2.1은 도입부 1회 "혼합(섞임) 엔트로피"·이후 "configurational 엔트로피/S_config", appendix는 "혼합" 계열 내부 일관. **재론 근거(참고용, 변경 제안 아님)**: rubric C2 canonical은 엔트로피 측에서 "섞임 엔트로피". 다만 ① Ch2의 실제 주 표기는 S_config(configurational)이고 "혼합(섞임)"은 BW 자유에너지 도입 1회 gloss, ② Ch2 §2.1이 "Chapter 1 §2 Part 0 의 섞임 엔트로피와 같은 양"으로 명시 등치, ③ P6이 "혼합 엔탈피 병용"을 승인 → **불일치가 독자 오해로 이어지지 않으므로 P6 유지 판정을 존중**하고 변경 제안하지 않음.
- **두-상/2상역 (확정 분업 존중)**: "두-상 전이"(전이 유형)와 "2상역·2상 공존"(상도표 영역)의 분업이 Ch1 §5·§7(두-상 전이)·§13("MIT 2상역")·§15("2상역")·Ch2 §2.1·§2.5(두-상)에서 일관 적용됨을 재확인. 위반 0.
- **appendix ξ=θ 배향 (존중)**: 상분리 부록의 ξ는 B(Li)-점유 자리 분율=본문 θ이며 본문 진행률 ξ(=1−θ)와 여집합. 부록 헤더 주석(8–9행)·서론(57–63행)이 우함수 대칭 논거로 명시 가드. 독립 초안 지위라 정합. (본문 편입 시 재점검 필요는 부록 자신이 명시.)

### (d-2) C2 밖 잔존 표기 흔들림 (미세, L급)
- **S_config ↔ S_mix 이름차**: 같은 양(점유 분포의 배치 엔트로피)을 Ch1은 S_mix(섞임)·Ch2는 S_config(configurational)로 명명. **결함 아님** — Ch2 §2.1 "Chapter 1 §2 Part 0 의 섞임 엔트로피와 같은 양", §2.2 "BW 둘째 항을 −T로 나눈 것과 일치"로 이중 등치. 다리 완비.
- **g_j^0(Ch1) vs g_0(Ch2)**: BW/정규용액 자유에너지의 상수·선형 묶음을 Ch1 §2 `eq:gxi`는 `g_j^0`(전이별 j첨자), Ch2 §2.1 `eq:BW`는 `g_0`(무첨자)로 표기. 같은 역할·다른 첨자 관례. appendix는 `g_j^0`(Ch1 표기) 인용. → L-2.
- **U_j 정의식 j첨자(§0 spine 그림)**: Ch1 §0 fig:spine N2 노드는 `U_j=(−ΔH_rxn+TΔS_rxn)/F`로 ΔH/ΔS에 j 없음. 본문 §3 `eq:Uj`·Ch2 §2.0은 `ΔH_{rxn,j}·ΔS_{rxn,j}`로 j 부기. 도식 간략화이나 챕터 간 정의식 대조 시 미세 불일치. → L-4.
- **Ξ₁⁰ 위첨자·ε₀ 재사용**: Ch1은 원형=`Ξ₁⁰`(bare)/완전형=`Ξ₁`(q(T) 포함) 위첨자로 구분하고 ε₀(bare)≠ε̃(effective). Ch2 `eq:Z1`은 `Ξ₁`으로 통일하되 형태가 Ch1 `Ξ₁⁰`와 같고, "ε₀를 유효값(=ε̃)으로 읽는다"고 재정의. 수치적으로는 Ch2 Ξ₁ = Ch1 완전 Ξ₁로 정합(ε₀^{Ch2}=ε̃ 대입 시 q(T) 복원). Ch2가 명시 가드하나, ⁰ 위첨자 규약과 "ε₀=ε̃" 재사용이 챕터 간 대조 시 순간 혼동 가능. → L-1.
- **Ch2 appA "Chapter 1 부록의 부호 사슬 검산표와 같은 용도"**: Ch1 appA는 부호 사슬 *명제* 검산표(S1–S8 정성·R1–R6 수치, 반증 가능 명제), Ch2 appA는 *기호 충돌 함정* 조회표(s/σ_d·g 4종·u_j·S_e 라벨 등). 둘 다 "검수 단일 조회점"이나 내용 성격(명제 검산 vs 충돌 조회)이 달라 "같은 용도"는 느슨한 등치. → L-6.

### (d) 소결
- C2 밖 **개념 오해를 낳는 어조 불일치는 없음**. 잔존은 전부 표기 관례차(g_j^0/g_0·j첨자·⁰위첨자) 또는 느슨한 등치 표현(appA "같은 용도")로 L급.
- 확정 판정(혼합/섞임·두-상/2상역·appendix 배향)은 근거 재확인 후 **존중, 변경 제안 없음**.

---

## (e) 보완 제안 목록 — 실수정 금지, 제안 스케치만

> 각 항: [위치] · 심각도 · 제안 문안 스케치. 전부 **표현·다리·주석 층위**(수식·라벨·부호·% 자산 불닿음)라 채택 시 CHANGE_LOG 등재 불요(rubric F2), phase RESULT Files Updated로 갈음 가능.

### M급 (1건)
**[M-1] ΔS^0_j 스코프 명시 보강 — 전체 중심(Ch2) vs config 슬롯 중심(Ch1 §14)**
- 위치: Ch2 §2.2 warnbox(`eq:` 삼분해 대응 문장) 또는 Ch1 §14 `eq:lco-decomp` 직후.
- 진단: 동일 기호 `ΔS^0_j`가 Ch2 §2.2에서는 vib·electronic 중심까지 흡수한 *전체* 중심값, Ch1 §14에서는 *config 슬롯 전용* 중심값(LCO는 vib·elec가 별개 슬롯). Ch2 warnbox가 "config 중심/분포 분리에 한정한 대응"으로 스코프를 부분 한정하나, 기호 수준의 재조정은 없음. 흑연(Ch2 §2.8 종합식·tab:ds)에서는 ΔS^0_j=전체 중심으로 두 챕터 정합하므로 위험은 LCO §14 교차독해에 국한. Ch1 §14가 정식 식에서 `ΔS_j^{0,config}`(config 상첨자)를 쓰는 점이 완화 요인.
- 제안 문안 스케치: Ch2 §2.2 warnbox의 "Chapter 1 §14…골격 공유" 문장에 한 절 추가 — "단 Chapter 1 §14의 ΔS_j^0는 vib·elec를 별개 슬롯으로 분리한 흑연 밖(LCO) 분해의 *config 슬롯 중심값*이고, 본 장 ΔS^0_j(흑연 주사례)는 그 세 슬롯 중심을 합산 흡수한 전체 중심값이다 — 두 기호를 직접 등치하지 말 것." (수식·라벨 불변, 문장 다리 1개 추가.)

### L급 (6건)
**[L-1] Ξ₁⁰ 위첨자 규약·ε₀=ε̃ 재사용 교차 각주**
- 위치: Ch2 §2.1 `eq:Z1` 직후 기호 문단(이미 "ε₀를 유효값으로 읽는다" 존재).
- 제안: 그 문장에 반 줄 — "(Chapter 1 §2의 위첨자 규약으로는 이 형태가 원형 Ξ₁⁰에 해당하나, 여기 ε₀=ε̃ 유효값 대입으로 완전형 Ξ₁과 동일하다)". 챕터 간 ⁰ 규약 대조 혼동 제거.

**[L-2] g_j^0 / g_0 표기 통일 또는 각주**
- 위치: Ch2 §2.1 `eq:BW`의 `g_0` 도입부.
- 제안: `g_0`를 `g_j^0`로 맞추거나(전이별 j 명시), 최소 각주 "(Chapter 1 §2 표기 g_j^0의 무첨자 축약 — 같은 상수·선형 묶음)". appendix가 이미 `g_j^0`를 쓰므로 j첨자 쪽이 3자 정합에 유리.

**[L-3] Ch1 §4 u_j 정의부 역방향 각주(선택)**
- 위치: Ch1 §4 `eq:spinodal` `u_j≡√(1−2RT/Ω_j)` 직후.
- 진단: u_j 동명 충돌(spinodal 근 ↔ Ch2 §2.4 Einstein 비 θ_E/T)의 가드가 Ch2쪽(각주+appA)에만 존재. Ch1→Ch2 순 독자는 경고를 못 받음(Ch2→Ch1 순은 받음).
- 제안(저우선): Ch1 §4에 각주 "(이 u_j는 절-국소 기호로, Chapter 2 §2.4의 Einstein 비 u_j=θ_E/T와 동명 별개)". Ch1이 정의 선행 챕터라 필수는 아님 — 대칭성 개선용.

**[L-4] §0 spine 그림 U_j 정의식 j첨자 정합**
- 위치: Ch1 §0 fig:spine N2 노드 라벨.
- 제안: `U_j=(−ΔH_rxn+TΔS_rxn)/F` → `U_j=(−ΔH_{rxn,j}+TΔS_{rxn,j})/F`로 본문 §3·Ch2 §2.0과 첨자 일치(도식 공간 허용 시). 미채택해도 무해.

**[L-5] Ch1 §14·appB "Ch2 vibrational 절" → "Ch2 §2.4 Einstein 절" 정정**
- 위치: Ch1 §14(`eq:lco-slots` 주변 2곳)·appB(`theta_E` 행).
- 진단: θ_E 온도 보정의 실제 상술은 Ch2 §2.4(Einstein 진동 보정)이고 §2.3(vibrational 절)이 아님. Ch1 §15는 이미 "Ch2 Einstein 절"로 정확 지칭 → Ch1 내부 지칭 통일 필요.
- 제안: 세 곳의 "Ch2 vibrational 절"을 "Ch2 §2.4(Einstein 진동 보정 절)"로 통일.

**[L-6] Ch2 appA 도입문 "같은 용도" 완화**
- 위치: Ch2 appA 도입 문단(9행).
- 진단: Ch1 appA(부호 사슬 명제 검산 S1–S8/R1–R6) vs Ch2 appA(기호 충돌 함정 조회표)는 성격이 다름.
- 제안: "Chapter 1 부록의 부호 사슬 검산표와 같은 용도다" → "Chapter 1 부록이 부호 사슬 명제를 검산하듯, 본 표는 기호·부호 충돌을 단일 조회점에 모은다"(역할 차이 명시).

### (e) 소결
- 총 **7건 제안**: H 0 / M 1 / L 6. 전건 표현·다리·주석 층위(수식·라벨·부호·자산·서지 불닿음).
- P5 Step 70(리터럴 참조)·P6(용어 grep) 통과를 심층에서 재확인 — 남은 것은 스코프 명시 1건(M-1)과 표기·참조 정밀도 6건(L). H급(식 정합·중대 오해) 위험 **0건**.
- 확정 판정(혼합/섞임·두-상/2상역·부록 번호·appendix 배향·Gn·% 자산)은 근거 재확인 후 존중, 변경 제안에 미포함.

## REPORT COMPLETE — 제안 7건 (H 0 / M 1 / L 6)
