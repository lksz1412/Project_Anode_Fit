# P2 경쟁 저작 브리프 — Ch1 §2.2 「단일 삽입 자리」 D7 2단 재배열 + 양자통계 배경 (N=4 독립 초안)

## 역할·경계 (하드)
- 당신은 **독립 초안 작성 sub-agent** 1인이다. 다른 초안과 무통신. **지정 산출 파일 1개만 write** — 그 외 어떤 저장소 파일도 수정 금지.
- 산출물은 후보 초안이다 — master 가 체리픽 통합한다. 허위 attribution(원문에 없는 문헌·수치 창작) 절대 금지.

## 목표
`/home/user/Project_Anode_Fit/Claude/docs/v1.0.20/_sections/ch1_sec02a_part0.tex` 의 **§2.2 소절 전체**(L106 `\subsection{단일 삽입 자리 --- 점유 통계와 그 요동}\label{sec:sm-site}` 부터 L219 까지 — L220 `\subsection{$M$ 개의 독립 자리` 직전)를 다음 구조로 **재작성한 대체 블록**을 산출:

1. **[원형]** 내부 자유도 없는 순수 2-상태 유도 — (a) 가정 1(hard-core 배타)·가정 2(자리 독립) + 미시상태 {빈(0), 점유(ε₀)} → (b) 대정준 합 → (c) 중간식 → (d) **박스**: Ξ₁⁰ = 1+e^{-β(ε₀-μ)}, ⟨n⟩ = 1/(1+e^{+β(ε₀-μ)}) — **신규 라벨**(eq:sm-* 계열, 예 eq:sm-bare). 이것이 국소화 흡착(Langmuir)의 표준 유도임을 인용과 함께(hill1960·fowler1939·mckinnon1983). Fermi–Dirac 함수와의 **동형**이 여기서 이미 보임을 언급.
2. **[배경 bgbox]** `\begin{bgbox} ... \end{bgbox}` (환경은 preamble 에 정의됨, 제목 인자 없음 — 첫 문장 굵은 도입 `\textbf{페르미온·보손과 양자 통계 ---}` 로 시작): 동일입자의 구별불가능성 → 파동함수 교환 대칭 두 부류 → **보손**(boson — 대칭·정수 스핀·한 상태 점유 무제한 → Bose–Einstein 분포)·**페르미온**(fermion — 반대칭·반정수 스핀·Pauli 배타로 준위당 0/1 → Fermi–Dirac 분포 f(E)=1/(e^{β(E-μ)}+1)) 를 학부 눈높이로 6–10문장. 핵심 대비: **본 절 격자 자리의 0/1 은 양자 배타가 아니라 기하(정전·입체 반발)** — "한 상태에 0 또는 1"이라는 셈법 구조가 같아 같은 대수형(logistic/FD 형)이 나온다. 마지막 1–2문장: 진짜 양자 통계는 Chapter 2 에서 포논(보손, BE)·전도 전자(페르미온, FD) 엔트로피에 실제로 쓰인다는 forward 다리. 인용: mcquarrie1976 (필요시 ashcroftmermin1976 — 장 수준만).
   - **자족 블록 요건**: bgbox 는 통째로 들어내 부록으로 옮겨도 성립해야 하고, 본문은 bgbox 없이도 읽혀야 한다(본문에서 bgbox 내용을 전제로 지시대명사 사용 금지).
3. **[확장]** 도입 동기 1–2문장(점유된 이온은 우물 안에서 진동하는 내부 자유도를 가진다 — 원형의 미시상태 목록을 넓혀야 함) → 내부 배치 Γ·q(T) 정의(기존 eq:partfn 의 형태 그대로 최종형) → ⟨n⟩(기존 eq:sm-occmid) → q(T) 흡수·ε̃ 정의(기존 eq:sm-epstilde) → **박스**(기존 eq:fermifn — 형태 불변) → 기존 조화 우물 q 의 고전/양자형 단락·q(T)↔용량 좌표 q 구분 각주 보존.
4. **[검산 verifybox]** 기존 극한 검산(β→∞ 계단·β→0·μ→±∞·q→1) + **"q≡1 이면 [원형] 박스 회수"** 명시 + 기존 ★함수형 가드(FD 동형≠동일 — 입자는 Li 이온·배타 근거는 기하) 유지·bgbox 와 접속.
5. 소절 후미의 기존 내용 — 요동(기존 eq:sm-flucres)·유효 자유에너지의 하류 두 갈래(U_j 흡수·s_int 기존 eq:sm-sint 와 Ch2 다리 단락) — **전량 보존**(문장 다듬기 허용, 내용·수치·각주 삭제 금지).

## 보존 체크리스트 (전 항목 의무 — 산출 NOTE 에 항목별 위치 자기보고)
P-1 가정 1/2 명시 · P-2 미시상태 온전 기술(점유 상태의 연속 내부 배치) · P-3 라벨 eq:partfn/eq:sm-occmid/eq:sm-epstilde/eq:fermifn/eq:sm-flucres/eq:sm-sint **전부 존재·최종 수식 내용 불변** · P-4 q(T)↔정규화 용량 좌표 q 구분 각주 · P-5 조화 우물 q 고전 (k_BT/ħω₀)³·양자 ∏[2sinh(βħω_i/2)]^{-1} 형 · P-6 ★FD 동형 가드 · P-7 요동-응답(var(n)=θ(1-θ)=∂θ/∂(βμ)) · P-8 ε̃ 하류 두 갈래(중심 U_j 로 흡수·s_int=k_B∂(T ln q)/∂T — Ch2 vib 원형, 영점 에너지 규약 주의 단락 포함) · P-9 grand canonical 문맥(eq:sm-gc 셋째 식 교차검증) · P-10 Ch2 와의 기호 통일 문장(Ξ₁).

## 문체·규율
- `/home/user/Project_Anode_Fit/Claude/docs/v1.0.20/results/V1020_STYLE_RUBRIC.md` **전문 정독 후 준수** — 특히 A1 완결 문장·A2 D1/D2(자기 diff·개정 서술 금지: "재배열했다/기존과 달리" 류 절대 금지 — 완성된 교재로)·B1 (a)–(d) 사슬·B2 중간식·B3 verifybox·B4 D7 양식·C 용어 대역(페르미온(fermion)·보손(boson) 병기).
- 인용 화이트리스트(이외 키 금지): hill1960·fowler1939·mcquarrie1976·mckinnon1983·ashcroftmermin1976(장 수준만). 새 문헌 창작 금지.
- 본문 코드 언급 0. 분량: 콘텐츠의 자연 결과(현행 ~114행 → 예상 140–180행).

## 필독(전문)
1. 대상 원문: `_sections/ch1_sec02a_part0.tex` **전문**(특히 L106–219) 2. 후속 접합: `_sections/ch1_sec02b_part0.tex` L1–60(§2.4 가 받는 것) 3. 챕터 정합: `_sections/ch2_sec01_partition.tex` L16–50(Ch2 의 원형-선행 서술과 모순 금지) 4. rubric 전문.
(모든 경로는 `/home/user/Project_Anode_Fit/Claude/docs/v1.0.20/` 기준.)

## 산출 형식
지정된 파일 하나에:
```
% ==== P2 draft <ID> — §2.2 대체 블록 (L106–L219 대체) ====
\subsection{단일 삽입 자리 --- 점유 통계와 그 요동}\label{sec:sm-site}
... (재작성 본문 전체) ...
% ==== 블록 끝 ====
% NOTE:
% Read Coverage: (실제 읽은 파일·행)
% 보존 체크: P-1 위치, P-2 위치, ... P-10 위치
% 신규 라벨: (목록)
% 자체검수: (극한·부호·rubric 자기점검 2–3줄)
```
LaTeX 는 kotex·amsmath 전제. 수식·라벨 이외의 파일 헤더/자산 주석은 블록에 포함하지 않는다(master 가 관리).
