# DIRECTION_GENERAL_REPORT — 일반 내용 확장 방향성 검토 (스트림 4 확대판, 읽기 전용)

> 담당: v1.0.20 **일반 내용 확장 방향성**(통계역학 축 외 전 축). **읽기 전용 — 문건·코드 무수정.** [[feedback_confirmed_items_policy]] 의 '추가 후보' 보고이며, 실집행은 사용자 GO 후에만.
> 작성일: 2026-07-16. 언어: 한국어.
>
> **사용자 피드백 취지**: "수식 유도가 더 매끄럽고, 절간·챕터간 연결이 자연스럽게, 이해하기 쉽게 — 방향성 검토·논문 검토·이공계 교과서 검토. **통계역학 확장은 예시였을 뿐 — 한 측면만 보라는 게 아니다.**"
>
> **통계역학 축 분업**: 통계역학 관통 확장(대정준 전하보존·TST 분배함수·CLT broadening·CNT 과주행·단상 Ω 재규격화·요동감수율·Part 0 사다리 대조표·FD 조기 signposting·기호충돌 가드표)은 `DIRECTION_STATMECH_REPORT.md` 소관 → **본 보고서는 그와 중복 제안 금지**. 본 보고서는 그 밖의 전 축(비-통계역학 유도 매끄러움·절간 전환·챕터간 **독자 항법**·이해 장치·측정 원리 배경·독자 경로·교과서 대조)만 다룬다.

---

## 0. 정독 범위와 방법

**전문 정독(full read)**: Ch1 전 절 — `ch1_sec00_intro`·`sec01_n0n1`·`sec03_center`·`sec04_hys`·`sec05_width`·`sec06_eqpeak`·`sec07_broadening`·`sec08_lag`·`sec09_tail`·`sec10_sum`·`sec11_lcointro`~`sec18_inputs`·`appA_signcheck`·`appB_codemap` + Ch2 `sec00`·`sec01`·`sec02`·`sec03`·`sec04`·`sec05`·`sec06`·`sec07`·`sec08`·`sec09`·`sec10`·`appA_traps` + `appendix_phase_separation` + 양 preamble.
**참고 정독**: `DIRECTION_STATMECH_REPORT.md`(중복 회피 기준선)·`INTERCHAPTER_REPORT.md`(기호·개념 다리 감사 — 본 보고서와 분업: 그쪽은 "다리가 존재·정합하는가" 감사, 본 보고서는 "독자가 페이지를 뒤지는가" 항법)·`V1020_STYLE_RUBRIC.md`(양식 규범)·`V1020_REFERENCE_LEDGER.md`(인용 가능 키)·`STEP_LOG_P7`·마스터플랜(F-1~F-10).
**금지 준수**: `results/comp_P7_review/`·`comp_P7_figs/` 미열람. 그림/그래프 제안 제외(별도 소관). 물리 모델·수치·코드 거동 불변. 확정 판정 존중.

**정독으로 드러난 3대 사실(후보 평가 기준선)**:
- **(G-1) 유도 매끄러움은 이미 최상급.** 흑연 본론 §4(artanh gap + ★Taylor 함정 완비)·§9(부분적분 + 지배수렴정리 명시)·§6(종 항등식)·§8(a-d 사슬)이 헌법 ③·rubric B1~B4 를 모범적으로 지킨다. **비-통계역학 축에서 "점프"는 거의 없다** — (i) 축의 진짜 후보는 소수이며, 큰 이득은 (iii)(iv)(v) 에 있다. 이 사실을 후보 우선순위에 반영한다.
- **(G-2) Ch1↔Ch2 는 별도 컴파일 PDF 라 상호참조가 클릭 불가.** Ch2 가 "Chapter 1 §5·§7·§13·§15·§17" 을 ~30 회 텍스트 참조하나, 독자는 물리적으로 다른 문건을 뒤져야 한다. INTERCHAPTER_REPORT 는 이 다리들의 **존재·정합**을 감사했고 통과시켰다 — 본 보고서는 그 위에 **독자 항법**(대응표·로드맵·자기완결 입력) 관점의 후보를 얹는다(중복 아님).
- **(G-3) LCO Part II(§11~17)는 P4 대보강으로 유도 밀도는 확보됐으나 ★가드·caveat 밀도가 매우 높다.** §12 verifybox·§15 "세 양의 구분"·단위환산 경고가 한 절에 층층이 쌓여, F-4("말이 너무 많다")의 잔여가 **가드 과밀**로 남았다. 이는 유도 결함이 아니라 **이해 장치(요약·수치 감각·worked example)로 완충할 대상**이다.

---

## 축 (i) — 유도 매끄러움 (비-통계역학)

> **총평**: (G-1)대로 이 축은 **의외로 얇다**. 흑연 본론의 부분적분·치환·근사 차수는 이미 중간식으로 노출돼 있다. P7 이 '결함 아님'으로 통과시킨 것 중 '더 매끄럽게' 만들 가치가 있는 후보는 아래 소수뿐이며, 대부분 **저분량 보조 lemma 또는 물리 직관 문장**이다. 전자구조·Sommerfeld 계열은 통계역학 축과 인접하므로 경계를 표기한다.

### 후보 (i-1) — 전달 계수 χ의 물리 직관 문장 (그림 의존 완화)
- **위치 → 무엇을**: `ch1_sec05_width` §5.1 `eq:bv`(정·역 속도 `r± = k_0 e^{-(ΔG_a ∓ χ𝒜)/RT}`) 도입부. 전이상태가 정·역 장벽을 **분율 χ 로 비대칭 분할**하는 물리 그림(왜 도착 골을 내리면 정벽이 χ𝒜, 역벽이 (1−χ)𝒜 만큼 갈리는가)을 **텍스트 1~2문장**으로.
- **왜(누가 이득)**: 현재 이 그림은 거의 전적으로 `fig:barrier`(그림)가 진다. 그림/그래프는 별도 경쟁 소관이라 **최종 그림 형태와 무관하게 텍스트가 자립**해야 한다. 학부 전기화학 독자가 `χ` 를 "임의 피팅 상수"로 오해하지 않게 하는 최소 안전장치. bazant2013 인용은 이미 있음.
- **예상 분량**: 1~2행(신규 식·박스 없음).
- **위험**: 낮음. 물리 불변. fig:barrier 캡션과 중복되지 않게 "구동력이 장벽을 기울이는 물리"만 산문화.
- **우선순위**: 중.

### 후보 (i-2) — logistic 종의 FWHM·분산 보조 lemma (한 줄 워밍업)
- **위치 → 무엇을**: `ch1_sec07_broadening` §7.2 식~`eq:widthbudget` 부근. `FWHM = 2ln(3+2√2)w ≈ 3.53w` 와 분산 `π²w²/3` 가 **결과로만 제시**된다. FWHM 은 반높이 조건 `e^{-z}/(1+e^{-z})² = 1/8 ⟹ y²−6y+1=0 ⟹ y=3±2√2` 의 **한 줄 대수**로 닫힌다 — 각주 또는 인라인.
- **왜(누가 이득)**: 학부 독자가 `3.53` 이라는 "마법 상수"의 출처를 스스로 확인. logistic 미분 종은 문건 전체 폭 논의의 기준이라 이 상수가 반복 등장.
- **예상 분량**: 각주 2~3행.
- **위험**: 낮음. 순수 대수. 분산 `π²/3` 은 표준 적분이라 무유도 관용 가능(각주 포인터만).
- **우선순위**: 하(저분량·저이득이나 저위험).

### 후보 (i-3) — 게이트 적분 = 끝점 항등식의 FTC 한 줄 (전자항, 경계 표기)
- **위치 → 무엇을**: `ch1_sec15_lcoelec` §`sec:lco-Se-scale`. "게이트가 스스로 예측하는 MIT 적분 전자 방출량 `∫|∂S_e/∂x|dx ≈ 1.1 k_B/atom` 이 완전 metal 끝점 `S_e` 와 항등"이 **주장으로만** 제시된다. 게이트 `g: 0→g_max` 라 `∫(∂g/∂x)dx = g_max`, 따라서 `∫ΔS_e dx = (π²/3)k_B²T·g_max = S_e(metal)` 라는 **미적분 기본정리 한 줄**을 노출.
- **왜(누가 이득)**: "작지만 빠지면 MIT 를 못 그린다"는 핵심 주장의 자기검산. 현재 독자는 두 수치(1.1)가 왜 같은지 믿어야만 한다.
- **예상 분량**: 1~2행.
- **위험**: 낮음. **★경계 표기**: 전자 엔트로피 = 전자구조 통계라 통계역학 축과 인접. DIRECTION_STATMECH 가 전자항 유도를 별도로 다루면 그쪽에 흡수. 본 후보는 순수 FTC lemma 라 중복 아님으로 판단하나, 집행 시 조율.
- **우선순위**: 하~중.

### 후보 (i-4) — 특성 온도 T_*의 수치 스케일·직관 (§8, (iv)와 연동)
- **위치 → 무엇을**: `ch1_sec08_lag` §`sec:lag-LV` 식~`eq:Lqfull` 의 `T_* ≡ |I|h/(Q_cell k_B)`. 이 "특성 온도"에 **수치 규모도 물리 직관도 없다** — 독자가 스케일을 가늠 못 한다. order-of-magnitude 1개 + "전류가 정하는 특성 온도" 직관 1문장.
- **왜(누가 이득)**: `L_q ∝ T_*/T` 가 꼬리 진폭을 정하는데 `T_*` 가 얼마나 작은지(대표 C/10·`Q_cell=1` 에서) 모르면 "왜 초기 상태에 꼬리가 안 보이나"(§10 이 `L_V~10⁻¹⁰V` 로 언급)와 연결이 안 된다.
- **예상 분량**: 2~3행(§10 의 기존 `L_V~10⁻¹⁰V` 수치와 교차 참조).
- **위험**: 낮음. 물리 불변(기존 수치 재활용).
- **우선순위**: 중(축 (iv) 수치 감각과 묶어 집행 권장).

**축 (i) 소결**: 진짜 후보 4건, 전부 저분량·저위험. **큰 이득 없음** — 이 축은 이미 최상급이라는 것이 주요 발견이다. (i-1)(i-4)만 중위, 나머지 하위.

---

## 축 (ii) — 절간 전환

> **총평**: rubric A3(절 도입 = [앞 절에서 받은 것 → 이 절이 놓는 것 → 어디로 전달], 절 마무리 = 다음 절 다리)가 잘 지켜져 **대부분의 전환은 매끄럽다**. 남은 거친 지점은 (a) forward-reference 로 순서가 꼬이는 곳, (b) 큰 구조 도약(Part 0 삽입·Part I→II) 앞뒤에서 "계산 흐름만 따라가려는 독자"를 위한 우회 안내가 없는 곳이다.

### 후보 (ii-1) — §6→§7→§8 forward-reference 매듭 풀기 (로드맵 1문장)
- **위치 → 무엇을**: `ch1_sec07_broadening` §7 도입부. §6(평형 peak)이 §7 로 넘기는데, §7 은 세 broadening 출처 중 **①(유한율속 꼬리)이 §8 에서 정의되는 `L_V`** 임을 계속 forward-참조한다(`"이것이 바로 §8–§9 의 동역학 지연 길이 L_V"`). 독자는 아직 안 나온 `L_V` 를 안고 §7 의 밀도 높은 forward-only·PSD 배제를 읽는다. §7 머리에 **"세 출처 중 ②③(평형·앙상블)은 이 절이, ①(동역학 꼬리 `L_V`)은 §8 이 닫는다 — 순서상 폭의 지위(§7)를 먼저 세우고 그 위에 꼬리(§8)를 얹는다"** 로드맵 1문장.
- **왜(누가 이득)**: F-3("히스테리시스 이후 신규 내용 과다")의 정확한 지점. §7 은 문건에서 가장 밀도 높은 절 중 하나인데, 왜 lag 보다 먼저 오는지 모르면 방향을 잃는다.
- **예상 분량**: 1~2행.
- **위험**: 낮음. §7 은 이미 "N6 확장"으로 자리매김하나 §8 과의 순서 논리는 명시 안 됨.
- **우선순위**: 중~상(F-3 정면 대응).

### 후보 (ii-2) — Part 0 우회 안내 (계산 흐름 독자용 skip 힌트)
- **위치 → 무엇을**: `ch1_sec01_n0n1` 말미(Part 0 로 넘기는 다리) 또는 `ch1_sec02a_part0` 머리. §1(N0/N1)에서 §3(N2)로 가는 "계산 진행" 사이에 **Part 0(§2a/§2b, 두 파일 ~54KB 통계역학 사다리)**가 삽입된다. 계산 흐름만 따라가려는 독자에게 **"Part 0 는 평형식의 통계역학 기원 — 결과 사슬만 따라가려면 §3 으로 건너뛰고 필요 시 되돌아와도 무방하다"** 우회 힌트 1문장.
- **왜(누가 이득)**: F-7(학부 독자 논리 추적)·마스터플랜의 "세 층(Part 0→I→II)" 설계를 독자가 능동적으로 항해. 이론 배경 독자는 Part 0 정독, 피팅 실무 독자는 skip 후 참조 — (v-2 독자 경로 안내)와 연동.
- **예상 분량**: 1문장. 단 rubric A6("독자 지칭·명령형 회피")와 조율 — "~해도 된다" 대신 "Part 0 는 배경 유도이며 §3 이하 결과 사슬과 독립적으로 참조 가능하다" 서술형.
- **위험**: 낮음. 단 A6 어조 가드 준수 필요.
- **우선순위**: 중(v-2 와 묶어 집행).

### 후보 (ii-3) — Part I→Part II 인계 요약 (§10 말미 / §11 머리)
- **위치 → 무엇을**: `ch1_sec10_sum` 말미 또는 `ch1_sec11_lcointro` 머리. §10 이 흑연 결과 사슬을 닫고 §11 이 LCO 를 여는데, 그 사이에 **"흑연에서 확정한 forward 골격 중 무엇을 그대로 들고 가고(전극-중립 5식) 무엇이 바뀌는가(파라미터 교체 + 전자항 1개)"** 를 한 눈에 보이는 인계 요약이 흩어져 있다(§11.1 이 5식을 산문 열거, §17 이 "바뀌는 것은 단 둘").
- **왜(누가 이득)**: 흑연 10절을 마친 독자가 새 전극으로 진입하는 최대 도약 지점. 요약이 있으면 "같은 골격 + 1항" 메시지가 진입 즉시 각인.
- **예상 분량**: (iv-2 흑연↔LCO 대조표와 **동일 후보로 통합** 권장 — 표 1개가 이 인계 요약을 겸함).
- **위험**: 낮음. §11.1·§18 tab:nodemap 과 중복되지 않게 "same/changed" 축으로 압축.
- **우선순위**: 중(→ iv-2 로 통합).

**축 (ii) 소결**: 후보 3건. (ii-1)이 F-3 정면 대응으로 최우선. (ii-2)는 (v-2)와, (ii-3)은 (iv-2)와 통합 집행 권장. 전환부는 대체로 건강하며 남은 것은 **큰 도약(§7 순서·Part 0 삽입·Part I→II) 앞의 로드맵 문장**에 집중된다.

---
