# HANDOVER — v1.0.14 검토 반영 + v1.0.15 계획 확정 세션 마무리 (2026-07-04)

> **Chain**: v1.0.10 문제점 대검수 → `v1.0.10/HANDOVER_v1.0.11.md` → v1.0.12(`process/V1012_EXECUTION_LEDGER.md`, ea4c169) → `v1.0.13/HANDOVER_v1.0.13.md`(5995d64) → `v1.0.14/HANDOVER_v1.0.14.md`(v1.0.14 완주분, 037d859) → **본 핸드오버(검토 반영·v1.0.15 kickoff, ~1462338 이후)**. 이 리스트만으로 역추적 가능.

## 1. 본 세션 지시 전문(v1.0.14 완주 이후분 — 완주까지는 HANDOVER_v1.0.14 소관)
1. "챕터1 피규어 15는 조금 이상한데? 다시 한번 확인해볼래? 챕터2는 피규어가 거의 없네? spinodal 부록은 별도 문건으로 그냥 놔두자. 나머지는 거의 대부분 마음에 든다. 최종 코드 업데이트가 필요한지 판단하고 필요하다고 판단되는 경우 작업 계획서를 작성해서 가져올 것."
2. "V[=ν] 기본값 처리라는게 뭔지 이해가 안된다 설명자세히 해봐"
3. "이산전압 격자를 꼭 적용해야돼? 수식으로 추세식이 나오고, 특정 V에 대한 값은 바로 산출이 가능하니, 피팅을 진행하는 샘플의 풀셀을 분리한 양음극 극판 성분의 전압값을 그냥 기입하고 처리하면 되는거 아니야? 그러면 격자 칸이니 뭐니 걱정 안해도되는거 아닌가? 이전 세션 어느 버전인가부터 묻지도 않고 그냥 도입했던건데 솔직히 실 데이터랑 비교하는 입장에서는 일을 몇배로 더 하게 되는 꼴이라 효율이 안좋거든? 한번만 구하면 되는걸 변환하고, 산출하고, 그걸 다시 재 변환해서 원데이터에 매칭 시켜야하는데 굳이....? 그리고 보이든 안보이든 그걸 끄고 쓸 이유가 있나? 안보이면 안보이는 대로 그냥 두고 쓰면 되는거지...."
4. "이산 전압 격자는 완전 퇴출 시킨다. 어차피 이전 버전에 이력이 남아있으니 참고하려면 찾아보면 될 일이고, 이번 버전은 싹 들어낸 버전으로 만든다. 그런데 페이블을 사용할 수 있는 한도가 다 떨어졌다. 이번 작업계획서를 업데이트 하고 나면 더 이상 못 쓸것으로 생각된다. 작업계획서 업데이트와, 핸드오버 한건 총 두건의 문건으로 세션 마무리하자."

## 2. 본 세션 작업 요약(완주 이후분)
- **fig15(fig:reversal) 정정**: 두 패널 세로 간격 부족(2.9 cm)으로 (b) 축·라벨이 (a) 눈금 띠 침범 → yshift −3.9 cm, 빌드 0-err/57p (커밋 8012a72).
- **spinodal 부록 = 별도 문건 유지 확정** 기록(HANDOVER_v1.0.14·INDEX·ledger).
- **ch2 그림 과소**(2장/14p) → v1.0.15 계획 P5.1(선택, 4매·경연 승자 노선 재기용)로 제안.
- **코드 업데이트 필요 판정 + 계획서 3단 진화**: rev.1(전자항 정밀형·고정점·ν 재베이스라인 A/B) → rev.2(점별 경로 신설·legacy 병행) → **rev.3(사용자 확정: 이산 격자 완전 퇴출·점별 단일 아키텍처)** = `Claude/plans/2026-07-04-v1015-code-update-plan.md` (Steps 1–29·Decision 잔여 4건 기본값 명시).
- ν 설명 제공(사용자 납득) → 격자 자체의 필요성 반박(사용자) → 진단 동의: 격자는 lfilter 벡터화 구현 편의였고, ν 스위치·23% 점프·재보간은 파생물. 연속 메모리 적분 형식은 $L_V\to0$ 에서 평형 종으로 해석적 매끈 환원(스위치 원천 불요).

## 3. 미완료(차기 세션 소관)
- **v1.0.15 실행 전체**(계획 rev.3, Steps 1–29): 격자 퇴출 재아키텍처(dqdv 점별 재구현·파라미터 제거·회귀 신규 골든 재정초)·전자항 정밀형·고정점 검증·문건 대동기(eq:vwork/eq:lowpass 이산형/eq:branch/ν/R5/부록 B/가이드)·검수 3회·(선택) Ch2 그림 4매·마감. 방향은 사용자 확정 — 차기 세션에서 GO 확인 후 착수.
- v1.0.13 승계 이월: LCO Ω^cat·dH_a 배정(실측 후)·Motohashi g_max 원문 재확인·[A2]–[A5] 서지 실시간 재검·ml2024 tier A 원문 대조.

## 4. 다음 세션 주의
- **격자 퇴출 시 원형 보존 구역(py L74 "1바이트도 변조 X") 불가침** — `_causal_lowpass`·`func_L_q` 등이 구역 내부에 있으므로 삭제·수정 금지, 미호출 orphan 으로 존치(기존 `func_U_j_hys` 와 동일 지위·부록 B 명기). 본 세션에 구역 침범→원복 이력 있음.
- **회귀 재정초 절차**: 신규 골든은 구판 동일 V_n 좌표로 캡처(비교 가능성), 캡처 전 반드시 구판 등가 검증(미세 격자 수렴 대조·꼬리 활성 케이스) — 의도적 기준선 교체임을 ledger 에 기록. 구 골든은 v1.0.14 폴더 이력으로 보존.
- **FITTING_GUIDE 문턱 수치(70–74 kJ/mol)는 ν·Δ_grid 기반 산출**이라 격자 퇴출 후 정의가 바뀐다 — 재산출·재기재 필수(계획 Step 18).
- 부호 함정 3종 가드 유지(σ_d 슬롯=탈리튬화·Bernardi 라벨·MSMR f=+σ_d) — 문건·코드·가이드의 가드 문구 삭제 금지.
- v1.0.14 는 동결(정정은 Addendum) — 이번 검토 정정(fig15)까지 반영된 상태가 동결본.
- Codex 연동 레시피: rescue 서브에이전트 Bash stdout 결함 시 companion CLI 직접 구동 — `task --write --fresh` + 대상 폴더 선생성(read-only 세션·스테일 스레드 재개 함정 회피).
- 문건 대동기의 층위: 본문은 연속형으로 재서술, 이산 구현의 이력은 각주 1개+v1.0.14 참조 지시(계획 Decision ⑤ 기본값).
