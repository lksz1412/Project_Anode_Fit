# BRIEF O-C — 통계역학 증축 초안: CLT 종형 + CNT 연결 (v1.0.22 R2)

## 공통 규칙 (3창 공통 — 위반 시 산출 기각)
1. **초안 전용**: 마스터 소스(`.tex`)·기존 문서 일체 **수정 금지**. 산출물은 본 폴더(`comp_R2/C_statmech/`)에만 신규 파일로.
2. `Codex/` 폴더 read/write 금지.
3. 서지: `results/V1022_REFERENCE_LEDGER.md` 의 **V1(검증) 키만**(표준 수학 정리는 무인용 허용 — rubric). 기억 서지 금지.
4. 기존 수식·라벨·기호 무변경. 신규 라벨은 `eq:clt-*`/`bgbox` 안 무라벨 권장 — 제안 표기.
5. **새 피팅 파라미터 0**(모델 차원 불변) — 형상 근거·연결 서술만.
6. 어조 = 기존 본문 동일(한국어 수식-구동 교과서체 · 배경 박스 = `bgbox`, 검산 = `verifybox` 관행).
7. **조기 저장**: 산출물을 작업 중 수시로 파일에 저장.

## 맥락·목적
사용자 지시: "통계역학 확장 가능한 영역을 조금 더 늘려보라"(D22 — 단, "이해를 돕지 못하고 혼란을 가중하면 빼라고 하겠다" 유보 있음 → **제거 용이한 독립 블록**으로 설계할 것). GO 확정 2건 = 방향성 보고서의 후보 (iii)·(iv). 문서 = `/home/user/Project_Anode_Fit/Claude/docs/v1.0.22/` (신 Ch1).

## 입력 (전부 정독)
- **`/home/user/Project_Anode_Fit/Claude/docs/v1.0.20/results/DIRECTION_STATMECH_REPORT.md`** — 후보 (iii) CLT·(iv) CNT 절 전체(설계 방침·분량·배치·회수 검산 서식이 이미 확정돼 있음 — 그대로 따를 것).
- `v1.0.22/_sections/ch1_sec07_broadening.tex` — (iii) 삽입처: `eq:ensavg`(앙상블 forward 평균)·`eq:widthbudget`(분산 가법)·`fig:widthbudget` 현행 서술.
- `v1.0.22/_sections/ch1_sec04_hys.tex` — (iv) 연결처: §4 과주행(준안정 가지) 서술.
- `v1.0.22/appendix_phase_separation.tex` §A.7(CNT: ΔG*=16πγ³/(3Δg_v²)·핵생성률 지수 억제)·§A.8(본문 연결 서술 — 이미 존재).
- `v1.0.22/_sections/ch1_sec02a_part0.tex`·`ch1_sec02b_part0.tex` — Part 0 통계역학 사슬(어조·수준 기준 + (iii) 이 Part 0 와 중복되지 않는지 확인).

## 작업 (산출 = `comp_R2/C_statmech/`)
1. **`CLT_DRAFT.tex`** — (iii): `eq:ensavg` 직후 삽입용 **8~12행** 초안. 보고서 서식 그대로: 원형(단일 지배 원천 — ρ 는 그 원천 분포, 비-Gaussian 가능) → 확장 동기(원천 다수·비지배) → 다원천 CLT 종형 + 분산 가법 `σ_η²=Σ_k σ_{η_k}²` 재유도(1식+1식) → 회수 검산(m=1/한 원천 지배 = Lindeberg 붕괴 → 종형 이탈 · ρ→δ → eq:ensavg 단일 델타 환원 — §7 기존 서술 참조). 두 판형 제시: (a) 본문 단락형 (b) `bgbox`[겉보기 이동의 CLT 종형]형 — 권고 판형 명시. `dreyer2010`·`dreyer2011`(다입자 앙상블) 연결 1문장. `fig:widthbudget` 캡션 승격 문구는 **제안만**(캡션 수정은 사용자 판단 영역).
2. **`CNT_LINK_DRAFT.tex`** — (iv): §4 과주행 서술에 상분리 부록 §A.7/A.8 을 잇는 **최소 연결**(신규 유도 금지 — 부록에 이미 완비): "준안정 가지에서 핵생성이 `exp(-ΔG*/k_BT)` 로 지수 억제되는 동안 소인이 전위를 옮긴다" 축의 연결 문단(2~5문장) + `\ref` 참조(부록 라벨 실확인). 부록이 별도 컴파일 문서인지 신 Ch1 에 포함인지 **마스터 tex 에서 확인 후** 참조 방식 결정(포함 아니면 서술형 참조 유지 — xr 대상 아님).
3. **`REMOVAL_CHECK.md`** — 제거 용이성 자가평가: 각 초안 블록을 삭제했을 때 본문 잔여 서술 무영향(참조 고아 0·논지 공백 0)임을 항목별 확인. + 혼란 가중 리스크 자가평가(독자 흐름에서 이 블록이 요구하는 선행 지식·Part 0 와의 중복 여부).

## 게이트
- 분량·배치·서식 = DIRECTION_STATMECH_REPORT 확정안 준수(iii: 8~12행·eq:ensavg 직후 / iv: 연결 성격).
- 새 파라미터 0·기존 수식/라벨 무변경·Lindeberg 한정 명시.
- 제거 시 영향 0 확인 전건.

## 완료 보고(최종 텍스트)
초안 2본 행수·권고 판형·제거 용이성 결론·중복/충돌 발견 사항.
