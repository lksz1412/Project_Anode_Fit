# BRIEF O-A — 이음매·구획 전환 초안 (v1.0.22 R2)

## 공통 규칙 (3창 공통 — 위반 시 산출 기각)
1. **초안 전용**: 마스터 소스(`.tex`)·기존 문서 일체 **수정 금지**. 산출물은 본 폴더(`comp_R2/A_seams/`)에만 신규 파일로.
2. `Codex/` 폴더 read/write 금지.
3. 서지: `results/V1022_REFERENCE_LEDGER.md` 의 **V1(검증) 키만** 인용. 기억 서지 금지.
4. 기존 변수명·기호·라벨·식 번호·한글 표현 임의 변경 금지 — 변경이 필요하면 "제안" 으로 표기만.
5. 수식·자산(절 말미 `% 자산: [A-###]...` 앵커가 대표하는 내용) 삭제·축소 금지.
6. 어조 = 기존 본문과 동일(한국어 수식-구동 교과서체 · `---` 강조 · `$\cdot$` 나열 · 기존 box 환경 관행).
7. **조기 저장**: 산출물을 작업 중 수시로 파일에 저장(중단 대비). 최종 보고 전 반드시 전부 디스크에 존재해야 함.

## 맥락
프로젝트 = `/home/user/Project_Anode_Fit` (LaTeX 물리 교과서, 리튬이온전지 dQ/dV 이론). v1.0.22 에서 활물질별 3챕터로 재편됨:
- 신 Ch1(`Claude/docs/v1.0.22/ch1_graphite_v1.0.22.tex`) = 흑연 곡선 사슬(구 Ch1 §0~10, `_sections/ch1_sec00~10`) + **Part T 열특성**(구 별도 문건 Ch2 전량, `_sections/ch2_sec00~10`) + §18(`ch1_sec18_inputs`) + 부록 4본(`ch1_appA/appB`·`ch2_appA/appB`).
- 신 Ch2(LCO) = 구 Ch1 §11~17(`_sections/ch1_sec11~17`) — **별도 문서**(xr 외부참조로 장 간 live 참조 가능).
- R1 은 "이동+조립"만 했으므로, 구 장 지칭("Chapter 2 의 …", "그 문건 식 (eq:…)")이 본문에 남아 있음 — 이걸 신 구조에 맞게 전환하는 초안이 네 임무.

## 입력 (전부 실재 — 절대 경로 기준 `/home/user/Project_Anode_Fit/Claude/docs/v1.0.22/`)
- **`results/R1B_SWEEP_LIST.md`** — 전환 대상 정본: T1 20건·T2 53건·T3 5건·W 29건, 파일:행 단위. 분류 정의·집행 지침 포함. **이 목록이 작업 범위다.**
- `plans/PLAN_R1_reorg.md` Step 14 — 이음매 8항.
- `plans/PLAN_R2_ch1_completion.md` — phase 계획서(게이트 포함).
- 대상 `_sections/*.tex` 원문 전부.

## 작업 (산출 = `comp_R2/A_seams/`)
1. **`SEAM_PLAN.md`** — 핵심 산출물. T1+T2+T3 전 78건의 행별 전환표:
   `| 파일:행 | 분류 | 현행 문장(해당 부분) | 개정안(정확한 LaTeX — live \S\ref{...}/\eqref{...} 라벨 명시) | 비고 |`
   - T1(Part I 파일→구 "Chapter 2" 열특성 지칭): 같은 문서가 됐으므로 "Part T"+`\S\ref{...}` live 참조로. 대상 절의 실제 라벨은 `_sections/ch2_sec*.tex` 에서 **직접 확인**(예: Einstein 절 = `sec:einstein`).
   - T2(Part T 파일→구 "Chapter 1" 지칭·"그 문건 식 (eq:…)" 서술형): 같은 문서 Part 0/I/부록으로 — 서술형 괄호 라벨은 실제 `\eqref{...}` 로 전환. "Chapter 2 종합식" 같은 자기 구지칭은 "본 파트" 계열로.
   - T3(5건 — Part T 파일이 구 Ch1 의 LCO 절 §11~17 지칭): 대상이 **신 Ch2 로 이동**했으므로 장 간 참조로 — "Chapter 2 (\S\ref{sec:...})" 식(xr 이 ch2 라벨을 해소함 — `ch2_lco_v1.0.22.tex` 가 조립하는 `ch1_sec11~17` 파일에서 라벨 확인). **T2 와 문면이 비슷해도 방향이 반대 — 행별 대조 필수, 기계 치환 금지.**
   - 문장 전체 재작성이 필요한 곳은 최소 diff 원칙(지칭 계층만 교체, 논지 무변경).
2. **`ch2_sec00_intro_partT.tex` 초안** — 구 Ch2 서론의 "장→파트" 격 개작 전문(현재 파일 기반, 문서 범위 서술을 "본 장 Part T" 문맥으로, ~6문장 수준 조정. 파일 안에 이미 R1 이 손댄 부분 있음 — 현행 기준으로).
3. **`ch2_sec10_closing_partT.tex` 초안** — 장 마감 어조 → 파트 마감(뒤에 §18·부록이 이어짐을 반영).
4. **`W_RULE.md`** — W 29건("본 문건/이 문건")의 일괄 규칙안: 권고 "본 장"(장 스코프)/"본서"(작업 전체 — 예: doc-leads 선언 ch2_appB:7) + 행별 적용표 + 예외(재현 선언 3곳 sec00:19·sec10:126·sec18:16 은 의미 보존 검토 — "이 장만 보고 재현" 이 원 의도와 동치인지 판정 포함). §제목·캡션 5곳은 TOC 연동 주의 표기.
5. **`MISC_8ITEMS.md`** — 8항 잔여 검토: sec18 LCO 행 지위(현행 유지 vs 분리 — 권고만), appA/appB 코드맵 2본 제목 "곡선/열특성" 분업 문안, `ch1v22_partT_divider.tex` 다리 문단 확정안.

## 게이트 (네 산출물이 통과해야 할 것)
- 전환표 78행 전수(누락 0) — R1B_SWEEP_LIST 와 1:1 대조 가능해야 함.
- 개정안의 모든 `\ref`/`\eqref` 라벨이 실존(원문에서 확인한 라벨만 — 추측 금지).
- 논지·수식·자산 무변경(지칭 계층만).

## 완료 보고(최종 텍스트)
산출 파일 목록 + 전환표 통계(분류별 건수·라벨 확인 건수) + 판단 보류 항목(있으면 사유).
