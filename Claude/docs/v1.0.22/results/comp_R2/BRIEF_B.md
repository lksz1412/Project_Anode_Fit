# BRIEF O-B — 인용 다리(흑연분) 수식 보강 초안 (v1.0.22 R2)

## 공통 규칙 (3창 공통 — 위반 시 산출 기각)
1. **초안 전용**: 마스터 소스(`.tex`)·기존 문서 일체 **수정 금지**. 산출물은 본 폴더(`comp_R2/B_bridges/`)에만 신규 파일로.
2. `Codex/` 폴더 read/write 금지.
3. 서지: `results/V1022_REFERENCE_LEDGER.md` 의 **V1(검증) 키만** 인용. 기억 서지 금지 — **원문에서 확인하지 못한 논문 수식은 쓰지 말고 "확인 필요" 로 표기**(대응 자리만 비워 제시).
4. 기존 변수명·기호·라벨·식 번호 임의 변경 금지. 신규 라벨이 필요하면 `eq:br-<키>-<n>` 형식으로 **제안** 표기.
5. 어조 = 기존 본문과 동일(한국어 수식-구동 교과서체 · `---` 강조 · `$\cdot$` 나열).
6. **조기 저장**: 산출물을 작업 중 수시로 파일에 저장.

## 맥락·목적
사용자 지시(2026-07-17): "논문 인용 구간은 기존 우리 논의에서 그 논문의 내용으로 가는 과정을 따라가기 쉽게 물리·화학 이론 수식들을 보강해야 한다." 현재 본문은 load-bearing 인용에서 결론만 가져오는 곳이 있음 — 우리 식과 논문 식 사이의 **다리(중간 수식·변수 대응)** 를 놓는 것이 네 임무. 문서 = `/home/user/Project_Anode_Fit/Claude/docs/v1.0.22/ch1_graphite_v1.0.22.tex`(신 Ch1 흑연+열특성 — `_sections/` 조립).

## 양식 (마스터플랜 §7 — 다리 1곳당 3요소 필수)
① 우리 식(라벨 명시)과 논문 식의 **변수 대응표 또는 등치 중간 수식 1~3개**
② 논문이 그 식을 얻는 경로 **한 문장**(방법 요지)
③ 우리 전제와의 **가정 차 1문장**(그대로 가져오면 안 되는 물리적 가정 차이)

## 대상 (D22-5 핵심 12곳 중 신 Ch1 해당분 — 본문 \cite 실위치를 grep 으로 전수 대조하라)
확정 후보(마스터플랜 §7): `dreyer2010`/`dreyer2011`(히스테리시스 다입자 — §4 부근), `mckinnon1983`(등온선/격자기체 — Part 0·§5 부근), `bazant2013`(화학퍼텐셜 분해 — §4/§7 부근), `weppner_huggins1977`(GITT — §1 측정 원리 bgbox), `baek_pilon2022`(엔트로피 측정 — Part T). 그 외 12곳 목록 중 신 Ch1 에 cite 가 실재하는 것(예: Part T 의 `allart2018`·`reynier2004` 등이 load-bearing 인지 판정 포함). **LCO 분(marianetti2004·vanderven1998·MSMR 계보)은 R3 소관 — 제외.** `larchecahn1973`·응력 계열은 R5(Si) 소관 — 제외.

## 작업 (산출 = `comp_R2/B_bridges/`)
1. **`BRIDGE_TARGETS.md`** — 대상 선별표: `| 키 | 본문 cite 위치(파일:행·절) | load-bearing 여부 판정·사유 | 다리 필요도(상/중/하) |` — 12곳 대조 + 신Ch1 cite 전수 스캔 결과.
2. **`BRIDGE_DRAFTS.md`** + 다리별 `.tex` 스니펫 파일 — 각 대상: 3요소 다리 초안(본문 삽입용 LaTeX — 문단 or bgbox/srcbox, 기존 관행 따름) + **삽입 위치**(파일:행, 앞뒤 문장 인용) + 분량(행수). 원문 확인 수단이 있으면(doi·웹) 논문 식 번호까지 명기; 없으면 ③ 규칙대로 "확인 필요".
3. **`BRIDGE_RISK.md`** — 각 다리가 기존 서술과 충돌할 위험(기호 충돌·중복 서술·자산 앵커 인접) 점검표.

## 게이트
- 대상 전수(12곳 대조 표 + 본문 스캔) — 누락·월권(R3/R5 소관 침범) 0.
- 다리 전건 3요소 완비. 원장 V1 외 키 인용 0.
- 우리 기호 체계 유지(논문 기호는 대응표에서만 — 본문 삽입문은 우리 기호로).

## 완료 보고(최종 텍스트)
대상 수·다리 초안 수·"확인 필요" 건수·삽입 위치 목록 요약.
