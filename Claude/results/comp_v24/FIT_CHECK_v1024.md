# v1.0.24 최종 코드 — 공개 실측 dQ/dV 피팅 재현 검증

> 재현: `python3 v24_final_fit_check.py` (main/Anode_Fit_v1.0.24.py 로드). 산출 = `final_fit_check.png`.
> 모델 = 출하 `equilibrium()` 자유폭 MSMR(`{U,w,Q}` 전이). dQ/dV 추출 = BDD 스무딩(`bdd_smoothing.py`). 피팅 = `scipy.curve_fit`.

## 결과 (최종 코드로 재실행)

| 데이터셋 | 화학 | 출처 | 전이 | 창내점 | **R²** |
|---|---|---|---:|---:|---:|
| 흑연 Chen2020 (LG M50) | graphite | PyBaMM 표준 공표 OCP(peer-reviewed 실측 fit) | 4 | 416 | **0.9678** |
| LCO O3 Ramadass2004 | LCO(O3) | 리포 `lco_data/` 실측 OCP | 3 | 580 | **0.9999** |
| LCO O2 Carlier2002 JES Fig4a | LCO(O2) | 리포 `lco_data/` 디지타이즈 실측 | 3 | 700 | **0.9392** |

- **흑연 0.968** = 기존 레지스트리(`fit_registry.md`) Chen2020 값 0.965 와 일치 → **@3/@5/토글 반영이 baseline 피팅을 깨지 않음** 확인.
- **LCO O3 0.9999** = 반쪽셀 OCP 거의 완벽 재현.
- **LCO O2 0.939** = 세 주 peak(≈4.02/4.15/4.41 V) 포착, 가장자리 미세 feature 3-전이 밖(정직).

## 결과 — SINTEF 공개 **실측 raw**(Zenodo 20086298) 흑연·실리콘·흑연+Si 블렌드

> ⚠ **정정(2026-07-20)**: 이 문건 이전 판의 SINTEF 결과는 **기본 로지스틱 커널(bit-exact baseline)** 로만
> 피팅했다(`equilibrium()` line 588 `if tr.get('kernel')=='regsol'` 분기 미탑승 — 전이를 `{'U','w','Q'}` 로만
> 생성). 즉 **@3(Si Frumkin regsol)·@5(흑연 5-feature)를 시험하지 않은 baseline** 이었다. 이전 판의
> "블렌드가 문건 Ch3v22 Frumkin 두-상 물리를 지지"라는 **주장은 철회**했다(0.42 V 급준 peak 를 Frumkin Ω>2RT
> 두-상이 아니라 임의의 좁은 로지스틱으로 잡은 것). 아래는 **@3/@5 를 각자의 실제 코드경로로 태운** 정본이다.
> 데이터=리포 `sintef_data/` CSV(영구보존). 재현=`python3 v24_sintef_fit.py`, 그림=`final_fit_sintef.png`.

**★핵심 정정 — @3/@5 의 실제 코드경로(둘은 서로 다른 커널이다):**
- **@5(흑연 5-feature staging)** = **로지스틱 커널**이다. 코드 상수 `GRAPHITE_STAGING_XRD_v1024` 에는
  `'kernel':'regsol'` 키가 **없다** → equilibrium 의 로지스틱(기본) 분기를 탄다. @5 의 실체는 stage-2L 분리로
  전이를 4→5개로 늘린 **staging 구조 + 열역학 U(T)** 이지 regsol 커널이 아니다. (흑연에 regsol 강제는 불충실.)
- **@3(Si Frumkin)** = **regsol 커널**(`'kernel':'regsol'`)이다. Si 전용.

### R² — baseline ↔ @3/@5 반영식 (동일 데이터·BDD·least_squares)

| 셀 | baseline | **@3/@5 반영식** | 판정 |
|---|---:|---:|---|
| 흑연 반쪽셀 | 0.9525 (4-feature 로지스틱) | **0.9731** (@5 5-feature) | **@5 가 개선(+0.021)** — stage-2L 분리 feature 가 실측을 더 잘 맞춤 |
| 실리콘 반쪽셀 | 0.9943 (로지스틱) | **0.9944** (@3 Frumkin regsol) | 동등 — @3 는 baseline 만큼 맞추며 **물리분류를 얹음**(아래) |
| 흑연+Si 블렌드 | 0.9820 | **0.9848** (@5 흑연+@3 Si) | @3/@5 가 소폭 개선(+0.003); f_Si≈0.75 |

### @3 Si regsol Ω/RT — 문건 §7/@3 분류가 실측에서 성립하는가 (핵심)
§7/@3 이 처방한 regime(a-Si 고용체 Ω<2RT · c-Li₁₅Si₄ 두-상 Ω>2RT)을 부과해 피팅한 결과, **부과가 데이터와 충돌 없이 성립**:

| Si 전이 | U [V] | **Ω/RT** | 상 | 문건 |
|---|---:|---:|---|---|
| a-Si 하단 | 0.307 | 2.0 | 고용체(경계) | §Ch3 si_fr: a-Si=단일상 고용체 ✓ |
| **c-Li₁₅Si₄** | 0.432 | **3.7** | **두-상** | §Ch3: c-Li₁₅Si₄=유일 두-상(1차) ✓ |
| a-Si 상단 | 0.455 | 0.1 | 고용체 | §Ch3 si_fr: a-Si=단일상 고용체 ✓ |

→ **문건 @3 의 "a-Si 고용체 + c-Li₁₅Si₄ 유일 두-상" 물리를 실측이 지지**(이번엔 실제 regsol 커널로, Ω/RT 증거와 함께).

### 정직 소견 — @3/@5 는 단일T 정적곡선에서 baseline 을 깨지 않고 흑연은 개선한다
- **@5**: 5번째 feature(stage-2L 분리)가 흑연 R² 를 실측에서 +0.021 올린다(단일T 에서도 유효한 구조 개선).
- **@3**: R² 는 baseline 과 동급이나, 자유-Ω 로 두면 Ω 는 **비-점식별**(정적 곡선만으론 두-상/고용체를 유일하게
  못 가름 — §Ch3 si_fr·§7 이 명시한 한계)이라, 문건 분류를 **부과**했을 때 R² 가 유지됨을 보이는 방식으로 확인.

## 광의 근거 (기존 20셀 레지스트리 — `fit_registry.md`)
이미 SINTEF Zenodo 20086298(CC-BY-4.0 실측 pOCV)·PyBaMM OCP 로 20셀 피팅 완료:
흑연 0.95–0.97 · **순수 Si 0.997–0.998** · SiGr 0.83–0.98 · NMC(층상 대리) 0.994–0.995.
(원 raw 자료는 스크래치 휘발 — 결과·파라미터 분포는 레지스트리에 보존. 본 재현은 그중 재취득 가능분을 최종 코드로 재실행.)

## 정직한 한계 (남은 것 = Task #38)
- 본 검증은 **단일온도 평형 OCP** dQ/dV 피팅이다(정적 곡선 형상 = 잘 맞음, R² 0.94–0.9999).
- **아직 안 된 것**: @5 stage-2L **온도 분리(0.30 mV/℃)**·@3 **유한율속 꼬리**의 검증은 **다온도·다율속(T/I-swept) 반쪽셀 데이터**가 필요하다 — 위 공개 OCP 셋엔 없다(전부 단일 T 평형). 회사 GITT/율속/온도 매트릭스 데이터로 닫는다.
- **Ω 비-점식별**: 자유-Ω regsol 은 정적 단일T 곡선만으론 Ω(→두-상/고용체 분류)를 유일하게 결정 못 한다(자유 피팅 시 흑연 5전이가 모두 Ω>2RT 로 몰림 = §7 line24 가 예고한 "초기값 Ω 과다배정"). 그래서 분류는 **XRD(§7·Dahn1991)** 에 위임하고, 피팅은 그 분류를 **부과**했을 때 R² 유지로 consistency 만 확인한다(위 Si 표).
- 흑연 2→1 near-delta(≈0.09 V) 정점은 약간 underfit — MSMR 유한폭이 델타를 못 내는 구조 한계(문건 IMPROVEMENT_DIRECTIONS §4 명시, 데이터 아님).

## 추가 후보 (보고만 — 문건 미수정, P5 준수)
- **코드 주석 ↔ §7 분류 표현 불일치**: 코드 상수 `GRAPHITE_STAGING_XRD_v1024` 주석(Anode_Fit_v1.0.24.py line 1136)은
  흑연 **"두-상 4개(1′↔4·3↔2L·2L↔2·2↔1)"** 로 적었으나, **§7(sec:broadening, 권위 분류)** 는 물리적 두-상을
  **2개(2L→2·2→1=LiC₁₂·LiC₆)** 로 한정하고 dilute·4→3·**3→2L 은 고용체**로 둔다(§7 line24 가 "초기값 Ω 로 네 전이가
  문턱을 넘지만 물리적으론 2 전이만 두-상"이라 명시). 즉 코드 주석은 **seed-Ω 문턱초과 집합**을 "두-상"으로 표기해
  §7 의 물리분류와 **어긋난다**(물리 오류가 아니라 주석 표현 문제). → **문건/코드 미수정, 후보로만 보고**(P5). 정정 시
  주석을 "seed Ω>2RT (문턱초과; 물리 두-상은 §7 대로 2L→2·2→1)" 로 바꾸면 §7 과 정합.

## 문건↔코드 정합 정정 — sifr Ω>2RT binodal (사용자 지시로 문건 수정)
- **발견(사용자 지적)**: 문건 sifr 절(ch3v22_sec02b_sifr) warnbox (v)가 "Ω>2RT 는 커널 유효범위 밖 → 이 커널에
  대입해 얻지 않고 현상학적 broadening 으로 다룬다"고 적었으나, **코드 `_regsol_dqdv` 는 Ω>2RT 를 `_regsol_binodal_xa`
  의 Maxwell 공존(binodal) 구성으로 실제 처리**한다(공존역 V=U° 평탄 → U° near-delta + δ=w_j broadening). 즉 문건이
  코드 능력을 축소·부정확 기술 = 문건↔코드 불일치.
- **판정**: 정확성 버그 아님(기본경로 bit-exact·a-Si Ω<2RT 는 doc=code). 순수 문서화 gap. lcoomega 절은 이미
  "커널의 두-상 델타 → 관측 폭 w_j" 로 코드와 정합했음(sifr 만 어긋났음).
- **조치(사용자 결정 "코드가 정상이니 문건을 고쳐라")**: sifr warnbox (v)를 **코드에 맞춰 정정** — Ω>2RT 에서
  Maxwell binodal(공통접선 평탄)로 vdW 고리 해소·U° near-delta+δ=w_j·lcoomega 서술과 정합·a-Si 는 단일상이라 미탑승·
  c-Li₁₅Si₄ 는 Ω>2RT 시드로 binodal 분기가 담음을 명기. **ch3 재빌드 GREEN**(0 err·0 undefined ref·20p·추가 ref 3개
  전부 해결). 나의 SINTEF 피팅이 c-Li₁₅Si₄ 를 regsol Ω/RT=3.7(binodal)로 잡은 것과도 이제 문건이 정합.
- **v1.0.23 여부**: v1.0.23 엔 regsol 커널 자체가 없음(grep 0) → 이 불일치는 v1.0.24 신규(@3), v23 엔 부재.
