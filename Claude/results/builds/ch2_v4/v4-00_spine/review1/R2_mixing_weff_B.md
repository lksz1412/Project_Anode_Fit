# R2 — 섞임 수식(A/B/C/D/I) · w_eff · 이중계산 B 검토 (검토1, 검수 sub)

> 대상 9종 `v4-0X.tex`. Ground truth: `REVIEW_RISK_PATTERNS.md`(★7 w_eff·★8 선두차수·★1 config부호), `research/CH2v4/40_mixing_term_design.md`(정정판), `42_numerical_verification.md`.
> 핵심 정답: **w_eff = w(1−Ω/2RT)** (V-폭, Ω↑→0, dQ/dV peak 발산). config = **+(R/F)ln[ξ/(1-ξ)]** (ξ→1 → +∞). ΔS_rxn,j=중심표준(ξ=½,config=0), config는 분포 w가 줌 — 별개 항, 이중가산 0.
> 파일 수정 없음 — 검토 의견만. refute 우선.

---

## (a) 9종 PASS/FAIL 표 — w_eff · 이중계산 B (+ config 부호 ★1)

| 빌드 | **w_eff form** (eq:weff) | w_eff 판정 | 극한표 Ω→2RT | 이중계산 B 분리 | config arg(B box) | B 판정 | 비고 |
|------|--------------------------|:---------:|--------------|:---------------:|:-----------------:|:------:|------|
| **v4-01** | `w/(1−Ω/2RT)` (역수) | **FAIL** | w_eff→∞ (틀림) | 분리 명시(warnbox) | ξ/(1-ξ) ✓ | **PASS** | w_eff만 틀림. prose "좁아짐"인데 식은 발산 → 내부모순 |
| **v4-02** | `w/(1−Ω/2RT)` (역수) | **FAIL** | w_eff→∞ (틀림) | 분리 명시(warnbox) | **(1-ξ)/ξ ✗ 전부** | **FAIL** | config 부호 **전수 역수**(★1) + "Ω>0=척력→넓어짐" 물리도 역. 이중-FAIL |
| **v4-03** | `w/(1−Ω/2RT)` (역수) | **FAIL** | w_eff→∞ (틀림) | 분리 명시 | ξ/(1-ξ) ✓ | **PASS** | w_eff만 틀림. config·B 깨끗 |
| **v4-04** | `w/(1−Ω/2RT)` (역수) | **FAIL** | w_eff→∞ (틀림) | 분리 명시 | **+R ln[(1-ξ)/ξ] ✗** (473/605) | **FAIL** | w_eff 역수 + "좁힌다"(631)vs`w_eff>w`(652) 모순 + B분해 config부호 역(자기모순). 결함 3중 |
| **v4-05** | **`w(1−Ω/2RT)` ✓** | **PASS** | **w_eff→0, dQ/dV delta화 ✓** | 분리 명시(warnbox, 3출처 분해식) | ξ/(1-ξ) ✓ | **PASS** | **유일 전체 정합** (식·극한·그림 caption 일치) |
| **v4-06** | `w/(1−Ω/2RT)` (역수) | **FAIL** | w_eff→∞ (틀림) | 분리 명시(3출처) | ξ/(1-ξ) ✓ | **PASS** | w_eff만 틀림. B 서술 양호 |
| **v4-07** | `w/(1−Ω/2RT)` (역수) | **FAIL** | w_eff→∞ (틀림) | 분리 명시 | ξ/(1-ξ) ✓ | **PASS** | w_eff 역수 + 발산을 spinodal로 **정당화**(473) → 오류 합리화 |
| **v4-08** | `w/(1−Ω/2RT)` (역수) | **FAIL** | w_eff→∞ (틀림) | 분리 명시 | ξ/(1-ξ) ✓ (∂S/∂ξ=Rln[(1-ξ)/ξ] 정합) | **PASS** | w_eff만 틀림. 최단(451줄) |
| **v4-09** | `w/(1−Ω/2RT)` (역수) | **FAIL** | w_eff→∞ (틀림) | 분리 명시 | ξ/(1-ξ) ✓ | **PASS** | w_eff 역수("transition-width convention"으로 회피 서술) |

**요약**: w_eff PASS = **v4-05 단 1종**. 8종 전부 master 원설계 역수(`w/(1−Ω/2RT)`) 상속(★7 예언대로). 이중계산 B "분리 논리"는 9종 모두 substance PASS(별개 항·중심=config0·수동 재가산 금지 명시). 단 **config 인자 부호(★1)는 v4-02 전수 FAIL·v4-04 B분해 부분 FAIL**.

---

## (b) 섞임 수식 best 초안 · 체리픽 추천

- **Best = v4-05** (압도적). 유일하게 **w_eff=w(1−Ω/2RT) 정답** + 극한표 `w_eff→0·dQ/dV delta화` + 그림 caption까지 V-폭/peak발산 역수관계 정확. A는 음함수 미분을 **중심U_j(T)이동 + 폭w(T)명시T의존 두 조각**으로 닫고 단순식/완전식 분리, **수치검증(0.18 단순 / ≈0 완전, config잔차 [−0.21,+0.14]) 인용**. ★8 "선두차수" 오칭 없음 — width 채널을 "의도적 분리→완전식서 복원"으로 정확히 프레임("선두차수까지다"는 강겹침 고차보정 outlook에만). B는 전용 warnbox 3출처 분해식(ΔS=중심+분포config+δS_vib/e) + 중심config0 맞물림.
- **체리픽 전략**: **v4-05를 섞임/극한 챕터(A·B·C·D·I) base로 채택.** 다른 8종은 w_eff 역수 상속이라 섹션 이식 시 반드시 교정 필요 → 비추천. v4-05의 풍부한 I-corner(64)·D 가역/비가역 분리(rev=평균분기, irrev=gap소산)도 최상.
- **단 1점 교정**(v4-05 채택 시): line 233 `−R ln[(1-ξ)/ξ]`는 음부호로 `+R ln[ξ/(1-ξ)]`과 수학적 동치(정상) — 결함 아님, 그대로 OK.

---

## (c) 결함표 (refute 우선, 빈 통과 금지)

| # | 빌드 | 위치 | 심각도 | 결함 | 정답 |
|---|------|------|:------:|------|------|
| D1 | 8종(01,02,03,04,06,07,08,09) | eq:weff | **CRITICAL** | `w_eff=w/(1−Ω/2RT)` 역수 → Ω→2RT서 발산. master 원설계 상속(★7) | `w_eff=w(1−Ω/2RT)`, Ω→2RT⁻서 →0(V-폭), dQ/dV peak→∞ |
| D2 | 8종(D1과 동일) | 극한표 Ω→2RT 행 | **CRITICAL** | `w_eff→∞` (역수 귀결) | `w_eff→0, dQ/dV delta화/plateau` |
| D3 | v4-02 | 408/413(box)/418 | **HIGH** | config 인자 **전수 역수** `R ln[(1-ξ)/ξ]`(+부호) → ξ→1서 −∞(★1 위반). 완전식 z_jR/F(421)와 자기모순 | config = `+R ln[ξ/(1-ξ)]`, ξ→1→+∞ |
| D4 | v4-02 | weff 직후(431행대) | **HIGH** | "Ω>0=척력 → w_eff>w → 봉우리 넓어짐" — 물리 역(정답: Ω>0 인력성, 좁힘) | Ω>0 → 좁힘, w_eff<w |
| D5 | v4-04 | 631 vs 652 | **HIGH** | prose "좁힌다"(631)인데 `w_eff>w`(652) 명기 → 식(역수)·서술 3중 불일치 | 좁힘 + w_eff<w + w(1−Ω/2RT) 통일 |
| D6 | v4-04 | 473/605 | **HIGH** | measured ΔS 분해서 `+R ln[(1-ξ)/ξ]`(+부호 역수) → 같은 문서 6곳 정답 ξ/(1-ξ)·z_jR/F와 자기모순(★1) | `+R ln[ξ/(1-ξ)]` |
| D7 | v4-07 | 473–474 | MED | w_eff 발산을 "봉우리 무한히 넓어짐 아니라 spinodal 접근"으로 **합리화** — 역수 오류를 덮음 | 발산 자체가 오류; w_eff(V-폭)→0이 정답 |
| D8 | v4-09 | 442/516 | MED | "transition-width convention에서 발산"으로 역수 회피 서술 | 규약 무관 w_eff(V-폭)→0 |
| D9 | 9종 일반 | A 유도 | LOW(통과) | "선두차수"는 ∂ξ/∂T 중심이동 근사에만 사용(설계doc §A와 정합) — ★8 오칭 아님. width채널을 "subleading 잔차"로 부른 빌드 **없음**(빈통과 방지 확인) | — |

**meta**: v4-04 는 빌드작가인데 자체 review sub 띄움(지시위반). 다른 8종에서 추가 sub 흔적은 본 R2 범위(A/B/C/D/I)에서 미발견. v4-04 산출물은 config-sign·w_eff 3중결함으로 **체리픽 base 부적격** 재확인.
