# A2 검수 — 섞임 수식·극한·Bernardi·범위 적대 반증 (v4-10)

> 역할: adversarial A2 (검수 sub). 대상 `Claude/results/ch2v4/v4-10/v4-10.tex` (737줄).
> Ground truth: `research/CH2v4/40_mixing_term_design.md`(정정판)·`42_numerical_verification.md`·`base_ch2_v3.tex`(Bernardi).
> 방법: 반증 시도 + 독립 수치 재현(scratchpad `check_a2.py`·`check_overlap.py`).
> 파일 수정·commit·push 없음. 검수 보고만.

---

## 0. 독립 수치 재현 (적대 검산)

`42_numerical_verification.md` 표6 을 독립 코드로 재현 — **정확 일치**:
- well-separated(원래 파라미터): max|FD−완전식|=0.0000, max|FD−단순식|=0.1841 mV/K. 표6 과 일치.
- ★**강한 겹침 적대 테스트**(U_j 중심을 15 mV 내로 몰아 봉우리 강중첩): max|FD−완전식|=**0.00001 mV/K**, max|FD−단순식|=0.188 mV/K.
  → 완전식은 **겹침 강도와 무관하게 부동소수점 정밀도로 정확**. 이는 완전식이 음함수 미분의 *완전한 해석적 도함수*(∂ξ_j/∂T 두 조각 전부 보존, truncation 0)이기 때문.

---

## 1. 결함표

| # | 식별자/위치 | 심각도 | 결함 | 정정 |
|---|---|---|---|---|
| **D1** | 한계·갭 (1), L678–680 ↔ §A srcbox L466·§B L502 | **HIGH** (consistency) | **"선두차수" 오귀속 — 자기모순.** §A srcbox·§B 는 완전식을 "FD 와 정확히 일치, 부동소수점 정밀도"로 확정. 그러나 한계·갭 (1) 은 "강한 겹침 영역의 더 높은 차수 보정 … (현재는 **선두차수까지다**)"라며 완전식이 leading-order 라 함축. 둘은 모순. 독립 강겹침 테스트(15 mV 중첩)에서 완전식이 여전히 exact(0.00001 mV/K)임을 확인 — 완전식엔 미계산 고차 보정이 **없다**. "선두차수"는 **단순식**(design doc §A L8: 선두차수 ∂ξ_j/∂T≈−g_j∂U_j/∂T)에만 적용되며 완전식에 오귀속됐다. | (1) 을 재서술: 완전식은 logistic 분포 가정 하 *닫힌·정확*(겹침 무관); 잔존 공백은 logistic 형 자체(상호작용 w_eff·비-logistic 봉우리)의 실데이터 round-trip 검증이지 "고차 보정"이 아님. "선두차수까지" 문구 삭제. |
| **D2** | §A L444–445 "첫 조각만(중심 이동)" 서술 | LOW (서술 정확성) | 단순식을 "중심값 식"으로 닫는 근거를 "첫 조각만 넣으면 분모 g_j 와 분자 −g_j∂U_j/∂T 가 만나"로 서술. 실제로 *분모는 항상 g_j(완전)이고 분자에서 w(T)-조각을 뺀 것*이 단순식 → "첫 조각만"이 분모·분자 **양쪽**에 적용된다는 오해 소지. (수치엔 무해, 결과식은 정확.) | "분자의 w(T)-조각(−g_jRz_j/F)을 생략하면" 으로 명확화. 분모는 g_j 전체 유지임을 명시. |
| **D3** | §A L456 "가중 분모 ΣQ_jg_j 가 정확히 측정 dQ/dV(=∂x/∂U)" | LOW (정규화) | ΣQ_jg_j = Σ Q_j ∂ξ_j/∂U = ∂(ΣQ_jξ_j)/∂U = Q·∂x/∂U. 즉 측정 dQ/dV 의 **Q_tot 배**(또는 x 가 정규화 SOC 면 비례상수). "정확히"는 비례상수까지 포함하면 과한 표현. | "측정 dQ/dV 에 비례(∝∂x/∂U)" 로 완화하거나 Q=ΣQ_j 정규화를 명시. |

---

## 2. 표적별 견고도 판정

**A 겹침 가중 (target 1) — 견고(D1 단서).** 음함수 미분 ∂U_oc/∂T = −Σ Q_j ∂ξ_j/∂T / Σ Q_j ∂ξ_j/∂U (L424) 정확. g_j=ξ_j(1−ξ_j)/w_j 정확. ∂ξ_j/∂T 두 조각(−g_j∂U_j/∂T − g_j(R/F)z_j) 정확(z_j=ln[ξ/(1−ξ)] 항등식 포함). 단순식=중심·완전식=완전 분리 정확. width 채널을 "완전식서 복원"으로 둔 framing 자체는 옳음(완전식이 진짜 exact). 수치(완전 0.000·단순 ≤0.184) 정확 인용. **단 D1 의 "선두차수" 오귀속이 이 framing 과 정면 충돌**(완전식을 leading-order 라 깎아내림).

**C w_eff (target 2) — 견고.** dV_eq/dθ|₁/₂=(4RT−2Ω)/F=4(RT−Ω/2)/F 정확. 이상 logistic 중심 기울기 dV/dξ|₁/₂=4w 정확(w/[ξ(1−ξ)]@½=4w). 정합 → w_eff=w(1−Ω/2RT) 정확. design doc §C 의 핵심 nuance(**역수 아님·V-폭은 줄되 dQ/dV peak 은 발산·delta화**)가 L533·L536·캡션에 반영됨. Ω→2RT⁻ 임계도 정확. θ/ξ 공간 혼용은 대칭이상부 때문에 |기울기| 동일 — 무해.

**D 히스 (target 2) — 견고.** 분기별 ∂U^(d)/∂T = 분기 봉우리 g_j^(d) 가중(L573) 정확. 가역=분기 평균(L580), 비가역=히스 gap ∝IΔU^hys, ∂/∂T 와 분리(L583–584) — design doc §D 와 정합. 혼동금지 박스 정확.

**I 극한 (target 2) — 견고.** 6 코너(ξ→1 +∞·ξ→0 −∞·ξ=½ 표준값·Ω→2RT plateau·단일봉우리·고온 Sommerfeld) 전부 옳은 극한. "상수+분포로 비선형 자동" 판정(L624–630) = design doc §I 정합. MIT 예외(electronic x-의존 유지) 보존.

**★범위 가드 (target 3) — 견고(CLEAN).** 전셀 합성 ∂U_cell=∂U_cat−∂U_an 은 **오직 범위-밖 warnbox(L97–98)·한계갭(5)(L683)에만** 등장. 어떤 유도에도 혼입 0. 코인 하프셀(단일 전극 vs Li) 단독. 전셀 수치 인용 없음. LCO 는 "분포 언어 사례"로만 라벨(L99·L384). 깨끗함.

**Bernardi (target 4) — 견고.** eq:qrev(L640–642) Q̇=I(U_oc−V)−IT∂U/∂T, Q̇_rev=−IT∂U/∂T=−(IT/F)ΔS — base_ch2_v3.tex eq:bernardi(L72)·가역/비가역 분리와 정확 일치. srcbox 부호 재유도(ΔG=−FU_oc, ΔS=+F∂U_oc/∂T)(L648–650) 자기일관. v3 의 일반 n → v4 n=1(Li 1몰) 특수화 일관. 분포 재배열 봉합 정확.

---

## 3. 가장 약한 1곳 (refute 집중)

**한계·갭 (1) 의 "선두차수까지다"(L680).** 강겹침 적대 수치(완전식 exact 유지)로 직접 반증됨 — 완전식은 leading-order 가 아니라 logistic 가정 하 *완전 닫힌식*이다. 이 한 문장이 §A·§B 의 "부동소수점 정밀도 일치" 확정과 모순하며, 본 장 최강 결과(완전식 exact)를 스스로 깎아내린다. design doc §A 의 단순식용 "선두차수" 수식어가 완전식 한계 서술로 잘못 이월된 흔적. → **D1 (HIGH) 정정 권고.**

(빈 통과 금지 준수: CLEAN 표적 4건도 위처럼 근거식·줄번호로 적대 확인함.)
