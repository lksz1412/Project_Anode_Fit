# A1 — 분포 유도·부호 적대 검수 (v4-10 체리픽본, 13p)

> 역할: adversarial A1 = 분포 유도·부호 회의자. v4-10 의 물리·유도·부호를 *반증 시도*. 대상 `Claude/results/ch2v4/v4-10/v4-10.tex`. Ground truth = `REVIEW_RISK_PATTERNS.md`(위험 8종)·`41_statmech_spine.md`·`40_mixing_term_design.md`·`42_numerical_verification.md`.
> 판정 요지: **config 부호·μ(V)·w_eff·exo/endo 4대 CRITICAL 면 전부 견고(반증 실패).** 실질 결함은 LOW 1건(선두차수 오칭 잔재)+HIGH 1건(분포→식 유도 점프). CRIT/HIGH-부호 결함 0.

---

## 1. 반증 표적별 검증

### 표적 2 ★config 부호 (위험 1·2) — **반증 실패(견고)**
- eq:logistic 박스가 ξ=1−θ(탈리튬화 진행률), ξ→1=Li 희박, ξ→0=만충 으로 정의(line 144–157). config 부분몰 항은 **모든 식에서 `+R ln[ξ/(1-ξ)]`** 로 일관:
  - eq:dVdT_config(229) · 발산 부호 itemize(241–248) · 파생 B 박스(296) · 핵심 박스(393) · single_config(505) · use-this 완전식(670) · 극한표(607–609) · fig caption(282) 전부 동일 인자.
- ★스파인 자체 모순 미상속: `41_statmech_spine.md` line 27 = `+R ln[(1-ξ)/ξ]`(역수·틀림)인데 **v4-10 은 이를 상속하지 않고** 정정판 `40_mixing_term_design.md`(line 13–15)의 `+R ln[ξ/(1-ξ)]` 채택. 위험 1 정답과 정확 일치.
- 역수 `(1-ξ)/ξ` 잔존 = **0**(config 항으로는). line 160(θ-form Nernst, 정상)·line 234(중간 치환 `-R ln[(1-ξ)/ξ]` 을 즉시 `=+R ln[ξ/(1-ξ)]` 로 변환·정상)뿐.
- ξ/θ 치환 **명시 대수 수행**(말로 때우기 아님): line 234 `∂S_config/∂θ|_{θ=1-ξ}=-R ln[(1-ξ)/ξ]=+R ln[ξ/(1-ξ)]`. 위험 2 정답 충족.

### 표적 1 분포 유도 단계성 (위험 4·5) — **반증 실패(견고), 단 HIGH 점프 1건**
- Z→⟨n⟩: eq:Z1(116)→eq:occ(122–124) 각 등식 명시, 점프 0. statmech_spine line 10 과 동일.
- μ(V) 부호 전파(위험 4): eq:muV(135) μ=μ⁰−σ_dF(V−U_j) → ε₀−μ=σ_dF(V−U_j) → θ=1/(1+e^{+(V−U_j)/w})(eq:logistic). prose(139 "V↑→θ 감소")↔박스 지수 부호 **일치**. Ch1 의 ξ_eq=1/(1+e^{−σ_d(V−U)/w}) 와 정합. **반증 실패.**
- 단위 다리(위험 5): line 148–149 자리당 β(ε₀−μ)→몰당 F(V−U)/(RT)=(V−U)/w, R=N_Ak_B·F=N_Ae·βF=1/w **1줄 명시**. 충족.
- **[HIGH·점프] vib/elec 분포→식**: BE mode entropy(356)는 "보손 통계에서" 라벨만 달고 `S=-k_B∑[f ln f-(1+f)ln(1+f)]` 중간단계 생략; Sommerfeld S_e(374)는 "Sommerfeld 전개" 라벨 후 저온 전개·∫C/T 대수 0 으로 결과 직착. **공식 자체는 교과서 정확**(부호·(1+n) 인자·π²/3 prefactor·선형 T 검증 완료)이나, "분포를 본체로 전개"라는 챕터 선언 대비 두 항만 *인용 점프*. 가장 약한 곳 = electronic S_e(372–374).

### 표적 3 ★exo/endo (위험 3) — **반증 실패(견고)**
- $\dot Q_\rev=-(IT/F)\Delta S$, ΔS=+F∂U/∂T 와 6개 서술 전부 정합(line 656–661, 병렬 검증 sub 확인):
  - ΔS>0→Q̇<0=흡열(656)·ΔS<0→Q̇>0=발열(656–7)·저-x +ΔS→방전 흡열(658–9)·고-x −ΔS→방전 발열(659–60)·stage2→1 큰 음 ΔS→방전 Q̇>0 발열(660–1). 전부 정답.
- ★v4-04 역매핑("∂U/∂T>0→발열", "희박 +ΔS→방전 발열") **잔존 0**. n=1·ΔS per mole Li 명시(657). srcbox(648–50) ΔS=+F∂U/∂T·Q̇=−(IT/F)ΔS 유도 일관. **교정 후 역매핑 0 확정.**

### 표적 4 ★w_eff (위험 7) — **반증 실패(견고)**
- eq:weff(529) `w_eff=w(1−Ω/2RT)` = **정답 형**(역수 아님). 유도: slope_BW(191)→slope_half(520) (4RT−2Ω)/F=4w(1−Ω/2RT), 이상 logistic 중심기울기 4w 정합 → w_eff. master 원설계 역수 `w/(1−Ω/2RT)` **미상속**.
- 극한 일관: line 535·극한표 610 모두 "Ω→2RT⁻: w_eff(V-폭)→0·dQ/dV peak 발산·plateau" = 위험 7 보완 정답. fig:weff 플롯·caption(563) 일관. 역수 잔존 0.
- ★이중계산 B(위험 7 2부): 파생 B 박스(286–302·501–514) ΔS⁰_j≡[ΔS]_{ξ=½}(config=0)·config 별개·중심서 0 이라 비중첩. ΔS(x)=ΔS⁰_j+R ln[ξ/(1-ξ)]+δS_vib/e 분해 자기일관. **반증 실패.**
- dQ/dV peak 높이 1/(4w_eff) 발산 방향(533·563)도 mixing_term_design line 20 과 일치.

---

## 2. 결함표

| # | 식별자/위치 | 심각도 | 반증근거 | 정정 |
|---|---|---|---|---|
| 1 | 한계·갭 (1), line 680 "현재는 선두차수까지다" | **LOW** | 위험 8 정답 = width/config 채널은 "선두차수 아님"(같은 O(1)·의도적 누락→완전식서 복원). 42_numerical 은 완전식이 *겹침 영역 포함* 175점 abs-err=0 으로 일치 확인 → "강한 겹침 영역은 선두차수까지"는 검증된 사실을 **보수적으로 과소진술**. 본문 eq:weighted 는 "단순식/중심값 식"·완전식은 "마저 넣으면"(의도적 복원)으로 *올바르게* 프레이밍하므로 위험 8 본체 결함은 아님 — 한계절의 잔재 단어. | "선두차수까지" → "단일전이 지배영역은 완전식이 FD 와 부동소수점 일치; 강한 겹침의 *고차 보정*은 실데이터 round-trip 으로 확정" 로 교체(채널을 subleading 으로 부르지 말 것). |
| 2 | vib eq:Svib_mode(356)·elec S_e(374) 유도 점프 | **HIGH** | "분포를 결론 인용이 아니라 본체로 전개"(서, line 82) 선언 대비, BE/Sommerfeld 두 항만 분포→엔트로피 중간대수 생략(라벨 인용). config 는 Z→⟨n⟩→S=−R∑p ln p 완전전개인데 vib/elec 만 비대칭. **공식·부호는 전부 정확**(반증 실패)이라 CRITICAL 아님. | BE: `S=-k_B∑[f ln f∓(1±f)ln(1±f)]`(보손 −) 1줄, Sommerfeld: `S_e=∫₀ᵀ C_e/T'dT', C_e=(π²/3)k_B²Tg(E_F)` 1줄 추가하면 점프 해소. |

---

## 3. 종합 판정

- **config 부호**: `+R ln[ξ/(1-ξ)]` (ξ→1 희박 +∞), 전 식 일관·역수 0·스파인 오류 미상속 — **견고**.
- **exo/endo**: 6 매핑 전부 $\dot Q_\rev=-(IT/F)\Delta S$ 정합, v4-04 역매핑 잔존 0 — **견고**.
- **w_eff**: `w(1−Ω/2RT)`(역수 아님)·이중계산 B 분리·극한 정답 — **견고**.
- **유도 견고도**: Z→⟨n⟩→config·μ(V)·단위다리·implicit-diff(eq:dxidT)·w_eff·Q̇_rev srcbox 전부 단계 명시·점프 0. vib/elec 2 항만 인용 점프(HIGH).
- **CRIT 결함 0. HIGH 1(vib/elec 유도 점프, 공식은 정확). LOW 1(선두차수 오칭 잔재).**
- 약한 1곳: electronic S_e(line 372–374) — "Sommerfeld 전개" 라벨 후 중간대수 0 으로 결과 직착(가장 비자기완결).

*4대 CRITICAL 부호 면(config·μ(V)·w_eff·exo/endo)에서 반증 시도 전부 실패. v4-10 은 v4-04 결함·스파인 모순을 정확히 교정한 견고 체리픽본. 잔여는 HIGH 1·LOW 1 의 비-부호 결함.*
