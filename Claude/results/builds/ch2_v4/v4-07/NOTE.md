# v4-07 short note

- Distribution development: explicit path from single-site partition function `Z_1=1+exp[-beta(epsilon_0-mu)]` to occupation, Ch1 logistic, `S_config=-R sum p ln p`, and partial-molar config term.
- Mixing math: includes overlap weighting, full overlap-plus-config expression, `w_eff(Omega)=w/(1-Omega/2RT)`, branch hysteresis handling, and limit checks.
- Double counting: `Delta S_rxn,j^0` is defined as the transition-center standard value; configurational variation is supplied by the logistic width/config term.
- Scope/sign: coin half-cell only; `xi` is de-lithiation progress; config term is `+R ln[xi/(1-xi)]`; `Q_rev=-IT dU/dT`, so discharge with `dU/dT>0` is endothermic.
- Pages: two-pass XeLaTeX output is 12 pages.
- Self-check: 10-pass checklist applied against prompt items: scope, sign, distribution, config derivation, vib/electronic decomposition, overlap, `w_eff`, hysteresis, limits, xelatex.
- Uncertainty: Electrochimica Acta 2019 occupation-transition prototype remains method-tier because full-text equations were not directly checked here.
