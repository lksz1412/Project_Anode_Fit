# v4-09 short note

- Distribution development: expanded v3 skeleton into a statistical-thermodynamics chapter from single-site partition function `Z_1=1+exp[-beta(epsilon_0-mu)]` to occupation logistic, then to `S_config=-R sum p ln p`; vib is treated through phonon BE occupation and electronic through FD/Sommerfeld.
- Mixing math: added overlap weighting `dU_oc/dT=(1/F) sum Q_j g_j DeltaS_j / sum Q_j g_j`, full configurational correction with `R z_j`, numerical-verification A summary, `w_eff(Omega)=w/[1-Omega/(2RT)]`, hysteresis branch averaging, and corner limits.
- Double counting: `Delta S^0_rxn,j` is explicitly the center standard value at `xi=1/2`; the within-peak configurational term is generated by `w=RT/F` and is not added again to `Delta S^0_rxn,j`.
- Scope: graphite coin half-cell / single electrode vs Li only; no full-cell synthesis formula is developed.
- Build/page: `xelatex -interaction=nonstopmode v4-09.tex` exits 0; final PDF is 12 pages.
- Self 10-pass convergence checklist: distribution spine, sign convention, config limits, vib decomposition, electronic decomposition, overlap weights, numerical A, double-counting B, hysteresis separation, and reversible-heat workflow were each checked against the build prompt.
- Uncertainty 1: the provided `w_eff(Omega)` convention is followed exactly; later Ch1 integration should keep clear whether `w_eff` is interpreted as voltage-logistic coefficient or capacity-domain transition-width parameter near the plateau limit.
