# v4-08 short note

- Distribution spine: explicit `Z_1 -> <n> -> S_config=-R sum p ln p`, then `config/vib/electronic` decomposition.
- Mixing math: local `dQ/dV` overlap weighting, full config-inclusive formula, `w_eff(Omega)=w/(1-Omega/2RT)`, branch hysteresis average.
- Double counting: `Delta S^0_rxn,j` is the center standard value; logistic/config term is separate and not re-added.
- Scope: graphite coin half-cell only; no full-cell synthesis.
- Build: `xelatex -interaction=nonstopmode v4-08.tex` run twice after final edit; output PDF is 7 pages.
- Self-check: 10-pass content checklist covered distribution derivation, sign convention, entropy decomposition, overlap, numerical-verification claim, `w_eff`, hysteresis split, limits, reversible heat, and citation tier.
- Uncertainty: Huggins DOI retained as indexed DOI; if a later bibliography audit uses a different canonical record, update that single bibitem.
