# V1.0.14 P4.1 R3 검수 보고 — 검수자 B(Sonnet) · 어투 재스캔

- 대상: `Claude/docs/v1.0.14/graphite_ica_ch1_v1.0.14.tex`(전문 1–3229) · `graphite_ica_ch2_v1.0.14.tex`(전문 1–782) · `appendix_phase_separation.tex`(전문 1–482) — 3파일 전문 통독(head→tail, 생략 없음).
- 렌즈: ①P3.1 정련 이후 R1·R2 정정으로 유입된 신규 문장의 문체 이상 ②전 문서 잔여 위반 재수색(지정 키워드·명령형 어미·비문) ③문장 문법 파손(주술 불일치·괄호 파손·대시 남용) ④편집 이음새(개시구·표 재지시 중복).
- 방법: 전문 정독 1회 + R1/R2 검수보고서(`V1014_REVIEW_R1_{A,B,C}.md`, `V1014_REVIEW_R2_{A,B,C}.md`) 및 `V1014_EXECUTION_LEDGER.md` 대조로 "이미 지적·수정된 곳"과 "지적됐으나 미수정으로 남은 곳"을 구분 → 후자에 재스캔 집중. grep 기반 잔여 위반 전수 수색(값어치·환율·옷·먹인다·못박·산다·죽는다·미끄러·갈아 끼·보자·그래서·(신규)·(v7-11)·명령형 "-하라/-말 것"·구어체·번역투·중복 조사) 결과 **0건**(P3.1+R1+R2 를 거치며 이미 해소).
- 파일 수정: 없음(수칙 준수). 판정: **조건부 PASS** — HIGH 1 · MED 3 · LOW 2.

---

## 1. 지적 목록 (심각도순, refute 생존분만)

### F1 | HIGH | ch1:2857–2858 — R1 B-02 지적의 쌍둥이 문장 중 한쪽만 미수정(★가장 약한 1곳)

- **원문**: "\textbf{(a) 출발 --- 좌표 매핑의 닫힌 꼴.} 게이트(식~\eqref{eq:ggate})는 조성 $x$ 의 함수인데 코드는 전압 격자 위에서 돌므로, T1 창의 $x$ 범위(...)를 T1 진행률로 선형 보간해 잇는다:"
- **결함**: R1 검수자 B(`V1014_REVIEW_R1_B.md` B-02)가 "코드는 전압 격자 위에서 돌므로"라는 구현-주어 문장을 **정확히 이 위치를 포함해 2곳**(구판 L2756–2757·L2829–2830) 지적했고, `V1014_EXECUTION_LEDGER.md` P2.1/P3.1 로그는 "구현-어투 잔존 9곳 물리 서술화"를 완료로 기록한다. 그런데 실제로는 **한쪽만 고쳐졌다** — 동일 내용을 서술하는 쌍둥이 문장이 ch1:2783("전자항 $\Delta S_{e,j}(x,T)$ 는 조성 $x$ 의 함수인데 곡선 계산은 전압 격자 위에서 진행되므로")으로는 물리-서술화됐지만, 위 인용 문장(2857–2858)은 "코드는 ... 돌므로"라는 구현-주어 표현이 원문 그대로 남았다. 같은 문건 안에서 같은 내용을 담는 두 문장이 한쪽만 정련되어 어투 불일치가 발생했고, 이는 부록 B 서두(ch1:3065)의 자기선언 "본문을 코드 언급 없이 물리로만 읽을 수 있게"와도 정면으로 어긋난다.
- **근거**: ch1:2783 대조 구문 확인(물리-서술화 완료형) vs ch1:2857–2858(미수정, 구현-주어 원형 잔존). refute 시도 — "코드"가 여기서 물리적 계산 절차를 가리키는 관용어일 뿐이라는 반론은, 바로 26행 아래 동일 의미의 문장이 "곡선 계산은"으로 대체된 사실 앞에서 성립하지 않는다(같은 저자·같은 라운드가 이미 그 표현을 어투 결함으로 판정했다는 증거).
- **정정 제안**: "게이트(식~\eqref{eq:ggate})는 조성 $x$ 의 함수인데 곡선 계산은 전압 격자 위에서 진행되므로,"로 2783행과 동형 치환.

### F2 | MED | ch1:1822 — R1 B-02 지적 문장이 미수정으로 잔존

- **원문**: "이다. 초기값은 $\xi_{\mathrm{lag},0}=\xi_{\eq,0}$ --- 패딩 격자 끝에서 $r(q_0)=0$, 곧 식~\eqref{eq:memory} 의 첫 항을 소거하는 선택이다. $L_V\le0$ 이거나 비유한이면 원신호를 그대로 돌린다."
- **결함**: R1 B-02가 "L1797: '$L_V\le0$ 이거나 비유한이면 원신호를 그대로 돌린다.'"를 구현-어투 잔해로 명시 지목했으나(코드 `return source_signal.copy()`의 직역), 현재 문장이 원문 그대로 남아 있다. "돌린다"는 함수의 반환(return) 동작을 그대로 옮긴 동사로, 같은 문단의 다른 서술("...선택이다", "...소거한다")과 어투 층위가 다르다.
- **근거**: 원문 대조. 같은 절 내 인접 문장들은 전부 서술형 물리 문장인데 이 문장만 구현-동사 "돌린다"를 쓴다.
- **정정 제안**: "$L_V\le0$ 이거나 비유한이면 진행률은 변형 없이 $\xi_\eq$ 그대로 남는다." 류로 물리 서술화.

### F3 | MED | ch1:1389 — 동일 범주의 신규(미지적) 잔존 — "코드가" 명시 주어

- **원문**: "평형 진행률에서 곧바로 얻어지는 것이 평형 peak 모양 $\xi_\eq(1-\xi_\eq)/w$ 이다. 이것이 전류가 없을 때($|I|\to0$)의 기준선이며, 동역학 꼬리가 무시될 만큼 작을 때 코드가 직접 쓰는 항이다."
- **결함**: sec:eqpeak(N6) 도입부로, R1 B-02 지적 범위(L1660–3201, N7 이후)보다 **앞쪽**이라 이번 라운드까지 미포착 상태로 남아 있던 동일 범주 결함이다. "코드가 직접 쓰는 항"은 명시적으로 "코드"를 문법 주어로 세워 물리량을 서술한다 — 부록 B "본문 코드 언급 0" 선언과 어긋나는 것은 F1·F2와 동일.
- **근거**: 원문 대조. 앞 문장("이것이 ... 기준선이며")과 뒷 절("코드가 직접 쓰는 항")이 접속사 "이며"로 이어져 있어, 앞은 물리 서술·뒤는 구현 서술로 어투가 한 문장 안에서 갈린다.
- **정정 제안**: "동역학 꼬리가 무시될 만큼 작을 때 이 종으로 직접 환원되는 항이다." 류로 대체.

### F4 | MED | ch1:1523–1528 — R2 F1 물리 정정이 기존 괄호 하나에 통째로 얹혀 3문장을 가둔 구조(읽기 끊김)

- **원문**: "(logistic 미분 종~\eqref{eq:belliden} 은 규격화하면 scale $w_j$ 의 logistic 분포 그 자체라 분산이 $\pi^2w_j^2/3$, 반높이 전폭은 $\mathrm{FWHM}=2\ln(3+2\sqrt2)\,w_j\approx3.53\,w_j$ 다 --- $n_j{=}1$, 298 K 에서 $\approx91$ mV. 기호 규약 --- 식~\eqref{eq:widthbudget} 의 $\sigma$ 는 모두 \emph{표준편차}(분산의 제곱근)이고, logistic scale $s$ 와는 $\sigma=\pi s/\sqrt3$ 로 환산된다; 성분비로 적으면 $\sigma_\mathrm{sym}/\sigma_\mathrm{int}=\sqrt{1+(\sigma_\eta/\sigma_\mathrm{int})^2}$ 이라 같은 비율이 scale 에도 그대로 적용된다). 실측 종에서 조작적으로 분리되는 것도 이 두 축이다:"
- **결함**: 여는 괄호 하나가 **온전한 세 문장**(FWHM 서술 종결 "...mV." → "기호 규약" 서술 종결 "...환산된다;" → "성분비로 적으면...적용된다.")을 담은 채 5줄 뒤에야 닫힌다. 문서 전체의 다른 괄호 삽입은 대개 구 하나 또는 한 문장(예: "(그렇지 않으면 $U_j$ 그대로)")인데, 이 자리만 R2(F1 정정, `V1014_REVIEW_R2_B.md`)에서 σ 규약(표준편차 vs scale) 정정 문단이 기존 괄호 안에 그대로 이어 붙으며 괄호가 비정상적으로 비대해졌다 — 정정 자체(σ=πs/√3 환산)는 물리적으로 옳으나(R2 판정대로), 괄호로 묶인 3문장 뭉치는 본 절 문체(짧은 괄호 삽입구)와 어긋나 읽기가 끊긴다.
- **근거**: 문서 전역에서 괄호 하나가 종결부호(".") 2개 이상을 내부에 포함한 사례는 이 자리가 사실상 유일(다른 다중-마침표 괄호는 각주·표 안에 한정).
- **정정 제안**: "...$\approx91$ mV." 뒤에서 괄호를 닫고, "기호 규약 --- ... 적용된다."를 괄호 밖 새 문장(또는 별도 문장 시작)으로 승격 — 예: "(...\approx91$ mV). 기호 규약 --- 식~\eqref{eq:widthbudget} 의 $\sigma$ 는 모두 \emph{표준편차}이고 ... 적용된다. 실측 종에서 ..."

### F5 | LOW | ch1:2300 — 구현-어투 경미 잔존

- **원문**: "이고 $\Omega_j^\mathrm{cat}<2RT$ 이면 $u_j^\mathrm{cat}$ 이 허수, 등호에선 $u=0$ 이라 그 전이의 spinodal gap 은 없다(gap $0$ 으로 연속)(흑연 코드 분기 그대로)."
- **결함**: "(흑연 코드 분기 그대로)"는 "흑연 코드의 분기 처리와 동일"이라는 구현-참조 괄호로, 물리 진술("gap 0으로 연속")에 코드 언급이 덧붙는다. 심각하지 않으나(짧은 괄호·문미) F1–F3 과 같은 범주.
- **정정 제안**: "(흑연 전이의 처리와 동일)" 또는 "(흑연과 같은 분기 처리)"로 대체.

### F6 | LOW | ch1:763 — 물리량에 "코드의" 소유격 부여

- **원문**: "폭에 다중도 $w_j=n_jRT/F$ 를 넣은 것이 코드의 평형 진행률 $\xi_\eq$ 다"
- **결함**: $\xi_\eq$ 는 본 문건이 유도하는 물리량인데 "코드의"로 소유격을 붙여 구현물 귀속으로 읽힌다. Part 0(통계역학 유도부)이라 독자가 가장 먼저 만나는 절 중 하나이며, 어투 정련의 취지(코드-귀속 언어 제거)와 결이 다르다.
- **정정 제안**: "폭에 다중도 $w_j=n_jRT/F$ 를 넣은 것이 모델의 평형 진행률 $\xi_\eq$ 다" 또는 "...넣으면 본론의 평형 진행률 $\xi_\eq$ 가 된다"로 대체.

---

## 2. 렌즈별 0-결함 근거 (빈 통과 금지 이행)

- **② 지정 키워드 grep**: 값어치·환율·옷·먹인다·못박·산다·죽는다·미끄러·갈아 끼·보자·그래서·(신규)·(v7-11) — 3파일 전수 grep 0건("먹인다"는 `Anode_Fit_v1.0.14.py`에만 존재, 검수 대상 외). 명령형 어미 "-하라/-말 것" 0건. 구어체 필러(일단·아무튼·뭔가·그니까 등) 0건. 번역투(~에 있어서·~에 다름 아니·~되어지다류) 0건. 중복 조사·단어 인접 중복 정규식 검색 0건(가양성 3건 확인 후 배제).
- **③ 문법 파손**: 괄호 균형 3파일 전건 일치(open=close, 국소 재확인 포함). 대시("---") 2회 이상 밀집 행 전수 확인 — TikZ 주석·bibliography 구분선 제외 시 본문 산문에서 정상 범위(열기+닫기 1쌍) 밖으로 벗어나는 곳은 F4 1건.
- **④ 편집 이음새**: R1 B-05(개시구 중복, 구판 L1662–1663)·B-06(표 재지시 중복, 구판 L1929–1930) 재확인 — 현재 ch1:1684–1687("이 절은 그 지연을...")·ch1:1953–1955("표~\ref{tab:staging} 가 네 전이 각...")로 **병합 완료 확인**(잔존 0). B-08(공간 중의성, ch1:1899–1901) 재확인 — "꼬리는 진행상 나중에 지나는 전위 쪽으로"로 **수정 완료 확인**. B-04(방향 한정 누락, ch1:1780) 재확인 — "방전 기준($\sigma_d=+1$; 충전은 진행 방향 ...)" 삽입 **완료 확인**. Appendix C-1(ξ 배향, appendix:6·54–61) 재확인 — "본 부록 ξ = B(Li 점유) 자리 분율 = 본문 θ" 명시 **완료 확인**. 위 5건 전부 재발 없음.
- **★가드·수식·라벨**: 어형만 판단(F1–F6 전부 어형/서술 층위 지적이며 물리·부호·라벨 내용은 판단하지 않음 — 특히 ★블록 22곳 전수 확인, 물리 판단 개입 0).

## 3. 총평

P3.1(어투 절별 루핑) + R1(22건) + R2(13건)를 거치며 문서 전역의 구어체·번역투·개시구/표 재지시 중복·공간 중의성은 이미 해소됐다(재발 0 확인). 남은 잔여는 **R1이 이미 정확히 지목했으나 실제로는 부분 적용에 그친 "구현-주어 문장" 범주**(F1–F3)와, **R2의 물리 정정문이 기존 괄호에 얹히며 생긴 구조적 이음새**(F4)로 좁혀진다. F1(2857–2858)이 가장 강한 증거를 갖는다 — 동일 저자·동일 라운드가 26행 앞의 쌍둥이 문장은 고치고 이 문장은 남겼다는 내부 모순이 문서 자체에서 검증되기 때문이다.

*검수자 B(Sonnet) · V1.0.14 P4.1 R3 · 파일 수정 0건.*
