# V1.0.14 P2.2 Step 11 — 인라인 치환 적용 결과 (Sonnet 작업 sub)

역할: 감사·판정 확정된 코드-언급 치환의 기계적 적용자. 새 판단·재작성 없음 — 감사 보고서 초벌 대체문을 원문 문자열로 재특정해 그대로 적용.

대상: `graphite_ica_ch1_v1.0.14.tex`(현재 2957줄 body + Appendix A/B), `graphite_ica_ch2_v1.0.14.tex`(현재 786줄).
지시 원천: `V1014_CODE_MENTION_AUDIT.md`(원문·처분·대체문) + `V1014_AUDIT_ADJUDICATION.md`(§B 판정).

★주의: 감사서 줄 번호는 v1.0.14 P2.1 통합(Part 0 신설·LCO 분해·부록 A/B 신설) 이전 기준이라 전면 실효 — 전 항목을 원문 문자열로 재검색해 현재 위치를 재특정한 후 적용했다.

## 전 항목 표

| 항목 | 상태 | 비고 |
|---|---|---|
| A1 (codebox N2) | 스킵-기적용 | master 가 이미 부록 A/B 체계로 반영 — 현재 codebox 잔존 없음(부록 B \S3164 부근에 N2 구현 스니펫만 정당 존재) |
| A2 (codebox N7) | 스킵-기적용 | 上同 — 부록 B \S3152 부근 |
| B1 (fig:spine 캡션) | 적용 | `\code{curve}`/`facade`/`코어 \code{dqdv}`/`\code{for tr in transitions}` 제거, 감사 대체문(오기 "그림~\ref{fig:sm-reservoir}." 제외) 그대로 캡션 교체 |
| C1 | 적용 | 서론: 파일명·curve/dqdv 언급 제거, 물리 서술(N0 환산→N1–N9 평가)로 대체 (E1 병합) |
| C2 | 적용 | "facade curve(...)" → "실험조건 (...)을 받아" 물리 서술 |
| C3 | 적용 | "코드의 \code{s} 인자이며" 삭제 |
| C4 | 적용 | `GRAPHITE_STAGING_LIT` → "흑연 staging 네 전이의 초기값(문헌 기반, 표)" |
| C5 | 적용 | "코어 \code{dqdv}" → "이 계산" |
| C6 | 적용 | "이것이 코드의 \code{V_n=...} 한 줄이다." 문장 삭제(boxed eq:vn 기존재) |
| C7 | 적용 | len(.) 코드 식별자 병기 제거, "스팬은 하한 …" 물리 서술만 잔류 |
| C8 | 적용 | "코드는 …func_U_j로 계산하거나…" → "이 값은 …식~\eqref{eq:Uj}로 계산되거나" |
| C9 | 적용 | "이것이 func_U_j(...)의 (...)다." 문장 삭제(boxed eq:Uj 기존재) |
| C10 | 적용 | "코드의 명시 분기 if Omega<=2RT..." → "이 문턱을 넘지 못하면(...) 분기 gap은 정확히 0(식 참조)" |
| C11 | 적용 | "이것이 func_dU_hys(...)의 (...)이며," 삭제 |
| C12 | 적용 | "이것이 func_U_branch(...)의 (...)다." 삭제 |
| C13 | 적용 | "코드는 전이당 상수인 분기 shift만 필요하므로… hys_shift=func_U_branch(...)" → "전이당 필요한 것은 상수인 분기 shift뿐이므로, 식~\eqref{eq:Ubranch}의 U_j 자리에 0을 대입해" (E4 병합) |
| C14 | 적용 | "이 \code{center}가" → "이 분기 중심(식~\eqref{eq:center})이" |
| C15 | 적용 | func_w/'w'/_n_factor/_width 식별자 제거, 물리 서술만 잔류 |
| C16 | 적용 | func_ksi_eq/func_w/np.where 식별자 제거 → "식~\eqref{eq:xieq}의 z=…", "오버플로를 피하기 위해 두 등가 형태로" |
| C17 | 적용 | peak_shape/equilibrium/dqdv 식별자 제거, "이 값이 전이별로 합산되어…" 물리 서술 |
| C18 | 적용 | "'w' 폴백은 'n'을 제거해야" → "폭 값을 직접 지정하는 경로는 다중도 n 입력을 배제해야" |
| C19 | 적용 | "resolver \_resolve_lag_length" 제거 → "유한 전류에서는 전이당 하나의 지연 길이를 구한다" |
| C20 | 적용 | "이것이 코드의 A=min(...)다." 삭제(boxed eq:Acut 기존재) (E12 병합, 아래) |
| C21 | 적용 | "이것이 func_chi_d(...)이며(생성자 chi_split...)." 문장 삭제 |
| C22 | 적용 | "이것이 func_dH_a_eff(...)이고, 코드는 use_dH_eff가…" → "단, 이 보강이 꺼져 있으면(토글 옵션) 보강 없이 ΔH_a를 그대로 쓴다." |
| C23 | 적용 | func_L_q/ln_Lq 로그식 구현 문장 전체 삭제, "암묵 전제 명시" 물리 문단만 잔류 |
| C24 | 적용 | boxed 식 안 `\|\code{dVdq_qa}\|` 이중표기 제거(순수 기호만), "코드의 return abs(...)다." 문장 삭제 |
| C25 | 적용 | "직접 지정 'L_V'가 있으면" → "지연 길이를 직접 지정하면", "dH_a 키가 없으면" → "활성화 엔탈피 입력이 없으면" |
| C26 | 적용 | "코드 \_causal_lowpass가 …scipy.signal.lfilter…" 구현 문장 삭제, eq:lowpass 뒤 "이다."로 연결 |
| C27 | 적용 | "이것이 코드의 peak_shape=(...)다" 삭제, "(ξ_lag는 점화식 eq:lowpass의 출력)"만 잔류 |
| C28 | 적용 | "ν=min_lag_grid_steps=2" → "ν=2" |
| C29 | 적용 | boxed cases 안 `\code{causal_lowpass}(...)[::-1]` 파이썬 슬라이스 표기 → LowPass/Reverse 물리 표기 |
| C30 | 적용 | "이것이 코드의 occ_lagged=lowpass(...)[::-1] (충전 분기)다." 문장 삭제 |
| C31 | 적용 | 두 위치(N9 도입부 + eq:sum 뒤) 모두 코드 변수(dqdv_work/np.interp/dqdv_out) 제거, 물리 서술로 대체 (E18 병합) |
| C32 | 적용 | "GRAPHITE_STAGING_LIT에 박혀 있다" → "네 전이의 초기값이 표~tab:staging에 있다" |
| C33 | 적용 | GRAPHITE_STAGING_LIT/LCO_MSMR_LIT 식별자 제거 → "흑연의 네 staging 전이 초기값(표)에 대응하는 LCO 초기값 리스트를 표~tab:lco-staging에 둔다" |
| C34 | 적용 | "…키를 전이별로 갖듯, LCO도 같은 키 구조를" → "…파라미터 묶음을 갖듯, LCO도 같은 파라미터 구조를" |
| C35 | 적용 | `\code{c_rate}` → `\text{c-rate}` |
| C36 | 적용 | "코드 LCO_MSMR_LIT도 같아서 미배정 시 구현은 Ω=0 폴백으로" → "미배정 시 Ω=0으로 두어 히스 분기가 비활성(0)인 것으로 취급한다" |
| C37 | 적용 | "흑연 GRAPHITE_STAGING_LIT가 초기값이고 폭이 피팅 대상이었듯" → "흑연의 네 staging 전이 초기값이 그러했듯" |
| C38 | 적용 | "흑연의 GRAPHITE_STAGING_LIT(표)가 stage 2→1 삽입에" → "흑연의 stage 2→1 삽입 초기값(표)이" |
| C39 | 적용 | "흑연 GRAPHITE_STAGING_LIT의 '문헌값=초기값...' 철학" → "흑연 staging 초기값의 '...' 원칙" |
| C40 | 적용 | "Ch1의 곡선 클래스(func_ksi_eq·func_U_j·합산)는 구조 변경 0으로" → "Ch1의 곡선 골격(평형 진행률 logistic·평형 중심·합산식)은 구조 변경 없이" |
| C41 | 적용 | "GRAPHITE_STAGING_LIT → LCO_MSMR_LIT" → "흑연 초기값(표) → LCO 초기값(표)", "코드의 전이 dict 키 구조는 동일하다" 문장 삭제 |
| C42 | 스킵-기적용 | 현재 `\|dV/dq\|_{q_a}` 순수 물리 기호로 이미 존재(`\code{dVdq_qa}` 없음) |
| C43 | 스킵-기적용 | 현재 "(1) 표~tab:staging의 4 전이 파라미터 집합을 구성"으로 이미 물리 서술(`\code{transitions}` 없음) |
| C44 | 스킵-기적용 | 현재 "R1–R3은 구현 self-test와 같은 양의 수기 재산출"로 이미 물리화(`\code{__main__}` 없음). 단, 위치가 부록 A(부호 사슬 검산표, L2938–3023) 내부로 이동해 있어 수정 권한 범위 밖(부록 A 불가침) — 확인만 하고 원문 유지 |
| C45 | 스킵-기적용 | sec:facade subsection 이 이미 "전체 진행 요약"으로 개명·요약 서술로 축약됨 |
| D1 | 스킵-기적용 | 기호표(longtable)에 코드 열 없음 — 이미 부록 B(tab:symcode)로 이관 완료, 본문엔 "부록 참조" 1문장만 |
| D2 | 적용 | tab:staging 캡션: `GRAPHITE_STAGING_LIT`/`'U'` 식별자 제거 → "전이 초기값(흑연 staging 4건) … U는 (ΔH_rxn,ΔS_rxn) 정합 목표." |
| D3 | 적용 | tab:lco-staging 캡션: `LCO_MSMR_LIT`(2회) 제거 + "— v1.0.14 재정렬 완료" 판이력 제거, 나머지(성격·전자항·tier-C·round-trip 설명) 불변 |
| D4 | 스킵-기적용 | tab:inputs 이미 삭제, 본문에 "부록 B(tab:inputs) 참조" 안내문으로 대체됨(L2765–2769) |
| D5 | 스킵-기적용 | tab:nodemap 이미 "노드/물리식/본문식" 3열로 축소, 코드 식별자 열은 부록 B(tab:nodecode)로 이관 완료 |
| E1 | 적용 | C1 에 병합 적용 |
| E2 | 적용 | "코드가 배열 T_work를 그대로 넣으므로" → "온도가 배열 T_work(V)(비등온)이면" |
| E3 | 적용 | "코드는 평형 중심 U_j를 곧바로 쓰지 않고,…" → "평형 중심 U_j는 …방향별 분기 중심으로 이동한다(그렇지 않으면 U_j 그대로)" |
| E4 | 적용 | C13 에 병합 적용 |
| E5 | 적용 | "중심이 정해지면 코드는 폭…평가한다" → "중심이 정해지면 다음으로 폭…정한다" |
| E6 | 적용 | "코드 분기가 아니라" → "인위적 구분이 아니라" |
| E7 | 적용 | "코드 전이 리스트의 폭 폴백 값" → "네 전이의 폭 출발값" |
| E8 | 적용 | "코드는 평형 진행률에서 곧바로…만든다" → "평형 진행률에서 곧바로 얻어지는 것이…이다" |
| E9 | 적용 | "코드의 사용 조건은" → "이 식이 쓰이는 조건은" |
| E10 | 적용 | "코드는 식~eqpeak의 평형 종을 직접 쓴다" → "식~eqpeak의 평형 종으로 직접 환원된다" |
| E11 | 적용 | "코드의 forward 모델은" → "모델은" |
| E12 | 적용 | C20 에 병합 적용("코드는 σ_d를 크기에서 빼고…" → "크기는 항상 양수로 두고 방향은 전달 계수 χ_d로만 반영한다") |
| E13 | 적용 | "코드 안에서 L_q는 V의 함수로 다시 미분되지 않는다: 운영상 실현 미분은" → "A가 전이당 컷 상수로 동결되므로, 실현되는 미분은" |
| E14 | 적용 | "코드는 평형 종 대신 인과 기억 꼬리를 만든다" → "평형 종 대신 인과 기억 꼬리가 나타난다" |
| E15 | 적용 | "코드의 분기 스위치가 담당한다" → "식~eq:branch의 이산 분기(threshold switch)가 담당한다" |
| E16 | 적용 | footnote(Optuna 피팅·v12 코드 ν 상향 검토) 전체 삭제, 부록 신설 없음(판정대로) |
| E17 | 적용 | "코드가 꼬리식으로 평가하지 않는 구간이다" → "꼬리식이 적용되지 않는 구간이다" |
| E18 | 적용 | C31 에 병합 적용(N9 도입부) |
| E19 | 적용 | "P4(후속 코드 개정 로드맵 단계) 코드 구현은 식~eq:gunit의 나눗셈 형을 쓴다 —" 삭제, "…어긋난다 — 부호는…"로 연결 |
| E20 | 적용 | "★Ch2/P4 코드 구현 예고(두 설계 항목). 본 분해를 forward 코드로 옮기는 다음 단계는" → "★다음 두 설계 항목. 이 분해를 적용하는 데 필요한" |
| E21 | 적용 | "같은 코드가 LCO를 그린다" → "같은 골격이 LCO 곡선을 낸다" |
| E22 | 적용 | "…의 코드 측 증거이자" → "…는 주장의 실증적 근거이자" |
| E23 | 스킵-기적용 | 해당 문단(사용자 목표/Optuna/생성자 인자/전이 dict 키/하드코딩) 이미 tab:inputs 부록 이관 + 안내문으로 대체(D4 와 동일 위치) |
| F1 | 적용 | `\subsection{forward 코드의 LCO 일반화 --- 파라미터 교체 + 전자항 plug-in}` → `\subsection{LCO 로의 일반화 --- 파라미터 교체 + 전자항 추가}`(label sec:lco-code 불변) |
| F2 | 스킵-기적용 | sec:facade 제목 이미 "전체 진행 요약"으로 개명됨 |
| G1 (ch2) | 적용 | "코드 \texttt{func_w}의 모델 선택이라는 점은" → "함수형을 유지할지는 두-상 폭의 모델 선택 문제이며" |
| G2 (ch2) | 적용 | "Chapter 1 코드(\texttt{Anode_Fit_v1.0.14})의" → "Chapter 1 의"(파일명·버전만 제거, 유한차분 비교 수치 서술은 불변 보존) |
| G3 (ch2) | 적용 | "현행 코드(\texttt{func_w}/\texttt{_width})가" → "현행 모델이", "'n' 키 보유 전이" → "다중도 n을 갖는 전이", "'w'-단독 전이" → "폭 w를 직접 지정한 전이" |
| G4 (ch2) | 적용 | "…과 코드 후속 과제가 확정한다." → "…과 후속 다온도 실데이터 검증이 확정한다." |
| G5 (ch2) | 스킵-기각 | 판정 §B-4: bibitem 서지 항목은 본문 프로즈 방침 대상 아님 — 유지(건드리지 않음), 확인만 |

## 잔존 검사 — 본문 `\code{` 출현 (부록 B `\section{구현 대응표}` L3024 이전 구간)

전수 grep(`\code{` 83건) 결과, **전 83건이 L3026 이후**(부록 B `구현 대응표` 본문, L3024 시작)에만 존재. 본문(L1–L2937, Part 0/N0–N9/Part II 전체) 및 부록 A(부호 사슬 검산표, L2938–3023) 구간에는 `\code{` 잔존 **0건** — 미처리 항목 없음.

## 빌드 결과

- ch1: `xelatex -interaction=nonstopmode` 2-pass, 두 pass 모두 `^!` 에러 매치 0건, exit code 0. `graphite_ica_ch1_v1.0.14.pdf` 55 pages 정상 생성.
- ch2: 동일 2-pass, `^!` 에러 매치 0건, exit code 0. `graphite_ica_ch2_v1.0.14.pdf` 14 pages 정상 생성.
- 수식$\cdot$라벨$\cdot$\eqref$\cdot$★가드 박스 내용 불변, 부록 A/B 무편집 확인.

## 집계

- **적용**: C1–C41(41), D2·D3(2), E1–E22(22, 이 중 E1/E4/E12/E18 은 C1/C13/C20/C31 에 각각 병합), B1(1), F1(1), G1–G4(4) = **본문 편집 51건**(중복 병합 4건 제외 실개소 기준)
- **스킵-기적용**: A1·A2·C42·C43·C44·C45·D1·D4·D5·F2·E23 = **11건**
- **스킵-기각(유지)**: G5 = **1건**
- **실패**: 0건
- **빌드**: ch1·ch2 각 2-pass 정상, `^!` 에러 0, PDF 정상 생성(55p/14p)
- **본문 `\code{` 잔존**: 0건(부록 B 83건만 정당 존재)
