# V1012 P4.3 review C3r — union map v10

## 0. 수행 범위

- base prompt: `C:/Users/lksz1/AppData/Local/Temp/claude/d--Projects/f45f576c-1efc-4f50-8591-1f84bb7d7f39/scratchpad/v1012_p43_review_base.txt` 1--9행 전문 정독.
- 대상: `Claude/results/process/V1012_P43_map_v10.md` 1--587행 전문 정독.
- 원천 TeX: `Claude/docs/v1.0.12/graphite_ica_ch1_v1.0.12.tex` 1--1964행, `graphite_ica_ch2_v1.0.12.tex` 1--755행 전문 정독.
- 드래프트 대조: `V1012_P43_draft_{F1,S1,S2,S3,O*,C1_s*,C2_s*,C3_s*}.md`는 제목/라벨/결함 키워드/채택 매트릭스 중심으로 대조했다. 전 드래프트 전문 정독은 수행하지 않았으므로, 아래의 draft 누락 판정은 "주요 후보 대조" 범위다.
- 수치 검산: `F*0.83e-3 = 80.0828`, `2RT@298.15K = 4957.914 J/mol`, `RT/F = 25.6926 mV`, `ΔS_e(x_MIT,298.15K) = -45.6808 J/(mol K)`, `S_e/k_B@300K = 1.10565`, `46/F*30K = 14.30 mV`.

## 1. [HIGH] LCO 방향 부호 슬롯이 unresolved 인데 §1--§6 편입 체크리스트는 이를 차단하지 않는다

- 위치: map §2 L213--218, §3 L306--320, §5 L522--531, §8 F-2 L621--630, finalizer L652--660.
- 무엇이: 통합본은 F-2를 HIGH 잠재 결함으로 정확히 적발했지만, §1--§6 최종 LaTeX는 F-2 판정 전에도 편입 가능하게 구성되어 있다. 이 상태로는 LCO 충전(탈리튬화, 전위 상승)에서 `σ_d=-1`을 그대로 넣을 때 `ξ_eq`가 전위 상승에 따라 감소한다.
- 재유도/실측 근거:
  - Ch1 L203은 `σ_d=+1` 방전, `-1` 충전으로 정의한다. Ch1 L211--217은 흑연에서 `ξ`를 탈리튬화 진행률로 두고, `σ_d=+1`에서 `V↑ -> ξ_eq↑`라고 못박는다.
  - Ch1 L309--315는 LCO에서는 충전(`σ_d=-1`)이 탈리튬화이고 `ξ:0->1`의 주 진행 방향이라고 적는다.
  - map §3 L309--315는 같은 `ξ_eq={1+exp[-σ_d(V-U)/w]}^{-1}`를 LCO에 대입한다. 따라서 LCO 충전에 `σ_d=-1`을 그대로 넣으면 `dξ/dV<0`; 그러나 LCO 충전은 `V↑`, `x↓`, `ξ↑`여야 한다.
  - map §5 L491--496의 `x(ξ)=x_hi-(x_hi-x_lo)ξ`도 `ξ↑ -> x↓`를 전제한다. 즉 LCO 충전에서 `σ_d=-1`과 xmap이 동시에 성립하지 않는다.
- refute 시도: "평형 peak 모양은 `ξ(1-ξ)`라 방향 불변이므로 무해"라고 반박할 수 있다. 그러나 이 반박은 N3 분기, N8 꼬리, §5 xmap 고정점에는 적용되지 않는다. map §8 F-2 자체도 비평형 두 자리에서 결함이 잠복한다고 적는다.
- 수정안: F-2를 §1--§6 편입 전 필수 gate로 승격한다. 최소 문안은 "LCO에서 모델 입력 `σ_d`는 셀 라벨이 아니라 탈리튬화(+1)/리튬화(-1) 물리 방향으로 준다"를 §3의 `eq:lco-charge` 직후와 §2의 `eq:lco-Ubranch` 해설 직후에 넣어야 한다. 이 결정을 보류한다면 §2/§3/§5는 "평형 모양만, LCO 비평형 방향 슬롯 미확정"으로 한정해야 한다.
- G-derive 판정: FAIL pending F-2. 학부 추종 사슬 자체는 보이나, 방향 변수의 물리 의미가 중간에서 바뀐다.

## 2. [HIGH] §5 전자항 plug-in은 `U_1(V,T)`를 도입하면서 기존 peak 미분식을 그대로 써 dU/dV 항을 누락한다

- 위치: map §5 L488--531, 특히 L512--516 `U_1(V,T)`와 L527--531 plug-in 사슬.
- 무엇이: `ΔS_e`를 `x(ξ_eq(V))`로 평가하고 이를 적분해 `U_1(V,T)`로 다시 `ξ_eq`에 넣으면 중심 전위가 더 이상 전이별 상수가 아니라 전압 의존 함수가 된다. 그런데 §3/§5의 peak는 여전히 `Q ξ(1-ξ)/w`를 사용한다.
- 재유도/실측 근거:
  - Ch1 L787--788의 기본식은 `ξ_eq=1/(1+exp[-σ_d(V-U^d)/w])`이고, Ch1 L1200--1208의 peak 유도는 `U^d`가 V에 대해 상수일 때 `dξ/dV=σ_d ξ(1-ξ)/w`이다.
  - map §5 L512--516은 `U_1(V,T)=U_1(T_ref)+1/F ∫ ΔS_rxn,1^cat(V,T') dT'`를 쓴다. 이 경우 `z=σ_d(V-U(V))/w`라서
    `dξ/dV = ξ(1-ξ) * σ_d(1-dU/dV)/w`
    이고, `1-dU/dV` 보정이 필요하다.
  - map §5 L522--526은 고정점 구조와 14 mV < w1 휴리스틱을 인정하지만, 이는 미분식 누락을 제거하지 않는다. "한 번 갱신이면 충분"은 수치 스케일 감각이지 G-derive 폐쇄가 아니다.
  - Ch1 L331--335와 L660--672 코드 주석/동작은 현 구현이 `ΔS_e`를 `T_ref`에서 동결한 상수 오프셋으로 쓰는 단일-기준 근사임을 말한다. map의 `V`-의존 plug-in은 현행 코드 불변 계약을 넘어선다.
- refute 시도: "map이 round-trip 피팅 과제와 고정점 구조를 명시했으므로 충분하다"고 반박할 수 있다. 그러나 boxed plug-in L527--531은 그 미해결 고정점을 이미 forward 사슬로 폐쇄한 것처럼 제시하고, §3의 `eq:lco-eqpeak`와 결합하면 잘못된 미분용량을 낸다.
- 수정안: P4.3 편입에서는 `ΔS_e`를 기존 구현처럼 T_ref/전이 중심의 스칼라 오프셋으로 한정하거나, `x(ξ(V))` 전자항은 "future implicit solver"로 빼야 한다. 만약 V-dependent center를 본문에 넣으려면 `dU/dV`가 포함된 새 `dξ/dV`와 고정점 해법을 독립 유도해야 하며, 이는 이번 "물리 불변/구조 변경 0" 범위를 넘는다.
- G-derive 판정: FAIL. 고정점 존재를 언급했지만 peak 미분까지 닫지 않았다.

## 3. [HIGH] §4 config 내부항 부호는 F-1과 원자적으로 결합되어야 하는데 finalizer는 F-1 보류를 허용한다

- 위치: map §4 L363--370, L401--407, §8 F-1 L614--620, finalizer L655.
- 무엇이: map §4의 `eq:lco-configsplit`은 `+R ln[ξ/(1-ξ)]`를 채택한다. 이는 물리적으로 맞다. 하지만 finalizer L655는 F-1 판정 전까지 Ch1 L1728--1729 원문을 유지하라고 하므로, 같은 절 안에 `+R ln[ξ/(1-ξ)]`와 기존 `R ln[(1-ξ)/ξ]`가 병존할 수 있다.
- 재유도/실측 근거:
  - Ch2 L226--243은 `∂S_config/∂θ=-R ln[θ/(1-θ)]`, `θ=1-ξ`에서 `+R ln[ξ/(1-ξ)]`라고 명시한다.
  - Ch2 L677--679 종합식도 `+(R/F)ln(ξ/(1-ξ))`를 사용한다.
  - Ch1 L1728--1729만 `R ln[(1-ξ)/ξ]`를 쓴다. 이는 map §8 F-1의 지적처럼 국소 역부호다.
- refute 시도: map §4 L363--370이 이미 "F-1 채택과 원자적으로 결합"이라고 경고하므로 충분하다고 볼 수 있다. 그러나 finalizer L655가 "F-1 판정 전까지 L1728--1729 원문 유지"를 허용하는 순간, 실제 편입 지시는 자기모순이다.
- 수정안: §4를 편입하려면 F-1도 함께 채택한다. F-1을 보류하려면 §4의 `eq:lco-configsplit`과 `eq:lco-slots`에서 내부 config 항 명시를 제거하고, 단순히 "기존 bullet의 부호 판정 후 보강"으로 남겨야 한다.
- G-derive 판정: PASS only if F-1 atomic adoption. 현 checklist 기준은 FAIL.

## 4. [MED] `Ω_j^\mathrm{cat}` 표기는 물리적으로 alias일 수 있으나 "Ω_j 기호 유지" 계약에는 위험하다

- 위치: map §0.3 L36--37, §2 전반 L149--283.
- 무엇이: 통합본은 `Ω_j^\mathrm{cat}`를 "같은 Ω 슬롯의 LCO 값 표기"라고 선언한다. 수치 날조나 새 파라미터는 아니다. 다만 base prompt의 물리 불변 렌즈가 `Ω_j` 기호 유지 여부를 별도 점검 항목으로 둔 점을 고려하면, 새 상첨자는 finalizer/후속 코드 매핑에서 별도 파라미터처럼 읽힐 위험이 있다.
- 재유도/실측 근거: Ch1 원문 L241, L524--564, L696--719, L1813은 전부 `Ω_j` 슬롯으로 말한다. map §2는 `U_j^\mathrm{cat}`, `ΔS^\mathrm{cat}`와 맞추기 위해 `Ω_j^\mathrm{cat}`를 도입한다.
- refute 시도: 상첨자 cat은 LCO 값 표기일 뿐이라 물리 위반이 아니라고 볼 수 있다. 이 반박은 대체로 성립한다. 그래서 등급은 MED가 아니라 LOW에 가까울 수 있다. 그러나 prompt가 "Ω_j 기호 유지"를 명시했으므로 검수상 위험으로 남긴다.
- 수정안: 가장 보수적인 편입은 `Ω_j`를 유지하고, 첫 문장에 "이 절의 `Ω_j`는 LCO 전이 dict의 Ω 값"이라고 쓴다. 꼭 `Ω_j^\mathrm{cat}`를 쓰려면 "alias only, no new parameter"를 박스 밖이 아니라 첫 정의식 바로 뒤에 둔다.
- G-derive 판정: PASS with notation-risk.

## 5. [LOW-MED] MSMR `f` 귀속 정정은 §8에 있으나 §5 본문은 여전히 오독 가능하다

- 위치: map §5 L437--465, §8 F-5 L638--641; Ch1 원문 L1768--1778, bibitem L1954.
- 무엇이: map §5는 원문 MSMR 두문자 풀이를 생략해 ④-1은 중립화했지만, L440--465에서는 여전히 `f`를 "방향 슬롯"으로 사용한다. §8 F-5가 원 논문에서는 `f=F/RT`일 가능성을 적고 재모수화 문안을 제안하므로, §5만 먼저 편입하면 출처 귀속이 부정확할 수 있다.
- 근거: Ch1 bibitem L1954는 "multi-species, multi-reaction (MSMR)"이며, 원문 L1768의 "multiphase species reaction"과 불일치한다. map §8도 이 불일치를 인정한다.
- 수정안: §5 (a)에 "본 문건은 원 MSMR의 `F/RT`를 전압 폭에 흡수한 재모수화 표기에서 방향 부호만 `f`로 남긴다"를 넣거나, F-5 판정 전에는 `f=-σ_d`를 "표기 대응"으로만 낮춰 쓴다.
- G-derive 판정: 물리식 자체는 PASS, 출처/표기 계약은 pending.

## 6. 삽입 정합·라벨·수치 판정

- 삽입 경계: map §0.1의 Ch1/Ch2 행수와 주요 경계는 실제 Ch1 1964행, Ch2 755행과 맞다. `sec:lco-center` L476, `sec:lco-hys` L696, `sec:lco-peak` L1220, `sec:lco-decomp` L1715, `sec:lco-code` L1767, Ch2 keybox L161--166, srcbox L485--496 모두 실측 확인했다.
- 라벨: 기존 `eq:lco-*` 정의는 Ch1 `eq:lco-dUdT` L487, `eq:lco-decomp` L1723 두 개뿐이다. `eq:lco-dUdT` 참조 6곳 중 L493은 교체 범위라 소멸하고, 잔존 5곳 L498/L1229/L1750/L1790/L1877은 새 박스 라벨 승계가 필요하다. `eq:lco-decomp` 참조 9곳은 박스 무변경이면 보존된다.
- 수치: +0.83 mV/K -> +80.08 J/(mol K), `2RT≈4958`, `RT/F≈25.7 mV`, 전자항 골 `≈-45.7 J/(mol K)`, `S_e≈1.1 k_B`는 검산 통과.
- H-2: Ch2 L161--166의 무한정 "열적 폭" 단정은 Ch2 L540--569, L681--686과 충돌하므로 map §6 정정 방향은 타당하다. `eq:Z1`이 Ω 없는 비상호작용 단일 자리라는 bottom-up 근거도 타당하다.
- 원 드래프트 대비: map은 F1의 신규 적발(F-1/F-2/F-4/F-5)과 S/O/C계열 보강 대부분을 수록했다. 주요 누락이라기보다 문제는 "§8에 수록한 HIGH 후보를 §1--§6 편입 gate로 올리지 않은 것"이다.

## 7. 가장 약한 1곳

가장 약한 곳은 §5 전자항 plug-in의 `U_1(V,T)` 고정점 사슬이다. 이유는 단순하다. LCO 방향 부호(F-2)와 config 부호(F-1)는 문안/규약 결정을 통해 국소적으로 고칠 수 있지만, §5는 `U`가 V에 의존하는 순간 기존 peak 미분식과 전하보존 사슬 전체가 바뀐다. 이건 "한정어 추가"가 아니라 새 implicit forward 모델이다. 이번 P4.3의 물리 불변 계약 안에서는 §5를 현재 코드의 T_ref 동결 스칼라 오프셋으로 낮추는 편이 안전하다.

## 8. 물리 불변 계약 판정

판정: **위반(pending repair)**.

- 결과 수치와 기본 열역학 식은 대체로 PASS다.
- 그러나 §5의 V-dependent electronic plug-in은 기존 `Q ξ(1-ξ)/w` peak 계약을 깨고, §1--§6 편입 체크리스트는 F-2/F-1 같은 HIGH 원자 결합 항목을 gate로 삼지 않는다.
- 따라서 "전개 형식만 변경 + H-2 한정어"라는 map L6 계약은 현 상태로는 성립하지 않는다.

## 9. 5줄 요약

1. 오적발 자기표시: `Ω_j^\mathrm{cat}`는 alias 선언이 있어 물리 위반 단정은 과할 수 있다. 그래서 notation-risk로만 남긴다.
2. H-2 정정 방향과 주요 수치(+80, -46, 1.1 k_B, 2RT, RT/F)는 검산 PASS.
3. F-2 LCO 방향 부호는 §8에만 두면 안 된다. §2/§3/§5 편입 전 gate로 승격해야 한다.
4. §5 `U_1(V,T)` plug-in은 기존 peak 미분식을 깨는 가장 약한 지점이며, G-derive FAIL이다.
5. §4 config 부호는 F-1과 원자적으로 함께 편입해야 하며, 보류 시 §4도 함께 보류해야 한다.
