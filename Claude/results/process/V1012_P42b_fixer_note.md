# V1.0.12 Phase 4.2b — Fixer 적용 노트 (13건 서술·귀속·한정·국소부호 정정)

- 역할: Phase 4.2b fixer(정정 전담 sub). 지정 13항목만 최소 diff 정정, 범위 밖 수정 금지, commit 금지(master 전용).
- 대상: `Claude/docs/v1.0.12/graphite_ica_ch1_v1.0.12.tex`(1826줄 기준→편집 후 증가) · `graphite_ica_ch2_v1.0.12.tex`.
- 원천 명세: `FABLE_REAUDIT_C1~C4_note.md` + `FABLE_AUDIT_02_ch1ch2_content.md` §1-3.
- **주의: 노트 좌표는 v1.0.11 기준. v1.0.12 는 Ch1 1826줄·Ch2 706줄로 v11(1937·750)과 달라, 전 항목을 줄번호가 아니라 내용·문맥으로 재특정해 정정.** 아래 줄번호는 정정 후 현재 값(±1).
- **빌드 검증: xelatex(kotex) 2-pass, Ch1·Ch2 모두 EXIT 0. undefined reference/citation/label 0. 잔여 경고는 기존부터 있던 한글 italic 폰트-쉐이프 치환뿐(내 편집 무관).**
- 물리 골격·결과식·부호(H-1 제외)·수치 불변 준수. 라벨 위생 미접촉.

---

## 항목별 상세

### [Ch1] M-1 — fig:staging 화살표 라벨 (L292)
- 근거: 규약 L211 "$\xi_j$(방전 시 $0\to1$ 증가)" vs 그림 "delithiation(discharge): $\xi_j:1\to0$" 직접 모순. 노트 leading = "θ 오기 추정".
- 전: `delithiation (discharge): main direction, $\xi_j:1\to0$`
- 후: `delithiation (discharge): main direction, $\theta_j:1\to0$`
- 선택: 노트 primary(θ_j:1→0) 채택 — 최소 diff(\xi→\theta 1토큰), "1→0" 값 보존, 캡션의 갤러리 비움(점유 θ:1→0) 서사와 정합. 대안 ξ_j:0→1(규약 축자 일치)도 유효하나 값 변경이라 미채택.
- 자기검증: 화살표 방향(delithiation ← 좌향)에서 점유 θ 1→0 물리 정합. 규약 L212 "θ_j=1-ξ_j, θ 1→0 = ξ 0→1"과 일관. PASS.

### [Ch1] M-2 — 0.18 k_B/atom 전자항 오귀속 (L1051 부호규약 박스 · L1131 검산 문장)
- 근거: C-2. 게이트 자기예측 적분 방출 = ∫|∂S_e/∂x|dx ≈ 1.1 k_B/atom(=metal 끝점). 0.18 = reynier O3 총 부분몰(config+vib+elec, tier B) — 전자항 단독 아님. L1131의 "1.1 vs 0.18 같은 자릿수 ✓"는 본문 스스로(세 양의 구분) 금지한 교차검산.
- L1051 전: "탈리튬화 … 시 $+0.18\,k_B$/atom 규모의 전자 엔트로피가 방출된다."
- L1051 후: "탈리튬화 … 시 전자 엔트로피가 방출된다 — 그 방출량은 게이트 적분 $\int|\partial S_e/\partial x|\dd x\approx1.1\,k_B$/atom(=metal 끝점 $S_e$)이며, reynier O3 총 부분몰 $\approx0.18\,k_B$/atom(config+vib+전자 총합, tier B)은 전자항 단독량 아닌 별개 anchor(sec:lco-gate 세 양의 구분)."
- L1131 전: "…$\approx1.1\,k_B$/atom … O3 부분몰 차 $\approx0.18\,k_B$/atom … — 같은 자릿수다(✓). 이는 config 의 O3 ΔS…보다 작다."
- L1131 후: "…$\approx1.1\,k_B$/atom … 이 값이 게이트 적분 전자 방출량과 항등. O3 부분몰 차 $\approx0.18$…는 config+vib+전자 총합의 부분몰량이라 척도가 다른 양(서로의 검산 안 함 → '같은 자릿수' 대조 삭제). O3 총합은 config 지배, 전자항은 작은 몫."
- 자기검증: 세 양의 구분(현 L1136 부근 "★세 양의 구분")과 정합. 금지된 교차검산 제거. PASS.
- ★특이사항(범위 밖 flag, master 결정 요망): **동일 오귀속이 L509(sec:lco-center verifybox "전자항은 몰당 $|\Delta S_e|\approx0.18\,k_B$/atom×N_A≈1.5 J/(mol K)")·L1740(sec:lco-decomp "탈리튬화 시 $|\Delta S_{e,j}|\approx0.18\,k_B$/atom 방출")에도 존재.** M-2 지정 좌표(L1040·L1118→v12 L1051·L1131) 밖이고 C-1·C-3가 보고도 미flag(C-1은 L509 산술만 검증·PASS)이라 미편집. 그러나 정정된 L1051/L1131(전자항≠0.18)과 프레이밍이 어긋남 — L509 수정 시 "+80에 얹힌 소수 보정" 논거 재작업 필요(minimal diff 초과)라 master 판단으로 이관.

### [Ch1] M-3 — broadening ① 이중귀속 · ③ 과결정 (L1309 · L1315 · w_j 박스 ~L1325)
- 근거: C-2. ①(유한율속 비대칭 꼬리)=별도 L_V(N7·N8, |I|→0 소멸)인데 "w_j 에 흡수"·"w_j⊃(②⊗③)+①"로 이중계산. ③은 |I|→0 아닌 "η 산포→0"에서만 소멸시키며 "U_j 어느 극한도 불변" 동시 주장(과결정).
- ① 이중귀속 정정 (L1309): "①은 현상학적 w_j 에 따로 흡수된다 … $w_j\supset(②\otimes③)+①$" → "①은 대칭 폭 $w_j$ 에도 담기지 않고 별도 동역학 파라미터 $L_V$(N7·N8, |I|→0 소멸)로 다룬다. $w_j$ 는 ②⊗③만 흡수, 두-상 **총** broadening = $w_j(②\otimes③)+L_V(①)$ (①의 전류 몫 재흡수=이중계산)."
- w_j 박스 정정 (~L1325): "위 세 broadening … 한꺼번에 흡수하는" → "②(평형 잔여)③(앙상블 η)만 흡수, ①(전류 비대칭 꼬리)은 별도 $L_V$ 로 $w_j$ 에 재삽입 안 함."
- ③ 과결정 정직화 (L1315): "$U_j$ 중심은 그 어느 극한에서도 움직이지 않는다" → "η 중 순수 동역학 몫은 ①처럼 |I|→0 소멸, 결정성·turbostratic 구조성 몫이 평형까지 남으면 조작적으로 작은 $U_j$ 산포와 구별 불가 — 본 문건은 잔여 $U_j$ 산포 ≈0(η 지배)의 보수적 가정, forward-only 에서 안전."
- 자기검증: keybox(현 ~L1372)의 "①=L_V, |I|→0 소멸"와 이제 일관. Dreyer→η 연역 골격 불변. PASS.

### [Ch1] M-4 — branch 문턱 "작은 불연속" 과소진술 (L1611-1614, 각주 추가)
- 근거: C-3 F1. 실측 점프 = 격자무관 $\approx46\%/\nu$(높이·면적 둘 다), 기본 ν=2 → ~23%(면적 0.77). "작은"은 과소진술.
- 전: "…작은 불연속이 생길 수 있다. … ($\nu$ 를 키우면 … 점프가 작아진다),"
- 후: "…불연속이 생긴다 — 격자무관 $\approx46\%/\nu$(높이·면적), **기본값 ν=2 에선 ~23%**(면적≈0.77) 상당한 점프('작은'은 과소진술). …($\nu$↑ 로 $\approx46\%/\nu$ 로 줄어 — 5% 이내엔 ν≈8–10 필요\footnote{Optuna 피팅 시 $L_V$ 가 문턱 지나면 목적함수 ~23% 점프→비평활 landscape. v12 코드에서 기본 ν 상향(8–10) 또는 문턱 blend 검토(물리 불변, 수치 처방만).}),"
- 자기검증: ν 상향 권고 각주 추가(task 명시). 물리 불변, ν 방향·크기 C-3 실측과 일치. \footnote article-class 표준, 컴파일 PASS.

### [Ch1] M-5 — 꼬리 수렴 대상 오표기 (fig:relaxode 캡션 L1577 · 본문 ~L1597 · R5 ~L1930)
- 근거: C-3 F2. $L_V\gg\Delta_\mathrm{grid}$ 수렴 대상 = 지연 점유 미분 $\dd\xi/\dd V$(=평형 종+꼬리)이지 평형 종 $\dd\xi_\eq/\dd V$ 아님. 종 환원엔 $L_V\ll w$ 별도.
- 3곳 모두 `$\dd\xi_\eq/\dd V$` → `$\dd\xi/\dd V$(지연 점유 = 평형 종+꼬리)` + "평형 종 $\dd\xi_\eq/\dd V$ 만으로 환원엔 $L_V\ll w$ 별도 · $L_V\gg\Delta_\mathrm{grid}$는 수치 충실성 조건" 명시.
- (L729의 `$\dd\xi_\eq/\dd V|_{1/2}$`는 평형 등온선 폭 정의로 정당 사용 — 미접촉.)
- 자기검증: R5 반증 명제("작은 L_V 종 환원 아님")는 불변. 세 문장 일관. PASS.

### [Ch1] M-6 — 1차항 제거의 우함수 대칭 명시 (eq:Veq 유도 (a), ~L590-597)
- 근거: C-1 MEDIUM-2. 공통접선 논거는 2계(spinodal) 타당, 1계(V_eq)엔 우함수 대칭 별도 단계 필요(최종식은 정확).
- 후(삽입 문장): "1차 몫 뗀 eq:gxi 가 1계 미분에서도 옳은 근거 = 조성 몫 $f(\xi)$ 가 $\xi\leftrightarrow1-\xi$ 우함수($f(1-\xi)=f(\xi)$)라 $f'(1-\xi)=-f'(\xi)$. eq:mu 의 $\mu_\mathrm{Li}(\theta)-\mu^0=f'(\theta)$ 에 θ=1-ξ 넣으면 $\mu_\mathrm{Li}-\mu^0=-f'(\xi)=-g_j'(\xi)$, 뗀 선형몫이 남긴 상수 μ⁰ 가 eq:eqcond 양변 상쇄 → 선형항 없이 $g_j'(\xi)=sF(V_\eq-U_j)$. (공통접선 논거는 2계엔 직접 타당, 1계엔 이 대칭 한 단계 더 필요.)"
- 자기검증: 손 재유도 확인 — f(ξ)=RT[ξlnξ+(1-ξ)ln(1-ξ)]+Ω ξ(1-ξ), f'=RT ln(ξ/(1-ξ))+Ω(1-2ξ)=g_j'; 우함수→f'(1-ξ)=-f'(ξ); eqcond 결합 시 eq:Veq(L599) 정확 재현. PASS.

### [Ch1] M-7 — park2021 과대인용 완화 (L1292 · ~L1319 · ~L1348, 3곳)
- 근거: C-2. 풀셀 GITT는 앙상블 준평형 전위를 줄 뿐 단일입자 U_j 산포≈0 직접 분해 불가. "측정" 3곳 → "일관/가정".
- L1292: "GITT 준평형이 staging 전위를 입자 무관 상수로 측정한다" → "입자 무관 상수로 **가정**(풀셀 GITT OCP 가 입자 무관 상수와 **일관** — 시사할 뿐 단일입자 산포 직접 분해 못 함)".
- ~L1319: "입자 무관 상수로 측정되고" → "입자 무관 상수 가정이 일관하고(직접 측정 아닌 동형 가정)".
- ~L1348: "입자 무관 상수로 측정하므로" → "입자 무관 상수 가정이 일관하므로(풀셀 OCP 는 시사, 단일입자 확정 아님)".
- 자기검증: 기존 LCO 헤지(L1280 "동형 가정·tier C")와 어조 통일. 4-tier=추정 유지. PASS.

### [Ch1] M-11 — 규약표에 s 행 추가 (L225)
- 근거: C-1 MEDIUM-1. 수학기호 $s$(항상 +1, eq:eqcond·eq:Veq 유도 전용)가 σ_d(코드 `s`, ±1)와 혼동. 표에 σ_d만 있고 $s$ 누락.
- 후(σ_d 행 직후 삽입): "$s$ (코드 미대응) & --- & 유도 전용 고정 부호 — 항상 +1(eq:eqcond·eq:Veq 의 U_j 정의 관례); ±1로 바뀌는 σ_d 와 별개, boxed 식에서 s=1 대입돼 소멸 \\"
- 자기검증: σ_d 행(코드 `s`, ±1)과 인접 배치로 대비 명확. 표 3열 정렬 유지, 컴파일 PASS.

### [Ch1] L-1 — eq:vwork |V_n| 카디널리티 표기 (L396)
- 근거: C-1 LOW-1. |·|는 전 문건 "물리량 크기" 전용인데 $n_\work=\max(2048,2|V_n|)$의 |V_n|은 배열 길이.
- 전: `$n_\work=\max(2048,\,2\,|V_n|)$ 이다(코드 …`
- 후: `$n_\work=\max(2048,\,2\,\mathrm{len}(V_n))$ 이다($\mathrm{len}(\cdot)$=배열 원소 개수 — |·|는 물리량 크기 전용이라 격자 점수엔 카디널리티 표기; 코드 …`
- 자기검증: 코드 idiom(len) 정합, |·| 중의성 해소. PASS.

### [Ch1] L-2 — 결합(교차)항 통제 문구 과장 (L1735)
- 근거: C-3 F4. 슬롯 분리는 과대계상만 막을 뿐 교차 엔트로피 항을 포착·한계짓지 않음. 그 항은 "무시"지 "통제" 아님.
- 전: "이 결합 몫은 아래 (나)의 슬롯 분리로 따로 통제하므로 분해 자체의 가산성은 선도 차수에서 성립한다"
- 후: "이 결합(교차) 몫은 슬롯 분리로 **통제**되는 게 아니라 선도 차수에서 **무시**되며(슬롯 분리[아래 (나)]는 과대계상만 막을 뿐 교차 엔트로피 항을 포착·한계짓지 않음), 그 선도 차수에서 분해의 가산성이 성립한다"
- 자기검증: (나) 이중계산 금지 규칙 서술과 모순 없음. PASS.

### [Ch1] L-3 — "차원 k_B²" 단위 문구 (L1784)
- 근거: C-3 F6. 자리당 엔트로피 차원은 $k_B$(J/K)이지 $k_B^2$ 아님 — eq:dSe 의 $k_B^2 g$ 인자 형태의 축약.
- 전: "자리당 식~\eqref{eq:dSe}(차원 $k_B^2$)가 아니라 …"
- 후: "자리당 식~\eqref{eq:dSe}($k_B^2\,g$ 인자 형태 — 자리당 엔트로피 차원은 $k_B$)가 아니라 …"
- 자기검증: N_A 몰환산 가드(정확) 문맥 유지. PASS.

### [Ch2] ★H-1 — eq(2.11)-(2.12) BW V_eq 부호 반전 (§ssec:BW, L191·194·198·200·204 — 5 연동 sub-edit)
- 근거: C-4 §1. eq:Veq_BW 가 eq(2.5)-(2.7)과 반대 부호(θ↑ 시 V↑는 흑연 실측 반대). 정정형 V=U_j-(RT/F)ln(θ/(1-θ))-(Ω/F)(1-2θ) (Ω=0 극한이 eq:Vxi 와 일치·수치 대입 확정).
- (1) eq:Veq_BW(L191): `+\frac{RT}{F}\ln… +\frac{\Omega}{F}(1-2\theta)` → `-\frac{RT}{F}\ln… -\frac{\Omega}{F}(1-2\theta)`
- (2) 산문(L194): "상호작용 항 $\Omega(1-2\theta)/F$ 가 더해진다" → "$-\Omega(1-2\theta)/F$ 가 더해진다"
- (3) eq:slope_BW(L198): `+\frac{RT}{F}\frac1{\theta(1-\theta)} -\frac{2\Omega}{F}` → `-\frac{RT}{F}\frac1{\theta(1-\theta)} +\frac{2\Omega}{F}` (반전된 V_eq 의 정확한 도함수 유지)
- (4) θ=1/2 값(L200): `\frac{4RT-2\Omega}{F}` → `\frac{2\Omega-4RT}{F}` (크기 동일·부호만, 도함수 반전 강제)
- (5) 산문(L204): "중앙에서 기울기가 **음으로** 뒤집혀(불안정)" → "**양으로** 뒤집혀(불안정)" (정정 규약에서 정상 dV/dθ<0, Ω>2RT 시 중앙 (2Ω-4RT)/F>0 로 양전환=불안정)
- 자기검증(손 재검): 정정 V 는 θ↑→V↓(물리 정합, eq:Vxi/eq(2.6-2.7) 일치). slope=정정 V 의 정확 도함수. **임계 Ω=2RT 불변**(2Ω-4RT=0 ⟺ Ω=2RT). Ω>2RT 시 중앙 slope>0=van der Waals loop 불안정 → "양으로 뒤집혀" 정합. 하류 무오염(C-4: 이후 계산은 eq(2.7) V(ξ) 재사용) 확인. 수치(Ω=2RT 임계) 불변. PASS.

### [Ch2] M-8 — 파생 D 분기평균 = 선형화 근사 명시 (eq:hys_rev 직후, L587)
- 근거: C-4 §3. eq(2.35) 분기평균이 $\Delta S_{\rxn,j}/F$ 로 정확히 떨어지는 건 두 분기 봉우리 형태 동일 가정(ΔU^hys≪w_j) 하 1차 근사인데 항등식 박스로 제시.
- 후(박스 직후 삽입): "이 상쇄는 두 분기 봉우리 $g_j^{(d)}$ 가 같다고 본 **선형화 근사**($\Delta U_j^\hys\ll w_j$)에서 정확 — 중심 $U_j\pm\tfrac12\Delta U_j^\hys$ 가 달라 $g_j^{(ch)}\ne g_j^{(dis)}$ 이면 config 곡률로 두 분기 평균이 $\Delta S_{\rxn,j}/F$ 로 떨어지는 건 ΔU^hys 1차 차수까지, 유한 ΔU^hys 엔 고차 보정 잔존."
- 자기검증: 겹침가중식 eq:hys_branch 참조 정합. 결과 박스 자체 불변(조건절만 추가). PASS.

### [Ch2] M-9 — 극한표 고온 코너 Sommerfeld 타당역 한정 (tab:limits, L620)
- 근거: C-4 §4. eq(2.29)/eq:Se 는 축퇴 극한 $k_BT\ll E_F$ 전용인데 고온($k_BT\sim E_F$) 코너에 정량 재사용(코너 과일반화).
- 전: "FD Sommerfeld~\eqref{eq:Se}; $S_e$ 선형 증가"
- 후: "FD Sommerfeld~\eqref{eq:Se} 는 축퇴 극한 $\kB T\ll E_F$ 전용 — **정성적** 방향(전자 기여 증대)만 유효, $\kB T\sim E_F$ 엔 정량 부적용"
- 자기검증: 코너케이스 본문 승격 아닌 표 셀 내 한정어(M-9 교훈 준수). 표 3열 유지, 컴파일 PASS.

---

## 13건 1줄 상태

1. **M-1**(Ch1 fig:staging) — DONE: `$\xi_j:1\to0$`→`$\theta_j:1\to0$`(규약 정합).
2. **M-2**(Ch1 0.18 오귀속) — DONE(2 지정 좌표 L1051·L1131): 전자항 적분=1.1 k_B/atom 명시·"같은 자릿수 ✓" 삭제. ※L509·L1740 동종 오귀속 범위 밖 flag(master 결정).
3. **M-3**(Ch1 broadening) — DONE: ①=별도 L_V(이중계산 해소)·w_j=②③ 한정·③ 과결정 정직화(U_j 산포≈0 가정).
4. **M-4**(Ch1 branch 문턱) — DONE: 기본 ν=2 ~23%(≈46%/ν) 정직 서술 + ν≈8–10 각주.
5. **M-5**(Ch1 꼬리 수렴) — DONE(3곳): `dξ_eq/dV`→`dξ/dV`(지연점유=종+꼬리), 종 환원 $L_V\ll w$ 별도.
6. **M-6**(Ch1 우함수 대칭) — DONE: $f'(1-\xi)=-f'(\xi)$ 대칭 다리 한 문장 명시(최종식 불변).
7. **M-7**(Ch1 park2021) — DONE(3곳): "측정"→"일관/가정" 완화.
8. **M-11**(Ch1 규약표 s) — DONE: $s$(고정 +1) 행 σ_d 직후 추가.
9. **L-1**(Ch1 vwork) — DONE: `2|V_n|`→`2·len(V_n)`(카디널리티).
10. **H-1**(Ch2 BW 부호) — DONE(5 연동): eq:Veq_BW·slope·θ½·산문 부호 반전, 임계 Ω=2RT 불변 확인, 하류 무오염.
11. **M-8**(Ch2 파생D) — DONE: 분기평균 "선형화 근사(ΔU^hys≪w_j)" 명시.
12. **M-9**(Ch2 고온 코너) — DONE: Sommerfeld 축퇴 타당역($k_BT\ll E_F$) 한정어.
13. **L-2·L-3**(공통) — DONE: 결합항 "무시"(통제 아님)·"$k_B^2$ 인자 형태(차원은 $k_B$)".

## 특이사항
- **H-1은 5개 연동 sub-edit 필요**: eq:slope_BW 를 반전된 eq:Veq_BW 의 정확 도함수로 유지하고 불안정 서술("음→양으로 뒤집혀")을 정합화하기 위함. 모두 §ssec:BW 국소, 임계 Ω=2RT·수치 불변.
- **M-2 범위 경계(master 결정 요망)**: 지정 2좌표(L1051·L1131) 정정 후, 동일 "0.18=전자항" 오귀속이 남은 **L509(sec:lco-center)·L1740(sec:lco-decomp)** 과 프레이밍 상충. 두 곳은 M-2 지정 좌표 밖 + C-1/C-3 미flag + 수정 시 "소수 보정" 논거 재작업(minimal diff 초과)이라 미편집·flag. 정합 위해 master 후속 정정 권고.
- **M-1 선택**: 노트 primary(θ_j:1→0) 채택. 대안 ξ_j:0→1(규약 축자 일치)도 유효 — master 재판단 여지.
- **빌드**: Ch1·Ch2 xelatex 2-pass EXIT 0, undefined ref/cite/label 0. \footnote(M-4)·신규 \eqref/\ref 전부 해결됨.
- commit 미수행(master 전용).
