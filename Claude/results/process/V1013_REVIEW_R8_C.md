# V1.0.13 P5 검수 라운드 8 — 검수자 C (렌즈 ⑧ 전담: R7 수정분 전수 재검)

- 대상: `Claude/docs/v1.0.13/graphite_ica_ch1_v1.0.13.tex`·`graphite_ica_ch2_v1.0.13.tex`·`Anode_Fit_v1.0.13.py` 의 **커밋 134631a(R7) 수정 지점 전수**. HEAD = 134631a, 세 파일 uncommitted 0 확인 — diff 신규(+) 줄번호가 현행과 일치.
- 수정 지점 원천: `git show 134631a`(세 파일 한정) diff 를 스크래치패드 파일로 떨궈 전문 정독(496줄) — **hunk 수 = py 3 + ch1 26 + ch2 17 = 46**.
- 방법: hunk 마다 현행 파일 해당 구간을 전후 문맥 포함 재정독 → ① 문법·완결성(주술·괄호 짝·조사) ② 병기 정책(이동 병기의 첫 출현 여부·이중 병기 잔재 grep) ③ 참조 무결(신규 \S\ref·\eqref·문자열 교차참조 실재) ④ 용어 통일 여파("평형 점유"·"2상" 잔여 grep 전수 분류) ⑤ ★기호 주의 삽입 흐름 ⑥ 물리 불변(수식·수치·코드 식별자 무접촉).
- ★검수 의견만 — tex/py 무수정. 모든 지적 = 현행 줄번호 + 원문 인용.

---

## 지적 목록

### C8-1 [HIGH·용어 정본화 누락] ch1 L190·L1114·L1311·L1320 — ξ_eq 를 지칭하는 "평형 점유" 4곳 잔존 (R7 sweep 의 커버리지 구멍)

**무엇이**: R7 은 "평형 점유 ξ_eq"→"평형 진행률" 을 ch1 7곳+py 3곳에서 정본화했고 여집합-분포 용법은 존치했다. 그러나 grep 전수 분류 결과, **ξ_eq(코드 파이프라인 양) 자체를 지칭하면서 "평형 점유"로 남은 곳이 4곳** 더 있다 — 전부 분포/여집합 용법이 아니다.

**근거(현행 원문)**:
1. **L190 (fig:spine 캡션)**: "전이 $j$ 마다 평형 중심$\cdot$히스 분기$\cdot$폭$\cdot$**평형 점유**$\cdot$평형 peak$\cdot$지연 길이$\cdot$인과 꼬리를" — 노드 N4/N5 의 파이프라인 양(그림 노드 자체가 "width $w$, $\xi_\eq$")이라 분포 용법 아님. 절 제목 L1106 은 "폭과 평형 진행률 $\xi_\eq$".
2. **L1114 (\S sec:width 첫 소절 첫 문장)**: "**평형 점유**의 폭 척도는 이상 극한에서 $w_j=n_jRT/F$ 다" — 바로 다음 줄이 "$\dd\xi_\eq/\dd V|_{1/2}$" 로 ξ_eq 의 폭을 말한다. 같은 절 L1106(제목)·L1108(본문)은 R7 이 진행률로 고쳤는데 여섯 줄 아래 소절 첫 문장은 놓쳤다.
3. **L1311 (fig:logistic 캡션)**: "**평형 점유**와 그 미분(v7-11 유래). 실선 $=\xi_\eq$ logistic(식~\eqref{eq:xieq})" — 캡션이 그리는 대상이 명시적으로 ξ_eq.
4. **L1320 (\S sec:eqpeak 첫 문장)**: "코드는 **평형 점유**에서 곧바로 평형 peak 모양 $\xi_\eq(1-\xi_\eq)/w$ 를 만든다" — 직전 절 제목이 "평형 진행률"인데 후속 절 도입이 점유로 되돌아간다.

**존치 정당(참고, 지적 아님)**: L631·L677·L909(Part 0 의 점유 θ)·L1256·L1274·L1279·L1285(여집합-분포 명제)·L2274(Fermi-함수형 점유 분포)·ch2 L155(여집합 도입)는 전건 분포/θ 용법 — R7 기준대로 존치가 맞다.

**수정안**: 4곳 모두 "평형 진행률"로 치환(L190 은 "폭$\cdot$평형 진행률$\cdot$평형 peak", L1114 는 "평형 진행률의 폭 척도는", L1311 은 "평형 진행률과 그 미분", L1320 은 "평형 진행률에서 곧바로"). 물리·수식·참조 무접촉, 각 1어절 치환.

### C8-2 [MED·이중 병기 잔재 3건] ch1 L1871(MSMR)·L524(정규용액)·L594(이중웰) — R7 의 전방 병기 삽입/이동이 만든 중복

**무엇이**: R7 이 병기를 앞으로 옮기거나(MSMR: L1994→L145) 첫 출현에 삽입(정규용액 L247·이중웰 L501)했는데, **하류의 기존 전체 병기를 제거하지 않아** 같은 용어가 두 번 풀어써진다. 세 건 모두 같은 패턴.

**근거(현행 원문)**:
1. MSMR — L145 "MSMR(multi-species, multi-reaction) 동형에" ↔ **L1871** "\code{LCO\_MSMR\_LIT} 로 둔다(**MSMR — multi-species, multi-reaction 파라미터화**, 상세 \S\ref{sec:lco-preview}$\cdot$\S\ref{sec:lco-code})". R7 은 L1995 의 병기만 bare 화("MSMR 모델은")하고 L1871 을 남겼다.
2. 정규용액 — L247(표) "정규용액(regular solution) 상호작용 에너지"(R7 신규) ↔ **L524** "\emph{정규용액}(regular solution)이고"(기존).
3. 이중웰 — L501(소절 제목) "이중웰(double-well) 문턱"(R7 신규) ↔ **L594**(그림 캡션) "가운데가 언덕이 된 이중웰(double-well)"(기존).

**첫 출현 검증(PASS 확인)**: 이동/삽입된 병기 위치 자체는 전건 진짜 첫 출현 — MSMR L145·정규용액 L247·이중웰 L501·SOC L1824(잔여 L2081·L2082 bare ✓)·OCV L260(잔여 L1407·L1616·L1825·L2774 bare ✓)·GITT L1405·OCP L1406. 역전 재발 없음.

**수정안**: L1871 을 "(상세 \S\ref{sec:lco-preview}$\cdot$\S\ref{sec:lco-code})" 로, L524 를 "\emph{정규용액}이고" 로, L594 를 "이중웰" 로 — 하류 병기만 제거(첫 출현 병기 유지).

### C8-3 [MED·이중 병기+동격 스타일] ch2 L618 ↔ L680 — entropy production 을 두 곳 모두에서 풀어씀, 둘째는 쉼표 동격

**무엇이**: R7 은 두 곳을 모두 손봤는데(둘 다 diff hunk), 결과적으로 첫 출현(L618)과 둘째 출현(L680) **양쪽에 영어 원어가 남았고**, 둘째는 병기 괄호도 아닌 쉼표 동격이라 괄호 안에서 어색하게 끊긴다.

**근거(현행 원문)**: L617–618 "$\propto Q_\mathrm{cycle}\,\Delta U^\hys$ 의 **소산(entropy production) 열**)을 만든다"(첫 출현, 병기 형식 정상·괄호 중첩 짝 ✓) ↔ L679–680 "첫 항은 과전압 소산(2법칙상 항상 발열, Chapter 1 의 동역학 꼬리$\cdot$분극이 만드는 **소산, entropy production**)이고" — 이미 병기된 용어의 재풀이 + "소산, entropy production)" 의 영어구 댕글링.

**수정안**: L680 을 "…동역학 꼬리$\cdot$분극이 만드는 소산 — \S\ref{ssec:hys} 의 entropy production)이고" 대신 최소침습으로 "…만드는 소산)이고" 로(원어 재병기 제거). 물리 서술 불변.

### C8-4 [MED·교차참조 정밀도] ch2 L709 — "(본 장 식~\eqref{eq:logistic} — Chapter 1 eq:xieq 의 $s{=}+1$ 형)" 이 분기 중심 caveat 누락

**무엇이**: R7 신규 괄호가 ch2 의 ξ_j 를 "Chapter 1 eq:xieq 의 s=+1 형"이라 규정하는데, **ch1 eq:xieq(L1173)는 $\xi_{\eq,j}=1/(1+\exp[-\sigma_d(V-U_j^{\,d})/w_j])$ — 식 안에 $s$ 가 없고 중심이 분기 중심 $U_j^{\,d}$ 다.** σ_d→+1 만으로는 ch2 eq:logistic(중심 $U_j$, 히스 shift 無)이 되지 않는다 — $U_j^{\,d}\to U_j$(γ=0/평형 중심) 환원이 함께 필요하다. 독자가 eq:xieq 로 가면 s 인자를 찾지 못하고, 방전 분기 shift(+½ΔU_hys)가 남는 형으로 오독할 수 있다.

**근거**: ch1 L1163–1165 의 s-형은 eq:logisticsolve($\xi_\eq=1/(1+e^{-sF(V_n-U_j)/RT})$, 중심 $U_j$)이고 eq:xieq 는 그 σ_d·$U_j^{\,d}$ 일반화다(L1168–1173). py docstring L566–567 도 이 지점을 정확히 명시한다: "히스 분기평균 가역열(Ch2 eq:hys_rev)은 **평형 중심 U_j(히스 shift 無)** 를 써서".

**수정안**: "(본 장 식~\eqref{eq:logistic} — Chapter 1 eq:xieq 에서 $\sigma_d\to s{=}+1$·평형 중심 $U_j$(분기 shift 無)로 둔 형)" 류로 한정어 2개 보강. (대안: eq:logisticsolve 상당형이라고 적시.)

### C8-5 [MED(경계)·동근 용어 잔재] ch1 L1691·L1707·L2899 — "지연 점유" 가 ξ_lag 표기 정본과 충돌

**무엇이**: 표기 정본(ch2 ★표기: θ=점유율·ξ=진행률)과 ch1 기호표 L263("$\xi_{\mathrm{lag},j}$ (\code{occ\_lagged}) — **인과 저역통과된 진행률**")이 ξ_lag 를 진행률로 규정하는데, 프로즈 3곳은 "지연 점유"로 부른다. R7 의 ξ_eq 정본화와 동근(ξ-양을 점유로 지칭)이며, 그중 L2899 는 R7 이 수정한 D-PEAK hunk 의 바로 옆 줄이다.

**근거(현행 원문)**: L1691 "이 매끈한 차분이 **지연 점유**의" · L1707 "매끈한 미분 $\dd\xi/\dd V$(**지연 점유**의 미분 $=$ 평형 종$+$꼬리)" · L2899 "(**지연 점유** $=$ 평형 종 $+$ 꼬리; …)".

**수정안**: 3곳 "지연 진행률"(또는 "$\xi_\mathrm{lag}$")로 통일. R7 범위 밖이었음은 인정 — R8 이후 라운드에서 일괄 처리 권고.

### C8-6 [LOW·주술] ch1 L1366–1367 — A-14 복구가 승격시킨 문장의 주술 비정합

**무엇이**: R7 의 괄호 복구(C7-1 수정)는 정확히 적용됐다(괄호 대차 ±0 재계수 ✓). 다만 승격된 독립 문장 "문헌의 L 접미 **명명은** … 액체상 stage 를 가리키며(…), 표의 정수 행 라벨과 같은 **전이다**." 에서 주어(명명)와 둘째 술어(전이다)가 호응하지 않는다 — 명명 ≠ 전이. 괄호 안 종속구였을 때보다 독립 문장이 되며 노출이 커졌다.

**수정안**: 말미를 "…표의 정수 행 라벨과 같은 전이를 가리킨다." 로(술어를 '가리키다' 로 일원화).

### C8-7 [LOW·변호 가능] ch2 L92·L118·L301 — "Chapter 1 (의) logistic 평형 점유" 표현과 정본화의 긴장

**무엇이**: ch2 도입·요약·그림 캡션 3곳이 Chapter 1 의 logistic 을 "평형 점유"로 부른다(L92 "Chapter 1 의 logistic 평형 점유의 통계역학적 기원" 등). Chapter 1 쪽 정본은 이제 "평형 진행률 ξ_eq" 다.

**변호(오적발 가능성)**: ch2 가 유도하는 대상은 점유 분포 ⟨n⟩=θ 이고, keybox L169 는 "Chapter 1 의 평형 종 ξ_eq 는 … **점유 분포의 여집합**"으로 정확히 서술한다 — 세 곳 모두 '분포의 기원' 문맥이라 R7 존치 기준(여집합-분포 용법)으로 읽을 수 있다. 존치 판정도 가능 — 다만 날을 세우려면 L92 를 "logistic 평형 점유$\cdot$진행률(θ·ξ)" 또는 "평형 logistic" 으로.

### C8-8 [LOW·구두점, pre-existing 노출] ch2 L464–467 — eq:gj 말미 쉼표 직후 ★기호 주의 새 문장

**무엇이**: eq:gj display 가 "$\equiv\; g_j(x),$" 쉼표로 끝나는데, R7 삽입 ★기호 주의가 그 직후에서 새 문장("★기호 주의 — … 별개 기호다.")을 시작해 쉼표–새문장 부정합이 생긴다. 단 이 쉼표 뒤 새 문장("온도 미분은…") 패턴은 삽입 이전부터 있었다 — 삽입이 만든 신규 파손이 아니라 가시화.

**수정안(선택)**: display 말미 쉼표를 마침표로. ★기호 주의 내용 자체는 전건 검증 PASS — $g_j(\xi)$(ch1 eq:gxi L544 실재, 첨자 j 포함 자구 일치)·$g(\theta)$(ch2 eq:BW L194 실재)·$g(E_F)$(ch2 L400–417 실재, L406 "상태밀도" 호칭 일치)·단위 [1/V]=무차원/V ✓·"봉우리(Chapter 1 의 peak)" 다리는 dQ/dV 정량 연결이 처음 생기는 지점(L461)에 위치 — 적절.

---

## 항목별 판정 (지시 점검 항목 1–6)

1. **문법·완결성**: "저SOC(state of charge)$\to$만충"(L1824) — 병기가 범위 화살표를 끊지만 오독 없음, PASS. "이 두-상 두 전이에만"(L1376) — 기존 L1394 "두-상 두 전이의" 와 같은 어순 패턴, PASS. "소산(entropy production) 열)"(L618) — 괄호 중첩 짝 ✓(스타일 지적은 C8-3 의 L680 쪽). MSMR 서론 재구성(L145–146) 주술 완결 ✓. L1725 마침표 복구 ✓("점프다." 종지). L1406–1407 괄호 대차 0 ✓. ch2 "만충에서 1·희박에서 1"(L175) ✓.
2. **병기 정합**: 구 병기 자리 bare 화 3/3 ✓(SOC L2081·OCV L1616·MSMR L1995), 이동 병기 첫 출현 7/7 ✓ — 단 하류 이중 병기 3건 잔존(C8-2)·entropy production 재병기 1건(C8-3).
3. **참조 무결**: \S\ref{sec:lco-map}(L1841 실재, tier 범례 각주 L1875–1877 그 절 안에 실재·L1406 이 본문 첫 tier 출현이라 전방 포인터 위치 정확) ✓ · eq:lco-comp(L2659 실재, θ↔ξ 여집합 쌍 식 — L2676 참조 의미 정합) ✓ · eq:BW(ch2 L196) ✓ · "Chapter 1 eq:xieq/eq:Ubranch" 문자열의 라벨 실재(ch1 L1174·L1057) ✓ — 단 eq:xieq 인용의 정밀도 이슈 1건(C8-4).
4. **용어 통일 여파**: "평형 점유"→"평형 진행률" 7곳 치환 전건 문맥 자연(특히 L692 "$\xi_\eq$ 다" 는 문서 관례 "$…$ 다" 다수 — L1114·L1144 동형 — 와 정합, L2498 문장 완결 ✓) — 단 sweep 누락 4곳(C8-1)·동근 "지연 점유" 3곳(C8-5). "2상" 잔여 grep 전수: 2상역(L2095·2215·2216·2229·2234·2269·2421·2423·2439)·2상 공존(L2193)·two-phase calibration(L1939·2114·2539) — **전건 존치 대상 형태만 잔존, "2상 전이/2상 plateau" 잔재 0** ✓. "배리어" 잔재 0(장벽 통일) ✓. MIT 확장형 ch1 L1873 ↔ ch2 L320 "insulator-to-metal transition" 일치 ✓(서지 L2918 의 "insulator--metal" 은 문헌 제목 원문 — 불가침 ✓).
5. **★기호 주의**: 삽입 위치·내용·지칭 3종 실재 전건 PASS(상세 C8-8), 새 수식 번호 없음 — eq 번호 체계 무충돌 ✓. "봉우리(Chapter 1 의 peak)" 다리 위치 적절 ✓.
6. **물리 불변**: 하단 별도 절.

## 물리 불변 확인 (46/46)

- **py 3 hunk**: L45(헤더 주석)·L483(인라인 주석)·L566(docstring) — 전건 주석/문서열 전용, 식별자 `func_ksi_eq`·`ksi_eq`·`occ_lagged`·시그니처·수치 무접촉. "평형 점유" py 잔재 0(grep). 커밋 게이트 "회귀 bit-exact 13/13 PASS" 와 정합.
- **ch1 26 + ch2 17 hunk**: 치환 전건 프로즈·캡션·각주·표 설명열·keybox 텍스트 — 수식 본체·수치(4958·25.7·0.2109·±1 mV·+0.83 mV/K·−0.37~+0.1 mV/K·ν=2/~23%/0.77·0.020/0.016/0.014/0.012 V)·부호($\sigma_d$·$s{=}+1$·$f=+\sigma_d$)·코드 식별자(\code{...}) 접촉 0. ★기호 주의는 프로즈 삽입(수식 추가 없음). eq:gj·eq:BW·eq:xieq 등 라벨 번호 불변.

## 가장 약한 1곳

**C8-1 의 L1311+L1320 연쇄**: 절 제목(L1106)·본문(L1108)을 "평형 진행률"로 고쳐 놓은 바로 그 절의 마무리 그림 캡션(L1311 "평형 점유와 그 미분")과 다음 절 첫 문장(L1320 "코드는 평형 점유에서")이 연속으로 점유로 되돌아간다 — 제목과 캡션이 한 화면에서 충돌해 정본화 취지(θ/ξ 여집합 함정 차단)를 정면으로 흐리는, 독자 가시성 최대 지점. 수정은 2어절 치환.

## Coverage 선언

- 재검한 수정 지점: **diff hunk 46개 전수**(py 3 + ch1 26 + ch2 17; `git show 134631a` 세 파일 한정, diff 496줄 전문 정독) — 각 hunk 를 현행 파일에서 전후 문맥 포함 재정독, missing 0.
- 독립 grep 전수 재검: "평형 점유"(ch1 12곳+ch2 4곳 전건 분류)·"2상|두-상|two-phase"·"배리어"·"SOC/OCV/MSMR/OCP/GITT/tier/히스/정규용액/이중웰/insulator/entropy production/지연 점유/D-PEAK" 첫 출현·잔재 — 라벨 실재 8종(sec:lco-map·eq:lco-comp·eq:xieq·eq:Ubranch·eq:gxi·eq:BW·eq:gj·eq:logistic) 전건 확인.

## 5줄 요약 (오적발 자기표시 포함)

1. R7 수정 46 hunk 전수 재검 — 물리·수치·부호·코드 식별자는 전건 무결(46/46), R7 자신의 복구 2건(L1366 괄호·L1725 마침표)은 정확 적용 확인. 결함은 용어·병기 위생에 집중.
2. [HIGH] 1건: ξ_eq 정본화 sweep 누락 4곳(L190 spine 캡션·L1114 소절 첫 문장·L1311 logistic 캡션·L1320 eqpeak 도입) — 전건 비-분포 용법, 각 1–2어절 치환(C8-1).
3. [MED] 4건: 이중 병기 3종(MSMR L1871·정규용액 L524·이중웰 L594, C8-2)·entropy production 재병기+쉼표 동격(ch2 L680, C8-3)·"eq:xieq 의 s=+1 형" 분기 중심 caveat 누락(ch2 L709, C8-4)·"지연 점유" 3곳 동근 잔재(C8-5, R7 범위 밖 경계 건).
4. [LOW] 3건: L1366–1367 주술(명명은…전이다, pre-existing 구조의 승격 노출, C8-6)·ch2 "logistic 평형 점유" 3곳(분포-용법 변호 가능 — 존치 판정 여지, C8-7)·eq:gj 쉼표–새문장(pre-existing 가시화, C8-8).
5. 오적발 자기표시: C8-7 은 ch2 keybox 의 여집합 서술로 변호 가능(반려 가능성 높음)·C8-6/C8-8 은 R7 이 만든 신규 파손이 아니라 기존 결의 노출 — 신규 결함으로는 C8-1~C8-4 만 세는 것이 공정하다.
