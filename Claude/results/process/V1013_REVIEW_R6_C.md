# V1.0.13 P5 검수 라운드 6 — 검수자 C (수식-주도 렌즈, 문단 단위 + 산문 예산 실측)

- 대상: `Claude/docs/v1.0.13/graphite_ica_ch2_v1.0.13.tex` (798행 전문, head→tail 정독 — Read 2회로 1-455·456-798 전 구간 커버, 누락 0)
- 체제: Fable 단독 10회 가변-청크 라운드 6 / 청크 스킴 = 문단 단위 수식-주도 렌즈 + 산문 예산 실측
- ★검수 의견만 — tex/코드 무수정. 측정 스크립트 `V1013_prose_count.py` 실행은 read-only(원본 무변경 확인: 스크립트 자체가 read-only 설계, 파일 쓰기 코드 없음).
- 등급: [중] = 규범 위반 명백·수정 권고 / [경미] = 경계 사례·판단 여지 있음(오적발 가능성 자기표시 포함)

---

## 과업 1 — 수식-주도 판정 (①다리 1-2문장/식 없는 3문장+ ②재진술 ③R2-R5 삽입 재팽창 ④괄호 전보체 ⑤orphan 0 확인)

### F-1 [중] ②+③ — w_j 이중지위 서술의 다중 중복(계통적, 8곳) + keybox 재팽창

w_j 의 "단상 평형 예측 vs 두-상 현상학적 자유 폭" 서술이 본문에 8곳 반복된다. 정본은 파생 C(§2.4.3, L579-608)와 사용처인 파생 A srcbox 전제 명시(L526-534)로 충분하며, 나머지는 재진술이다. 라운드별 정정 삽입(v5 w_eff 제거 정정 + R2-R5)이 같은 방어 문장을 여러 자리에 심으며 팽창한 전형.

**(a) 최대 팽창 지점 — L168-185 keybox (ssec:logistic).** 이 박스의 주제는 "ξ_eq = 점유 분포의 여집합"인데, 7문장 중 4문장(L170-182)이 w 지위·모델 선택 논의로 파생 C·srcbox 전제 명시와 중복된다. 원문(발췌):

> L170-171: "logistic 폭 $w=RT/F$ 는(자리당 $n_j{=}1$ 기준; 전이별 유효 폭 서식 $w_j=n_jRT/F$ 는 후술 \S\ref{ssec:overlap}$\cdot$\S\ref{sec:revheat}$\cdot$코드 \texttt{func\_w}) …"
> L178-182: "여기서 두 층위를 가른다 — 두-상에서 자유 피팅되는 것은 폭의 \emph{값}(진폭 $n_j$, 곧 기준온도의 $w_j$)이고, 같은 $T$-의존 \emph{함수형} $w_j=n_jRT/F$($\partial w_j/\partial T=n_jR/F$)을 두-상에도 계속 쓰는 것은 물리 유도가 아니라 코드 \texttt{func\_w} 가 $\Omega_j$ 를 가리지 않고 (\texttt{'n'} 키 보유 시) 모든 전이에 공통으로 두는 \emph{모델 선택}이며, 파생 A(\S\ref{ssec:overlap}) 수치검증의 전제가 바로 이 선택이다. 곧 어느 지위인지는 코드 분기가 아니라 그 전이의 $\Omega_j$ 값이 가른다."

"두 층위" 내용은 L526-534 srcbox 전제 명시(사용처)에 전문이 다시 있다. **수정문(전문 대체안, keybox 본문):**

> Chapter 1 의 평형 종 $\xi_\eq$ 는 \emph{현상학적 곡선맞춤}이 아니라 \emph{격자기체 점유 분포의 여집합}(탈리튬화 진행률 $=1-\theta$)이다. logistic 폭 $w=RT/F$($n_j{=}1$ 서식; 일반형 $w_j=n_jRT/F$)는 \emph{단상 전이($\Omega_j\le2RT$)에 한해} 단일 자리 분배함수~\eqref{eq:Z1} 가 정하는 검증 가능한 평형 예측이고, \emph{두-상 전이}($\Omega_j>2RT$ — 흑연 staging 의 $2\mathrm L\!\to\!2\cdot2\!\to\!1$)에는 이 유도가 닿지 않아 폭은 현상학적 자유 피팅 파라미터가 된다. 값과 $T$-의존 함수형의 두 층위 구분, 그리고 함수형 유지가 코드 \texttt{func\_w} 의 모델 선택이라는 점은 파생 C(\S\ref{ssec:weff})와 파생 A 전제 명시(\S\ref{ssec:overlap})가 다룬다. ★표기: $\theta=$ Li 점유율(만충서 1), $\xi=1-\theta=$ 탈리튬화 진행률(희박서 1); 둘은 같은 점유 분포의 두 이름이다. Chapter 2 는 이 분포를 결론이 아니라 출발점으로 삼아 엔트로피를 쌓는다.

refute 검토: "w 오독 방지 경고는 첫 등장 지점에 있어야 한다"는 반론 성립 — 위 대체안은 1문장 경고 + 포인터를 보존하므로 방어력 유지. orphan 확인: keybox 는 무라벨·피참조 0, 삭제분의 포인터 대상(ssec:weff·ssec:overlap)은 대체안에 잔존 ✓.

**(b) L224-225 (ssec:BW 끝, 괄호 문장 통째 재진술).** 원문:

> "(한편 그 두-상 영역에서 \emph{실측} 봉우리가 갖는 유한 폭은 평형 등온선이 정하는 양이 아니라 현상학적 피팅 폭이다 — \S\ref{sec:revheat} 종합식의 $w_j$ 지위와 Chapter 1 broadening 절을 보라.)"

**수정문: 문장 전체 삭제.** 같은 내용이 sec:mixing 도입 (C)항(L457-459)·tab:limits 4행(L658)·파생 C 본문에 있음. orphan 확인: 무라벨·피참조 0, 직전 문장(L222-223 "이 임계 $\Omega=2RT$ 가 \S\ref{sec:limits} 에서 핵심 코너로 다시 나온다")이 절 마무리 다리로 그대로 기능 ✓.

**(c) L669 (sec:limits (iii) 끝 괄호).** 원문: "(그 plateau 위 실측 봉우리의 유한 폭은 평형이 아니라 broadening 이 정함, \S\ref{ssec:weff})" — 같은 표의 4행(L658)에 "\emph{실측} 두-상 폭은 현상학적 피팅(\S\ref{ssec:weff})"이 이미 있어 한 절 안 이중 서술. **수정문: 괄호 삭제**, (iii)은 "…상호작용 모형이 상전이 임계를 스스로 짚는다." 로 종결. orphan ✓.

**(d) L723-728 (revheat keybox 내 w_j 3문장 재설명).** 원문(발췌): "$w_j$ 는 전이별 폭이다 — \emph{단상} 전이($\Omega<2RT$)에서는 평형이 예측하는 양(이상 극한 $w_j=RT/F$)이지만, 흑연 staging 의 \emph{두-상} 전이($\Omega>2RT$)에서는 평형 예측 $nRT/F$ 가 아니라 다온도 $dQ/dV$ 로 \emph{피팅하는 현상학적 자유 폭}이다(파생 C \S\ref{ssec:weff}; 폭의 broadening 기원은 Chapter 1 broadening 절)." **수정문(1문장 압축):**

> $w_j$ 는 전이별 폭이다(단상 = 평형 예측, 이상 극한 $w_j{=}RT/F$; 흑연 두-상 = 다온도 $dQ/dV$ 피팅의 현상학적 자유 폭 — 파생 C \S\ref{ssec:weff}).

use-this 박스의 자기완결성(입력 지위 명시)은 유지됨. orphan ✓. — L456-459(로드맵)·L773-775(맺음 회고)는 구조적 기능이 있어 존치 판단.

### F-2 [중] ② — ΔS(x) 분해 display 식의 이중 게시 (L322-326 vs L437-441)

동일 분해식이 파생 B warnbox 와 sec:vibel keybox 에 항 순서·underbrace 라벨만 달리하여 두 번 게시된다(이미 두 판이 어긋나 있음 — 항 순서 상이 = drift 실증). 원문: L322-326 `\[\Delta S(x)=\Delta S^0_j+R\ln\frac{\xi}{1-\xi}+\delta S_{\vib/e}(x)\]`(underbrace: 중심 표준값/분포 config/급변 시만 분리) vs L437-441 `\[\Delta S(x)=R\ln\frac{\xi}{1-\xi}+\Delta S^0_j+\delta S_{\vib/e}(x)\]`(underbrace: config 분포/vib+elec 중심 흡수/급변 시만). **수정문(sec:vibel keybox 해당 문장 대체, display 제거):**

> 세 분포가 한 챕터의 엔트로피를 이룬다: Li 배열의 \emph{격자기체}(config), 격자 진동의 \emph{Bose--Einstein}(vib), 전도 전자의 \emph{Fermi--Dirac}(electronic). 측정 부분몰 엔트로피는 파생 B 의 분해 $\Delta S(x)=\Delta S^0_j+R\ln[\xi/(1-\xi)]+\delta S_{\vib/e}(x)$ 그대로이고, 가역 발열은 이 세 분포를 재배열하는 열이다. config 는 봉우리 \emph{내부}를 비선형으로 만들고($\pm\infty$ 발산), vib 는 고-$x$ 음 baseline 을, electronic 은 (MIT 가 없으면) 작은 상수를 준다.

refute 검토: "박스 자기완결" 반론 — 인라인 수식으로 식 자체는 보존되므로 자기완결 유지. orphan 확인: 두 display 모두 무라벨(`\[...\]`), `\eqref` 피참조 0 ✓. 정본은 파생 B warnbox 판(표기 정의를 함께 못박는 쪽) 유지가 맞다.

### F-3 [중] ② — "중심에서 config=0 이라 모순 없음" 4중 반복 (L327·L576-577·L657·L665-667)

같은 자기일관성 문장이 파생 B warnbox(L327), 파생 B 소절(L576-577), tab:limits 3행(L657), 극한 종합 (i)(L665-667)에 반복. 특히 L576-577 은 한 문장 안에서 또 겹친다. 원문:

> L576-577: "봉우리 중심에서 config$=0$ 이므로 두 정의가 모순 없이 맞물린다(중심에서 측정한 표준값에 config 가 겹쳐 들어가지 않는다)."

괄호가 직전 절(문장 본체)을 그대로 재진술(②+④). **수정문:** "봉우리 중심에서 config$=0$ 이므로 두 정의가 모순 없이 맞물린다." (괄호 삭제). L665-667 (i)는 "…(i) 중심 $\xi=\tfrac12$ 에서 config$=0$ 이라 파생 B 의 분리가 자기일관적이다;" 로 축약("표준값이 정확히 회수되므로"는 표 3행과 중복). warnbox L327·표 3행은 정본으로 존치. orphan ✓.

### F-4 [중] ② — 자인된 재진술 (L367-368, "다시 강조한다")

원문: "중심 표준값 $\Delta S^0_j$ 는 봉우리 중심에서 측정되는 상수이고, 부분몰 config 항은 그 위에 분포가 더하는 $x$-의존이라는 점을 다시 강조한다(파생 B)." — 파생 B warnbox(L315-330) 요지의 문면 그대로 재진술이며 "다시 강조한다"로 자인. **수정문: 문장 삭제**, 후속 문장을 문단 머리로 승격: "엔탈피 쪽 표준값 $\Delta H^0_j$ 는 \emph{기준 차이}에 주의해야 한다: …" ("한편" 제거). orphan 확인: 피참조 0, 후속 엔탈피 논의는 이 문장에 의존하지 않음 ✓.

### F-5 [중] ④ — 괄호 보충 전보체: eq:logistic 직후 단위 환산 괄호 (L162-164)

원문: "이고 여기서 $w\equiv RT/F$ 다(몰 단위 환산 $R=N_A\kB$, $F=N_A e$ 이므로 $\beta(\varepsilon_0-\mu)$ 의 분자를 몰 에너지로 쓰면 지수가 $(V-U_j)/(RT/F)=(V-U_j)/w$ — 곧 몰 척도 역온도 $1/RT$ 기준으로 $F/RT=1/w$; 자리당 $\beta=1/\kB T$ 그대로는 $\beta F=N_A/w$ 다)." — 3행짜리 유도가 괄호 안에 대시·세미콜론 전보체로 압입(교과서 문체 규범 위반). **수정문(완결 문장 분리, 길이 중립):**

> 이고 여기서 $w\equiv RT/F$ 다. 몰 단위 환산 $R=N_A\kB$, $F=N_Ae$ 로 $\beta(\varepsilon_0-\mu)$ 의 분자를 몰 에너지로 쓰면 지수는 $(V-U_j)/(RT/F)=(V-U_j)/w$, 곧 몰 척도에서 $F/RT=1/w$ 이고 자리당 $\beta=1/\kB T$ 그대로는 $\beta F=N_A/w$ 다. 곧 \emph{점유} $\theta$ 는 …

### F-6 [중] ④+③ — eq:dVdT_config 직후 3중 세미콜론 괄호 (L260-263)

원문: "임을 확인한다(둘째 항의 계수 $R/F$ 는 자리당 $n_j{=}1$ 서식 — 전이별 일반형은 $n_jR/F$ 다, 후술 \S\ref{ssec:overlap} 식~\eqref{eq:dxidT}; $\partial U_j/\partial T=\Delta S_{\rxn,j}/F$ 를 첫 항으로 쓰고, $w=RT/F$ 의 명시적 $T$ 의존이 둘째 항을 낳음; 점유 $\theta=1-\xi$ 이므로 $\partial S_\config/\partial\theta|_{\theta=1-\xi}=-R\ln[(1-\xi)/\xi]=+R\ln[\xi/(1-\xi)]$ — Li 삽입은 점유 $\theta$ 를 늘리는 방향이라 부호가 $+$ 로 맞물린다)." — 세 개의 독립 유도 노트가 한 괄호에 전보체로 중첩(R-라운드 정정 삽입 흔적: n_j 서식 한정어). **수정문:**

> 임을 확인한다. 첫 항은 $\partial U_j/\partial T=\Delta S_{\rxn,j}/F$ 이고, 둘째 항은 $w=RT/F$ 의 명시적 $T$ 의존이 낳는다 — 점유 $\theta=1-\xi$ 에서 $\partial S_\config/\partial\theta|_{\theta=1-\xi}=-R\ln[(1-\xi)/\xi]=+R\ln[\xi/(1-\xi)]$ 이고, Li 삽입이 점유를 늘리는 방향이라 부호가 $+$ 로 맞물린다. 계수 $R/F$ 는 자리당 $n_j{=}1$ 서식이며 전이별 일반형은 $n_jR/F$ 다(후술 식~\eqref{eq:dxidT}).

### F-7 [중] ④+③ — 문헌 검증 ★단위 주의 괄호 (L338-340)

원문: "…부호 반전한다고 보고된다\cite{msmr_partII}(★단위 주의 — 이 $+3\sim4$ mV/K 는 본 문서 스케일($\Delta S{\approx}+29$ J/(mol\,K) ⇒ $+0.30$ mV/K)과 자릿수가 다르다; 보고 조건$\cdot$단위 상이 가능성이 있어 원문 재확인 대상[미검증]이며, 본 문서 입력값과 무관한 참고 인용이다)." — 이중 괄호 중첩 + 세미콜론 전보체. [미검증] 플래그는 필수 보존. **수정문(각주 전환):**

> …부호 반전한다고 보고된다\cite{msmr_partII}.\footnote{★단위 주의: 이 $+3\sim4$ mV/K 는 본 문서 스케일($\Delta S\approx+29$ J\,mol$^{-1}$K$^{-1}$ ⇒ $+0.30$ mV/K)과 자릿수가 다르다. 보고 조건$\cdot$단위가 다를 가능성이 있어 원문 재확인 대상[미검증]이며, 본 문서 입력값과 무관한 참고 인용이다.}

### F-8 [경미] ④+③ — 파생 A srcbox 그리드 스팬 괄호 (L518-521)

원문: "…(그리드 실측 최대 0.18 은 극값을 정확히 샘플하지 않은 결과 — 해석 상한은 0.21; config 항은 $z_j$ 의 로그 구조 탓에 그리드 \emph{양끝에서 계속 커지는 스팬-의존 양}이라, 이 수치들은 \cite{numverif2026} 검증 그리드의 $\bar x$ 스팬 기준이다)." **수정문(2문장 분리):** "그리드 실측 최대값 0.18 은 극값을 정확히 샘플하지 않은 결과로, 해석 상한은 0.21 이다. config 항은 $z_j$ 의 로그 구조 탓에 그리드 양끝에서 계속 커지는 스팬-의존 양이므로, 이 수치들은 검증 그리드의 $\bar x$ 스팬 기준이다 \cite{numverif2026}." (srcbox 는 박스 예외권이 있으나 전보체 규범은 박스에도 적용된다고 판단 — 오적발 가능성 자기표시.)

### F-9 [경미] ③ — 전제 명시 첫 문장 3중 괄호 중첩 (L526-528)

원문: "이 검증의 $w_j(T)$ 는 현행 코드(\texttt{func\_w}/\texttt{\_width})가 \texttt{'n'} 키 보유 전이(검증 데이터셋은 전 전이 해당; \texttt{'w'}-단독 전이는 $T$-동결 폭)에 강제하는 열적 서식 $w_j=n_jRT/F$ ($\partial w_j/\partial T=n_jR/F$) 하에서 평가된 것이다" — 한 문장에 괄호 4개(그중 하나는 세미콜론 내장). **수정문:** "이 검증의 $w_j(T)$ 는 현행 코드(\texttt{func\_w}/\texttt{\_width})가 강제하는 열적 서식 $w_j=n_jRT/F$($\partial w_j/\partial T=n_jR/F$) 하에서 평가된 것이다. 이 서식은 \texttt{'n'} 키 보유 전이에 적용되며 검증 데이터셋은 전 전이가 해당하고, \texttt{'w'}-단독 전이는 $T$-동결 폭이다."

### F-10 [경미] ③ — 파생 D 선형화 한정 1문장 과중첩 (L627-630)

원문: "이 상쇄는 두 분기의 봉우리 형태 $g_j^{(d)}$ 가 같다고 본 \emph{선형화 근사}($\Delta U_j^\hys\!\ll\!w_j$)에서 정확하다 — 분기 중심 $U_j^{(d)}=U_j+\tfrac12\sigma_d\,\Delta U_j^\hys$ 가 서로 달라 같은 $x$ 에서 $g_j^{(\mathrm{ch})}\!\ne\!g_j^{(\mathrm{dis})}$ 이면 겹침 가중식~\eqref{eq:hys_branch} 의 config 곡률 때문에 두 분기 평균이 $\Delta S_{\rxn,j}/F$ 로 정확히 떨어지는 것은 $\Delta U^\hys$ 의 1차 차수까지이고, 유한 $\Delta U^\hys$ 에선 고차 보정이 남는다." — R-라운드 정정 삽입 특유의 단문 과적. **수정문(2문장 분리):** "이 상쇄는 두 분기의 봉우리 형태 $g_j^{(d)}$ 가 같다고 본 \emph{선형화 근사}($\Delta U_j^\hys\!\ll\!w_j$)에서 정확하다. 분기 중심이 서로 달라 같은 $x$ 에서 $g_j^{(\mathrm{ch})}\!\ne\!g_j^{(\mathrm{dis})}$ 이면, 겹침 가중식~\eqref{eq:hys_branch} 의 config 곡률 때문에 평균이 $\Delta S_{\rxn,j}/F$ 로 떨어지는 것은 $\Delta U^\hys$ 의 1차까지이고 유한 $\Delta U^\hys$ 에선 고차 보정이 남는다." (분기 중심 정의는 L612 에 이미 있어 재기재 불요.)

### F-11 [경미] ① — eq:occ 직후 식 없는 3문장 (L138-141)

원문: "이 $\avg{n}$ 은 자리가 채워질 확률 $p_1$ 그 자체이고, 빌 확률은 $p_0=1-\avg{n}$ 이다. 식~\eqref{eq:occ} 는 구조상 \emph{Fermi--Dirac 함수}와 동형이다 — … 대수 구조를 낳는다. 다른 점은 … $\varepsilon_0-\mu$ 라는 것뿐이다." **수정문(2·3문장 병합):** "이 $\avg{n}$ 은 자리가 채워질 확률 $p_1$ 그 자체이고, 빌 확률은 $p_0=1-\avg{n}$ 이다. 식~\eqref{eq:occ} 는 구조상 \emph{Fermi--Dirac 함수}와 동형이다 — 자리당 2-상태(채움/빔)가 전자 준위의 점유/공위와 같은 대수 구조를 낳으며, 다른 점은 ``입자''가 Li 이온이고 단일 준위 에너지가 $\varepsilon_0-\mu$ 라는 것뿐이다." (오적발 가능성: 세 문장 모두 하중 있음 — 병합은 리듬 개선 수준.)

### F-12 [경미] ① — eq:BW→eq:Veq_BW 사이 3문장 다리 (L206-208)

원문: "이고, 우변 둘째 항이 \emph{혼합 엔트로피}(곧 \S\ref{sec:config}), 셋째 항이 \emph{혼합 엔탈피}(상호작용 모수 $\Omega$, regular-solution 형)다. $\Omega>0$ 은 같은 종끼리 뭉치려는 인력(상분리 경향), $\Omega<0$ 은 교대 정렬(규칙화) 경향을 뜻한다. 평형 전위는 $\theta$ 1몰 삽입당 자유에너지 변화이므로" **수정문(1·2문장 병합으로 2문장 다리화):** "이고, 우변 둘째 항이 \emph{혼합 엔트로피}(곧 \S\ref{sec:config}), 셋째 항이 regular-solution 형 \emph{혼합 엔탈피}다 — 상호작용 모수 $\Omega>0$ 은 같은 종끼리 뭉치려는 인력(상분리 경향), $\Omega<0$ 은 교대 정렬(규칙화) 경향을 뜻한다. 평형 전위는 $\theta$ 1몰 삽입당 자유에너지 변화이므로" (오적발 가능성 자기표시: Ω 부호 물리는 필수 내용이라 삭제 불가 — 병합만 제안.)

### F-13 [경미] ② — 메타 문장 (L250)

원문: "가 된다. 이 한 줄이 이 장의 중심 결과 중 하나다. 식~\eqref{eq:dSconfig} 와 식~\eqref{eq:Vxi} 의 logistic 역함수를 나란히 두면," — 가운데 문장은 내용 없는 메타 강조. **수정문:** "가 되는데, 이것이 이 장의 중심 결과 중 하나다. 식~\eqref{eq:dSconfig} 와 식~\eqref{eq:Vxi} 의 logistic 역함수를 나란히 두면," (3문장→2문장.)

### F-14 [중] ①+② — revheat 본문 문단, 식 없는 6문장 + 서 재진술 (L704-713)

eq:qrev 이후 L704-713 문단은 display 식 없이 6문장. 마지막 문장이 서(L87-88 "가역 발열…은 …분포를 재배열하면서 주고받는 열이다")·본 절 첫 문장(L704-706)과 삼중 재진술이다. 원문: "충/방전 대칭(가역)이고, 비선형 $\partial U_\oc/\partial T(x)$ 는 분포의 $x$-의존을 직접 반영한다 — 모양이 곧 분포다." **수정문: 문장 삭제**(직전 문장의 calorimetry 정합 \cite{standardised2024} 이 문단 종결로 충분). orphan 확인: "모양이 곧 분포다" 피참조 0 ✓. 추가로 첫 문장(L704-706)의 "(\S\ref{sec:vibel} 핵심 박스)" 참조는 존치 — F-2 정본 지정과 정합.

### F-15 [중] ② — 한계·갭 (1)항의 파생 A 검증 결과 재서술 (L734-740)

원문: "(1) 봉우리 내부 config 항을 포함한 \emph{완전식}은 단일 전이 지배 구간뿐 아니라 전이 경계의 겹침 구간을 포함한 전 범위에서 유한차분과 표시 정밀도로 일치한다(파생 A 의 175 점 검증, 절대오차 $\approx0$); 봉우리 내부 $x$-의존을 무시한 \emph{단순식}(중심 근사)만 그만큼 체계적으로 빗나간다. 남는 공백은 …" — 앞 두 절은 L511-524 srcbox 의 재서술이고, "한계·갭" 항목의 고유 내용은 뒤의 round-trip 공백뿐. **수정문:**

> (1) 완전식의 표시-정밀도 일치(파생 A, 175 점)는 \emph{시뮬레이션 자기일관성} 검증이다 — 남는 공백은 그 완전식이 가정하는 logistic 전이형$\cdot$파라미터 집합 $\{\Delta S^0_j,Q_j,U_j,w_j\}$ 가 실측 흑연을 얼마나 충실히 재현하는지를 \emph{실데이터 round-trip 피팅}으로 확정하는 일이다(시뮬레이션 검증과 실측 검증의 구분).

orphan 확인: 삭제분(전 범위 일치·단순식 이탈)은 srcbox 가 정본으로 보유 ✓.

### F-16 [경미] ② — vib 절 마지막 문단의 파생 B 재인용 (L399-402)

원문: "…흡수된다 — 곧 표~\ref{tab:ds} 의 고-$x$ 음수값($-5,-16$ J\,mol$^{-1}$K$^{-1}$)이 이미 vibrational 기여를 품고 있다. 이것이 파생 B 에서 ``$\Delta S^0_j$ 가 vib$\cdot$electronic 의 중심값을 흡수''라고 말한 의미다. config 항만 분포가 따로 떼어 $x$-의존으로 살린다." — 2·3문장이 파생 B warnbox 재진술. **수정문(3문장→1문장):** "전이 폭 안에서 $\Delta S_\vib$ 가 천천히 변하면(흑연의 통상적 경우) 그 값은 봉우리 중심 표준값 $\Delta S^0_j$ 에 흡수된다(파생 B 의 ``중심 흡수'') — 표~\ref{tab:ds} 의 고-$x$ 음수값($-5,-16$ J\,mol$^{-1}$K$^{-1}$)이 이미 vibrational 기여를 품고 있다."

### F-17 [경미] ③+④ — S_e 차원·몰 환산 2문장 (L419-422)

원문: "식~\eqref{eq:Se} 의 $S_e$ 는 \emph{자리당}(원자 1개당) 엔트로피로 $\kB$ 차원이다 — 계수의 $\kB^2$ 중 한 몫은 $T\,g(E_F)$($g$ 가 1/에너지 차원)가 상쇄해 알짜로 $\kB$ 가 남는다. 따라서 몰당 $\Delta S(x)$ 분해에 넣을 때는 Avogadro 수를 곱해 환산하며($N_A\kB=R$, 곧 자리당 $\kB$ 가 몰당 $R$ 로 올라감 — Chapter 1 의 단위 다리와 같다)." — 차원 감사 정정 삽입의 재팽창(괄호 3개). **수정문(1문장):** "식~\eqref{eq:Se} 의 $S_e$ 는 자리당 엔트로피다 — $g$ 가 1/에너지 차원이라 $\kB^2 T\,g(E_F)$ 의 알짜 차원은 $\kB$ 이고, 몰당 $\Delta S(x)$ 분해에는 $N_A\kB=R$ 로 환산해 넣는다(Chapter 1 의 단위 다리)."

### F-18 [경미] ① — 반응 vs 활성화 소절, 식 없는 4문장 (L446-450)

혼동-금지 경고 내용이나 박스 밖 본문(박스 예외 비적용). 3·4문장 병합 제안: "활성화 엔트로피는 \emph{비가역} 동역학을 지배하고 가역 발열에는 들어가지 않으므로 \cite{msmr_partI}, 혼동하면 가역열을 동역학 lag 와 섞게 된다." (4문장→3문장. 오적발 가능성 자기표시: 이 소절은 기능상 warnbox 에 준해 예외로 볼 여지 있음 — 그 경우 존치.)

### 판정 보류(위반 아님으로 판단한 경계 사례)

- 서(L67-108)·각 section 도입 로드맵(L113-121, L228-230, L378-381, L455-460, L643-644)·맺음(L766-777): 개념 도입/구조 예외로 ① 비적용.
- 파생 A 본문(L462-509)·파생 D 유도부: 다리 대부분 1-2문장, 식-주도 구조 준수.
- eq:qrev 직후 ★라벨 층위 주의(L695-698): 3문장 경고문이나 부호 함정 방어로 하중 있음 — warnbox 승격은 선택 사항으로만 제안(위반 아님).

---

## 과업 2 — 산문 예산 실측 (`V1013_prose_count.py` 실행, read-only)

스크립트 실행 성공(2026-07-03, 현행 파일 대상). P1 기준선 = `V1013_PROSE_BUDGET.md` §5. **주의: Ch1 은 P1 이후 대개편(P0 통계역학 기초 신설·LCO Part II 재구성, 2358→2925행)이라 그룹 합계 비교만 유효, 절별 1:1 대응 불가. Ch2 는 절 구조 동일하여 절별 비교 유효.**

### 그룹 요약 — P1 기준선 vs 현재 실측

| 그룹 | 지표 | P1 기준선 | 현재 실측 | 변화 | 판정 |
|---|---|---:|---:|---:|---|
| 흑연 본체(Ch1, 흑연+공통 병합) | 문장/수식 비 | **8.04** (386/48) | **6.52** (476/73) | −1.52 | 예산 방향 안(비율 개선; 문장 증가는 P0 신설분) |
| LCO(Ch1, `sec:lco-*`) | 문장/수식 비 | **5.75** (253/44) | **4.31** (194/45) | −1.44 | 비율 개선 |
| LCO 문장 수 | 문장 | 253 (목표 161, −36%) | 194 (−23.3%) | −59 | **확정 목표(−35%) 미달** — 목표 대비 +33문장. 단 LCO 재구성(신설 3절 포함)으로 목표 절대치의 현행 유효성은 [미검증] |
| Ch2 전체 | 문장/수식 비 | **8.52** (230/27) | **8.30** (224/27) | −0.22 | 재팽창 없음(net −6문장), 단 3그룹 중 최고 비율 유지 |

### Ch2 절별 — P1 vs 현재 (문장 수; 식 수는 전 절 불변 27)

| 절 | P1 | 현재 | Δ | 비고 |
|---|---:|---:|---:|---|
| 서 | 24 | 24 | 0 | |
| sec:partition(도입) | 4 | 5 | **+1** | R-정정 삽입 팽창(Ch1 Part 0 참조 문장) |
| 단일 자리 분배함수 | 4 | 4 | 0 | |
| ssec:logistic | 14 | 15 | **+1** | keybox 재팽창 — F-1(a) 지점과 일치 |
| ssec:BW | 11 | 11 | 0 | |
| sec:config(도입) | 3 | 3 | 0 | |
| 부분몰 config | 22 | 22 | 0 | |
| 문헌 검증 | 7 | 7 | 0 | |
| sec:vibel(도입) | 3 | 3 | 0 | |
| vib | 12 | 12 | 0 | |
| ssec:elec | 16 | 16 | 0 | |
| 반응 vs 활성화 | 4 | 4 | 0 | |
| sec:mixing(도입) | 4 | 3 | −1 | |
| 파생 A | 18 | 18 | 0 | |
| 파생 B | 5 | 5 | 0 | |
| 파생 C (ssec:weff) | 18 | 15 | −3 | P1 제안(~−15%) **달성**(−16.7%) |
| 파생 D | 8 | 8 | 0 | |
| sec:limits | 11 | 11 | 0 | |
| sec:revheat | 37 | 33 | −4 | P1 제안(28-30, −19~−24%) **미달**(−10.8%); 전체 압축 우선순위 4위 잔존(33/2=16.5) |
| 맺음 | 5 | 5 | 0 | |
| **합계** | **230** | **224** | **−6** | 식 27 불변 — 증감은 전부 산문 |

### 종합 판정

1. **Ch2 예산: 조건부 안(內).** 총량은 P1 대비 −6문장으로 재팽창 없음. 그러나 (i) P1 §7.2 제안 중 revheat 는 미달(33 vs 28-30 — 단 §7.2 는 "사용자 확정 필요" 제안값이지 확정 게이트가 아님), (ii) 절별 +1 팽창 2곳(sec:partition·ssec:logistic)은 과업 1 의 F-1(a)·정정 삽입 지점과 정확히 일치 — 과업 1 수정문 반영 시 revheat 33→~30·logistic 15→~13 으로 제안 범위 진입 전망.
2. **LCO: 확정 목표(−35%) 기준 미달(−23.3%).** 194 vs 목표 161. 단 P1 이후 LCO 파트가 재구성(도입·방향규약·MSMR 예고 3절 신설 = 신규 콘텐츠 +29문장 상당)되어 P1 목표 절대치가 그대로 유효한지는 근거 미발견 — 상위 계획 문서 대조 필요[미검증]. 비율(5.75→4.31)로는 개선.
3. **흑연 본체: 비율 8.04→6.52 개선.** sec:broadening 은 59→50(−15%)로 P1 제안(35-40) 미달이나 Ch1 소관.
4. 측정 방법 동일성: 동일 스크립트·동일 규칙(문장 분리 규칙·figure/table 제외·decimal 보호) — 절대값이 아닌 상대 비교 신뢰 원칙(P1 §1)을 그대로 승계. 추정값 없음, 전 수치 실측.

---

## 말미 선언

**가장 약한 1곳:** L168-185 keybox(ssec:logistic) — 박스 주제(ξ_eq 의 통계역학적 기원)에서 벗어난 w-지위·코드 모델선택 논의 4문장이 파생 C·파생 A 전제 명시와 삼중 중복되고, 문장당 한정어 중첩(괄호 내 세미콜론·중첩 괄호)이 챕터 최고 밀도다. 실측으로도 ssec:logistic 은 P1 대비 +1문장 팽창한 2곳 중 하나(F-1(a) 수정문 제시 완료).

**물리 불변 확인:** 수정 제안 전건이 문장 재배열·삭제·괄호 해체이며 물리 내용 불변을 확인함 — 검산한 불변량: eq:occ FD 동형 부호, eq:logistic 의 θ 감소/ξ 증가 방향(s=+1), eq:Vxi↔eq:Veq_BW 정합(θ=1−ξ), eq:slope_BW 임계 Ω=2RT(θ=½ 에서 (2Ω−4RT)/F), eq:gj·eq:dxidT 의 w(T) 두 조각, eq:weighted 가중 평균 부호, 종합식의 +n_jR z_j/F 부호, eq:qrev 의 Bernardi 분해(방전 I>0·ΔS>0 → 흡열), ΔS⁰_j = +29/0/−5/−16 의 표·본문 일치, stage 2→1 ΔH⁰_j=−13.0 kJ/mol 산술(−FU_j+FTΔS/F, U_j≈0.085 V), 히스 분기 dis=+½ 상지(흑연 탈리튬화 OCV 상측), Sommerfeld S_e 선형-T. 전부 이상 없음 — 본 라운드 물리 결함 0건(H-1 BW 부호 정정 반영 확인 포함).

**Coverage 선언:** Ch2 tex 1-798행 전 구간 문단 단위 정독(1-455·456-798 두 창, preamble 1-62 는 헤더 주석·매크로 — 산문 없음 확인). missing = 0. 검수 렌즈 = 수식-주도(①~⑤) + 산문 예산 실측. 스크립트·기준선 문서(V1013_prose_count.py 407행, V1013_PROSE_BUDGET.md 176행)도 전문 정독.

**5줄 요약 (오적발 자기표시 포함):**
1. [중] 6건: w-지위 8중 중복(F-1, 4개 지점 수정문)·ΔS(x) 분해식 이중 게시(F-2, drift 실증)·config=0 4중 반복(F-3)·자인된 재진술(F-4)·전보체 괄호 2건(F-5·F-6)·revheat 6문장 문단+한계갭 재서술(F-14·F-15) — 전건 orphan 0 확인 후 구체 수정문 제시.
2. [경미] 8건(F-7~F-13·F-16~F-18): 정정 삽입 재팽창·전보체 해체·3문장 다리 병합 — F-8(srcbox 전보체)·F-11·F-12(하중 있는 3문장)·F-18(경고 성격 소절)은 오적발 가능성 자기표시.
3. 실측: Ch2 224문장/27식(비 8.30) — P1(230/8.52) 대비 재팽창 없음이나 3그룹 중 최고 비율, revheat 33 은 P1 제안(28-30) 미달·ssec:weff 15 는 달성.
4. LCO 194문장(−23.3%)은 사용자 확정 목표 −35%(161) 미달 — 단 LCO 파트 재구성으로 목표 절대치의 현행 유효성 [미검증], 상위 계획 대조 필요.
5. 물리 결함 0건(불변량 12항 검산 통과) — 본 라운드 결함은 전부 문체·중복 계열이며, 정정 삽입 팽창 실측(+1문장 2곳)이 F-1(a)·F-9 지점과 일치해 과업 1 판정을 정량 뒷받침.
