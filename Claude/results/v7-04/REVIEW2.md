# REVIEW2 — v7-04b 검토2 (체리픽 베이스 후보, 엄격)

> 검수 sub (리뷰 전용, 수정·커밋 X). 대상 `v7-04b.tex` (630줄, 전문 정독).
> 기준 `Anode_Fit_v11_final.py` (707줄)·`v11_flowchart.md` (분극 정정본, 2026-06-29)·`AUTHOR_BRIEF.md`.
> 청크: N0~N9 절별 정독 + 식 사슬 단위 재유도 + tikz/라벨 전수 스크립트 + 부호 8항 코드 대조.

---

## ① 보완 반영 — w^eff 게이트 명시 (검증: 반영됨·정확)

코드 `_width` (L283–288): `use_weff and tr.get('Omega') is not None and tr.get('w') is None`.
`GRAPHITE_STAGING_LIT` 네 전이 모두 `'w'` 키 보유 (L537/544/551/558) → `use_w_eff=True` 라도 게이트 차단.
v7-04b 가 이 사실을 **세 곳에 일관 명시** (요청한 §1.5/N0표/§1.9 모두 충족):

- **N0 표** (L156): `w | func_w` 행에 "dict 에 `'w'` 키 있으면 $w^\eff$ 게이트 차단(§1.5)" 명기.
- **§1.5(폭)** (L341): `_width` 의 `tr.get('w') is None` 조건 인용 + "`'w'` 키가 하나라도 들어 있으면 ... $w^\eff$ 좁힘이 실효 없이 우회" + 회피법(dict 에서 `'w'` 제거) + GRAPHITE_STAGING_LIT 가 "정확히 이 함정"임을 전방 참조.
- **§1.5 codebox** (L363): `★tr.get('w') is None 게이트 — dict 에 'w' 키 있으면 use_w_eff=True 라도 w^eff 우회` 명시.
- **§1.9(N9) staging 표** (L610): `'w'` 폴백값 설명에 더해 "이 `'w'` 키의 존재는 ... `_width` 의 `tr.get('w') is None` 조건을 막는다 ... `use_w_eff=True` 로 모델을 만들어도 Ω-좁힘이 실효 없이 우회" + 회피법 재명시.

코드 대조: `_n_factor` (L274–280) 가 `'n'` 우선이므로 `'w'` 는 폭 폴백으로는 안 쓰임(n=1) → 폭 = nRT/F.
동시에 `'w'` 가 게이트 차단키. 두 역할 모두 정확히 서술. **헛주장·과대 없음.** 베이스의 6차원 무결 상태는 유지됨(아래 ②·③).

## ② 신규 회귀 — 없음 (refute 시도 후)

보완이 새 결함을 들이지 않았는지 적대 검증. 신규 텍스트(§1.5 게이트 문단·N0 표 단서·§1.9 단락) 전수 재독:

- §1.5 보편식(eq:weff) → 극한(Ω→2RT, Ω=0) → 게이트 단서 순서 = AUTHOR_BRIEF "보편식 먼저" 준수. 게이트를 먼저 깔아 식 흐름 왜곡한 흔적 없음.
- §1.9 staging 표 단락의 게이트 재서술이 §1.5 와 모순 없이 정합(둘 다 `tr.get('w') is None` 동일 조건 인용).
- 전방 참조(§1.5→§1.9)·후방 참조(§1.9→§1.5) 라벨 `sec:width`/`sec:sum` 양방향 존재, dangling 0.
- 괄호 전보체 신규 유입 없음(게이트 단서는 완결 문장).

**판정: 신규 회귀 0건.** (가장 의심한 지점 = fig:hysbranch 우단 좌표 `(0.97,1.06)` → ξ→1 에서 logit→+∞ 발산이 물리적으로 옳음, 회귀 아님. ③에 기록.)

## ③ 재점수 — 28/30 (베이스 무결, 미세 시각 2점 감)

| 차원 | 점 | 근거 |
|---|---|---|
| 1 spine 정합(N0–N9 순서) | 5/5 | 절 순서 = 플로우차트 N0→N9 정확. 단 서론(L88–99)·표(N2/N4/N5/N6)는 코드 진행(N2→N3→N4…)과 절 배치(N4·N5 먼저, N6, N3 뒤)가 일부 재배열 — AUTHOR_BRIEF §3 권장 골격(4=폭·점유, 5=peak, 6=히스)과 일치, 의도된 어레인지. 감점 없음. |
| 2 물리·부호 사슬 | 5/5 | 부호 8항 전건 코드 1:1 정합(④). gap 양수성·Taylor 8RT/3F·z_cut=4.357·L_q 닫힌꼴 전부 재검산 통과. |
| 3 G-follow | 5/5 | 각 절 도입·마무리 다리 존재. logistic 을 속도식 정지점에서 유도(eq:relax→eq:logistic), detailed balance 로 χ 약분 명시. 전제 없이 등장하는 개념 없음. |
| 4 G-usable | 5/5 | §1.9 "재현하려면" 6단계 + 수치 상수 전부 닫힘(z_cut/A_cap/m/pad/floor_frac/χ/dS_a). Q_cell 이 \|I\|/Q_cell 비에만 든다는 N0 주장 코드 대조 정확(func_L_q L94 유일). w^eff 게이트까지 닫아 재현 코드가 함정을 피함. |
| 5 완결성(orphan) | 5/5 | 스크립트 전수: 라벨 30 eq + 5 fig 전부 referenced, orphan 0·dangling 0. |
| 6 그림·시각 | 3/5 | tikz 5블록 Hangul 0(ASCII 준수). 단 (a) fig:hysbranch 우단 `(0.95,-0.20)→(0.97,1.06)` 의 smooth Bézier 가 급수직 점프라 렌더 시 우상단 과주(overshoot) 가능 — 물리는 옳으나 시각 혼동 여지. (b) fig:hysbranch 의 `U-chg (minus)` 점선이 좌측 라벨이라 `U-dis center (plus)` 우측 라벨과 비대칭 — 경미. |

**합계 28/30.** (베이스 6차원 무결 주장 중 시각만 만점 미달; 정확성·완결성·G-사슬은 만점.)

## ④ 부호 8항 — 8/8 PASS (코드 1:1)

1. `U_j=(−ΔH+TΔS)/F`, ΔG=−FU — L237/L243; 재검산 U21=0.0853·U43=0.2109 V 정합 ✓
2. `ξ_eq=logistic[σ_d(V−U)/w]`, 방전 V↑→ξ↑ — L352/L359, 코드 func_ksi_eq L86 (`z=s*(V_n−U)/w`) ✓
3. `dξ/dV=σ_d ξ(1−ξ)/w`, peak 양수(방향 불변) — L379–385/L422, 코드 L468(σ_d 없는 ξ(1−ξ)/w) ✓
4. `ΔU_hys≥0`, Ω≤2RT→0, 분기 ±½σ_d — L278/L287/L322; gap 양수·임계 소멸 재검산 ✓, 코드 func_dU_hys L127 ✓
5. χ_d 방전 χ/충전 1−χ, `ΔH_a^eff=ΔH_a−χ_dΩ` — L457/L462, 코드 func_chi_d L160·func_dH_a_eff L152 ✓
6. `∂lnL_q/∂V=−χ_d σ_d F/RT<0` (방전) — L444/L476; L_q 닫힌꼴 doc=code 재검산 ✓ (연속 의존, 컷서 동결 명시)
7. 충전 격자 역전 `ξ[::-1]…[::-1]`, 충전 dQ/dV 방전 거울·양수 — L505–508/L548, 코드 L474–477 ✓
8. `V_n=V_app−σ_d|I|R_n` — L198/L218, 코드 L412 ✓; **분극 방향 서술(방전 시 V_app>V_n, L201) = 2026-06-29 정정 플로우차트(L88)와 일치** — v7-05 가 정정한 오류를 v7-04b 는 처음부터 정방향으로 서술. 회귀 없음.

## ⑤ 체리픽 베이스 적합도 — 적합(권장) + 잔여 보완점

**적합 판정.** 부호 8/8·식 사슬·완결성·G-follow·G-usable 만점, w^eff 게이트 보완이 정확·일관 3중 명시, 분극 방향이 정정본과 정합(이식 시 부호 회귀 위험 없음). 코드 식별자·라인 인용이 v11_final 과 1:1. 체리픽 통합본의 텍스트 베이스로 가장 안전.

**잔여 보완점(이식 후 다듬기 — 결함 아님, 시각/미세):**
- (B1) fig:hysbranch 우단 좌표열에 ξ=0.99 근처 중간점 1개 추가하면 `(0.95,-0.20)→(0.97,+1.06)` 수직 점프의 Bézier 과주를 억제(물리는 이미 옳음, 시각만).
- (B2) fig:hysbranch 의 `U-chg (minus)` 라벨을 `U-dis center` 와 같은 우측 정렬로 통일하면 대칭성 향상.
- (B3) N0 본문(L184)의 "Q_cell 은 꼬리 길이의 |I|/Q_cell 비에만 들어간다" 는 코드상 정확(func_L_q 유일)하나, seed_L_V 진단경로도 같은 비를 쓴다는 1구 각주가 있으면 G-usable 더 닫힘(선택).
- (B4) 코드 L418 주석("저전위쪽 더 넓게")은 stale(실제 pad_lo=pad_hi=0.15 대칭)인데, v7-04b 본문(L207·L210)은 대칭값을 정확히 서술 — 문건은 무결, 코드 주석 불일치는 v7 범위 밖(보고만).

**결론: v7-04b 를 체리픽 텍스트 베이스로 채택 권장. 28/30, 부호 8/8, 신규 회귀 0.**
