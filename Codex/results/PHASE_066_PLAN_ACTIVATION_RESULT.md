# Phase 066 v1.0.25–v1.0.25.2 Lineage Reaudit Plan Activation Result

Date: 2026-09-01

Phase: `066`

Unit: detailed-plan activation before cumulative Step 76

Status: `PASS_PENDING_PERSISTENCE`

Selected Gate: `PASS_P066_PLAN_ACTIVATION`

Persistence terminal: `PASS_P066_PLAN_ACTIVATION_PERSISTENCE`

Containing commit: `PENDING_AT_PRECOMMIT_BY_DESIGN`

Expected parent: `a2920fba07ab9ce75191134f0d68ed3b6ffda4e5`

Expected subject: `docs(phase066): plan v1025 lineage reaudit`

Plan: `Codex/plans/2026-09-01-phase066-v1025-v1025_2-lineage-detailed-plan.md`

Validator: `Codex/work/v1025_phase066/validate_phase066_plan.py`

Machine validation: `Codex/results/PHASE_066_PLAN_ACTIVATION_VALIDATION.json`

## Result

The Phase 066 detailed-plan content is accepted for the v1.0.25 through
v1.0.25.2 lineage reaudit. This is an activation-content decision, not an
execution result and not a persisted checkpoint.

Phase 065 remains closed at the selected conditional Gate
`CONDITIONAL_P065`. Its Step 75.2 exact-eight checkpoint is commit
`a2920fba07ab9ce75191134f0d68ed3b6ffda4e5`, parent
`26e2ce9559220d5782e1303d68b4449a36309e94`, subject
`audit(phase065): close v1024 lineage gate`; push/live-remote and Python
3.12/3.14 `PASS_P065_STEP75_2_PERSISTENCE` were completed. The unresolved
Ref. 7 original remains owned by `PHASE-071-PRIMARY-SOURCE-ACQUISITION` and is
not silently transferred to Phase 066.

Phase 066 activation remains `PASS_PENDING_PERSISTENCE`. Its exact-seven
commit, push, live-remote verification and dual-runtime persistence validation
must complete before cumulative Step 76 begins.

## Activated Step Range

Step numbers remain cumulative across phases.

| Step | Activated work | Required boundary |
|---:|---|---|
| 76 | freeze v1.0.25–v1.0.25.2 source, document, plan, result and handover genealogy | fixed occurrence/blob/text/PDF/image and commit denominators; no silent release substitution |
| 77 | audit skew derivative and Direct14 calculation flow | distinguish equations, executable routes, calibration outputs and external scientific authority |
| 78 | adjudicate stored 8-decimal parameters against transient optimizer state | no full-precision reconstruction claim without persisted state |
| 79 | separate empirical fit behavior from material interpretation | preserve `UNVERIFIED` external/material/experimental claims |
| 80 | verify defaults, `4+2`/`7+7`, temperature and fresh-import behavior | isolated runtime only; no historical behavior inferred from current state |
| 81 | integrate dispositions and select the Phase 066 lineage Gate | all source identities and inherited owners routed; persistence precedes Phase 067 |

This activation does not execute any of Steps 76–81.

## Fixed Source and Read Denominators

The Phase 056 inventory and Phase 057 recovery evidence fix the Phase 066
release-source denominator as follows:

| Surface | Fixed denominator |
|---|---:|
| path occurrences across v1.0.25, v1.0.25.1 and v1.0.25.2 | `433` |
| per-release path occurrences | `143 / 144 / 146` |
| unique blobs | `167` |
| unique text blobs | `158` |
| unique-text physical lines | `30,597` |
| unique PDFs / pages | `6 / 308` |
| unique images | `3` |

The narrative/control denominator is separately fixed:

| Surface | Fixed denominator |
|---|---:|
| manifest-backed narrative documents | `40` |
| manifest-backed narrative physical lines | `9,019` |
| root control documents outside the manifest narrative queue | `2` |
| root control-document lines | `240 + 415 = 655` |
| expanded narrative/control documents | `42` |
| expanded narrative/control physical lines | `9,674` |

An occurrence count, unique-blob count and unique-text count are different
denominators and must not be substituted for one another. The two root control
documents are direct recovery inputs; they must not be retroactively inserted
into the Phase 056 manifest counts.

## Fixed Git Genealogy Boundary

The release-process denominator is `17` commits and the broader routed-process
denominator is `20` commits. Release `17` is the core genealogy; routed `20`
includes supplemental lineage and is the upper process universe. Neither may be
substituted for the other.

| Partition | Count | Authority |
|---|---:|---|
| release process | `17` | core release genealogy input |
| routed process | `20` | release plus supplemental lineage input |

The first selected process commit is
`edbc4a2c68cda0dd21662cb6dd68ba8bed699a76`; the last selected process commit is
`e3e1a634f34b711aa4803fd190fe9120f1755f13`. Exact commit identities, parent
edges, subjects, timestamps, changed paths and relevance are Step 76 evidence;
directory names or later touch-ups cannot move an implementation backward into
v1.0.24.

Supplemental transitional v1.0.26/carryover evidence and v1.0.26A/B fitting
directories are comparison and genealogy inputs only. They do not define a new
canonical release and do not supply scientific, material, experimental or
publication authority for v1.0.25.2.

## PDF Authority Boundary

The stored v1.0.25.2 PDFs are byte-identical to the corresponding v1.0.25.1
PDF blobs. They are stale for v1.0.25.2 release verification.

Therefore PDF presence and page counts establish inventory coverage only. They
do not establish a v1.0.25.2 LaTeX rebuild, visual adoption of v1.0.25.2 source,
final scholarly correctness or publication readiness. A fresh build and visual
audit remain later evidence requirements.

## Direct14 and Optimizer-State Authority Boundary

The recovered Direct14 fitting flow contains a transient full-precision
optimizer vector in memory, while stored output retains parameter values rounded
to 8 decimal places and transition values rounded to 6 decimal places. The
stored direct-fit vector has `57` parameters.

No original full-precision optimizer-state artifact was found in the recovered
release evidence. Stored parameters and metrics may therefore support bounded
calibration genealogy and reproducibility checks at their stored precision, but
they do not prove:

- exact reconstruction of the optimizer terminal state;
- global optimality or parameter identifiability;
- held-out predictive validity;
- material, chemical or experimental truth; or
- equivalence between a transitional candidate and the canonical release.

Step 78 must preserve this distinction. It may not manufacture missing digits,
infer a unique optimizer state from rounded output, or promote the Direct14 fit
to external scientific authority.

## Activation Evidence Boundary

This result records the selected activation-content Gate only. It does not
claim that `validate_phase066_plan.py` has been executed, that either Python
runtime has returned PASS, or that the JSON machine artifact is current staged
evidence. Those statements may be added only after the controller creates the
complete exact-seven set and actually runs the staged checks.

The validation artifact is JSON-last: it may be considered current only after
all six non-JSON activation outputs have their final staged bytes and the
validator writes the seventh artifact from that exact snapshot.

## Exact-Seven Persistence Set

1. `Codex/plans/2026-09-01-phase066-v1025-v1025_2-lineage-detailed-plan.md`
2. `Codex/work/v1025_phase066/validate_phase066_plan.py`
3. `Codex/results/PHASE_066_PLAN_ACTIVATION_VALIDATION.json`
4. `Codex/results/PHASE_066_PLAN_ACTIVATION_RESULT.md`
5. `Codex/results/PHASE_055_069_FULL_LINEAGE_REAUDIT_EXECUTION_LEDGER.md`
6. `Codex/results/PHASE_059_090_CANONICAL_COMPLETION_EXECUTION_LEDGER.md`
7. `Codex/results/ACTIVE_HANDOVER_CANONICAL_COMPLETION.md`

No eighth path, `Claude/**` path, protected Codex branch, or `main` path belongs
to this activation checkpoint.

The required precommit status map is exactly `A/A/A/A/M/M/M` in the path order
listed above; a rename, deletion, extra path or different status blocks activation.

## Persistence Contract

The activation checkpoint must satisfy all of the following before Step 76:

- active local `HEAD`, upstream/tracking and live origin all equal the new
  exact-seven commit;
- the commit parent is exactly
  `a2920fba07ab9ce75191134f0d68ed3b6ffda4e5`;
- the commit subject is exactly `docs(phase066): plan v1025 lineage reaudit`;
- committed paths and blob bytes equal the reviewed exact-seven snapshot;
- Python 3.12 and Python 3.14 staged validation both pass;
- independent plan, validator and records reviews have P0/P1/P2=`0/0/0`;
- protected tip `fc5f1776cfe1de5cb5d8336a74b05f35e3f95d71`, `main` and Claude branches
  remain unchanged; and
- the postcommit check returns `PASS_P066_PLAN_ACTIVATION_PERSISTENCE` with a
  clean worktree.

## Confirmed Decisions

- `PASS_P066_PLAN_ACTIVATION` is the sole activation-content Gate.
- `PASS_PENDING_PERSISTENCE` is the current status.
- `PENDING_AT_PRECOMMIT_BY_DESIGN` is the only valid containing-commit value.
- `PASS_P066_PLAN_ACTIVATION_PERSISTENCE` is the terminal required before Step
  76.
- Fixed source, narrative and commit denominators above are activation inputs,
  not optional sampling targets.
- Stale PDFs, rounded fit output and transitional v1.0.26 evidence retain their
  bounded authority.
- Phase 068 retains Claude/Codex fork-adjudication ownership; this activation
  does not pre-adjudicate those competing assessments.

## Unresolved at Activation

- No Step 76 source/genealogy adjudication has been executed.
- No fresh v1.0.25.2 PDF build or visual audit has been executed.
- No full-precision optimizer state has been recovered.
- No external scientific, material, chemical, experimental, primary-literature
  or publication-validity claim has been established.
- The containing activation commit, push, live-remote equality and dual-runtime
  persistence checks remain pending.

## Exact Next Action

The controller must complete the exact-seven staged snapshot, run the activation
validator under Python 3.12 and Python 3.14, obtain independent P0/P1/P2
`0/0/0`, commit with the exact expected parent and subject, push, verify the live
remote and protected boundaries, then run the postcommit persistence mode.

Cumulative Step 76 may begin only when the persisted terminal is exactly
`PASS_P066_PLAN_ACTIVATION_PERSISTENCE`.
