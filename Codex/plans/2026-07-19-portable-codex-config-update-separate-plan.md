# Portable Codex Configuration Semantic Update Separate Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` and `superpowers:verification-before-completion`. This plan is separate from the Project_Anode_Fit scientific audit and remains parked until separately executed.

**Goal:** Port applicable operational lessons from `portable_claude_config` into `portable_codex_config` without regressing Codex-specific harness, privacy, portability, or approval behavior.

**Architecture:** Fully inventory and read both packages, classify semantic deltas, reproduce runtime-relevant issues before changing runtime code, apply targeted portable updates, regenerate manifests, and run privacy and acceptance verification.

**Tech Stack:** Markdown, JSON, CSV, PowerShell, Bash syntax checks, SHA256 manifests, harness-core validation tests.

---

## Status

`PARKED_SEPARATE_WORKSTREAM`. This plan is not a prerequisite for, phase of, or output of the Chapter 1-3 scientific audit.

## Scope

- Source: `D:\Projects\Project_skills\portable_claude_config`.
- Target: `D:\Projects\Project_skills\portable_codex_config`.
- Preserve Superpowers primacy, harness-core-v4, PowerShell/Bash parity, `Codex/instincts`, memory projection, sanitization, and approval-gated global promotion.
- Do not copy `continuous-learning-v2` into Codex.
- Do not mutate installed skills, global Codex configuration, Superpowers, MCP registration, or live project instinct storage.

## Phase CFG-001 - Semantic Update

- [ ] **Step C1:** Inventory all source and target files with relative path, bytes, lines, SHA256, encoding, and newline style.
- [ ] **Step C2:** Read every source and target file first-to-last with tracked ranges.
- [ ] **Step C3:** Classify every Claude lesson as already stronger, instruction delta, memory delta, reproduced runtime defect, Claude-specific, or privacy-rejected.
- [ ] **Step C4:** Reproduce every proposed runtime fix against Codex before authorizing runtime edits.
- [ ] **Step C5:** Save before-state copies of only approved target files under the active task's `Codex/work` backup folder.
- [ ] **Step C6:** Apply targeted documentation, memory, template, and verified runtime updates.
- [ ] **Step C7:** Regenerate manifests and hashes from actual target files.
- [ ] **Step C8:** Run Markdown/JSON/CSV, PowerShell, Bash, privacy, sanitizer, and harness validation checks.
- [ ] **Step C9:** Run semantic regression checks for Codex-specific capabilities and approval boundaries.
- [ ] **Step C10:** Have Sol independently review the delta and close `PORTABLE_CONFIG_PASS` only with zero unreviewed regressions.

## Outputs

- Config semantic delta matrix.
- Exact changed-file list and before/after hashes.
- Validation and privacy report.
- Updated package manifests.
- Separate handover describing adopted, rejected, and deferred Claude lessons.

## Stop Conditions

- Stop on an unreproduced runtime claim, an unavailable acceptance test, a privacy leak, or any required global/installed-skill mutation.
- No scientific Project_Anode_Fit phase depends on this plan.

